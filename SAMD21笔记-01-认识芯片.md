# 第 1 章　认识这颗芯片

> **芯片**：ATSAMD21G18A（Cortex-M0+ @48 MHz，256 KB Flash，32 KB SRAM，**48 脚 / 38 个 I/O**）
> **工具链**：MPLAB X 6.30 + XC32 5.10 + SAMD21_DFP 3.7.262
> **本章目标**：把 `ATSAMD21G18A` 这串字符翻译成一份具体规格——几个引脚、几伏供电、内存怎么分、外设有哪些、它们住在地址空间的哪里；最后让芯片自己报出身份。
> **学习顺序**：型号怎么读 → 内核是什么水平 → 存储怎么组织 → 电源从哪进 → 复位怎么回事 → 有哪些外设 → 外设住在哪 → 和 STM32F0 对照 → 上电确认顺序 → 寄存器 → 例程 → 清单。
> 本章只回答"这颗芯片是什么"。寄存器怎么读写是第 2 章，时钟怎么配是第 5 章；这里只用**复位后默认就打开**的那部分。

## 1.0 认识一颗芯片，要回答哪几个问题

拿到一块陌生的板子，你脑子里应该自动冒出六个问题。本章就是按这个顺序展开的：

| 问题 | 为什么必须先问 | 在哪节 |
|---|---|---|
| 它是谁？ | 型号里写着引脚数、Flash 大小、功能变体 | 1.1 |
| 它算得快不快？ | 决定能不能用浮点、能跑多复杂的算法 | 1.2 |
| 内存怎么分？ | 程序、变量、掉电保存的数据各放哪 | 1.3 |
| 电从哪进？ | 接错电源域是最容易烧板子的事 | 1.4 |
| 它什么时候会重启？ | 复位源决定了"跑飞了怎么查" | 1.5 |
| 它能干什么？ | 外设清单决定方案可行不可行 | 1.6、1.7 |

## 1.1 型号怎么读：一串字符就是一份规格书

Microchip 的型号是**字段拼接**的，按位置读就行（手册第 3 章 Ordering Information）：

```text
ATSAMD 21 G 18 A - M U T
 家族  系列 引脚 Flash 变体  封装 温度 包装
```

| 片段 | 可选值 | 含义 |
|---|---|---|
| `ATSAMD` | — | `AT` 是原 Atmel 前缀；`SAMD` = 通用 MCU 产品家族（`SAMDA` 是同家族的车规版 DA1） |
| `21` | 21 | 产品系列：Cortex-M0+ 内核，**带 DMA 和 USB** |
| `G` | E / G / J | 引脚数：**E = 32 脚，G = 48 脚，J = 64 脚** |
| `18` | 15 / 16 / 17 / 18 | Flash 容量：15 = 32 KB，16 = 64 KB，17 = 128 KB，**18 = 256 KB** |
| `A` | A / B / C / D / L | 器件变体，表示**功能差异**，不是版本号 |
| `-M` | A / M / U / C / MM | 封装：A = TQFP，M = VQFN，U = WLCSP，C = UFBGA |
| `U` | U / N / F / Z | 温度等级：U = −40~85 °C，N = −40~105 °C，F/Z = −40~125 °C |
| `T` | 空 / T | 包装：空 = 托盘，T = 编带 |

所以 `ATSAMD21G18A-MUT` 读出来是：**Cortex-M0+ 带 USB 的 48 脚芯片，256 KB Flash，默认变体，VQFN 封装，−40~85 °C，编带**。

### 1.1.1 为什么 Flash 那一栏特别重要

Flash 容量不只决定"能装多少程序"，它还**连带决定 SRAM**——在这颗芯片上，SRAM 永远是 Flash 的 1/8：

| 型号数字 | Flash | SRAM | 页数（64 B/页） |
|---|---|---|---|
| 15 | 32 KB | 4 KB | 512 |
| 16 | 64 KB | 8 KB | 1024 |
| 17 | 128 KB | 16 KB | 2048 |
| **18** | **256 KB** | **32 KB** | **4096** |

同时也决定**有没有 RWWEE 区**：只有 128/64/32 KB 的型号有，256 KB 型号没有（1.3 节细说）。买芯片时只盯着"256 KB 够大"是不够的，要连 SRAM 和 RWWEE 一起看。

### 1.1.2 变体 A、B、C、D、L 到底差什么

变体字母是这套命名里最容易忽略、又最容易踩坑的一位。它跟"芯片是新版还是旧版"没关系——**硅片版本看的是 `DSU.DID.REVISION`，变体看的是功能**（手册 13.13.9 专门有一句 Note 说明这件事）。

| 变体 | 手册原文的含义 | 白话 |
|---|---|---|
| `A` | Default Variant | 基础版。ADC 只有单端/差分基本功能，**没有 RWWEE**，只有 2 个模拟比较器 |
| `B` | Added RWWEE support for 32 KB and 64 KB memory options | 给 32/64 KB 型号补上 RWWEE 区 |
| `C` | Silicon revision F for WLCSP45 package option | 45 球 WLCSP 专用（等于 B 的功能） |
| `D` | Silicon Revision G with RWWEE Support in 128KB memory options | 128 KB 型号有 RWWEE，而且**多一个 TCC3** |
| `L` | Pinout optimized for Analog and PWM | 引脚为模拟和 PWM 重新排过：**没有 USB、没有 I2S、没有 PTC**，但模拟比较器变成 4 个 |

G18A 是 `A`，所以：**没有 RWWEE、只有 2 个模拟比较器、没有 TCC3**。这三条会在 1.6 节再出现一次。

### 1.1.3 引脚数决定了你能用多少 I/O 和多少 ADC 通道

引脚多的型号不是"多了几个 I/O"这么简单，它往往同时带来更多外设实例和更多模拟通道（手册 Table 2-1）：

| 引脚数 | 型号字母 | I/O 引脚 | ADC 通道 | SERCOM | TC 实例 | 封装 |
|---|---|---|---|---|---|---|
| 32 | E | 26 | 10 | 4 | TC3–TC5 | TQFP32 / QFN32 |
| **48** | **G** | **38** | **14** | **6** | **TC3–TC5** | **TQFP48 / QFN48** |
| 64 | J | 52 | 20 | 6 | TC3–TC7 | TQFP64 / QFN64 / UFBGA64 |

注意 **I/O 引脚数远小于引脚总数**（48 脚只有 38 个能当 I/O）：剩下的给了电源、地、复位、晶振和调试口。同样的道理，**ADC 通道数是"能接到 ADC 的引脚数"，不是"ADC 有几个"**——ADC 只有一个，它通过一个模拟多路开关轮流接到这些引脚上。

> 关于封装：WLCSP 是例外。E 的 WLCSP 封装是 35 球，G 的 WLCSP 是 45 球，和上表的引脚数对不上，型号里也带单独的变体字母（如 `C`）。做选型时不要用引脚数去反推 WLCSP 型号。

## 1.2 内核：Cortex-M0+ 处在什么水平

内核是 ARM 授权的标准件，Microchip 只是选了一个配置。手册 Table 11-1 把选中的配置逐项列了出来，这张表决定了你写代码时的很多"能不能"：

| 配置项 | G18A 的取值 | 对你的影响 |
|---|---|---|
| 内核 | ARM Cortex-M0+ r0p1，ARMv6-M 指令集 | 只有 Thumb-2 的一个子集，没有 `SDIV`/`UDIV` 这类指令 |
| 主频 | 最高 **48 MHz** | 复位后只有 1 MHz（1.5 节），要用满必须自己配时钟（第 5 章） |
| **浮点单元 FPU** | **没有** | `float` 运算由软件库实现，一次浮点乘除几十个周期。能用定点就别用 `float` |
| 乘法器 | **Fast（单周期）** | 32×32 位乘法一条指令出结果；除法是软件库函数 |
| 内存保护单元 MPU | 没有 | 没有"某块内存只读"这种保护 |
| 特权等级 | 只有特权模式 | 所有代码都是特权级，没有用户态 |
| 外部中断线 | **28 条**，4 个优先级 | 优先级只有 4 档（0/1/2/3），不是 STM32 上常见的 16 档 |
| SysTick | 有（24 位） | 可以做一个和内核绑定的节拍定时器 |
| 断点 / 观察点 | 4 个断点、2 个观察点 | 硬件断点数量有限，调试时注意 |
| 单周期 I/O 端口 | 有 | CPU 有一条**独立的**总线直连 PORT，见下 |
| 取指宽度 | 32 位 | 一次取一条 32 位指令，比 Cortex-M0 流畅 |

### 1.2.1 M0+ 比 M0 多了什么

M0+ 和 M0 指令集 100% 兼容，但多了一条**单周期 I/O 端口（IOBUS）**：CPU 除了那条走 AHB 总线矩阵的大路，还有一条小路直接连到 PORT。

```text
Cortex-M0+ ──┬── AHB-Lite 总线 ──▶ Flash / SRAM / 三个外设桥
             └── IOBUS 单周期口 ──▶ PORT（地址 0x6000_0000，0.5 KB）
```

所以读引脚、写引脚可以**和取指令同时进行**，不抢总线。手册里 PORT 出现两个基址就是这个原因：`0x4100_4400`（走 AHB，和别的外设一样）和 `0x6000_0000`（走 IOBUS）。本章用前者，后者在第 4 章讨论引脚速度时再说。

### 1.2.2 MTB：不花钱的"黑匣子"

MTB = Micro Trace Buffer（微跟踪缓冲）。它盯着 CPU 的程序计数器，**只要 PC 不是顺序变化（跳转、进中断），就把这件事写一对 32 位字到 SRAM 里**。调试器事后把这段 SRAM 读出来，就能还原程序实际执行过哪条路径。

| 特性 | 内容 |
|---|---|
| 寄存器基址 | `0x4100_6000`（桥 B，DFP 宏 `MTB_REGS`） |
| 缓冲区位置 | **在 SRAM 里**，位置和大小由软件/调试器配置 |
| 谁优先 | MTB 的写入优先于 CPU 对同一块 SRAM 的访问 |
| 何时启用 | `MASTER.EN = 1`；可以设水位自动停 |
| 溢出行为 | 没设水位就**环形覆盖**，留下的是最近一段 |

对写应用代码的人来说，MTB 平时是关着的；它的意义是"板子上跑崩了，不用示波器也能看到最后跳去了哪"。代价是：一旦启用，它会占掉一部分 SRAM。

## 1.3 存储：256 KB Flash 和 32 KB SRAM 怎么组织

### 1.3.1 地址空间全景

SAM D21 的地址映射由 Cortex-M0+ 架构定死，Microchip 只往里填内容（手册 Table 10-1）。这张表是全书的地图，建议直接记住：

| 区域 | 起始地址 | G18A 大小 | 里面是什么 |
|---|---|---|---|
| Internal Flash | `0x0000_0000` | 256 KB | 程序代码、常量、只读数据 |
| RWWEE Emulation | `0x0040_0000` | **无** | 256 KB 型号不提供 |
| Internal SRAM | `0x2000_0000` | 32 KB | 变量、堆、栈 |
| Peripheral Bridge A | `0x4000_0000` | 64 KB | 电源、时钟、复位、外部中断 |
| Peripheral Bridge B | `0x4100_0000` | 64 KB | 存储控制器、引脚、DMA、USB、调试 |
| Peripheral Bridge C | `0x4200_0000` | 64 KB | 通信、定时、模拟外设 |
| IOBUS | `0x6000_0000` | 0.5 KB | CPU 直连 PORT 的单周期窗口 |
| Cortex-M0+ 私有区 | `0xE000_0000` | — | NVIC、SysTick、系统控制块（内核自带，不属于外设桥） |

**地址永不重映射**：手册明确写着 High-Speed 总线上的地址"never remapped in any way, even during boot"。也就是说调试器里看到的 `0x0000_0000` 永远是 Flash 的起点，不会像某些 MCU 那样 boot 阶段变来变去。

### 1.3.2 Flash 的擦写单位：页和行是两个不同的东西

Flash 256 KB 被切成 **4096 页，每页 64 字节**；而**每 4 页组成一行，一行 256 字节**（手册 22.6.2："each row contains four pages"）。

| 操作 | 单位 | 大小 | 含义 |
|---|---|---|---|
| 读 | 字节 | — | 像读内存一样直接读，不占特殊命令 |
| 写（编程） | 页 | 64 B | 先把数据装进页缓冲，再一次写一页 |
| 擦除 | **行** | 256 B | 擦一行会把这一行里 4 页全擦掉 |

这条规矩的后果很实际：**想改一个字节，得先把整行 256 字节读出来、改掉那一个字节、擦掉整行、再写回 4 页**。这就是"EEPROM 模拟"要写一整套算法的原因（第 13 章）。

另外两点手册里写明的限制：同一行在擦除之前**最多连续写 8 次**；Flash 还被分成 **16 个区域，每个区域 16 KB**，各有一个锁位（Lock Bit），默认全开。

### 1.3.3 为什么 256 KB 型号没有 RWWEE 区

RWWEE = Read-While-Write EEPROM Emulation，"写 EEPROM 的同时能读主阵列"。它是一块**独立的小 Flash 区**，映射在 `0x0040_0000`。

| 型号 | RWWEE 大小 | 页数 |
|---|---|---|
| x17（128 KB） | 4 KB | 64 |
| x16（64 KB） | 2 KB | 32 |
| x15（32 KB） | 1 KB | 16 |
| **x18（256 KB）** | **没有** | — |

原因是功能特性表里那句 "`4/2/1/0.5 KB RWWEE Flash section (not available on 256 KB devices)`"，而且它只对 **B、C、D、L** 变体开放——A 变体连小容量型号也没有。

那 G18A 想做掉电保存的数据怎么办？只能占用**主阵列末尾**的一段做 EEPROM 模拟（大小由 NVM User Row 里的 `EEPROM` 熔丝位选），代价是：手册 22.6.4.1 明确写着，**主阵列正在编程或擦除时，读主阵列会让 AHB 总线停顿**。也就是说，写数据的那几毫秒里 CPU 取指会被拖住——这正是 RWWEE 存在的意义，也正是 G18A 没有它的代价。

### 1.3.4 三块藏起来的"小存储"

除了主阵列，还有三处按地址映射的辅助空间，它们不占你的 256 KB：

| 空间 | 地址 | 内容 |
|---|---|---|
| NVM User Row | `0x0080_4000` | 上电配置：BOOTPROT、EEPROM 区大小、**BOD33 阈值/使能/动作**、WDT 上电使能、区域锁位 |
| 软件校准区 | `0x0080_6020` | 出厂测出来的 ADC 线性度/偏置、OSC32K、USB 收发器、DFLL48M 粗调值，**只读** |
| 器件序列号 | `0x0080_A00C` / `40` / `44` / `48` | 128 位唯一序列号，四个 32 位字拼起来才保证唯一 |

自动校准行里的值会在**每次上电时被硬件自动装载**到对应寄存器，所以正常情况下你不需要手动读它。NVM User Row 里的值则要等到下一次复位才生效——写熔丝和复位是绑在一起的。

## 1.4 电源：一颗芯片里的四个电源域

### 1.4.1 四个电源脚各管什么

SAMD21 的外部电源只有一个电压，但它被分成几路**独立的引脚**，因为内部有数字、模拟和内核三种不同的供电需求（手册 8.2.1）：

| 引脚        | 电压              | 给谁供电                                                 |
| --------- | --------------- | ---------------------------------------------------- |
| `VDDIN`   | 1.62–3.63 V     | I/O 引脚，以及**内部稳压器**的输入                                |
| `VDDIO`   | 1.62–3.63 V     | I/O 引脚、`OSC8M`、`XOSC`（外部高速晶振）                        |
| `VDDANA`  | 1.62–3.63 V     | I/O 引脚、ADC、DAC、AC、PTC、`OSCULP32K`、`OSC32K`、`XOSC32K` |
| `VDDCORE` | **1.2 V**（内部产生） | 内核、Flash/SRAM、外设、`DFLL48M`、`FDPLL96M`                |
| `GND`     | 0 V             | `VDDCORE` / `VDDIO` / `VDDIN` 的公共地                   |
| `GNDANA`  | 0 V             | `VDDANA` 单独的地                                        |

三条外部电源**必须接同一个电压**，手册把这个公共电压统称为 `VDD`：

```text
1.62–3.63 V ──┬── VDDIN  ──▶ 内部稳压器 ──▶ VDDCORE 1.2 V ──▶ 内核 / Flash / SRAM / 外设 / 锁相环
              ├── VDDIO  ──▶ I/O 引脚、OSC8M、XOSC
              └── VDDANA ──▶ ADC、DAC、AC、PTC、OSC32K、OSCULP32K、XOSC32K
GND  = VDDIN/VDDIO/VDDCORE 的地    GNDANA = VDDANA 单独的地（分开走线，最后单点汇合）
```

**VDDCORE 是输出，不是输入。** 它是内部稳压器的结果，只在芯片旁边接一个电容。手册用一整句 Note 强调：**不允许用 VDDCORE 给任何外部元件供电**。新手最常见的错误之一，就是把 VDDCORE 当成"另一个电源输入"接上 3.3 V。

### 1.4.2 去耦电容怎么放

手册把推荐值写在"Schematics Checklist"（第 45 章）和电气特性里（Table 37-19、Table 45-1）：

| 电源脚 | 电容 | 备注 |
|---|---|---|
| `VDDIN` | 10 µF + 100 nF 到 GND | 稳压器输入；建议 X7R 陶瓷，低 ESR |
| `VDDCORE` | **1 µF** 到 GND | 稳压器输出；ESR ≤ 1 Ω，允许 0.8–1 µF |
| `VDDIO` | 每组电源脚旁 100 nF + 10 µF | 每一对电源/地引脚都要就近放 |
| `VDDANA` | 100 nF + 10 µF | 在 VDD 与 VDDANA 之间加**磁珠**（手册举例 20 MHz 时 50 Ω、100 MHz 时 220 Ω） |

磁珠的作用是把数字电源的高频噪声挡在模拟域外面。它不贵，但不放的话 ADC 读数会莫名其妙地抖。

### 1.4.3 上电顺序：三条电源一起上，而且不能上太快

手册 8.2.4 对上升速率提了要求：**POR 电路要求一个最小上升率，同时上升率也不能超过上限**。Table 37-5 给的**最大**值是 **0.1 V/µs**。上升太慢，POR 可能一直不放行；上升太快，POR 可能来不及动作。

| 要点 | 内容 |
|---|---|
| 三条电源 | VDDIN / VDDIO / VDDANA **必须同压、同时建立**，不存在"先给哪一路"的讲究 |
| 上升率 | 上限 0.1 V/µs（最大额定值，Table 37-5） |
| 电源不稳时 | 芯片被 POR 一直按在复位状态，直到电源稳定 |
| 上电后 I/O | **全部三态（高阻）**，不会主动输出任何电平（手册 8.3.2） |
| 稳压器模式 | Normal（CPU 和外设运行时）/ Low Power（待机时省静态电流） |

还有一条选型时的硬指标：**SAM D21 是 1.62–3.63 V，SAM DA1 是 2.7–3.63 V**。同样是"3.3 V 的 Cortex-M0+"，DA1 在 2.5 V 下根本不工作。

## 1.5 复位与上电：谁能让芯片重新开始

### 1.5.1 六个复位源

芯片里有一个**复位控制器**，把所有可能的复位原因收集起来，产生一次统一的复位，并记下是谁干的（手册 16.6.2.7）。复位源分两类：

| 类别 | 复位源 | 由谁产生 | 手册给的说明 |
|---|---|---|---|
| **电源类** | `POR` | VDDANA 上的上电复位电路 | **永远使能**，上电时和所有睡眠模式下都在监视；VDDANA 掉到阈值以下就复位整颗芯片 |
| 电源类 | `BOD33` | VDDANA 上的欠压检测器 | 阈值和动作可配（复位 / 中断 / 不动作），上电默认值来自 NVM User Row |
| 电源类 | `BOD12` | VDDCORE 上的内部欠压检测器 | 出厂校准，配置存在 NVM User Row 里，**用户行写过后不要动这一项** |
| **用户类** | 外部复位 `EXT` | 拉低 `RESET` 引脚 | 该引脚**内部有上拉**，正常工作时不用外部驱动 |
| 用户类 | `WDT` | 看门狗超时 | 第 13 章 |
| 用户类 | `SYST` | 软件复位（CPU 的 `SYSRESETREQ`） | Cortex-M0+ 指令触发的"我自己重启" |

两类复位的区别在于"复位得有多彻底"：**电源类复位连调试逻辑一起复位**，而 WDT 复位和软件复位**不动调试逻辑**（手册 Table 16-2）。所以软件复位之后还能接着调试，电源复位之后调试器得重新连。

### 1.5.2 复位之后芯片是什么状态

```text
电源稳定 ──▶ POR 放行 ──▶ 时钟系统启动 ──▶ CPU 取指 ──▶ main()
                          │                │
                    OSC8M ÷ 8 = 1 MHz     从地址 0x0000_0000 读
                    （GCLK0 = 主时钟）      第 1 个字 → 栈指针 SP
                                           第 2 个字 → 复位向量 PC
```

| 复位后的默认状态 | 值 | 出处 |
|---|---|---|
| 主时钟 | **1 MHz**（OSC8M 八分频，经 GCLK0 送给 PM） | 手册 8.3.1 |
| 部分同步时钟 | 已开，不分频 | 手册 8.3.1 |
| 通用时钟（GCLK） | 除 `GCLK_WDT` 外全部关闭 | 手册 8.3.1 |
| I/O 引脚 | 三态 | 手册 8.3.2 |
| CPU 起点 | 从 `0x0000_0000` 读 SP 和 PC | 手册 8.3.3 |
| 代码位置 | 内部 Flash 的第一个可执行地址 | 手册 8.3.3 |

**"复位后是 1 MHz"是本章最值钱的一条。** 很多人写完点灯程序发现延时函数比预期慢 48 倍，就是因为忘了这件事；第 5 章会把主频提到 48 MHz，同一个忙等循环会快 48 倍。

### 1.5.3 怎么知道上次为什么重启

复位控制器把最近一次的复位原因留在 `PM.RCAUSE` 里（PM 基址 `0x4000_0400` + 偏移 `0x38`）：

| 位 | 名称 | 置 1 表示 |
|---|---|---|
| 6 | `SYST` | 软件复位请求 |
| 5 | `WDT` | 看门狗复位 |
| 4 | `EXT` | 外部 `RESET` 引脚被拉低 |
| 2 | `BOD33` | VDDANA 欠压复位 |
| 1 | `BOD12` | VDDCORE 欠压复位 |
| 0 | `POR` | 上电复位（**复位默认值就是 1**） |

它是"最近一次"不是"历史记录"：同时可能有多个位置 1（比如掉电时 POR 和 BOD33 一起置位），但下一次复位会重新写。启动时读一次，就能区分"正常上电"和"跑飞被看门狗救回来"。

## 1.6 外设清单：这颗芯片能干什么

### 1.6.1 家族最大配置 vs. G18A 实际有什么

手册的 Features 列表和第 2 章配置表写的是**整个 SAM D21 家族的上限**（"up to"），不是你手上这颗。下面这张表把两者并排放，第二列是手册的家族上限，第三列是 **ATSAMD21G18A 的真实情况**：

| 外设                     | 家族上限                | **G18A 实际**                    | 一句话用途                         | 基址                         |
| ---------------------- | ------------------- | ------------------------------ | ----------------------------- | -------------------------- |
| SERCOM（串口/SPI/I2C 三合一） | 6                   | **6**（SERCOM0–5）               | 一个模块用寄存器切换成 USART / SPI / I2C | `0x4200_0800` 起，每 0x400 一个 |
| TC（16 位定时器）            | 5（TC3–TC7）          | **3**（TC3、TC4、TC5）             | 定时、计数、频率测量、2 路 PWM            | `0x4200_2C00` 起，每 0x400 一个 |
| TCC（24 位控制定时器）         | 4                   | **3**（TCC0、TCC1、TCC2）          | 电机/灯光用 PWM：互补输出、死区、故障保护、抖动    | `0x4200_2000` 起，每 0x400 一个 |
| ADC                    | 12 位、20 通道、350 ksps | **12 位、14 通道**，可过采样到 16 位      | 读模拟电压；带可编程增益                  | `0x4200_4000`              |
| DAC                    | 10 位、350 ksps、1 路   | **10 位、1 路**（PA02 的 VOUT）      | 输出模拟电压                        | `0x4200_4800`              |
| AC（模拟比较器）              | 4 个（AC0 + AC1）      | **AC0 一个模块，内含 2 个比较器**（没有 AC1） | 比较两个模拟电压，带窗口模式                | `0x4200_4400`              |
| PTC（触摸控制器）             | 256 通道              | **120 互容 / 10 自容**             | 电容触摸按键、滑条、滚轮                  | `0x4200_4C00`              |
| USB                    | 全速 12 Mbps、8 端点     | **1 个，全速**                     | USB 设备/主机                     | `0x4100_5000`              |
| I2S                    | 2 通道                | **1 个，2 通道**                   | 数字音频                          | `0x4200_5000`              |
| DMAC                   | 12 通道               | **12 通道**                      | 不经过 CPU 搬数据                   | `0x4100_4800`              |
| EVSYS                  | 12 通道               | **12 通道**                      | 外设之间互相触发，不经 CPU 也不经中断         | `0x4200_0400`              |
| EIC                    | 16 条外部中断线           | **16 条** + 1 条 NMI             | 引脚上的边沿/电平触发中断                 | `0x4000_1800`              |
| RTC                    | 32 位，带日历            | **1 个**                        | 走时、闹钟、周期唤醒                    | `0x4000_1400`              |
| WDT                    | 1 个                 | **1 个**                        | 跑飞了自动复位                       | `0x4000_1000`              |
| CRC32                  | 1 个                 | **1 个**（在 DSU 里）               | 给一段存储算 CRC32 校验值              | `0x4100_2000` + 偏移         |

关于最后一行要说清楚：手册 Features 里写 "`CRC-32 generator`"，但它**不是一个独立外设**——G18A 的头文件里没有 CRC 基址。CRC32 是 **DSU（Device Service Unit）**的一个功能（手册 13.11.3，多项式 `0xEDB88320`），可以对 Flash 或 SRAM 的一段算校验值。另外 DMAC 里还内置了一个传输用的 CRC 引擎，支持 CRC-16 和 CRC-32。两处不是同一个东西。

### 1.6.2 哪些外设"看型号才有"

这一节是 1.1.2 变体表的延伸——手册在不同章节用了三条脚注来声明外设的可用性，很容易漏看：

| 外设 | 手册原文 | 结论 |
|---|---|---|
| **TC6、TC7** | "TC6 and TC7 are not supported on the SAM D21E and SAM D21G"（Table 7-1 注 4） | 只有 **J（64 脚）**才有 5 个 TC；**G18A 只有 TC3–TC5** |
| **SERCOM4、SERCOM5** | "SERCOM4 and SERCOM5 are not supported in SAMD21ExxA…"（Table 7-1 注 7） | 只有 **G 和 J** 有 6 个 SERCOM；**E（32 脚）只有 SERCOM0–3** |
| **TCC3** | "TCC3 is supported only in SAMD21x17D"（Table 7-1 注 8） | 只有 **128 KB 的 D 变体**才有 TCC3；**G18A 只有 TCC0–TCC2** |
| **AC1** | "The second pair of comparators…are only available on the 'L' variant devices" | 只有 **L 变体**才有 4 个比较器 |
| **RWWEE** | "Only available on device variant B, C, D, and L" | **A 变体没有**；256 KB 型号都没有 |

**为什么这件事必须记住**：这些外设"不存在"的表现不是编译报错，而是**写它的寄存器毫无反应**。头文件里连 `TC6_REGS` 都没有，你只能从手册注脚里发现问题。选型和移植时第一件事就是拿型号去 Table 2-1 核对一遍。

## 1.7 三条外设桥：外设住在地址空间的哪里

### 1.7.1 为什么要分三条桥

高速的 AHB 总线和低速省电的 APB 总线之间需要翻译，Microchip 的做法是放**三个 AHB-APB 桥（HPB0/1/2）**，每个桥管一片 64 KB 的地址窗口（手册 11.5、Table 10-1）：

| 桥 | 基址 | 窗口 | 上面挂了什么 | 复位后 APB 时钟 |
|---|---|---|---|---|
| **桥 A**（HPB0） | `0x4000_0000` | 64 KB | PAC0、PM、SYSCTRL、GCLK、WDT、RTC、EIC | **开** |
| **桥 B**（HPB1） | `0x4100_0000` | 64 KB | PAC1、DSU、NVMCTRL、PORT、DMAC、USB、MTB、SBMATRIX | **开** |
| **桥 C**（HPB2） | `0x4200_0000` | 64 KB | PAC2、EVSYS、SERCOM0–5、TCC0–2、TC3–5、ADC、AC、DAC、PTC、I2S | **关** |

这个划分是有逻辑的，不是随便分的：

```text
桥 A：让芯片"能跑起来"的东西   —— 电源、复位、时钟、外部中断
桥 B：让程序"能存取"的东西     —— Flash 控制器、引脚、DMA、USB、调试
桥 C：应用真正"想用"的东西     —— 通信、定时、模拟        （复位后时钟关着，要手动开）
```

**桥 A 和桥 B 的外设复位后总线时钟就是开的，桥 C 的全部是关的。** 这是第 2 章那条"外设能用 = 开总线时钟 + 分配通用时钟 + 配引脚 + 配功能寄存器"公式的由来：桥 C 上的外设如果忘了开 APB 时钟，写进去的值会**静默消失**——不报错、不复位，就是没反应。

### 1.7.2 每个桥配一个"门锁"：PAC

每个桥旁边都有一个 **PAC（Peripheral Access Controller）**，负责给桥上的外设寄存器加写保护，防止跑飞的代码改坏配置（手册 11.6）：

| PAC | 基址 | 管哪个桥 |
|---|---|---|
| PAC0 | `0x4000_0000` | 桥 A |
| PAC1 | `0x4100_0000` | 桥 B |
| PAC2 | `0x4200_0000` | 桥 C |

它的每一位对应桥上的一个外设。写一个受保护的寄存器而没先解锁，结果是**数据被丢弃并触发一个 CPU 异常**（不是静默失败）。注意 `CLK_PAC2_APB` 复位后是**关的**，PAC2 自己要先把时钟打开才能用。具体写法属于第 2 章。

## 1.8 和 STM32F0 对照

如果你写过 STM32F0，下面这张表就是"哪些习惯要改掉"：

| 项目 | STM32F0 的做法 | SAM D21 的做法 | 你要改什么 |
|---|---|---|---|
| **外设组织** | AHB1/AHB2 + APB1/APB2，每个外设一个独立基址 | **三条桥各 64 KB**，外设基址都挤在 `0x4000_0xxx` / `0x4100_0xxx` / `0x4200_0xxx` | 记地址时先记"在哪个桥"，再记桥内偏移 |
| 写保护 | 没有（靠寄存器的 KEY 序列，如 `RCC` 的时钟安全） | **每个桥一个 PAC**，写受保护寄存器前要解锁 | 见第 2 章 |
| **时钟系统** | `RCC` 一棵树：HSI/HSE → PLL → AHB/APB 分频 + 外设门控位 | **三个外设分工**：SYSCTRL 管振荡器，GCLK 管 9 个通用时钟发生器，PM 管主时钟分频和 APB 门控 | 别只在"使能位"上找问题；还要问"这路外设的 GCLK 选了哪个源" |
| 复位后主频 | 通常 8 MHz HSI（F0） | **1 MHz**（OSC8M ÷ 8） | 延时常数要按 1 MHz 算 |
| **GPIO 复用** | `AFR[2]` 填 AF0–AF15 的编号 | 每引脚只有 **4 位 `PMUX`**，从 A–H 八个功能里挑一个；**不是每个脚都有全部功能** | 复用表要一个脚一个脚查，不能类推 |
| GPIO 上下拉 | `PUPDR` 里上拉/下拉两个独立选择 | `PINCFG.PULLEN` 只负责"开电阻"，**方向由输出值 `OUT` 决定** | 想要上拉必须同时设 `OUT = 1` |
| GPIO 原子操作 | `BSRR` / `BRR` 特殊待遇 | `DIRSET/CLR/TGL`、`OUTSET/CLR/TGL` 这类"三件套"**普遍存在** | 习惯用 SET/CLR 寄存器代替读-改-写 |
| 中断标志 | 各有各的清法 | 几乎所有外设都是 `INTFLAG` **写 1 清标志** | 写 1 是清，不是置位 |
| **工具链** | Keil / IAR / STM32CubeMX + HAL | **MPLAB X + XC32 + DFP**；库可选 ASF/MCC | 头文件里寄存器名长 `DSU_REGS->DSU_DID` 这样 |
| 内核 | Cortex-M0（F0 系列） | **Cortex-M0+ r0p1** | 多了单周期 I/O 端口和 MTB，指令集兼容 |

一句话总结这张表：**STM32 把很多决定塞进了外设自己，SAMD21 把它们拆成了独立模块**。时钟拆成三个、写保护拆成 PAC、引脚复用拆成 PORT——好处是每个模块都简单，代价是你得知道它们分别在哪。

## 1.9 使用逻辑：拿到一块新板子，按什么顺序确认

这一节的顺序不是随便排的，它对应信号的流动方向：**没有电就没有复位，没有复位就没有时钟，没有时钟 CPU 不取指，CPU 不跑外设就配不了**。

```text
电源（1.4）──▶ 复位（1.5）──▶ 时钟（1.5.2）──▶ 取指执行 ──▶ 外设可用（1.6）
   │             │              │
 三路同压      RESET 不被     复位后只有 1 MHz
 去耦到位      外部拉低       要 48 MHz 得自己配（第 5 章）
```

| 步骤 | 做什么 | 怎么判断对了 | 错了会怎样 |
|---|---|---|---|
| 1. 认型号 | 看丝印，回手册 Table 2-1 核对外设 | 型号字母、Flash 数字、封装能对上 | 用了不存在的外设，寄存器写了没反应 |
| 2. 量电源 | 万用表量 VDDIN / VDDIO / VDDANA 是否同压 | 三路都是 3.3 V（或 1.8 V 等） | 某一路没接，部分功能不工作 |
| 3. 量 VDDCORE | 量到 **1.2 V 左右** | 0.8–1 µF 电容在位 | 电容漏放或短路 → 芯片不启动 |
| 4. 看复位 | 量 `RESET` 引脚，确认没有被外部电路长期拉低 | 高电平（内部有上拉） | 芯片一直复位，看起来像"没烧进去" |
| 5. 认时钟 | 记住现在是 1 MHz | 延时函数按 1 MHz 估算 | 通信波特率、延时全部偏 48 倍 |
| 6. 连调试口 | `SWCLK = PA30`、`SWDIO = PA31` | 调试器能识别到 DSU 的 ID | 连不上，误以为芯片坏了 |
| 7. 查外设 | 按 1.6.2 的表确认这颗型号有没有 | 头文件里有对应的 `XXX_REGS` 宏 | 同上：写了没反应 |

第 6 步顺带说一句：`SWCLK` 是映射到普通 PORT 功能上的，而 `SWDIO` 不是——调试器冷插拔/热插拔时，硬件会**自动把 PA31 切到 SWDIO 功能**（手册 7.2.2）。所以 PA30/PA31 平时当普通 I/O 用是可以的，插上调试器就会被接管。

## 1.10 寄存器：本章用到的几个，以及它们为什么存在

到这里硬件已经讲完了。下面这几个寄存器**不是新知识**，它们只是上面那些硬件事实的"读数窗口"——每一个都回答一个你必然会问的问题。

| 我要回答的问题 | 看哪个寄存器 | 为什么需要它 |
|---|---|---|
| 我现在到底跑在哪颗芯片上？ | `DSU.DID` | 同一份固件要兼容多个型号时，必须能在运行时问出来 |
| 这片 Flash 到底多大、一页多少字节？ | `NVMCTRL.PARAM` | 页数/页大小是**出厂测出来的**，不是写死的常量 |
| 上次为什么重启？ | `PM.RCAUSE` | 看门狗复位和上电复位在现象上很像，必须能区分 |
| 掉到多少伏会复位？ | `SYSCTRL.BOD33` | 阈值和动作可配，且上电默认值来自熔丝 |
| 这个外设的寄存器能不能写？ | `PAC0/1/2` | 防止跑飞的代码改坏配置（第 2 章细讲） |

### 1.10.1 DSU.DID：芯片的身份证

DSU 挂在桥 B（基址 `0x4100_2000`），`DID` 是它的只读寄存器，偏移 `0x18`，所以绝对地址是 **`0x4100_2018`**。手册 13.13.9 给的字段划分正好对应 1.1 节的型号字段：

| 位 | 名称 | G18A 的值 | 含义 |
|---|---|---|---|
| 31:28 | `PROCESSOR` | `0x1` | Cortex-M0+ |
| 27:23 | `FAMILY` | `0x0` | SAM D 家族 |
| 21:16 | `SERIES` | `0x01` | 带 DMA 和 USB 的产品系列（就是型号里的 `21`） |
| 15:12 | `DIE` | — | 芯片编号 |
| 11:8 | `REVISION` | — | **硅片版本**，和变体字母是两件事 |
| 7:0 | `DEVSEL` | `0x05` | 器件选择值，对应"Flash 密度 + 引脚数 + 变体" |

整个寄存器连起来读是 **`0x10010305`**，芯片出厂就是这一个值。DFP 头文件直接把它定义成了常量 `CHIP_DSU_DID`，所以代码里不用自己拼：

```c
#define CHIP_DSU_DID   _UINT32_(0X10010305)   /* samd21g18a.h 里就有这一行 */
```

它的意义是：**固件可以自检"我烧对芯片了吗"**。如果不匹配，多数情况下应该立刻停住而不是继续跑——在外设不存在的型号上继续初始化，等着的就是"写了没反应"。

### 1.10.2 NVMCTRL.PARAM：Flash 的尺寸说明书

NVMCTRL 也在桥 B（基址 `0x4100_4000`），`PARAM` 偏移 `0x08`，绝对地址 **`0x4100_4008`**：

| 位 | 名称 | G18A 的值 | 含义 |
|---|---|---|---|
| 31:20 | `RWWEEP` | `0` | RWWEE 区的页数；**256 KB 型号是 0** |
| 18:16 | `PSZ` | `0x3` | 页大小编码：0→8 B、1→16 B、2→32 B、**3→64 B**、…、7→1024 B |
| 15:0 | `NVMP` | `4096` | 主阵列的页数 |

**为什么不让软件直接写常量 256 KB？** 因为同一份代码可能编到 G17A 或 G16A 上；而 `NVMP × 页大小` 是这颗芯片自己报出来的真相。手册 Table 10-3 的注 2 说得很直白：这两个值"可以从 NVMCTRL 的 PARAM 寄存器读出来"。

注意 `PSZ` 不是字节数，是**以 2 为底的指数减 3**：页大小 = `1 << (PSZ + 3)`。G18A 读到 `PSZ = 3`，`1 << 6 = 64` 字节，和 1.3.2 节的表对上。

### 1.10.3 PM.RCAUSE：上一次的"死因"

PM 在桥 A（基址 `0x4000_0400`），`RCAUSE` 偏移 `0x38`，绝对地址 **`0x4000_0438`**，是个 8 位只读寄存器，**复位值是 `0x01`（即 POR 位为 1）**。位定义见 1.5.3 节的表。

用法很固定：**在 `main()` 一开始读一次，存进变量，然后清掉或忽略**。它只有"最近一次"，晚读就被下一次复位覆盖了。

## 1.11 完整例程：让芯片先自报身份，再点灯

### 1.11.1 需求

假设你手上有一块标着"ATSAMD21G18A"的板子，PA05 接了一个低电平点亮的 LED。你最担心的不是点不亮，而是**这块板子到底是不是 G18A**——市场上同名板子不少，有的其实是 128 KB 的 G17A。所以程序的顺序是：

```text
读 DSU.DID ──▶ 是不是 G18A？──否──▶ 停住，什么也不做
                   │是
读 NVMCTRL.PARAM ──▶ Flash 是不是 256 KB？──否──▶ 停住
                   │是
            配置 PA05 ──▶ 点亮 LED
```

这样做的理由：**在确认硬件身份之前不要碰外设**。如果型号不对，"点灯"这个动作可能在错误的引脚上生效，反而掩盖了问题。

### 1.11.2 代码

```c
#include "sam.h"

/* 只用复位后默认就打开的外设：DSU / NVMCTRL / PORT 都在桥 B，
 * 它们的 APB 时钟复位后就是开的，所以这一段没有时钟配置（第 5 章再讲）。 */

#define LED_MASK  (1UL << 5)        /* PA05，低电平点亮 */

/* DID 的字段位置（手册 13.13.9），掩码宏由 DFP 提供 */
#define DID_PROCESSOR(v)  (((v) & DSU_DID_PROCESSOR_Msk) >> DSU_DID_PROCESSOR_Pos)
#define DID_SERIES(v)     (((v) & DSU_DID_SERIES_Msk)    >> DSU_DID_SERIES_Pos)

static int chip_is_expected(void)
{
    /* DID 是只读寄存器，读它不改变任何硬件状态，可以放心在启动时读 */
    uint32_t did = DSU_REGS->DSU_DID;

    /* 0x1 = Cortex-M0+；0x01 = 带 DMA 和 USB 的系列（型号里的 "21"） */
    if (DID_PROCESSOR(did) != 0x1U)  { return 0; }
    if (DID_SERIES(did)    != 0x01U) { return 0; }

    /* DFP 头文件里写好了本器件应有的整值（0x10010305），
     * 直接比整值能一次性覆盖 FAMILY / DIE / DEVSEL —— 连变体和封装一起查了 */
    return (did == CHIP_DSU_DID);
}

static uint32_t flash_size_bytes(void)
{
    /* PARAM 是 NVMCTRL 上电时自己填好的：页数 × 页大小 */
    uint32_t pages = (NVMCTRL_REGS->NVMCTRL_PARAM & NVMCTRL_PARAM_NVMP_Msk)
                     >> NVMCTRL_PARAM_NVMP_Pos;

    /* PSZ 是以 2 为底的指数减 3：3 → 2^6 = 64 字节/页 */
    uint32_t psz = (NVMCTRL_REGS->NVMCTRL_PARAM & NVMCTRL_PARAM_PSZ_Msk)
                   >> NVMCTRL_PARAM_PSZ_Pos;

    return pages * (1UL << (psz + 3U));      /* 4096 × 64 = 262144 = 256 KB */
}

int main(void)
{
    /* 1) 先问芯片是谁。型号不符就停住，不要在未知硬件上初始化外设。 */
    if (!chip_is_expected()) { while (1) { } }

    /* 2) 再核对存储容量：256 KB 才是 G18A，128 KB 的 G17A 会在这里被拦下 */
    if (flash_size_bytes() != FLASH_SIZE) { while (1) { } }

    /* 3) 身份确认通过，才碰引脚。
     *    先写 OUTSET 把输出状态设成高电平（LED 灭），再写 DIRSET 打开输出驱动：
     *    打开驱动的那一瞬间引脚就是"灭"，不会闪一下（第 4 章）。 */
    PORT_REGS->GROUP[0].PORT_OUTSET = LED_MASK;
    PORT_REGS->GROUP[0].PORT_DIRSET = LED_MASK;

    /* 4) 拉低 → 电流从 LED 灌进引脚 → 亮 */
    PORT_REGS->GROUP[0].PORT_OUTCLR = LED_MASK;

    while (1) { }
}
```

> **关于寄存器名**：DFP 3.7.262 的头文件长这样——`外设_REGS->外设_寄存器`，比如 `DSU_REGS->DSU_DID`、`PORT_REGS->GROUP[0].PORT_OUTSET`。早期 DFP 和网上大量例程用的是 CMSIS 风格 `DSU->DID.reg`、`PORT->Group[0].OUTSET.reg`，**两者指的是同一块硬件**。如果你工程里报"找不到 `PORT_REGS`"，说明 DFP 版本不同，**以你工程里的头文件为准**。

### 1.11.3 这段代码为什么是这个顺序

| 步骤 | 硬件动作 | 为什么不能换位置 |
|---|---|---|
| 读 `DSU.DID` | 读桥 B 上一个只读寄存器 | 只读、无副作用，最适合放在最前面 |
| 读 `NVMCTRL.PARAM` | 读 Flash 控制器报出的尺寸 | 同样是只读；放在点灯前，避免"灯亮了但其实型号错了" |
| 写 `PORT_OUTSET` | 把 PA05 的输出状态预置成高（灭） | 必须在打开输出驱动之前 |
| 写 `PORT_DIRSET` | 打开 PA05 的输出驱动 | 打开的这一瞬间引脚电平由 `OUT` 决定，所以要先准备 |
| 写 `PORT_OUTCLR` | 拉低 PA05，灌电流点亮 LED | 最后才产生可见效果 |

### 1.11.4 怎么确认它在跑

烧进去以后接调试器，在 `main()` 第一行设断点，看 `DSU_REGS->DSU_DID` 的值——应该是 `0x10010305`；再看 `NVMCTRL_REGS->NVMCTRL_PARAM` 的低 16 位——应该是 `4096`（`0x1000`）。两个值都对，说明你对这颗芯片的理解和它的实际状态是一致的。

调试器连不上时，回头按 1.9 节的表从第 1 步往下查：**先电源，再复位，再时钟，最后才是软件**。

## 1.12 初始化清单：从插上电到 `main()`

这份清单回答"芯片自己做了什么、我还需要确认什么"。左边是硬件自动完成的，右边是你写代码前必须心里有数的。

| 阶段 | 谁做的 | 内容 |
|---|---|---|
| 上电 | 硬件 | POR 监视 VDDANA，电源不稳就一直保持复位 |
| 上电 | 硬件 | 三路外部电源必须同压；VDDCORE 由内部稳压器产生约 1.2 V |
| 复位释放 | 硬件 | 振荡器起振，GCLK0 用 **OSC8M ÷ 8 = 1 MHz** 作为主时钟 |
| 复位释放 | 硬件 | I/O 引脚全部三态；除 `GCLK_WDT` 外，通用时钟都关着 |
| 复位释放 | 硬件 | 部分同步时钟已开，所以桥 A / 桥 B 的外设**复位后就能写** |
| 复位释放 | 硬件 | 从 `0x0000_0000` 读栈顶和复位向量，跳到启动代码 |
| 复位释放 | 硬件 | 自动校准行里的值被装载进 ADC / OSC32K / USB / DFLL 的校准寄存器 |
| 进 `main()` 前 | 你的启动代码 | 复制 `.data`、清零 `.bss`、设置 `SystemInit()`（DFP 的 `SystemInit()` 是空的） |
| 进 `main()` 后 | **你** | 确认型号与容量（DSU.DID / NVMCTRL.PARAM） |
| 进 `main()` 后 | **你** | 读一次 `PM.RCAUSE`，区分冷启动和看门狗救回 |
| 进 `main()` 后 | **你** | 按需提频到 48 MHz（第 5 章）；不提频就按 1 MHz 估时间 |
| 进 `main()` 后 | **你** | 桥 C 的外设要先开 APB 时钟再配置（第 2 章） |
| 进 `main()` 后 | **你** | 按 1.6.2 的表确认要用的外设在这颗型号上真的存在 |

## 1.13 思维模型

```text
ATSAMD21G18A
├─ 内核  Cortex-M0+ @48MHz，无 FPU，单周期乘法，28 条中断线，MTB 跟踪
├─ 存储  Flash 0x0000_0000（256KB = 4096×64B，按 256B 行擦除）｜ SRAM 0x2000_0000（32KB）
├─ 电源  1.62–3.63V 同压进 VDDIN / VDDIO / VDDANA ──▶ 内部稳压器出 VDDCORE 1.2V
├─ 复位  POR / BOD33 / BOD12 / RESET / WDT / SYSRESETREQ ──▶ PM.RCAUSE
└─ 外设  桥A 0x4000_0000 电源时钟 ｜ 桥B 0x4100_0000 存储引脚调试 ｜ 桥C 0x4200_0000 应用外设
```

图中的存储那一行还有两条本章提过的信息：**G18A 没有 RWWEE 区**，掉电保存的数据只能占用主阵列末尾；而 BOOTPROT、BOD33 默认值这类上电配置存在 NVM User Row（`0x0080_4000`），改完要复位才生效。

一句话读这张图：**左边一列是"它是什么"，右边一列是"我能在哪找到它"**。任何一次"某个功能不工作"，都是沿着这张图从上往下找——先问电和复位，再问时钟，最后才问外设。

## 1.14 常见问题

| 现象 | 原因 | 怎么查 |
|---|---|---|
| 写外设寄存器毫无反应 | 该外设在桥 C，APB 时钟复位后是关的 | 打开 `PM.APBCMASK` 对应位（第 2 章） |
| 写外设寄存器没反应，而且是这颗型号没有的外设 | 型号变体不支持（TC6/TC7、SERCOM4/5、TCC3…） | 拿型号查 Table 2-1 和 1.6.2 的表 |
| 延时函数比预期慢 48 倍 | 忘了复位后主频只有 1 MHz | 按 1 MHz 重算，或第 5 章提到 48 MHz |
| 别人说这颗是 64 脚，我的板子数出来只有 48 个 | 型号里的 **G 就是 48 脚**；64 脚是 `ATSAMD21J18A` | 看丝印最后几位；`DSU.DID` 也能报出 DEVSEL |
| 想用 EEPROM 保存数据，找不到 RWWEE 区 | 256 KB 型号没有 RWWEE，A 变体也没有 | 只能用主阵列末尾做模拟（第 13 章） |
| 写数据时程序卡顿几毫秒 | 写主阵列会 stall AHB 总线，取指被拖住 | 这是没有 RWWEE 的代价；避开在实时路径上写 Flash |
| 芯片完全不启动，量 VDDCORE 是 0 V | VDDCORE 电容漏放/短路，或误把它当输入接了 3.3 V | VDDCORE 只接 1 µF 到地，**不给外部供电** |
| 调试器连不上 | SWDIO/SWCLK 被当成普通 I/O 用了，或 `RESET` 被拉低 | `SWCLK = PA30`、`SWDIO = PA31`；量 `RESET` 电平 |
| 上电时 LED 闪一下 | 先打开输出驱动、后设输出电平 | 先写 `OUTSET`/OUT 寄存器，再写 `DIRSET` |
| 编译报错找不到 `PORT_REGS` | DFP 版本不同，寄存器命名风格不同 | 以你工程里 DFP 头文件为准（见 1.11.2 的注） |

## 1.15 寄存器速查

| 寄存器 | 绝对地址 | 位 / 字段 | 作用 |
|---|---|---|---|
| `DSU.DID` | `0x4100_2018` | 31:28 PROCESSOR、27:23 FAMILY、21:16 SERIES、15:12 DIE、11:8 REVISION、7:0 DEVSEL | 只读，芯片身份证；G18A = `0x10010305` |
| `NVMCTRL.PARAM` | `0x4100_4008` | 31:20 RWWEEP、18:16 PSZ、15:0 NVMP | 只读，Flash 页数与页大小编码 |
| `PM.RCAUSE` | `0x4000_0438` | bit6 SYST、bit5 WDT、bit4 EXT、bit2 BOD33、bit1 BOD12、bit0 POR | 只读，最近一次复位源；复位值 `0x01` |
| `SYSCTRL.BOD33` | `0x4000_0834` | ENABLE / HYST / ACTION / LEVEL / RUNSTDBY | VDDANA 欠压检测器的阈值与动作 |
| `PAC0/1/2.WPCLR` | `0x4000_0000` / `0x4100_0000` / `0x4200_0000` | 每桥一个 | 解除该外设的写保护（第 2 章） |

DFP 里对应的结构体宏：`DSU_REGS`、`NVMCTRL_REGS`、`PM_REGS`、`SYSCTRL_REGS`、`PAC0_REGS` / `PAC1_REGS` / `PAC2_REGS`。

外设基址速查见 1.6.1 节的表；三条桥的完整清单见 1.7.1 节的表。

## 1.16 最重要的几句话

1. **型号就是规格书**：`ATSAMD21G18A` 里的 `G` 是 48 脚、`18` 是 256 KB Flash（同时意味着 32 KB SRAM）、`A` 是没有 RWWEE 的基础变体。
2. **家族上限不等于你这颗芯片**：手册里的 "up to 20 通道 ADC"、"up to 5 个 TC" 是 J 型号的数字；G18A 是 14 通道 ADC、TC3–TC5。
3. **变体字母决定功能**：TC6/TC7 只有 J 有，SERCOM4/5 只有 G/J 有，TCC3 只有 D21x17D 有，AC1 只有 L 有。
4. **电源是四路一个电压**：VDDIN / VDDIO / VDDANA 必须同压（1.62–3.63 V），VDDCORE 是内部稳压器的 1.2 V 输出，只接 1 µF 电容，不给外部供电。
5. **复位后主频只有 1 MHz**（OSC8M ÷ 8），I/O 全部三态，CPU 从 `0x0000_0000` 取指。
6. **复位源分电源类和用户类**：POR / BOD33 / BOD12 会连调试逻辑一起复位，外部复位、看门狗和软件复位不会；去 `PM.RCAUSE` 看是谁干的。
7. **外设被分成三条 64 KB 的桥**：桥 A 和桥 B 复位后总线时钟就开着，桥 C 全关——这就是"配了没反应"的第一嫌疑人。
8. **和 STM32F0 最大的差别是"拆开"**：时钟拆成 SYSCTRL / GCLK / PM，写保护拆成 PAC，引脚复用拆成 PORT 的 4 位 PMUX。

## 1.17 自测

1. `ATSAMD21G18A-MUT` 这串型号里的 `G`、`18`、`A`、`M` 分别是什么意思？SRAM 有多大，为什么？
2. 为什么 256 KB 的 G18A 没有 RWWEE 区？没有它，做"掉电保存数据"会付出什么代价？
3. VDDIN、VDDIO、VDDANA、VDDCORE 分别给谁供电？哪一个不能接外部电源，为什么？
4. 复位之后，主频是多少？I/O 引脚处于什么状态？CPU 从哪里取第一条指令？
5. 手册 Features 里写着"up to 20-channel ADC"和"up to five 16-bit TC"，G18A 实际是多少？这个差别在代码里会表现成什么现象？
6. 一段代码要读 `DSU.DID` 和 `NVMCTRL.PARAM`，需要先配时钟吗？为什么？

## 附录：以后查手册怎么找

手册 4900 多页，不要通读。按下面这张"问题 → 章节"的对照表直接跳：

| 我想知道 | 查手册哪里 |
|---|---|
| 这颗型号有哪些外设、Flash/SRAM 多大 | 第 2 章 **Table 2-1**（Configuration Summary） |
| 型号字母怎么读 | 第 3 章 **Ordering Information** |
| 引脚号和封装 | 第 5 章 Pinout、第 6 章 Signal Descriptions |
| 某个引脚能复用成什么 | 第 7 章 **Table 7-1/7-2**（I/O Multiplexing），注意表下的脚注 |
| 电源怎么接、怎么上电、复位源 | 第 8 章 Power Supply and Start-Up Considerations |
| 各区域的地址 | 第 10.2 节 **Table 10-1**（Physical Memory Map） |
| 内核配置、MTB、总线矩阵 | 第 11 章 Processor And Architecture |
| **外设基址、中断号、挂在哪条桥** | 第 12 章 **Table 12-1**（Peripherals Configuration Summary） |
| 某个寄存器在哪个偏移、位定义 | 该外设章节最后的 **Register Summary** + Register Description |
| 电气参数（电压、电流、上升率、去耦） | 第 37 章 Electrical Characteristics |
| 原理图参考电路 | 第 45 章 Schematic Checklist |

查一个寄存器的标准四步（第 2 章还会再走一遍）：

```text
① 找外设章节 → ② 翻到 Register Summary 看 Offset → ③ 翻到该寄存器的 Register Description
→ ④ 读表头的 Property（PAC 保护？同步？）和每一位的定义
```

最后一条经验：**手册里的 "up to" 和表下的脚注，比正文更值得读**。本章里 TC6/TC7、SERCOM4/5、TCC3 的可用性全都藏在脚注里——正文说"最多 5 个 TC"，脚注才告诉你 G 只有 3 个。

## 本章结束

学完这一章，你应该能把 `ATSAMD21G18A` 这串字符翻译成人话：**Cortex-M0+ 内核、48 MHz、没有浮点单元、256 KB Flash（4096 页 × 64 字节）、32 KB SRAM（没有 RWWEE）、48 个引脚里 38 个能当 I/O、14 个能接 ADC，供电 1.62–3.63 V 三路同压，外设分住在三条 64 KB 的桥上。**

再往下一层：拿到任何一块 SAM D21 板子，先按 1.9 节的七步确认一遍——**电源、复位、时钟、调试口**，然后才轮到外设。顺序错了，后面每一步看起来都像坏了。
