# 第 10 章 模拟外设：ADC、DAC、AC

> **芯片**：ATSAMD21G18A（48 脚 / 38 个 I/O，ADC 在 48 脚上只有 14 路输入）
> **本章目标**：搞清"电压"怎么变成"数字"、数字怎么变回电压、两个电压怎么比较；知道 ADC/DAC/AC 各有哪些开关、一次转换按什么顺序配置；最后做出"电位器 → LED 条"和"DAC 锯齿波"两个实验。
> **学习顺序**：模拟与数字的区别 → ADC 的能力 → 通道与引脚 → 参考电压 → 单端/差分 → 后处理开关 → 采样时间 → 时钟 → 单次/自由运行 → 窗口监控 → DAC → AC → 配置顺序 → 寄存器 → 例程 → 初始化清单 → 思维模型 → 常见问题 → 速查 → 自测。

## 10.0 模拟外设三兄弟：一个测、一个造、一个比

前面九章处理的全是"只有高和低两种状态"的信号：GPIO 读电平、串口收字节、定时器数节拍。但真实世界里的温度、光照、电位器位置、电池电压，都是**连续变化的电压**——它可以是 0 到 3.3 V 之间的任何一个值，而不是只有 0 和 1。这一章讲的三个外设就是数字世界和模拟世界之间的三座桥：

| 外设 | 全称 | 一句话 | 引脚 |
|---|---|---|---|
| ADC | Analog-to-Digital Converter | 把引脚上的电压测成一个数字 | AIN[x] 与 VREFA/VREFB |
| DAC | Digital-to-Analog Converter | 把一个数字变成引脚上的电压 | VOUT（本器件是 PA02） |
| AC | Analog Comparator | 比较两个电压谁大，只给 1 bit 结果 | AIN[3:0]（PA04–PA07） |

为什么需要三个而不是一个？因为用途完全不同：**ADC 是"测量"，DAC 是"输出"，AC 是"判断"**。比如"电池低于 3.0 V 就报警"这件事，用 ADC 做是"每 10 ms 测一次再比较"，用 AC 做是"硬件一直盯着，越线立刻给一个信号"——后者不占 CPU，但只知道"过线了"，不知道具体电压。

### 数字量的最小刻度：LSB

ADC 不可能表达无穷多个电压，它只能输出**有限个整数**。12 位 ADC 把量程切成 2¹² = 4096 份，每一份叫一个 **LSB（Least Significant Bit，最低有效位）**：

```text
LSB = VREF / 4096       读数 → 电压：V = 读数 × VREF / 4096
0 V ──────────────────────────────────────── VREF
 0    1    2    3  …                       4095   ← 读数（12 位）
```

| 参考电压 VREF | 1 个 LSB | 读数 4095 对应 |
|---|---|---|
| 1.000 V（内部 INT1V） | 0.244 mV | 0.9998 V |
| 2.230 V（VDDANA/1.48，3.3 V 供电） | 0.545 mV | 2.229 V |
| 3.300 V（外部 VREFA 接 3.3 V） | 0.806 mV | 3.299 V |

两个直接结论：**参考电压决定了量程和精度**（同一个电压，参考越小分辨率越高，量程越窄）；**输入超过参考只会读到 4095，低于 0 只会读到 0**，超范围不会报错，只会"顶格"。

## 10.1 ADC 的能力：手册给了六件事

手册 33.2 列了很多，本章只需要记住这六条（每条后面都对应一个开关）：

| 能力 | 数字 | 由谁控制 |
|---|---|---|
| 分辨率 8 / 10 / 12 位（默认 12） | 12 位 = 4096 档 | `CTRLB.RESSEL` |
| 速度 | 最高 350 ksps（每秒 35 万次） | `CTRLB.PRESCALER` + GCLK |
| 单端或差分测量 | 差分可以测负电压 | `CTRLB.DIFFMODE`、`INPUTCTRL.MUXNEG` |
| 增益级 1/2× ~ 16× | 小信号放大后再量化 | `INPUTCTRL.GAIN` |
| 硬件过采样（平均到 16 位） | 牺牲速度换分辨率 | `AVGCTRL.SAMPLENUM/ADJRES` |
| 自动失调与增益补偿 | 修正零点偏移和增益误差 | `CTRLB.CORREN` + `GAINCORR/OFFSETCORR` |

还有几条顺带知道即可：内部通道可以直接测**温度**（`MUXPOS = 0x18`）、**带隙基准**（0x19）、**1/4 核心电压**（0x1A）、**1/4 I/O 电压**（0x1B）、**DAC 输出**（0x1C）——用内部通道时采样时间要按手册要求单独设（`SAMPCTRL`）。

350 ksps 不是"每秒能问 35 万次"，而是**模拟部分转换一次需要固定几个时钟**：1× 增益下一次转换占 6 个 `CLK_ADC`（手册 Table 37-24 注 3），所以 2.1 MHz ÷ 6 ≈ 350 k 次/秒。这也解释了为什么分辨率、增益、平均次数都会让转换变慢——350 ksps 是"12 位、1× 增益、单端、不平均"这条最理想路径上的数字。

## 10.2 通道数随封装变化：48 脚只有 14 路

ADC 内部最多能选 20 个模拟输入（`AIN[19:0]`），但**这些 AIN 要落到芯片引脚上才算数**。E=32 脚、G=48 脚、J=64 脚三种封装引出的路数完全不同，手册 Table 2-1 的 "ADC Channels" 一列写得很清楚：**E = 10 路、G = 14 路、J = 20 路**。本章用的 ATSAMD21G18A 是 48 脚，能用的 14 路在这里（脚号按 QFN48/TQFP48，来自手册 Table 7-1）：

| AIN | 引脚 | 脚号 | 供电域 | 备注 |
|---|---|---|---|---|
| AIN[0] | PA02 | 3 | VDDANA | 同时是 DAC 的 VOUT |
| AIN[1] | PA03 | 4 | VDDANA | 同时是 VREFA（外部参考输入） |
| AIN[2] | PB08 | 7 | VDDANA | |
| AIN[3] | PB09 | 8 | VDDANA | |
| AIN[4] | PA04 | 9 | VDDANA | 同时是 VREFB，也是 AC 的 AIN[0] |
| AIN[5] | PA05 | 10 | VDDANA | 也是 AC 的 AIN[1] |
| AIN[6] | PA06 | 11 | VDDANA | 也是 AC 的 AIN[2] |
| AIN[7] | PA07 | 12 | VDDANA | 也是 AC 的 AIN[3] |
| AIN[16] | PA08 | 13 | VDDIO | 和上面几路不同域，精度指标不一样 |
| AIN[17] | PA09 | 14 | VDDIO | |
| AIN[18] | PA10 | 15 | VDDIO | |
| AIN[19] | PA11 | 16 | VDDIO | |
| AIN[10] | PB02 | 47 | VDDANA | |
| AIN[11] | PB03 | 48 | VDDANA | |

**48 脚上用不到的**：AIN[8]（PB00）、AIN[9]（PB01）、AIN[12]–AIN[15]（PB04–PB07）——这 6 个信号只引到 64 脚封装上。所以 `MUXPOS` 写 0x0C 不会报错，只是永远读不到你要的东西。写代码前先查这张表，是 ADC 最容易踩的坑之一。另外 PA08–PA11 挂在 VDDIO 域、其余十路挂在 VDDANA 域，手册 Table 37-28 注 3 明确说这四路的 ADC 性能与其他路不同。

## 10.3 模拟输入怎么落到引脚上：功能 B，以及为什么要关掉数字功能

第 2、4 章讲过：一个引脚默认归 GPIO 管，要让外设接管，得打开 `PINCFG.PMUXEN` 并在 `PMUX` 里选一个功能（A~H）。模拟外设有一条**全器件统一的规矩**，手册 Table 7-1 的注 2 写得很直白：**All analog pin functions are on peripheral function B. Peripheral function B must be selected to disable the digital control of the pin.** 翻译过来有两层意思，第二层比第一层重要：

| 层 | 含义 | 怎么做到 |
|---|---|---|
| ① 选功能 B | 模拟信号要从引脚进到 ADC，必须让 PMUX 选 B | `PINCFG.PMUXEN = 1`、`PMUX = B` |
| ② 关掉数字控制 | 选了 B 之后，该脚的数字输入缓冲和输出驱动被断开 | 不要再去开 `INEN`、`PULLEN`、`DIR` |

为什么必须关掉数字功能？因为一个 CMOS 输入缓冲就是一对互补 MOS 管，输入电压停在 VDD/2 附近时**上下管同时半导通**，会从电源抽电流；模拟信号恰恰经常停在这类中间电平上。更糟的是输入缓冲会把引脚噪声整形成方波，反过来通过电源和衬底干扰模拟测量。数字输出驱动更危险：如果 ADC 工作时这个脚还在输出，引脚电压会被强行拉到 0 或 VDD，测出来的就是你自己的输出而不是外部电压。

```text
配错了（引脚仍是数字输入）         配对了（功能 B）
  AIN ──┬── 输入缓冲 ── 数字逻辑     AIN ──┬──       （数字侧断开）
        │  ↑ 中间电平时耗电、噪声        │
      ADC ──┘ （还读得到，但脏）      ADC ──┘ （干净）
```

所以配一个 ADC 引脚只有两句话，而且**没有** `DIRCLR`、**没有** `INEN`：

```c
PORT_REGS->GROUP[0].PORT_PINCFG[5] = PORT_PINCFG_PMUXEN_Msk;   /* PA05：只留复用使能，数字属性全清 0 */
PORT_REGS->GROUP[0].PORT_PMUX[2] = PORT_PMUX_PMUXO_B;          /* PMUX[2] 高半字节 = PA05，选功能 B */
```

注意 `PMUX[n]` 一个字节管**两个**引脚：低半字节（`PMUXE`）是 2n，高半字节（`PMUXO`）是 2n+1。所以上面这句顺带把 PA04 的复用清成了功能 A——如果 PA04 另有用途（比如当 VREFB），就要读—改—写而不是直接赋值。

## 10.4 参考电压：读数在跟谁比

ADC 输出的是"输入电压是参考电压的几分之几"，所以**参考电压选错，全部读数都是错的**，而且错得很隐蔽（数字看起来完全正常）。`REFCTRL.REFSEL` 一共四档有用（手册 Table 33-5）：

| `REFSEL` | 代码宏 | 参考电压 | 3.3 V 供电时的量程 | 什么时候用 |
|---|---|---|---|---|
| 0x0 | `ADC_REFCTRL_REFSEL_INT1V` | 内部 1.0 V | 0 – 1.0 V | 测小信号；不受电源波动影响 |
| 0x1 | `ADC_REFCTRL_REFSEL_INTVCC0` | VDDANA / 1.48 | 0 – 2.23 V | 3.3 V 供电下最常用的折中 |
| 0x2 | `ADC_REFCTRL_REFSEL_INTVCC1` | VDDANA / 2 | 0 – 1.65 V | 只在 VDDANA > 2.0 V 时可用 |
| 0x3 | `ADC_REFCTRL_REFSEL_AREFA` | 外部 VREFA 引脚 | 由外部决定（1 V ~ VDDANA−0.6 V） | 要高精度、要满量程 |
| 0x4 | `ADC_REFCTRL_REFSEL_AREFB` | 外部 VREFB 引脚 | 同上 | 同上 |

**这里有个新手必踩的坑：ADC 没有"VDDANA 直接当参考"这一档。** 想测 0–3.3 V 的电位器，最接近的是 `INTVCC0`（2.23 V），超过 2.23 V 的部分全部顶到 4095；要真正量满 3.3 V，只有接外部基准到 VREFA（本器件是 PA03），或者把信号分压后再测。

内部 1.0 V 参考（INT1V）是手册特别标注的：它由内部 1.1 V 带隙基准缓冲而来，**与电源无关**，测"绝对电压"时比 VDDANA 派生的参考更稳；反过来，如果是"分压比"型传感器（电位器、应变片），信号本身就跟电源成比例，用 VDDANA 派生的参考反而能抵消电源波动。`REFCTRL` 的最高位是 `REFCOMP`：打开**参考缓冲的失调补偿**，增益级精度更好，代价是输入阻抗下降、参考建立时间变长；用增益（`GAIN > 1`）时建议打开。最后一条硬规矩（手册 33.6.2.1）：**换过参考电压之后，第一次转换的结果不能用，必须丢掉。**

## 10.5 单端还是差分：什么时候用哪个

ADC 有两个输入端：正端 `MUXPOS`、负端 `MUXNEG`。区别只在负端接什么：

| 模式 | 负端 | 结果含义 | 用在哪 |
|---|---|---|---|
| 单端（默认） | `MUXNEG = GND`（内部地） | 输入对地的电压，0 ~ VREF，12 位全部有效 | 绝大多数测量 |
| 差分 | `DIFFMODE = 1`，负端选一个 AIN 引脚 | 两个引脚之差，**可以是负数** | 电桥、热电偶、带直流偏置的小信号 |

单端模式的负端必须接一个"地"：内部 GND（`MUXNEG = 0x18`）或 I/O 地（`0x19`）。差分模式则要先在 `CTRLB` 里把 `DIFFMODE` 置 1，再把 `MUXNEG` 选成另一个 AIN 引脚（`0x00`–`0x07`）。差分模式的读数**是二进制补码（有符号）**：12 位模式下 bit11 就是符号位，`0x800` 表示 −2048、`0xFFF` 表示 −1。手册在窗口监控一节写明了这一点（"差分模式下第 8 位被当作符号位"）。所以读差分结果一定要做**符号扩展**，否则负数会变成 2048~4095 的正数：

```c
/* 12 位差分结果：RESULT[11:0] 是补码，bit11 是符号位 */
static int16_t adc_result_signed(uint16_t raw)
{
    int32_t v = (int32_t)raw;
    if ((v & 0x800) != 0) { v -= 0x1000; }   /* bit11 = 1 → 减去 2^12，得到负数 */
    return (int16_t)v;
}
```

单端模式没有这个问题，但**别把差分结果当无符号用**——这是"读数莫名其妙变成 4000 多"的最常见原因。

## 10.6 三个数字后处理开关：增益、平均、校正

模拟部分把电压变成码之后，数字部分还能再加工。三个开关分别在 `INPUTCTRL`、`AVGCTRL`、`CTRLB` 里，理解它们只要抓住"拿什么换什么"：

| 开关 | 拿什么换什么 | 关键寄存器 |
|---|---|---|
| 增益级 | 用量程换灵敏度（小信号放大 2~16 倍） | `INPUTCTRL.GAIN` |
| 平均 / 过采样 | 用速度换分辨率和信噪比 | `AVGCTRL` + `CTRLB.RESSEL` |
| 失调与增益校正 | 用一点点延迟换绝对精度 | `CTRLB.CORREN`、`GAINCORR`、`OFFSETCORR` |

### 增益：`GAIN[3:0]`

| `GAIN` | 0x0 | 0x1 | 0x2 | 0x3 | 0x4 | 0xF |
|---|---|---|---|---|---|---|
| 倍数 | 1× | 2× | 4× | 8× | 16× | 1/2× |

增益放大的是**输入信号**，所以有效量程变成 `VREF / GAIN`：VREF 取 1.0 V、GAIN = 16× 时，满量程输入只有 62.5 mV，一个 LSB 相当于 15 µV。这就是"用分辨率换量程"——但它对信号本身的噪声、失调也一视同仁地放大，所以 16× 只适合源阻抗低、信号干净的小信号（比如分流电阻上的压降）。

### 平均与过采样：12 位怎么变成 16 位

`AVGCTRL.SAMPLENUM` 让硬件连做 2~1024 次转换并把结果累加，`ADJRES` 决定最后右移几位。手册 Table 33-4 给了"过采样 + 抽取"的标准配方，**要多少位就按这张表填**：

| 想要的分辨率 | 累加次数 `SAMPLENUM` | `ADJRES` | `RESSEL` | 代价 |
|---|---|---|---|---|
| 12 位（不平均） | 0x0（1 次） | 0x0 | 12BIT | — |
| 13 位 | 0x2（4 次） | 0x1 | 16BIT | 速度 ÷4 |
| 14 位 | 0x4（16 次） | 0x2 | 16BIT | 速度 ÷16 |
| 15 位 | 0x6（64 次） | 0x1 | 16BIT | 速度 ÷64 |
| 16 位 | 0x8（256 次） | 0x0 | 16BIT | 速度 ÷256 |

两个必须记住的细节：**只要累加次数 ≥ 2，`RESSEL` 就必须改成 `16BIT`（= 0x1）**，因为中间结果会超过 12 位；**平均是把 `SAMPLENUM` 次转换的时间加在一起**，所以 1.5 MHz 的 `CLK_ADC` 下做 256 次平均，一次"读数"要等几十毫秒。

### 自动失调与增益补偿：`CORREN`

真实的 ADC 有零点偏移（输入 0 V 时读到的不是 0）和增益误差（满量程不是 4095）。`GAINCORR` 是一个 1 位整数 + 11 位小数的系数（0.5 ~ 2.0），`OFFSETCORR` 是 12 位补码的偏移量，硬件在 `CTRLB.CORREN = 1` 时按 **RESULT = (转换值 − OFFSETCORR) × GAINCORR** 先修正再写进 `RESULT`。代价是每次转换多 13 个 `CLK_ADC` 的延迟（自由运行模式下只有第一次付出这个代价）。这两个值不是拍脑袋填的：要用准确的基准电压测出实际偏移和满量程再反算——本章只讲"它存在、怎么打开"，标定时再回来看手册 33.6.10。

## 10.7 采样时间与输入阻抗：为什么采样时间不够读数会偏

ADC 并不是"一直看着"输入电压，而是**先采样、再转换**：内部有一个采样保持电容 `C_SAMPLE`，采样阶段把它和输入引脚接通、充电到输入电压，然后断开，转换阶段再慢慢量化这个电容上的电压。

```text
   R_SOURCE        R_SAMPLE
AIN ──[外部源阻抗]──[ADC 内部串联电阻]──┬── C_SAMPLE ── GND
                                       └─（转换时断开，量这个电压）
```

于是采样就是一个 RC 充电过程。要充到 n 位精度，手册 37.11.4.3 给出最小采样时间 `t_SAMPLE ≥ (R_SAMPLE + R_SOURCE) × C_SAMPLE × (n+1) × ln2`，12 位时系数是 9.02；其中 `R_SAMPLE` 最大 3.5 kΩ、`C_SAMPLE` 典型 3.5 pF（手册 Table 37-24）。**充电没充满就去转换，测到的电压一定偏低**（电容是从低往高充的），而且这个偏差跟前一次测的是哪个通道有关——这就是"读数偏小、还跟着上一个通道跑"的原因。

| 信号源 | R_SOURCE | 需要的采样时间 | 1.5 MHz 下的 `SAMPLEN` |
|---|---|---|---|
| 10 kΩ 电位器 | 最坏 2.5 kΩ | (3.5k+2.5k)×3.5p×9.02 ≈ 190 ns | 0 就够 |
| 100 kΩ 分压电阻 | 25 kΩ | (3.5k+25k)×3.5p×9.02 ≈ 900 ns | ≥ 2 |
| 直接接高阻传感器 | 100 kΩ | (3.5k+100k)×3.5p×9.02 ≈ 3.3 µs | ≥ 9 |

采样时间就是 `SAMPCTRL.SAMPLEN`：**采样时间 = (SAMPLEN+1) × `CLK_ADC` / 2**，单位是"半个 `CLK_ADC` 周期"。所以它由两个旋钮共同决定：`SAMPCTRL.SAMPLEN`（1~64 个半周期）和 `CTRLB.PRESCALER`（决定 `CLK_ADC` 有多快）。三个实用结论：**源阻抗越大，采样时间就要越长**；**增益 ≥ 4× 时手册的 Delay Gain 表会额外增加内部延时**，等于采样窗口更紧；**实在不能加长采样时间，就在引脚外面加一个运放缓冲器（电压跟随器）**，把源阻抗降到几十欧姆——这是模拟电路的通用解法。

## 10.8 CLK_ADC 从哪来：一次转换要多久

ADC 的时钟链是 **GCLK（第 5 章）→ `CTRLB.PRESCALER` → `CLK_ADC`**。手册规定 `f_CLK_ADC` 必须在 **30 kHz ~ 2.1 MHz** 之间（Table 37-24）：太慢没意义，太快模拟部分来不及比较，结果会失真。

| `PRESCALER` | 宏 | 48 MHz 下的 `CLK_ADC` | 能不能用 |
|---|---|---|---|
| DIV4 | `ADC_CTRLB_PRESCALER_DIV4` | 12 MHz | ✗ 超标 5.7 倍 |
| DIV16 | `ADC_CTRLB_PRESCALER_DIV16` | 3 MHz | ✗ 超标 |
| DIV32 | `ADC_CTRLB_PRESCALER_DIV32` | 1.5 MHz | ✓ 本章用这档 |
| DIV64 | `ADC_CTRLB_PRESCALER_DIV64` | 750 kHz | ✓ |
| DIV128 | `ADC_CTRLB_PRESCALER_DIV128` | 375 kHz | ✓ 更省电 |
| DIV512 | `ADC_CTRLB_PRESCALER_DIV512` | 93.75 kHz | ✓ 最慢 |

如果还没做第 5 章、芯片还跑在复位默认的 1 MHz，DIV32 会得到 31.25 kHz——勉强在手册下限之上，但一次转换要几百微秒，这时应该改用 DIV4（250 kHz）。**换主频之后回来检查一遍预分频，是 ADC 出问题的常见原因。**

## 10.9 单次转换与自由运行

ADC 有两种起跑方式，区别只在"谁按开始键"：

| 模式 | 怎么开始 | 结束标志 | 用在哪 |
|---|---|---|---|
| 单次（默认） | 每次都要写 `SWTRIG.START = 1` | `INTFLAG.RESRDY` 置 1，结果在 `RESULT` | 按需测量、多通道轮询 |
| 自由运行（`CTRLB.FREERUN = 1`） | 使能后自己一直转，上一次结束下一次立刻开始 | 每次转换完都置 `RESRDY` | 连续监测一个通道 |

单次模式最需要注意的是**"结果被覆盖"**：`RESULT` 只有一个，上一次没读走就被新的覆盖了，硬件会在 `INTFLAG.OVERRUN` 里记一笔。多通道轮询时（换通道要写 `INPUTCTRL`，属于写同步寄存器），读到结果就要马上处理。自由运行模式在手册里还有一个附带好处：33.6.10 说校正带来的 13 个 `CLK_ADC` 延迟**只在第一次转换发生**，因为后续转换的间隔本来就比转换时间长。

> 想让转换由定时器精确触发、或者让结果自动搬进内存，那是事件系统和 DMA 的活（第 11 章）；ADC 这边只需要打开 `EVCTRL.STARTEI`（事件触发启动）或 `EVCTRL.RESRDYEO`（转换完发一个事件）。中断的通用机制见第 12 章。

## 10.10 窗口监控：不用 CPU 也能盯着阈值

"电压超出某个范围就做点什么"是模拟测量的常见需求。如果靠软件轮询，就得不停地读、不停地比；ADC 自带**窗口监控**，硬件替你比，符合条件才置一个标志：`WINLT`/`WINUT` 是上下限，`WINCTRL.WINMODE` 选命中条件：

| `WINMODE` | 0x0 | 0x1 | 0x2 | 0x3 | 0x4 |
|---|---|---|---|---|---|
| 命中条件 | 关闭 | `RESULT > WINLT` | `RESULT < WINUT` | 落在窗口内 | 落在窗口外 |

命中时置 `INTFLAG.WINMON`；打开 `INTENSET.WINMON` 就能产生中断（第 12 章），打开 `EVCTRL.WINMONEO` 就能产生事件（第 11 章）——**完全不用 CPU 参与**。注意：差分模式下 `WINLT`/`WINUT` 按**有符号**比较，单端模式下按无符号比较；上下限的有效位数跟着 `CTRLB.RESSEL` 走（8 位模式下只比较低 8 位）。

## 10.11 DAC：10 位，一个 VOUT 引脚

DAC 是 ADC 的镜像：给定一个数字，在引脚上输出对应电压。本器件只有一路，10 位，输出引脚固定是 **PA02**（手册 Table 7-1 的 VOUT 列），关系是 `VOUT = DATA / 0x3FF × VREF`：DATA 写 0x200、参考取 VDDANA（3.3 V）时，输出约 1.65 V。

| 特性 | 数值 | 出处 |
|---|---|---|
| 分辨率 | 10 位（1024 档） | 手册 35.2 |
| 刷新率 | 最高 350 ksps | Table 37-33 |
| 启动时间 | 最大 2.85 µs（VDDANA > 2.6 V） | Table 37-33 |
| 线性输出范围 | 0.05 V ~ VDDANA − 0.05 V | Table 37-32 |
| 负载能力 | 阻性 ≥ 5 kΩ，容性 ≤ 100 pF | Table 37-32 |

"线性输出范围"要留意：DAC 的上下两端各有 50 mV 左右的非线性区，所以**"输出 0 V"和"输出 3.3 V"实际都做不到**，锯齿波的两端会稍微削一点；要驱动真正的负载（扬声器、电机）还得加运放，它只能给出很小的电流。

`CTRLB` 里有四个开关各管一件事：`REFSEL` 选参考（内部 1.0 V / VDDANA / 外部 VREFA），`EOEN` 打开**外部输出缓冲**（不打开 VOUT 引脚上什么都没有），`IOEN` 把 DAC 输出**内部**送给 AC 当参考（不占用引脚），`LEFTADJ` 决定 10 位数据在 `DATA` 里怎么摆：右对齐（默认）放在 `DATA[9:0]`，左对齐放在 `DATA[15:6]`，这样只写高 8 位就相当于用 8 位精度输出。写 `DATA` 就是"开始转换"——DAC 没有"转换完成"标志，手册明确要求**两次写入的间隔不能小于转换时间**（35.6.2.4），所以软件里要自己保证节奏。

## 10.12 AC：一个模块，两个比较器

比较器是"1 位 ADC"：正端比负端高就输出 1，否则输出 0。它的价值不在精度，而在**快且不占 CPU**：传播延迟典型 60 ns（高速档），可以一直开着盯着电压，配合中断或事件在越线的一瞬间就响应。本器件的 AC 只有**一个模块**（基址 `0x42004400`），里面**两个比较器 COMP0 / COMP1**，没有 AC1（AC1 只存在于 L 变体器件上，手册 34.1 的注写得很清楚）。两个比较器可以各干各的，也可以配成一对做**窗口比较**。

| 部件 | 说明 |
|---|---|
| 正端 `MUXPOS` | 只能从 4 个引脚里选（PIN0–PIN3，本器件即 PA04–PA07 上的 AIN[3:0]） |
| 负端 `MUXNEG` | 4 个引脚之一，或 GND、VDD 分压器、内部带隙、DAC 输出 |
| VDD 分压器 `SCALERx` | 64 级：`VSCALE = VDDANA × (VALUE+1) / 64` |
| 结果 | `STATUSA.STATEx`（1 = 正端更高），`STATUSB.READYx` 表示结果已就绪 |
| 窗口状态 | `STATUSA.WSTATE0`：ABOVE / INSIDE / BELOW |

负端那五种内部来源是 AC 最有用的地方——不用外接基准就能造出门限：`GND`（过零检测）、`VSCALE`（64 级分压器设任意门限）、`BANDGAP`（1.1 V 带隙，与电源无关）、`DAC`（想要多细就多细，还能动态改）。门限算例：VDDANA = 3.3 V，想让"低于 1.65 V 报警"，就选 `MUXNEG = VSCALE` 并写 `SCALER0 = 31`，因为 3.3 × (31+1)/64 = 1.65 V。

**窗口比较**（`WINCTRL.WEN0 = 1`）要求这一对比较器满足两个条件：正端必须选同一个引脚（两个比较器看同一个信号），负端各设一个门限（一个当上限、一个当下限）。于是两个输出合起来就有三种状态：

```text
信号 ──┬── 高于上限：COMP0 = 1     →  高于窗口（ABOVE）
        ├── 在两者之间：            →  窗口内（INSIDE）
        └── 低于下限：COMP1 = 0     →  低于窗口（BELOW）
```

`STATUSA.WSTATE0` 直接给出这三种状态，`WINCTRL.WINTSEL0` 还能选"只在进入窗口时提醒"还是"只在离开窗口时提醒"。注意两个比较器的 `SINGLE` 设置必须一致，否则窗口行为不确定。比较结果有两条出路：写 `COMPCTRLx.OUT` 把原始或同步后的结果送到 `CMP[x]` 引脚（手册 Table 7-1 的 H 列里，本器件只在 64 脚的 PB16 上标了 AC/CMP[1]，48 脚封装没有引出 CMP 引脚）；或者在 `EVCTRL` 里打开 `COMPEOx`（比较器输出事件）和 `WINEO0`（窗口事件），把结果作为事件送给别的外设（第 11 章）——**后者完全不需要 CPU 参与**，例如"电压越线就启动一次 ADC 转换"。

## 10.13 使用逻辑：ADC 的配置顺序，以及为什么是这个顺序

把前面所有硬件结论串起来，一次能用的 ADC 转换要按下面的顺序准备。每一步的位置都是硬件逼出来的，不是风格问题：

| 步骤 | 做什么 | 为什么必须在这一步 |
|---|---|---|
| ① 引脚 | 选功能 B，清掉 `INEN/PULLEN/DIR` | 引脚得先"对数字电路消失"，模拟信号才干净（10.3） |
| ② 通用时钟 | `GCLK_CLKCTRL` 配 `ID_ADC` + 等 `SYNCBUSY` | 外设没时钟，寄存器写了也不动（第 2、5 章） |
| ③ 装校准值 | `CALIB` 写 `LINEARITY_CAL` / `BIAS_CAL` | 出厂校准值不装，精度达不到手册指标 |
| ④ 参考电压 | `REFCTRL.REFSEL`（+ `REFCOMP`） | 参考决定量程；换参考后第一次结果要丢（10.4） |
| ⑤ 时钟与分辨率 | `CTRLB.PRESCALER` + `RESSEL` | `CLK_ADC` 必须落在 30 kHz~2.1 MHz（10.8） |
| ⑥ 采样时间 | `SAMPCTRL.SAMPLEN` | 由源阻抗决定，转换还没开始就要定好（10.7） |
| ⑦ 通道与增益 | `INPUTCTRL`：`MUXPOS/MUXNEG/GAIN` | 告诉 ADC 测谁、跟谁比、放大多少（10.5、10.6） |
| ⑧ 其它 | 平均 `AVGCTRL`、窗口 `WINCTRL`、校正 `CORREN` | 都是"结果怎么算"的规则，开跑前定好 |
| ⑨ 使能 | `CTRLA.ENABLE = 1` + 等 `STATUS.SYNCBUSY` | 参数都稳定了才通电，避免半配置状态跑一次 |
| ⑩ 第一次丢弃 | 启动一次并忽略结果 | 手册 33.6.2.1 的硬要求（参考刚变过） |

为什么要"先配置、后使能"？因为手册 33.6.2.1 要求**转换期间所有配置寄存器必须保持稳定**。ADC 的配置寄存器大多不是"使能保护"的（不像 DAC 的 `CTRLB`），也就是说你可以在 `ENABLE = 1` 时改它们——但它不会阻止你改出一个错误的中间状态。先使能后配置，就可能出现"用新通道 + 老参考"的一次脏转换。

## 10.14 把硬件概念翻译成寄存器

现在才看寄存器。每个寄存器都应该能回答一个明确的问题：

| 我要做什么 | SAMD21 的配置 |
|---|---|
| 让引脚进入模拟模式 | `PORT.PINCFG.PMUXEN` + `PORT.PMUX = B` |
| 给 ADC 配时钟 | `GCLK.CLKCTRL`（ID = ADC）+ `CTRLB.PRESCALER` |
| 选参考电压 / 测哪个引脚 | `REFCTRL.REFSEL` / `INPUTCTRL.MUXPOS` |
| 单端 / 差分 / 增益 | `INPUTCTRL.MUXNEG` / `CTRLB.DIFFMODE` / `INPUTCTRL.GAIN` |
| 决定采样时间 | `SAMPCTRL.SAMPLEN` + `CTRLB.PRESCALER` |
| 决定分辨率和平均 | `CTRLB.RESSEL` + `AVGCTRL.SAMPLENUM/ADJRES` |
| 启动一次转换 / 知道完事了 | `SWTRIG.START`（或 `CTRLB.FREERUN`）/ `INTFLAG.RESRDY` |
| 取结果 | `RESULT` |
| 范围报警 | `WINCTRL.WINMODE` + `WINLT` + `WINUT` → `INTFLAG.WINMON` |
| 补偿零点与增益 | `CTRLB.CORREN` + `GAINCORR` + `OFFSETCORR` |
| 输出一个电压 | DAC：`CTRLB.REFSEL/EOEN` + `CTRLA.ENABLE` + `DATA` |
| 比较两个电压 | AC：`COMPCTRLx.MUXPOS/MUXNEG` + `SCALERx` + `CTRLA.ENABLE` |

**寄存器不是新知识，它只是硬件的控制开关。** 例如"让 ADC 测 PA05 上的电压"这件事：

```text
我要测 PA05 上的电压
      ↓ 硬件上
断开数字缓冲 → 把 AIN5 接到采样电容 → 跟内部地比 → 12 位量化
      ↓ SAMD21 上
PMUXEN + PMUX=B  →  INPUTCTRL.MUXPOS = PIN5 （MUXNEG = GND）
```

**关于代码里的名字**：本章统一用 SAMD21_DFP 3.7.262 的平铺写法，网上（以及 ASF 时代）的写法指的是同一块硬件：

| 本章写法（DFP 3.7.262） | 老写法 / 网上常见 | 说明 |
|---|---|---|
| `ADC_REGS->ADC_CTRLB` | `ADC->CTRLB.reg` | 16 位，含 `PRESCALER` |
| `ADC_REGS->ADC_INPUTCTRL` | `ADC->INPUTCTRL.reg` | 32 位，四个字段挤在一起 |
| `DAC_REGS->DAC_DATA` | `DAC->DATA.reg` | 16 位 |
| `AC_REGS->AC_COMPCTRL[0]` | `AC->COMPCTRL[0].reg` | 32 位，数组形式 |

还有一条**通用陷阱**（第 5 章提过，模拟外设同样适用）：DFP 里多位字段的 `_Val` 宏是**没有移位的裸值**。比如 `ADC_INPUTCTRL_MUXNEG_GND_Val` 是 0x18，而它的 `_Pos` 是 8——直接写 `ADC_INPUTCTRL_MUXNEG_GND_Val` 会把 0x18 塞到 bit 7:0 上，**能编译，但功能完全错**。判断方法很机械：**看 `_Pos`，不为 0 就必须移位**：用 DFP 里已经移好位的宏（`ADC_INPUTCTRL_MUXNEG_GND`），或者自己写 `(_Val << _Pos)`。

## 10.15 寄存器逐个讲（ADC）

### CTRLA（0x00）：唯一的开关，和"我在忙"

`ENABLE`（bit1）是 ADC 的总开关。它和 `SWRST`（bit0）都是**写同步位**：写完立刻读回是新值，但硬件要几个时钟才真正生效，这期间 `STATUS.SYNCBUSY = 1`——写完等一下，是养成了就不出错的做法。`RUNSTDBY`（bit2）决定睡眠时 ADC 停不停（第 6 章）。

### REFCTRL（0x01）：量程和精度都从这里来

`REFSEL[3:0]` 四档有效（10.4 的表），`REFCOMP`（bit7）打开参考缓冲的失调补偿。**只在 ADC 使能前或转换间隙改**，改完第一次转换丢掉。

### CTRLB（0x04）：速度、分辨率、单端/差分

16 位，本章用到四个字段：`PRESCALER[10:8]` 分频（`CLK_ADC` = 外设时钟 ÷ 4…512）、`RESSEL[5:4]` 分辨率、`FREERUN`（bit2）自由运行、`DIFFMODE`（bit0）差分；`CORREN`（bit3）打开数字校正。这个寄存器是**写同步**的：改 `MUXPOS` 或 `PRESCALER` 之后要等 `SYNCBUSY` 归零，否则新配置可能没生效——"改了通道还在读上一个通道"多半就是这个原因。

### SAMPCTRL（0x03）与 AVGCTRL（0x02）：采样多久、平均几次

`SAMPCTRL.SAMPLEN` 是 6 位采样长度，采样时间 = `(SAMPLEN+1) × CLK_ADC / 2`；复位值 0 表示只给半个 `CLK_ADC`，源阻抗稍大就不够。手册还有一条：**如果选了内部带隙通道（`MUXPOS = BANDGAP`），必须显式写 `SAMPCTRL`**。`AVGCTRL.SAMPLENUM` 决定累加几次（1/2/4/…/1024），`ADJRES` 决定额外右移几位，要"12 位变 16 位"就照 10.6 的表填，同时把 `CTRLB.RESSEL` 设成 `16BIT`。

### INPUTCTRL（0x10）：测谁、跟谁比、放大多少

32 位挤了四个字段，本章用三个：`MUXPOS[4:0]`（bit4:0，正端）、`MUXNEG[4:0]`（bit12:8，负端）、`GAIN[3:0]`（bit27:24）。这是**写同步 + 读同步**的寄存器：写完之后要等 `SYNCBUSY`，下一个通道才真的接上。

| 字段 | 常用取值 | 说明 |
|---|---|---|
| `MUXPOS` | `PIN0`–`PIN19`（0x00–0x13）、`TEMP`(0x18)、`BANDGAP`(0x19)、`SCALEDCOREVCC`(0x1A)、`SCALEDIOVCC`(0x1B)、`DAC`(0x1C) | 编号是 AIN 号，不是引脚号 |
| `MUXNEG` | `PIN0`–`PIN7`、`GND`(0x18)、`IOGND`(0x19) | 单端就填 `GND` |
| `GAIN` | `1X` / `2X` / `4X` / `8X` / `16X` / `DIV2` | 放大输入，量程变成 VREF/GAIN |

### SWTRIG（0x0C）、INTFLAG（0x18）、STATUS（0x19）、RESULT（0x1A）

`SWTRIG.START` 写 1 启动一次转换，硬件在转换开始后自动清掉它（不必手动清）。`INTFLAG.RESRDY` 置 1 表示结果已经同步到 `RESULT`，**清标志的办法是往这一位写 1**（写 0 无效）——注意别写成 `&= ~`。`INTFLAG.OVERRUN` 是"上一次结果还没读走"，`STATUS.SYNCBUSY` 是"同步还没完成"。`RESULT` 是只读的 16 位寄存器，数值含义取决于 `RESSEL` 和 `LEFTADJ`。

> "读 `RESULT`"和"清 `RESRDY`"是两件事：手册 33.6.11 说读 `RESULT` 清掉的是 **DMA 请求**，中断标志要写 1 才清。轮询时两者都做一遍最省心。

### WINCTRL / WINLT / WINUT（0x08 / 0x1C / 0x20）与 CALIB（0x28）

`WINMODE[2:0]` 选命中条件（10.10 的表），`WINLT`/`WINUT` 是上下限，三个寄存器都是写同步的，命中后置 `INTFLAG.WINMON`。`CALIB` 里的 `LINEARITY_CAL[7:0]` 和 `BIAS_CAL[2:0]` 必须由软件从 NVM 软件校准区拷进来（手册 10.3.2 的 Table 10-8，地址 `0x806020`）：LINEARITY 占 bit 34:27、BIASCAL 占 bit 37:35，**不要修改这两个值**。不装它们 ADC 也能跑，但达不到手册标称的线性度。

## 10.16 寄存器逐个讲（DAC 与 AC）

### DAC：三个寄存器就够

`CTRLA`（0x00）管开不开（`ENABLE`）和睡眠时停不停（`RUNSTDBY`）；`CTRLB`（0x01）管参考选谁（`REFSEL`）、VOUT 引脚输出开不开（`EOEN`）、送不送给 AC（`IOEN`）、左右对齐（`LEFTADJ`），**它是使能保护的，只能在 `ENABLE = 0` 时写**；`DATA`（0x08）是要输出的 10 位数字，写它就开始一次转换。

### AC：一个模块的 7 个寄存器

| 寄存器 | 偏移 | 回答什么问题 |
|---|---|---|
| `CTRLA` | 0x00 | 整个 AC 开不开（`ENABLE`） |
| `COMPCTRL0/1` | 0x10 / 0x14 | 每个比较器：正端（`MUXPOS`）、负端（`MUXNEG`）、速度（`SPEED`）、迟滞（`HYST`）、滤波（`FLEN`）、中断条件（`INTSEL`）、自己开不开（`ENABLE`） |
| `SCALER0/1` | 0x20 / 0x21 | 负端选 `VSCALE` 时用哪一级分压（64 级） |
| `WINCTRL` | 0x0C | 窗口模式开关（`WEN0`）和提示条件（`WINTSEL0`） |
| `STATUSA` / `STATUSB` | 0x08 / 0x09 | 比较结果 `STATEx`、窗口状态 `WSTATE0` / `READYx`（结果好了没）、`SYNCBUSY` |
| `EVCTRL` | 0x02 | 要不要输出事件（`COMPEOx` / `WINEO0`）。**使能保护：只能在 AC 关闭时写** |

`COMPCTRLx` 有一条特殊规矩：**写了 `ENABLE = 1` 之后，同一个寄存器里的其它位就锁住了**，要改必须先写 `ENABLE = 0`。所以配置必须一次写完——这也解释了为什么 `MUXPOS`/`MUXNEG` 的手册描述都写着"只能在比较器禁用时写"。把上面的开关连起来，一个"PA05 电压高于 1.65 V 就输出 1"的比较器是这样配的：

```c
/* PA05（AIN[1]）的电压与 VDDANA/2 比较，结果看 STATUSA.STATE0 */
static void ac_init(void)
{
    /* ① AC 的 APB 时钟复位默认是关的（PM_APBCMASK 复位值 0x00010000 只有 ADC 开着）；
          ② 两路通用时钟：AC_DIG 是采样节拍，AC_ANA 保证低压时模拟开关导通电阻够低 */
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_AC_Msk;
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_AC_DIG | GCLK_CLKCTRL_GEN_GCLK0 | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_AC_ANA | GCLK_CLKCTRL_GEN_GCLK0 | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
    /* ③ 负端门限用 VDD 分压器：VSCALE = VDDANA × (VALUE+1)/64，VALUE = 31 → 1.65 V */
    AC_REGS->AC_SCALER[0] = 31U;
    /* ④ 一次写完 COMPCTRL0：正端 AIN[1]（PA05）、负端 VSCALE、低速档、使能本比较器 */
    AC_REGS->AC_COMPCTRL[0] = AC_COMPCTRL_MUXPOS_PIN1 | AC_COMPCTRL_MUXNEG_VSCALE
                            | AC_COMPCTRL_SPEED_LOW | AC_COMPCTRL_ENABLE_Msk;
    /* ⑤ 打开 AC 模块（这一步才真正给比较器上电），再等启动时间和同步 */
    AC_REGS->AC_CTRLA = AC_CTRLA_ENABLE_Msk;
    while (AC_REGS->AC_STATUSB & AC_STATUSB_SYNCBUSY_Msk) { }
    while ((AC_REGS->AC_STATUSB & AC_STATUSB_READY0_Msk) == 0) { }
}

/* 返回 1 表示 PA05 的电压高于门限 */
static uint32_t ac_is_above(void)
{
    return (AC_REGS->AC_STATUSA & AC_STATUSA_STATE0_Msk) ? 1U : 0U;
}
```

窗口模式在此基础上多做两件事：写 `WINCTRL = AC_WINCTRL_WEN0_Msk | AC_WINCTRL_WINTSEL0_OUTSIDE`，并把两个比较器的 `MUXPOS` 选成同一个引脚、`MUXNEG` 一个选高门限一个选低门限；结果读 `STATUSA.WSTATE0`。

## 10.17 例程一：读电位器电压，用 LED 条指示

| 硬件 | 接法 |
|---|---|
| 10 kΩ 电位器 | 两端接 VDDANA 和 GND，中间抽头接 **PA05（第 10 脚，AIN[5]）** |
| 4 个 LED | **PA14–PA17**（第 23–26 脚），串联限流电阻后接 GND，低电平点亮（第 4 章接法） |

思路：不停地读电位器，把 0–4095 的码分成 4 档，档位越高亮的灯越多。每一步的来路都用注释标出来，和 10.13 的表一一对应。（PA14/PA15 是 XOSC 的 XIN/XOUT，没接晶振时就是普通 GPIO；见手册 Table 7-3。）

```c
#include "sam.h"

/* 硬件：10 kΩ 电位器两端接 VDDANA(3.3 V) 和 GND，抽头接 PA05（第 10 脚，AIN[5]）；
 *       PA14~PA17 各接一个 LED 到 GND，低电平点亮（第 4 章接法） */

#define POT_PIN   5
#define LED_MASK  0x0003C000UL      /* PA14 ~ PA17 */
#define LED_SHIFT 14

static void led_bar_init(void)
{
    PORT_REGS->GROUP[0].PORT_OUTSET = LED_MASK;   /* 先准备电平：高 = 灭 */
    PORT_REGS->GROUP[0].PORT_DIRSET = LED_MASK;   /* 再打开输出驱动 */
}

/* PA05 交给 ADC：功能 B + 关掉数字功能（10.3）。PINCFG 只留 PMUXEN，
   INEN/PULLEN 都是 0、DIR 保持复位默认的输入 */
static void pot_pin_init(void)
{
    PORT_REGS->GROUP[0].PORT_PINCFG[POT_PIN] = PORT_PINCFG_PMUXEN_Msk;
    PORT_REGS->GROUP[0].PORT_PMUX[2] = PORT_PMUX_PMUXO_B;   /* 低半字节 PA04 被清成功能 A */
}

static void adc_init(void)
{
    uint32_t w0, w1;
    uint16_t linearity, biascal;
    /* ① 通用时钟（第 5 章）：ADC 必须先有时钟 */
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_ADC | GCLK_CLKCTRL_GEN_GCLK0 | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
    /* ② 装出厂校准值：NVM 软件校准区 0x806020（Table 10-8），
          LINEARITY 占 bit 34:27（跨两个 32 位字），BIASCAL 占 bit 37:35 */
    w0 = *(volatile uint32_t *)0x806020UL;
    w1 = *(volatile uint32_t *)0x806024UL;
    linearity = (uint16_t)(((w0 & FUSES_OTP4_WORD_0_ADC_LINEARITY_0_Msk) >> FUSES_OTP4_WORD_0_ADC_LINEARITY_0_Pos)
                         | ((w1 & FUSES_OTP4_WORD_1_ADC_LINEARITY_1_Msk) << 5));
    biascal   = (uint16_t)((w1 & FUSES_OTP4_WORD_1_ADC_BIASCAL_Msk) >> FUSES_OTP4_WORD_1_ADC_BIASCAL_Pos);
    ADC_REGS->ADC_CALIB = (uint16_t)(ADC_CALIB_LINEARITY_CAL(linearity) | ADC_CALIB_BIAS_CAL(biascal));
    /* ③ 参考取 VDDANA/1.48 ≈ 2.23 V；REFCOMP 补偿参考缓冲的失调（10.4） */
    ADC_REGS->ADC_REFCTRL = ADC_REFCTRL_REFSEL_INTVCC0 | ADC_REFCTRL_REFCOMP_Msk;
    /* ④ 48 MHz / 32 = 1.5 MHz，落在 30 kHz ~ 2.1 MHz 内（10.8） */
    ADC_REGS->ADC_CTRLB = ADC_CTRLB_PRESCALER_DIV32 | ADC_CTRLB_RESSEL_12BIT;
    /* ⑤ 采样 4 × 333 ns ≈ 1.33 µs，对 10 kΩ 电位器（最坏 2.5 kΩ）够用（10.7） */
    ADC_REGS->ADC_SAMPCTRL = ADC_SAMPCTRL_SAMPLEN(3);
    /* ⑥ 正端 AIN[5]、负端内部地（单端）、增益 1 倍（10.5、10.6） */
    ADC_REGS->ADC_INPUTCTRL = ADC_INPUTCTRL_MUXPOS_PIN5 | ADC_INPUTCTRL_MUXNEG_GND | ADC_INPUTCTRL_GAIN_1X;
    /* ⑦ 参数稳了才使能；ENABLE 是写同步位 */
    ADC_REGS->ADC_CTRLA = ADC_CTRLA_ENABLE_Msk;
    while (ADC_REGS->ADC_STATUS & ADC_STATUS_SYNCBUSY_Msk) { }
}

/* 启动一次转换，等 RESRDY，返回 0 ~ 4095 的原始码 */
static uint16_t adc_read(void)
{
    ADC_REGS->ADC_SWTRIG = ADC_SWTRIG_START_Msk;      /* 写 1 启动，硬件随后自动清掉 */
    while ((ADC_REGS->ADC_INTFLAG & ADC_INTFLAG_RESRDY_Msk) == 0) { }
    ADC_REGS->ADC_INTFLAG = ADC_INTFLAG_RESRDY_Msk;   /* 写 1 清标志（写 0 无效） */
    return ADC_REGS->ADC_RESULT;                      /* 只读寄存器，读它取结果 */
}

int main(void)
{
    uint16_t code;
    uint32_t level, mask;
    led_bar_init();
    pot_pin_init();
    adc_init();
    (void)adc_read();      /* 手册 33.6.2.1：参考配置后的第一次转换不能用 */

    while (1)
    {
        code  = adc_read();
        level = ((uint32_t)code >> 10) + 1U;                /* 0~1023→1 个灯 … 3072~4095→4 个 */
        mask  = LED_MASK & (((1UL << level) - 1UL) << LED_SHIFT);
        PORT_REGS->GROUP[0].PORT_OUTCLR = mask;             /* 选中的灯：输出低 → 亮 */
        PORT_REGS->GROUP[0].PORT_OUTSET = LED_MASK & ~mask; /* 其余：输出高 → 灭 */
    }
}
```

这里没有做精确定时——那需要定时器（第 9 章）。循环速度由"一次转换的时间 + 循环开销"决定，肉眼看到的就是电位器一转灯就跟着变。

## 10.18 例程二：DAC 输出锯齿波

PA02（第 3 脚）是 DAC 的 `VOUT`，接示波器探头到 PA02 和 GND 即可。思路：让一个 10 位变量从 0 数到 0x3FF 再回卷，每次数完就写进 `DATA`，`VOUT` 上就是上升的斜坡 + 回卷的陡边。

```c
#include "sam.h"

/* 硬件：示波器接 PA02（第 3 脚，DAC 的 VOUT）与 GND */

static void dac_pin_init(void)
{
    PORT_REGS->GROUP[0].PORT_PINCFG[2] = PORT_PINCFG_PMUXEN_Msk;   /* 关掉 PA02 的数字功能 */
    PORT_REGS->GROUP[0].PORT_PMUX[1] = PORT_PMUX_PMUXE_B;          /* 低半字节 = PA02，功能 B */
}

static void dac_init(void)
{
    /* ① DAC 的 APB 时钟复位默认是关的（PM_APBCMASK 复位值 0x00010000 只有 ADC 开着） */
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_DAC_Msk;
    /* ② 通用时钟（第 5 章） */
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_DAC | GCLK_CLKCTRL_GEN_GCLK0 | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
    /* ③ CTRLB 是使能保护的，必须在 ENABLE = 0 时写：REFSEL 取 VDDANA（输出 0~3.3 V），
          EOEN 打开 VOUT 的对外输出缓冲 */
    DAC_REGS->DAC_CTRLB = DAC_CTRLB_REFSEL_AVCC | DAC_CTRLB_EOEN_Msk;
    /* ④ 使能 */
    DAC_REGS->DAC_CTRLA = DAC_CTRLA_ENABLE_Msk;
    while (DAC_REGS->DAC_STATUS & DAC_STATUS_SYNCBUSY_Msk) { }
}

/* 忙等：DAC 启动时间最大 2.85 µs、刷新率上限 350 ksps，间隔远大于 3 µs 即可（10.11） */
static void step_delay(void)
{
    for (volatile uint32_t i = 0; i < 400U; i++) { }
}

int main(void)
{
    uint16_t code = 0;                             /* 10 位：0 ~ 0x3FF */

    dac_pin_init();
    dac_init();
    while (1)
    {
        DAC_REGS->DAC_DATA = code;                 /* 写 DATA 就立刻开始一次转换 */
        code = (uint16_t)((code + 1U) & 0x3FFU);   /* 到顶回卷 → 锯齿 */
        step_delay();
    }
}
```

| 示波器上看到 | 解释 |
|---|---|
| 斜坡 + 陡直的回落边 | 斜坡是 `code` 每步 +1，陡边是回卷 |
| 顶端和底端各有约 50 mV 削平 | 线性输出范围是 0.05 V ~ VDDANA−0.05 V（10.11） |
| 周期约几十毫秒 | 由 `step_delay()` 的循环次数和主频决定，不是精确值 |
| 万用表读到约 1.65 V | 表读平均值，锯齿波平均值正好在量程中间 |

想变成三角波或正弦波？那只是 `code` 的算法问题（第 9 章的定时器可以让周期精确）。想让 DAC 当 AC 的门限？打开 `CTRLB.IOEN`，再把 AC 的 `MUXNEG` 选成 `DAC`（10.12）——**不用占用任何引脚**。

## 10.19 初始化清单

**ADC**，按这个顺序想就不会漏：

1. **引脚**：`PMUXEN = 1` + `PMUX = B`（不要开 `INEN`/`DIR`/`PULLEN`）。
2. **通用时钟**：`GCLK_CLKCTRL` 配 `ID_ADC` + `GEN` + `CLKEN`，等 `SYNCBUSY`。
3. **校准值**：从 `0x806020` 装 `LINEARITY` / `BIASCAL` 到 `CALIB`。
4. **参考**：`REFCTRL.REFSEL`（+ `REFCOMP`）。
5. **时钟、采样与通道**：`CTRLB.PRESCALER`（`CLK_ADC ≤ 2.1 MHz`）+ `RESSEL` + `SAMPCTRL.SAMPLEN`（按源阻抗算）+ `INPUTCTRL` 的 `MUXPOS`/`MUXNEG`/`GAIN`。
6. **可选**：平均 `AVGCTRL`、窗口 `WINCTRL/WINLT/WINUT`、校正 `CORREN`。
7. **使能**：`CTRLA.ENABLE = 1`，等 `SYNCBUSY`，第一次转换丢弃。

**DAC**：`PM_APBCMASK` 开 DAC → GCLK（`ID_DAC`）→ 引脚功能 B → `CTRLB`（`REFSEL` + `EOEN`，使能保护，必须在使能前写）→ `CTRLA.ENABLE` → 写 `DATA`。**AC**：`PM_APBCMASK` 开 AC → GCLK 两路（AC_DIG + AC_ANA）→ 引脚功能 B → `SCALERx` → 一次写完整 `COMPCTRLx`（含 `ENABLE`）→ `CTRLA.ENABLE` → 等 `STATUSB.READYx` → 读 `STATUSA`。

**运行中只做三件事**：写 `SWTRIG` 触发、等 `INTFLAG`、读 `RESULT`（DAC 则是写 `DATA`）。通道、参考、采样时间这些都在初始化时定好——中途改它们要重新等 `SYNCBUSY`，还容易留下一笔脏转换。

## 10.20 一个模拟外设的完整思维模型

```text
                模拟外设
                   │
   ┌───────────────┼───────────────┐
  ADC             DAC              AC
 测电压          出电压          比大小
 引脚→时钟→参考→参数→使能→结果（三个外设同一条主线）
```

三个外设共享同一条主线：**引脚先进模拟模式（功能 B）→ 通用时钟 / APB 时钟 → 参考或门限 → 具体参数 → 使能 → 出结果**。程序真正做的事只有两件：**初始化时配置属性和参数，运行中触发并读结果**。

## 10.21 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 读数永远是 4095 | 输入超过了参考电压；或参考选得太小 | `REFCTRL.REFSEL`、实际输入范围（10.4） |
| 读数比实际值小、换通道还跟着变 | 采样时间不够，采样电容没充满 | `SAMPCTRL.SAMPLEN` 加大、降 `PRESCALER`、加缓冲器（10.7） |
| 某个通道完全读不到（或读到别人的值） | `MUXPOS` 的 AIN 号和引脚对不上；48 脚只有 14 路 | 手册 Table 2-1 / Table 7-1（10.2） |
| 读数乱跳、噪声大 | 引脚还配着数字功能（`INEN`/上下拉/输出） | `PINCFG` 是否只剩 `PMUXEN`（10.3） |
| 读数不动、一直是 0 | 通用时钟没配、`CTRLA.ENABLE` 没写、`SWTRIG` 没触发 | `GCLK_CLKCTRL`、`CTRLA.ENABLE`、`SWTRIG.START` |
| 改了通道还是老值 | `INPUTCTRL` 是写同步寄存器，没等生效 | 写完 `INPUTCTRL` 等 `STATUS.SYNCBUSY` |
| 差分读到 4000 多而不是负数 | `RESULT` 是补码，没做符号扩展 | `DIFFMODE` 是否开、有无符号扩展（10.5） |
| 平均/过采样后结果不对 | 累加次数 ≥ 2 但 `RESSEL` 还是 12 位 | `CTRLB.RESSEL = 16BIT`（10.6） |
| DAC 引脚上什么都没有 | `EOEN` 没开、引脚不是功能 B、APB 时钟没开 | `CTRLB.EOEN`、`PINCFG/PMUX`、`PM_APBCMASK.DAC` |
| 比较器结果不动 | 启动时间没过（`READYx = 0`）、门限选错 | `STATUSB.READYx`、`MUXNEG` 与 `SCALERx`（10.12） |
| 写 `_Val` 宏程序不报错但功能全错 | 多位字段的 `_Val` 没有移位 | 用移好位的宏，或 `(_Val << _Pos)`（10.14） |

## 10.22 寄存器速查

**ADC**（基址 `0x42004000`，宏 `ADC_REGS`）：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `CTRLA` | 0x00 | bit0 `SWRST`、bit1 `ENABLE`、bit2 `RUNSTDBY` |
| `REFCTRL` | 0x01 | bit7 `REFCOMP`、bit3:0 `REFSEL`（0=1V、1=VDDANA/1.48、2=VDDANA/2、3=VREFA、4=VREFB） |
| `AVGCTRL` | 0x02 | bit6:4 `ADJRES`、bit3:0 `SAMPLENUM` |
| `SAMPCTRL` | 0x03 | bit5:0 `SAMPLEN`（采样时间 = (SAMPLEN+1)×CLK_ADC/2） |
| `CTRLB` | 0x04 | bit10:8 `PRESCALER`、bit5:4 `RESSEL`、bit3 `CORREN`、bit2 `FREERUN`、bit1 `LEFTADJ`、bit0 `DIFFMODE` |
| `WINCTRL` | 0x08 | bit2:0 `WINMODE` |
| `SWTRIG` | 0x0C | bit1 `START`、bit0 `FLUSH` |
| `INPUTCTRL` | 0x10 | bit27:24 `GAIN`、bit23:20 `INPUTOFFSET`、bit19:16 `INPUTSCAN`、bit12:8 `MUXNEG`、bit4:0 `MUXPOS` |
| `INTFLAG` | 0x18 | bit3 `SYNCRDY`、bit2 `WINMON`、bit1 `OVERRUN`、bit0 `RESRDY`（写 1 清） |
| `STATUS` | 0x19 | bit7 `SYNCBUSY` |
| `RESULT` | 0x1A | 只读，转换结果 |
| `WINLT` / `WINUT` | 0x1C / 0x20 | 窗口下限 / 上限 |
| `GAINCORR` / `OFFSETCORR` | 0x24 / 0x26 | 增益系数（1 位整数 + 11 位小数）/ 偏移（补码） |
| `CALIB` | 0x28 | bit10:8 `BIAS_CAL`、bit7:0 `LINEARITY_CAL` |

**DAC**（`0x42004800`，`DAC_REGS`）：`CTRLA` 0x00（`ENABLE` 在 bit1）、`CTRLB` 0x01（bit7:6 `REFSEL`、bit4 `BDWP`、bit3 `VPD`、bit2 `LEFTADJ`、bit1 `IOEN`、bit0 `EOEN`）、`STATUS` 0x07、`DATA` 0x08、`DATABUF` 0x0C。

**AC**（`0x42004400`，`AC_REGS`）：`CTRLA` 0x00（bit7 `LPMUX`、bit2 `RUNSTDBY`、bit1 `ENABLE`）、`CTRLB` 0x01（`START1/0`，只写）、`STATUSA` 0x08、`STATUSB` 0x09（bit0/1 `READY0/1`）、`STATUSC` 0x0A（读它顺便启动一次单次比较）、`WINCTRL` 0x0C、`COMPCTRL0/1` 0x10/0x14（bit26:24 `FLEN`、bit19 `HYST`、bit17:16 `OUT`、bit15 `SWAP`、bit13:12 `MUXPOS`、bit10:8 `MUXNEG`、bit6:5 `INTSEL`、bit3:2 `SPEED`、bit1 `SINGLE`、bit0 `ENABLE`）、`SCALER0/1` 0x20/0x21。

引脚侧：模拟功能一律是 **PORT 的功能 B**（`PINCFG.PMUXEN = 1` + `PMUX = B`）；AC 的四个输入 `AIN[3:0]` 是 PA04–PA07；DAC 的 VOUT 是 PA02；外部参考 VREFA 是 PA03、VREFB 是 PA04。

## 10.23 最重要的几句话

1. ADC 的输出是"输入电压是参考电压的几分之几"，所以**参考电压选错，全部读数都是错的**；SAMD21 的 ADC 没有"VDDANA 直接当参考"这一档。
2. 48 脚的 ATSAMD21G18A 只有 **14 路** ADC 输入，写 `MUXPOS` 之前必须查手册 Table 2-1 和 Table 7-1。
3. 模拟引脚必须选**功能 B**，并且不要再配数字输入/输出/上下拉——数字缓冲在中间电平上既费电又引噪声。
4. 采样就是给内部采样电容充电：**源阻抗越大，需要的采样时间越长；充不满，读数偏低还跟着上一个通道跑**。
5. `CLK_ADC` 必须落在 **30 kHz ~ 2.1 MHz**；350 ksps 是"12 位、1× 增益、单端、不平均"这条最理想路径上的数字。
6. 单端是 `MUXNEG = GND`；差分要开 `DIFFMODE`，结果是**二进制补码**，负数必须做符号扩展。
7. 累加次数 ≥ 2 做平均或过采样时，`CTRLB.RESSEL` 必须改成 `16BIT`，否则结果没有意义。
8. 出错先按这条顺序查：**参考电压 → 通道号 → 采样时间 → 时钟与使能 → 引脚功能**。

## 10.24 自测

1. 在 48 脚的 ATSAMD21G18A 上，`MUXPOS` 写 `PIN12` 会怎样？为什么？
2. 参考电压选 `INT1V`（1.0 V），输入接 2.0 V，会读到什么？换成 `INTVCC0`（VDDANA/1.48，3.3 V 供电）又会读到什么？
3. 为什么配置顺序必须是"先配 GCLK 和 REFCTRL，最后才写 `CTRLA.ENABLE`"？把顺序反过来可能出现什么问题？
4. 一个 100 kΩ 的传感器直接接到 AIN 引脚上，1.5 MHz 的 `CLK_ADC` 下 `SAMPLEN` 至少要多少？不理会看到什么现象？
5. DAC 的 `DATA` 写 0x200，`REFSEL` 选 VDDANA（3.3 V），`VOUT` 大约是几伏？为什么写 0x3FF 也到不了 3.3 V？
6. 用 AC 做"电压落在 1.0 V ~ 2.0 V 之间就报警"，为什么要用两个比较器，并且它们的 `MUXPOS` 必须相同？

## 附录：以后查手册怎么找 ADC / DAC / AC

```text
① 找外设章  →  ② 看 Register Summary 的偏移  →  ③ 逐个寄存器看 bit 定义
   手册 33 章 = ADC，34 章 = AC，35 章 = DAC
④ 引脚与通道：7.1 节 Table 7-1（模拟全在 B 列，注 2 说明要关数字功能）、Table 2-1 的 "ADC Channels"（E=10 / G=14 / J=20）
⑤ 电气与采样：37.11.4 = ADC、37.11.5 = DAC、37.11.6 = AC；采样时间公式在 37.11.4.3
```

本章用到的偏移：`ADC` 的 `CTRLA 0x00 / REFCTRL 0x01 / AVGCTRL 0x02 / SAMPCTRL 0x03 / CTRLB 0x04 / WINCTRL 0x08 / SWTRIG 0x0C / INPUTCTRL 0x10 / INTFLAG 0x18 / STATUS 0x19 / RESULT 0x1A / WINLT 0x1C / WINUT 0x20 / GAINCORR 0x24 / OFFSETCORR 0x26 / CALIB 0x28`。手册里几处容易看漏的注记：Table 7-1 注 2（模拟都在功能 B、必须关数字控制）、33.6.2.1（换参考后第一次转换不能用）、Table 37-24 注 3（350 ksps 对应的 6 个 `CLK_ADC`）、37.11.4.3（12 位精度的 9.02 系数）、34.1 的注（AC1 只在 L 变体上）。

## 本章结束

学完这一章，你应该能把下面这段代码从"寄存器魔法"翻译成人话：

```c
ADC_REGS->ADC_REFCTRL   = ADC_REFCTRL_REFSEL_INTVCC0;
ADC_REGS->ADC_CTRLB     = ADC_CTRLB_PRESCALER_DIV32;
ADC_REGS->ADC_INPUTCTRL = ADC_INPUTCTRL_MUXPOS_PIN5 | ADC_INPUTCTRL_MUXNEG_GND;
ADC_REGS->ADC_CTRLA     = ADC_CTRLA_ENABLE_Msk;
```

不是"记住这四句"，而是：**参考取 VDDANA 的 1/1.48；`CLK_ADC` 取 48 MHz 的 1/32；测 PA05 对内部地的电压；然后才上电。** 能把寄存器操作翻译成硬件动作，模拟外设才算真正学会。
