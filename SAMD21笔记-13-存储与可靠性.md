# 第 13 章 存储与可靠性（外设速览）

> **芯片**：ATSAMD21G18A（256 KB Flash / 32 KB SRAM）
> **本章目标**：搞清"想掉电不丢的数据放在哪、怎么放进去、怎么读回来"；搞清程序跑飞时谁把芯片拉回来；最后用同一副眼光扫一遍 USB / I2S / PTC 三个大外设。
> **学习顺序**：三种存储的分工 → Flash 的物理脾气 → 行 / 页 / 页缓冲 → 命令与钥匙 → 地址映射 → 三种读法 → 写的时候 CPU 在干什么 → 完整例程 → WDT → CRC32 → 三个大外设速览。

## 13.0 本章要回答的三个问题

前面十二章的数据都住在 SRAM 里。SRAM 快、随便写、**一掉电全没**。可总有些数据必须活过断电：校准值、设备序列号、用户设置、累计运行时间、故障记录。它们得有别的去处。

所以本章有三条线，最后汇成同一件事——让系统在没人看着的时候也不出错：

| 问题 | 谁来管 | 一句话 |
|---|---|---|
| 掉电不丢的数据存哪、怎么写 | NVMCTRL + Flash | 擦除按行、写入按页、写完读回校验 |
| 程序跑飞、卡死了怎么办 | WDT（看门狗） | 到点没喂狗就复位 |
| 存进去的数据坏了怎么发现 | DSU 里的 CRC32 | 一次算出一整段内存的校验值 |
| 芯片还能接什么大外设 | USB / I2S / PTC | 知道它们能干什么、什么时候值得用 |

前三条正好是可靠性的三个层次：**存得住、有人管、查得出**。本章主菜是第一条，后两条各占一节，三个大外设只做"速览"——认脸，不深入。

## 13.1 先分清三种存储

芯片里能放东西的地方有三处，性质完全不同：

| 存储 | 容量 | 掉电后 | 写入方式 | 典型内容 |
|---|---|---|---|---|
| SRAM | 32 KB | 全丢 | 直接赋值，随便写 | 变量、栈、缓冲区 |
| Flash 主阵列 | 256 KB | 保留 | 必须先擦再写，速度慢 | 程序本体 + 要保存的参数 |
| 辅助区（User Row / 校准区 / 序列号） | 几百字节 | 保留 | 出厂写好，一般只读 | 启动配置、校准值、芯片唯一 ID |

**Flash 和 SRAM 最本质的差别不是"哪个快"，而是"能不能随便改"。** 一句 `x = 5;` 写到 SRAM 上就是一条存储指令；写到 Flash 上要先擦掉一整块、再整页搬过去，中间还得等几毫秒。本章后面所有麻烦都是这一条派生出来的。

先记住这颗芯片的 Flash 尺寸，后面每处都要用：

```text
256 KB = 262144 字节 = 4096 页 × 64 字节；一行 = 4 页 = 256 字节，4096 页 = 1024 行
擦除的最小单位是"行"（256 字节），写入的最小单位是"页"（64 字节）
```

## 13.2 Flash 的物理脾气：擦除整行，写只能 1→0

Flash 的存储单元是浮栅晶体管，靠电荷的有无表示 0 和 1。这个物理特性决定了三件事：

```text
擦除后：  1 1 1 1 1 1 1 1    擦除 = 把所有位写成 1
写入后：  1 1 1 0 0 1 1 1    写只能把 1 变成 0
再想改：  1 1 1 天 天 1 0 1   想把 0 变回 1？只能整行擦掉重来
```

由此得到三条铁律，本章所有代码都在伺候它们：

1. **写之前必须先擦**：不擦就写，只有原本是 1 的位能变成 0，原本是 0 的位写不动，结果是新旧数据按位"与"在一起。
2. **擦除的代价是一整行**：只想改 1 个字节，也要先牺牲这一行里的 256 字节——所以这一行的内容要先读到 RAM 里存着，擦完再写回去。
3. **同一行不能无限次连续写**：手册 Table 41-33 的注写明，**同一行最多连续写 8 次，第 9 次之前必须擦一次**；擦写寿命最小 25k 次（Table 41-34）。频繁改的数据要摊开写，不能每次原地擦写。

还有一个新手最容易误解的点：**往 Flash 的地址上赋值，并不等于写进 Flash**。`*(volatile uint32_t *)0x0003FFC0UL = 0x12345678UL;` 这一句只是把数据装进了后面要讲的"页缓冲"，Flash 里什么都没变，再读回来还是原来的值。

## 13.3 行、页、页缓冲：为什么写要分两步

Flash 的物理写入需要一段相对稳定的高压时间，不可能"CPU 写一个字就烧一个字"。所以芯片在中间放了一个 64 字节的中转站，叫**页缓冲（Page Buffer）**，大小正好一页：

```text
CPU 写 0x3FFC0 ──▶ [ 64 字节页缓冲，先攒齐 ] ──WP 命令──▶ Flash 的一页
```

页缓冲存在的理由有三条，理解了就不用背流程：

| 理由 | 结果 |
|---|---|
| 攒齐一页再统一烧写 | 所以第一步是"装缓冲"，第二步才是"提交" |
| 缓冲是普通寄存器，写起来很快 | 所以装缓冲期间不卡 CPU，只有提交后才卡 |
| 高速总线按字传输才够快 | 所以**页缓冲只接受 16 位和 32 位写，8 位写会触发系统异常**（手册 22.6.4.3） |

装缓冲时写的就是 Flash 的地址（例如 `0x0003FFC0`），硬件自动把数据送进缓冲，同时把你写的地址记到 `ADDR` 寄存器里。提交有两种方式，由 `CTRLB.MANW` 决定：

| `CTRLB.MANW` | 行为 | 适用 |
|---|---|---|
| 1（复位默认） | 必须自己发 `WP` 命令才提交 | 手动控制，最常用 |
| 0 | 写满这一页最后一个字时自动提交 | 追求速度、一次写满整页 |

装了一半又想放弃怎么办？发 `PBC`（Page Buffer Clear）把缓冲清成全 1。页缓冲不会自动清空，但**每写完一页硬件会把它设成全 1**（手册 22.6.4.4），连续写多页时不用手动清。

## 13.4 把硬件动作翻译成命令：CTRLA.CMD

现在才看寄存器。NVMCTRL 的寄存器只有九个，第一个要认识的是 `CTRLA`：

| 我要做什么 | 寄存器里的哪一段 |
|---|---|
| 告诉控制器"干哪件事" | `CTRLA.CMD`（bit 6:0） |
| 证明这个命令是软件故意发的 | `CTRLA.CMDEX`（bit 15:8），必须是钥匙值 `0xA5` |
| 告诉控制器"对哪个地址下手" | `ADDR`（半字偏移，见 13.8） |
| 知道它干完没有 | `INTFLAG.READY` |
| 知道它干坏了没有 | `STATUS` 的三个错误位 |

`CMD` 支持的命令比想象中多，常用的就几条。这张表按"什么时候会用到"排列：

| CMD | 名字 | 做什么 | 本章用不用 |
|---|---|---|---|
| 0x02 | `ER` | 擦除 `ADDR` 指向的那一行（主阵列） | 用，写之前必须做 |
| 0x04 | `WP` | 把页缓冲写进 `ADDR` 指向的那一页 | 用，提交数据 |
| 0x05 / 0x06 | `EAR` / `WAP` | 擦 / 写辅助区的一行 / 一页（只能用于 User Row） | 改启动配置时用 |
| 0x44 | `PBC` | 把页缓冲清成全 1 | 写了一半想放弃时用 |
| 0x40 / 0x41 | `LR` / `UR` | 锁住 / 解锁 `ADDR` 所在的 16 KB 区域 | 想保护代码区不被误写时用 |
| 0x46 | `INVALL` | 让指令缓存全部失效 | 平时不用，写命令会自动失效相关缓存 |
| 0x42 / 0x43 | `SPRM` / `CPRM` | 进 / 出省电模式 | 配合第 6 章的睡眠才用 |
| 0x45 | `SSB` | 置安全位，置上后只能整片擦除才能再烧写 | 量产才用 |
| 0x1A / 0x1C | `RWWEEER` / `RWWEEWP` | 擦 / 写 RWWEE 区 | **本型号没有这个区**，见 13.6 |

发一条命令只有三步，顺序是硬件逼出来的：

```c
void issue_erase_row(uint32_t byte_addr)
{
    /* ① 先等 READY=1：READY=0 时写进去的命令会被直接丢掉（手册 22.6.4） */
    while ((NVMCTRL_REGS->NVMCTRL_INTFLAG & NVMCTRL_INTFLAG_READY_Msk) == 0U) { }

    /* ② 告诉它擦哪一行：ADDR 里放半字偏移（字节地址 ÷ 2），见 13.8 */
    NVMCTRL_REGS->NVMCTRL_ADDR = (byte_addr >> 1);

    /* ③ 命令 + 钥匙，一次 16 位写完。ER 的编号是 0x02，钥匙在 bit15:8 */
    NVMCTRL_REGS->NVMCTRL_CTRLA = (uint16_t)(NVMCTRL_CTRLA_CMDEX_KEY | NVMCTRL_CTRLA_CMD_ER);
}
```

## 13.5 关键字保护：命令为什么要配一把钥匙

`CTRLA` 高 8 位叫 `CMDEX`，只有写入 `0xA5` 时低位的 `CMD` 才会执行；写别的值不但不执行，还会把 `STATUS.PROGE` 置 1 报错。

这不是故弄玄虚，理由和 GPIO 那一章 `OUTSET` 存在的理由是一个家族：**让"误操作"和"故意操作"在硬件上区分开**。Flash 写错的代价是数据永久丢失，而 `CTRLA` 只占一个地址，程序跑飞时随手一个写就可能落在这里。

| 情形 | 结果 |
|---|---|
| 一次写入 `CMD=0x02` 加 `CMDEX=0xA5` | 正常擦除 |
| 只写 `CMD=0x02`，钥匙位没写 | 不执行，`STATUS.PROGE=1` |
| 钥匙写成别的值 | 不执行，`STATUS.PROGE=1` |
| 先写 `CMD`、过一会儿再写 `CMDEX` | 不执行——**钥匙必须和命令在同一次写里** |

所以代码里永远是这一句：`NVMCTRL_REGS->NVMCTRL_CTRLA = (uint16_t)(NVMCTRL_CTRLA_CMDEX_KEY | 命令);`。DFP 里 `NVMCTRL_CTRLA_CMDEX_KEY` 已经把 `0xA5` 移到了 bit15:8，`NVMCTRL_CTRLA_CMD_ER` 也已经在 bit 6:0，两个直接或起来就是一次正确写入。

## 13.6 地址映射：Flash 里都住着谁

256 KB 主阵列只是整块 NVM 空间的一部分。下面这张图要记住，后面读校准值、读序列号都要用：

```text
0x00000000 ┌────────────────┐ 主阵列 256 KB：4096 页，程序和参数都住这儿
0x00040000 └────────────────┘
0x00400000   RWWEE 区：本型号（A 版）没有
0x00804000   User Row：启动配置和区域锁位
0x00806020   软件校准区；0x00806030 温度日志；0x0080A00C 起 4 个字是序列号
```

| 区域 | 地址 | 里面是什么 | 能不能写 |
|---|---|---|---|
| 主阵列 | `0x00000000` – `0x0003FFFF` | 程序 + 你要保存的数据 | 能，先擦后写 |
| RWWEE 区 | `0x00400000` | 本芯片没有 | — |
| User Row | `0x00804000` | BOOTPROT、EEPROM 大小、BOD33 阈值、WDT 初值、16 个区域锁位 | 能，用 `EAR`+`WAP`，复位后才生效 |
| 软件校准区 | `0x00806020` | ADC 线性度、OSC32K、DFLL48M、USB 的出厂校准值 | 不能（手册 10.3.2） |
| 温度日志 | `0x00806030` | 出厂测的室温 / 高温 ADC 值与 1 V 基准 | 不能 |
| 序列号 | `0x0080A00C` 起 4 个字 | 128 位唯一 ID，4 个字一起用才保证唯一 | 不能 |

三点值得多说一句。**RWWEE** 是"读主阵列的同时写另一个小阵列"，只有 B / C / D / L 版本才有（手册 10.1 和 Table 10-5）；ATSAMD21G18A 是 A 版，没有这个区，所以命令表里的 `RWWEEER` / `RWWEEWP` 在这颗芯片上没有意义，实际后果在 13.9 节。**User Row** 是第 6 章那句"BOD33 默认值不改代码也能改"的落脚点，WDT 上电时是否使能、窗口多大（`PER`、`WINDOW`、`WEN`、`ALWAYSON`）也从这里加载；写完必须复位才生效，写错可能让芯片一上电就复位、连调试器都连不上，动手前先确认手里有整片擦除的手段。**校准区和序列号** 就映射在普通地址上，用指针直接读即可，不需要发任何命令。

## 13.7 读 Flash 的三种方式

先纠正一个直觉：**NVMCTRL 没有"读"命令**。翻遍 13.4 那张命令表，全是擦、写、锁、缓存、省电，没有一条是读——因为读根本不用命令，是 AHB 总线上的普通访问。所以"读 Flash"的三种方式其实是：

| 方式 | 怎么做 | 适合读什么 | 代价 |
|---|---|---|---|
| 直接指针读主阵列 | `*(volatile uint32_t *)addr` | 刚写进去的参数、常量表 | 走总线，被缓存加速，也可能被 stall（13.9） |
| 直接指针读辅助区 | 同样的指针，地址换成 `0x0080_xxxx` | 校准值、序列号、User Row | 不进缓存，需要时读一次 |
| 用 DSU 的 CRC32 批量校验 | 硬件自己遍历一段内存算校验值 | 一整段程序或参数有没有坏 | CPU 不用逐个读，结果在 `DSU.DATA`（13.18） |

前两种的写法完全一样，例如 `uint32_t serial = *(const volatile uint32_t *)(uintptr_t)0x0080A00CUL;` 就把序列号的第一个字读了出来。注意一个前提：**三种方式都要求 NVMCTRL 的 AHB 时钟是开着的**。复位后它默认开着（`PM.AHBMASK` 复位值 bit4=1），但如果你在第 5、6 章里把它关掉过，程序连取指都会出问题——手册 16.6.2.6 专门警告过这一条。

## 13.8 ADDR 寄存器：几乎人人踩过的坑

`ADDR` 里放的不是字节地址，而是**相对于该存储区起始地址的半字偏移**：`ADDR = (字节地址 − 区起始) / 2`，也就是"这里是第几个 16 位半字"。手册 22.8.8 给了例子：擦 0x00000200–0x000002FF 这一行，`ADDR` 要写 `0x100` 到 `0x17F` 之间的任意值——正好是 `0x200/2`。

| 目标 | 区起始 | `ADDR` 要写 |
|---|---|---|
| 主阵列 `0x0003FF00`（最后一行的开头） | `0x00000000` | `0x0001FF80` |
| 主阵列 `0x0003FFC0`（最后一页的开头） | `0x00000000` | `0x0001FFE0` |
| User Row `0x00804000` | `0x00800000` | `0x00002000` |
| RWWEE `0x00400400`（本型号无） | `0x00400000` | `0x00000200` |

因为主阵列的区起始就是 0，"减区起始"这一步看不出来，于是几乎所有人的代码都写成 `ADDR = addr >> 1` 并且能用。**但改 User Row 的时候就露馅了**：这里必须先减掉 `0x00800000`，直接写 `0x00804000 >> 1` 是不对的。养成习惯：先问"这个地址属于哪个区"，再写 `ADDR`。

## 13.9 写 Flash 的时候 CPU 在干什么

这是本章最需要"心里有数"的一节。代码也存在同一块 Flash 里、CPU 靠它取指，于是一旦开始擦除或写入：

```text
CPU 取下一条指令 ──▶ AHB 读主阵列 ──▶ 主阵列正在擦 / 写 ──▶ 总线 stall：CPU 停下等
```

手册 22.6.4.1 的原话是：在主阵列上执行编程或擦除期间，去读主阵列会导致 AHB 总线 stall，直到操作结束。**ATSAMD21G18A 没有 RWWEE 区，所以这个 stall 躲不掉**——擦除时 CPU 真的会停住，最长 6 ms；写一页最长 2.5 ms（Table 41-36）。

| 现象 | 数字 | 后果 |
|---|---|---|
| 写一页（64 字节） | ≤ 2.5 ms | 中断延迟被拉长，串口可能丢字节 |
| 擦一行（256 字节） | ≤ 6 ms | 明显卡顿；看门狗超时设太短会被自己触发 |
| 进睡眠前页缓冲丢失 | — | 手册 22.5.1：进 Standby 一定丢，写命令必须发完再睡 |

所以有两件实际的事要做：**喂狗和写 Flash 不能互相打架**（写之前先喂一次狗）；**对时序敏感的应用要把擦写安排在空闲时段**，比如上电初始化时一次做完。

还有几个和速度有关的位，都通过 `CTRLB` 设置：

| 位 | 复位值 | 作用 |
|---|---|---|
| `RWS[3:0]` | 0（0 个等待周期） | 读 Flash 的等待周期数。手册 Table 41-33：0 周期最高 24 MHz，1 周期可到 48 MHz。**把主频提到 48 MHz 之前必须先把它设成 1** |
| `CACHEDIS` | 0（缓存开） | Flash 有一个直接映射的小缓存（手册 22.6.7），读主阵列时命中和取指都会快 |
| `READMODE[1:0]` | 0 | 缓存的三种取舍：0 最快、1 最省电、2 时间可预测 |
| `SLEEPPRM[1:0]` | 0 | 睡眠时 Flash 是否自动进低功耗（第 6 章讲过它和页缓冲的关系） |

## 13.10 就绪与错误：INTFLAG 和 STATUS

软硬件之间的对话全靠这两个寄存器，它们回答两个不同的问题：

| 寄存器 | 位 | 回答什么问题 | 怎么处理 |
|---|---|---|---|
| `INTFLAG` | `READY` (bit0) | 现在能不能收新命令？ | 只读。0 = 正在擦写，写完自己变 1 |
| `INTFLAG` | `ERROR` (bit1) | 出过错没有？ | 写 1 清 |
| `STATUS` | `PROGE` (bit2) | 命令或钥匙不合法？ | 写 1 清。多半是忘了 `0xA5` |
| `STATUS` | `LOCKE` (bit3) | 写到了被锁的区域？ | 写 1 清。查 `LOCK` 寄存器 |
| `STATUS` | `NVME` (bit4) | Flash 自己报错了？ | 写 1 清。要考虑擦写寿命 |
| `STATUS` | `LOAD` (bit1) | 页缓冲里有没有装过东西？ | 写完一页或发 `PBC` 后自动清 |
| `STATUS` | `PRM` / `SB` | 在省电模式吗 / 安全位置上了吗？ | 都只读 |

关于中断：NVMCTRL 的中断号是 5，能产生 `READY` 和 `ERROR` 两个中断源，用法是"命令发出去 → 去干别的 → 中断里收尾"。中断的通用机制（NVIC、优先级、`INTENSET`）在第 12 章，本章例程用最简单的轮询写法，先把流程走通。

现在可以把完整的写流程画出来了：

```text
① 等 READY=1 → ② ADDR = 地址 >> 1 → 发 ER（擦行）→ 等 READY=1 → 查 STATUS
③ 按 32 位把 64 字节装进页缓冲 → ④ 发 WP（写页）→ 等 READY=1 → 查 STATUS
⑤ 读回来逐字节比对
```

## 13.11 完整例程：把参数写进最后一页，上电读回

需求：把一个参数结构体保存到 Flash 里，掉电不丢；每次上电读回、计数器加一、再存回去。选 256 KB 的**最后一页**（`0x0003FFC0`）作参数区——它离代码区最远，且默认配置下最后一行既不属于 Bootloader（`BOOTPROT` 默认 0x7 = 无）也不属于 EEPROM 仿真区（`EEPROM` 默认 0x7 = 无）。

代码以外还有两件事：

| 事项 | 怎么做 | 为什么 |
|---|---|---|
| 链接脚本 | 把 `ROM_LENGTH` 从 `0x40000` 改成 `0x3FF00`，或单独挖掉最后一页 | DFP 的 `samd21g18a_flash.ld` 默认把整个 0x40000 都给代码，链接器可能正好把某个函数放在这一页上 |
| 结构体大小 | 必须 ≤ 64 字节（一页） | 一次写页只能提交一页，跨页就得拆成多次 |

```c
#include "sam.h"
#include <string.h>

/* 参数区 = 256 KB 主阵列的最后一页：0x3FFC0 ~ 0x3FFFF，它所在的行是 0x3FF00 ~ 0x3FFFF。
   链接脚本要把这一页从 ROM 长度里挖掉（0x40000 改成 0x3FF00），否则代码可能住进去 */
#define PARAM_ADDR     0x0003FFC0UL
#define NVM_PAGE_SIZE  64UL
#define PARAM_MAGIC    0x5041524DUL      /* 小端读出来是 "MRAP"：判断写过没有 */

#define NVM_OK           0
#define NVM_ERR_BUSY    -1               /* 上一条命令还没做完 */
#define NVM_ERR_PROGE   -2               /* 命令或钥匙不合法 */
#define NVM_ERR_LOCKE   -3               /* 目标区域被锁 */
#define NVM_ERR_NVME    -4               /* Flash 报错 */
#define NVM_ERR_VERIFY  -5               /* 读回来和写进去不一致 */
#define NVM_ERR_EMPTY   -6               /* 从没写过 */

typedef struct
{
    uint32_t magic;
    uint32_t version;
    uint32_t counter;                    /* 上电次数 */
    uint8_t  name[16];
    uint32_t sum;                        /* 前面所有字节的累加和，必须是最后一个成员 */
} param_t;

/* RAM 里的"页镜像"。用 uint32_t 数组是为了保证 4 字节对齐：页缓冲只收 16 / 32 位写 */
static uint32_t page_image[NVM_PAGE_SIZE / 4U];

static uint32_t param_sum(const param_t *p)
{
    const uint8_t *b = (const uint8_t *)p;
    uint32_t s = 0U;

    for (uint32_t i = 0U; i < (sizeof(param_t) - sizeof(uint32_t)); i++) { s += b[i]; }
    return s;
}

/* 发一条命令并等它做完。错误码都是负值 */
static int nvm_exec(uint16_t cmd)
{
    uint16_t st;

    /* READY=0 时写进去的命令会被直接丢掉，所以先确认它空闲 */
    if ((NVMCTRL_REGS->NVMCTRL_INTFLAG & NVMCTRL_INTFLAG_READY_Msk) == 0U) { return NVM_ERR_BUSY; }

    /* 命令 + 钥匙 0xA5 必须在同一次 16 位写里落下，否则只有 PROGE */
    NVMCTRL_REGS->NVMCTRL_CTRLA = (uint16_t)(NVMCTRL_CTRLA_CMDEX_KEY | (cmd & NVMCTRL_CTRLA_CMD_Msk));

    /* 擦一行最长 6 ms、写一页最长 2.5 ms，这段时间 AHB 被 stall，CPU 就在这句里等 */
    while ((NVMCTRL_REGS->NVMCTRL_INTFLAG & NVMCTRL_INTFLAG_READY_Msk) == 0U) { }

    /* 三个错误位都是"写 1 清"：先读出来判断，再清掉，免得下次误判 */
    st = NVMCTRL_REGS->NVMCTRL_STATUS;
    NVMCTRL_REGS->NVMCTRL_STATUS = (uint16_t)(NVMCTRL_STATUS_PROGE_Msk
                                             | NVMCTRL_STATUS_LOCKE_Msk
                                             | NVMCTRL_STATUS_NVME_Msk);
    NVMCTRL_REGS->NVMCTRL_INTFLAG = NVMCTRL_INTFLAG_ERROR_Msk;

    if ((st & NVMCTRL_STATUS_PROGE_Msk) != 0U) { return NVM_ERR_PROGE; }
    if ((st & NVMCTRL_STATUS_LOCKE_Msk) != 0U) { return NVM_ERR_LOCKE; }
    if ((st & NVMCTRL_STATUS_NVME_Msk)  != 0U) { return NVM_ERR_NVME;  }
    return NVM_OK;
}

/* 保存：先擦后写，最后读回核对 */
int param_save(const param_t *p)
{
    const param_t *q      = (const param_t *)(uintptr_t)PARAM_ADDR;
    volatile uint32_t *dst = (volatile uint32_t *)(uintptr_t)PARAM_ADDR;
    int rc;

    /* ① 在 RAM 里拼好整页：没用到的字节留 0xFF，和擦除后的状态一致 */
    for (uint32_t i = 0U; i < (NVM_PAGE_SIZE / 4U); i++) { page_image[i] = 0xFFFFFFFFUL; }
    (void)memcpy((void *)page_image, (const void *)p, sizeof(param_t));

    /* ② 先擦：ADDR 是半字偏移，主阵列区起始是 0，所以正好是 addr >> 1。
          一次擦掉整行 256 字节——Flash 只能把 1 写成 0，要让位回到 1 只能这样 */
    NVMCTRL_REGS->NVMCTRL_ADDR = (PARAM_ADDR >> 1);
    rc = nvm_exec(NVMCTRL_CTRLA_CMD_ER);
    if (rc != NVM_OK) { return rc; }

    /* ③ 后写：逐个 32 位写进页缓冲（8 位写会触发系统异常），再发 WP 提交 */
    for (uint32_t i = 0U; i < (NVM_PAGE_SIZE / 4U); i++) { dst[i] = page_image[i]; }
    rc = nvm_exec(NVMCTRL_CTRLA_CMD_WP);
    if (rc != NVM_OK) { return rc; }

    /* ④ 读回核对：写完的 Flash 用普通指针就能读 */
    if ((q->magic != p->magic) || (q->counter != p->counter) || (q->sum != p->sum))
    {
        return NVM_ERR_VERIFY;
    }
    return NVM_OK;
}

/* 读回：魔数判断"写过没有"，累加和判断"内容完不完整" */
int param_load(param_t *out)
{
    const param_t *p = (const param_t *)(uintptr_t)PARAM_ADDR;

    if (p->magic != PARAM_MAGIC) { return NVM_ERR_EMPTY; }   /* 没写过的 Flash 全是 0xFF */
    if (p->sum != param_sum(p))  { return NVM_ERR_VERIFY; }
    (void)memcpy((void *)out, (const void *)p, sizeof(param_t));
    return NVM_OK;
}

int main(void)
{
    param_t p;
    int rc = param_load(&p);

    if (rc == NVM_ERR_EMPTY)              /* 第一次上电：写一份默认值 */
    {
        (void)memset((void *)&p, 0, sizeof(p));
        p.magic = PARAM_MAGIC; p.version = 1U; p.counter = 1U;
        (void)memcpy((void *)p.name, "samd21", 6U);
        p.sum = param_sum(&p);
        rc = param_save(&p);
    }
    else if (rc == NVM_OK)                /* 以后每次上电：计数加一存回去 */
    {
        p.counter++;
        p.sum = param_sum(&p);
        rc = param_save(&p);
    }

    while (1) { __NOP(); }                /* 出错就停在这里，用调试器看 rc */
}
```

这段代码每一句都能在前面找到理由：`CTRLA` 里为什么有钥匙（13.5）、为什么先 `ER` 后 `WP`（13.2）、为什么 `ADDR` 要右移一位（13.8）、为什么写 32 位而不是 8 位（13.3）、为什么会卡住几毫秒（13.9）、为什么最后要读回（13.10）。

还有三个工程上的注意点，代码里体现不出来但迟早会遇到：

| 注意点 | 说明 |
|---|---|
| 频繁保存会写坏 Flash | 每次保存都是一次擦 + 写，寿命 25k 次。计数器这种"每次都变"的数据最好攒一批再写，或轮流用两页 |
| 掉电时机 | 擦完还没写的时候掉电，这一行就是空的。要抗这个得用"双页 + 序号"：新数据写另一页，写完做标记，读的时候取序号大的那份 |
| 中断和 Flash 写 | 写 Flash 期间中断被推迟最长 6 ms。如果中断里也要读写参数，必须保证不会在擦写过程中重入 |

## 13.12 NVMCTRL 初始化清单

以后遇到"要保存数据"，按这个顺序想就不会漏：

| 步骤 | 做什么 | 关键点 |
|---|---|---|
| ① | 选地址 | 避开 Bootloader 区、EEPROM 仿真区（本芯片默认都是"无"），并按行对齐 |
| ② | 改链接脚本 | 把参数区从 ROM 里挖掉，否则代码可能住进去 |
| ③ | 确认没被锁 | `LOCK` 复位值 0xFFFF = 全部未锁；位为 0 才是锁住 |
| ④ | 提高主频前先设等待周期 | `CTRLB.RWS = 1` 对应到 48 MHz |
| ⑤ | 擦行 | `ADDR = addr >> 1`，然后 `CTRLA` 里同时写 `CMDEX_KEY` 和 `ER`，等 `READY` |
| ⑥ | 装页缓冲 | 只写 16 / 32 位，写的是 Flash 地址 |
| ⑦ | 写页 | `CTRLA` 里同时写 `CMDEX_KEY` 和 `WP`，等 `READY` |
| ⑧ | 查错误 | `STATUS.PROGE / LOCKE / NVME`，都是写 1 清 |
| ⑨ | 读回校验 | 魔数 + 校验和 / CRC32 |

## 13.13 WDT：程序跑飞了谁来收拾

Flash 让数据活得比电久，WDT（Watchdog Timer，看门狗）负责程序跑飞时把芯片拉回来。三种典型死法 CPU 自己都恢复不了：死循环（等一个永远不来的标志）、指针跑飞（跳到数据区当地址执行）、时钟停摆（程序一条也走不动）。

WDT 的思路很朴素：**一块不依赖 CPU、也不依赖主时钟的独立计数器**。软件定时喂它，喂了就说明程序还活着；超时没喂就复位整个芯片（手册 18.1）。

| 特性 | 含义 | 好处 |
|---|---|---|
| 异步 | 跑在独立的超低功耗振荡器 OSCULP32K 上 | 主时钟停了它还在数 |
| 常开 | 使能后在 Active 和所有睡眠模式下都运行 | 睡着死机也能被拉回来 |
| 独立 | 除 GCLK_WDT 外不依赖任何外设，不需要 CPU 参与 | 越简单越不容易跟着一起坏 |

代价是它不准：手册 18.5.3 明确说 OSCULP32K 精度不高，超时时间因片而异，设计时要留余量。

## 13.14 普通模式与窗口模式

```text
普通模式： ├──────── 16384 周期 ────────┤   这一段里任何时刻喂狗都算数
窗口模式： ├─ 关闭窗 256 ─┤├── 开窗 ──┤   只能在开窗里喂
           ↑ 这里喂 = 立刻复位              不喂（到点）= 复位
```

普通模式由 `CTRL.ENABLE=1`、`CTRL.WEN=0` 决定，抓的是卡死和跑飞；窗口模式由 `CTRL.WEN=1` 加 `CONFIG.WINDOW` 决定，额外抓"喂得太勤"——比如某个中断疯狂触发，主循环其实一步没走。它的额外价值在于：**如果喂狗动作本身被异常代码抢着执行，普通模式是抓不到的**，因为狗一直在被喂；加上关闭窗，喂早了也复位，等于要求程序按节奏活着。窗口模式的总超时时间是两段相加：关闭窗 `WINDOW` + 开窗 `PER`。

## 13.15 WDT 的寄存器、喂狗与复位原因

WDT 一共八个寄存器，本章要认识五个：

| 寄存器 | 偏移 | 回答什么问题 | 关键位 |
|---|---|---|---|
| `CTRL` | 0x00 | 使能吗？窗口模式吗？永远开着吗？ | `ENABLE`(bit1)、`WEN`(bit2)、`ALWAYSON`(bit7) |
| `CONFIG` | 0x01 | 开窗多久、超时多久？ | `WINDOW[3:0]`(bit7:4)、`PER[3:0]`(bit3:0) |
| `EWCTRL` | 0x02 | 提前多久报个警？ | `EWOFFSET[3:0]` |
| `INTENSET` / `INTFLAG` | 0x05 / 0x06 | 早期警告中断 | `EW` |
| `CLEAR` | 0x08 | 喂狗 | 必须写 `0xA5` |

最需要小心的是 `ALWAYSON`：一旦写 1，**只能靠上电复位清除**，之后 `CTRL`、`CONFIG`、`EWCTRL` 全变只读。它的用途是防止跑飞的程序顺手把狗关了，代价是调试时很痛苦——所以在置它之前，必须保证 GCLK_WDT 和它的时钟源都已经在跑（手册 18.6.3 列了三种禁止情形）。

`PER` 和 `WINDOW` 都是"GCLK_WDT 周期数"，取值 0x0–0xB 对应 8 到 16384 个周期，所以时间取决于时钟源：0x5 = 256 周期 ≈ 7.8 ms、0x8 = 2048 周期 ≈ 62.5 ms、0xB = 16384 周期 ≈ 500 ms（按 OSCULP32K 的 32768 Hz 算）。出厂的 User Row 里 `WDT Period` 默认就是 0x0B，`WDT Window` 默认 0x05，`Enable` 和 `Always-On` 默认都是 0——**复位后 WDT 是关着的，要用就得自己开**。

芯片复位的原因不止一种，`PM.RCAUSE` 就是那份事故记录：bit0 `POR` 上电、bit1/bit2 `BOD12`/`BOD33` 掉电、bit4 `EXT` 外部复位、**bit5 `WDT` 看门狗超时**、bit6 `SYST` 软件请求。它只读，反映最近一次复位的原因，**要在程序启动最开头读**，判断逻辑就是一句 `(PM_REGS->PM_RCAUSE & PM_RCAUSE_WDT_Msk) != 0U`。

一个实际会遇到的死循环：程序有 bug 一直不喂狗 → WDT 复位 → 复位后又跑同样的代码 → 再复位，现象是"板子一直重启，调试器都连不上"。处理办法是判断出 `WDT` 复位后**先把狗喂上**，再把错误信息留下来——例程里就是这么做的。

## 13.16 WDT 例程与初始化清单

需求：让 WDT 每 500 ms 超时一次；用窗口模式，喂早了也复位；如果复位是看门狗造成的，先喂一口再继续，避免无限重启。

```c
#include "sam.h"

void wdt_feed(void)
{
    /* 只有 0xA5 是"喂狗"；写别的值会立刻触发系统复位 */
    WDT_REGS->WDT_CLEAR = (uint8_t)WDT_CLEAR_CLEAR_KEY;
}

int main(void)
{
    /* ① WDT 有自己的异步时钟域，先给它配通用时钟（GCLK 的完整用法在第 5 章）：
          GCLK2 的源选 OSCULP32K（32.768 kHz），再把这个发生器接到 WDT（ID = 0x03） */
    GCLK_REGS->GCLK_GENCTRL = GCLK_GENCTRL_ID(2U)
                            | GCLK_GENCTRL_SRC_OSCULP32K
                            | GCLK_GENCTRL_GENEN_Msk;
    while ((GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) != 0U) { }

    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_WDT
                                       | GCLK_CLKCTRL_GEN_GCLK2
                                       | GCLK_CLKCTRL_CLKEN_Msk);
    while ((GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) != 0U) { }

    /* ② 是看门狗把我拉起来的？先喂一口，别让板子无限重启 */
    if ((PM_REGS->PM_RCAUSE & PM_RCAUSE_WDT_Msk) != 0U) { wdt_feed(); }

    /* ③ CONFIG 是 Enable-Protected：必须在 ENABLE=0 时写，否则被丢掉。
          还要先确认 CTRL.ALWAYSON=0，否则它和 EWCTRL 都是只读的 */
    WDT_REGS->WDT_CONFIG = (uint8_t)(WDT_CONFIG_PER_16K | WDT_CONFIG_WINDOW_256);
    /* PER = 16384 周期 ≈ 500 ms 超时；WINDOW = 256 周期 ≈ 7.8 ms 的关闭窗 */

    /* ④ CTRL 是写同步的：WEN=1 进窗口模式，ENABLE=1 开始数 */
    WDT_REGS->WDT_CTRL = (uint8_t)(WDT_CTRL_WEN_Msk | WDT_CTRL_ENABLE_Msk);
    while ((WDT_REGS->WDT_STATUS & WDT_STATUS_SYNCBUSY_Msk) != 0U) { }

    while (1)
    {
        /* ⑤ 主循环每转一圈喂一次。窗口模式要求两次喂狗间隔大于 7.8 ms，所以
              循环里必须真的干活；写 Flash 前要先喂一次，因为擦一行最长 6 ms */
        wdt_feed();
        for (volatile uint32_t i = 0U; i < 20000U; i++) { __NOP(); }
    }
}
```

| 步骤 | 做什么 | 关键点 |
|---|---|---|
| ① | 配 GCLK_WDT（源选 OSCULP32K） | 不配时钟，WDT 根本不动 |
| ② | 确认 `CTRL.ALWAYSON=0` | 它一旦写 1 就只能靠上电复位清掉 |
| ③ | 写 `CONFIG.PER` / `CONFIG.WINDOW` | Enable-Protected，必须在使能前写 |
| ④ | 写 `CTRL.WEN` / `CTRL.ENABLE` | 写同步，等 `STATUS.SYNCBUSY` |
| ⑤ | 主循环里定时喂 `CLEAR = 0xA5` | 喂狗点要覆盖"最长的那条路径" |
| ⑥ | 启动时读 `PM.RCAUSE` | 判断是不是 `WDT` 复位，避免死循环重启 |

## 13.17 CRC32：它在 DSU 里

前面用"魔数 + 累加和"判断参数有没有坏，够用但不专业。工业做法是 **CRC32**：把一段内存的所有字节按多项式算出一个 32 位数，任何一位变了结果几乎必然不同。

SAMD21 上的关键事实是：**它不在一个叫"CRC"的外设里，而在 DSU 里**。DSU 全称 Device Service Unit（器件服务单元），本职工作是调试接口、芯片识别、整片擦除和内存自检，CRC32 只是它提供的服务之一（手册 13.11.3）。这一点和很多人的印象不同——有些芯片上 CRC 是独立外设，这颗芯片上要借用 DSU。

| 问题 | 答案 |
|---|---|
| 算法 | 标准 CRC32，多项式 0xEDB88320（手册 13.11.3） |
| 能算哪些内存 | 主阵列 Flash 和 AHB 上的 SRAM 都能算 |
| 谁在算 | DSU 硬件自己遍历，CPU 只管等 |
| 结果在哪 | `DSU.DATA`，**要取反才是常见工具算出来的值** |
| 初值 | 写到 `DSU.DATA`，标准用法是 `0xFFFFFFFF` |

适合这些场景：上电自检程序完整性（一次算完整个 `.text`，比逐字节读快）、保存大批参数后校验、内存自检。不适合需要逐字节定位错误、或需要纠错的场合——CRC 只能告诉你"坏了"，不能告诉你"哪里坏了"。

## 13.18 用 DSU 校验一段内存

DSU 有五个寄存器参与这件事：`ADDR`（起始地址）、`LENGTH`（字节数）、`DATA`（种子 / 结果）、`CTRL`（发命令）、`STATUSA`（看结果）。顺序固定：**先填三个参数寄存器，最后写 `CTRL` 发命令**；命令进行中 `STATUSA.DONE` 是 0，此时发新命令会被丢弃。

```c
#include "sam.h"

/* PAC 没有"每个外设一位"的宏，位号要按手册 11.6.2.2 的 PAC1 位图自己定义。
   PAC1 复位值是 0x000002，也就是 bit1 的 DSU 默认就被写保护了 */
#define PAC1_WP_DSU  (1UL << 1)

/* 算 [addr, addr+len) 这一段的 CRC32；出错返回 0 */
uint32_t dsu_crc32(uint32_t addr, uint32_t len)
{
    /* ① 解保护：DSU 的寄存器受 PAC1 管，不解锁写进去也白写 */
    PAC1_REGS->PAC_WPCLR = PAC1_WP_DSU;

    /* ② 清掉上一次的 DONE（写 1 清） */
    DSU_REGS->DSU_STATUSA = DSU_STATUSA_DONE_Msk;

    /* ③ 填参数：地址和长度都必须是 4 的倍数（手册 13.11.3.1） */
    DSU_REGS->DSU_ADDR   = addr;
    DSU_REGS->DSU_LENGTH = len;
    DSU_REGS->DSU_DATA   = 0xFFFFFFFFUL;      /* 标准 CRC32 的初值 */

    /* ④ 写 1 到 CTRL.CRC 开始算；硬件在算时 CPU 可以去做别的，这里为了简单就等着 */
    DSU_REGS->DSU_CTRL = DSU_CTRL_CRC_Msk;
    while ((DSU_REGS->DSU_STATUSA & DSU_STATUSA_DONE_Msk) == 0U) { }

    /* ⑤ 先查总线错误（地址或长度不合法会置 BERR），再取结果 */
    if ((DSU_REGS->DSU_STATUSA & DSU_STATUSA_BERR_Msk) != 0U) { return 0U; }
    return ~(DSU_REGS->DSU_DATA);             /* 取反后才是标准 CRC32 结果 */
}

int main(void)
{
    /* 算前 1 KB 的 CRC32（0x400 是 4 的倍数）；真实项目里和 PC 上算出的期望值比一比 */
    return (int)(dsu_crc32(0x00000000UL, 0x400UL) & 0x1U);
}
```

三点提醒：**取反**（忘了会一直对不上）、**对齐**（地址和长度都要字对齐，否则 `STATUSA.BERR` 置位）、**安全位**（置了安全位后从外部访问 DSU 会被限制，从 CPU 内部访问没这个限制，手册 13.11.2.3）。

## 13.19 USB / I2S / PTC 速览

这三个外设任何一个是能写一整章的。这里只回答四个问题：**它能干什么、最小步骤是什么、什么时候值得用、本笔记为什么不展开。**

### USB：一条能直接和 PC 说话的线

| 项目 | 内容 |
|---|---|
| 能干什么 | USB 2.1 全速（12 Mbps）设备模式和嵌入式主机模式；设备模式 8 组端点共 16 个；内置收发器和串阻，不需要外接 PHY；模块自带 DMA，收发不占 CPU |
| 最小步骤 | ① 主频 ≥ 8 MHz（USB 活动期间）；② `GCLK_USB`（ID 0x06）给 48 MHz，设备模式精度 ±0.25%；③ USB 的总线时钟复位默认已开；④ 把 PA24 / PA25 交给 USB；⑤ 在 SRAM 里分配端点描述符和数据缓冲；⑥ 用 USB 中断（IRQ 7）完成枚举 |
| 什么时候值得用 | 要和 PC 通信（虚拟串口 / HID / 数据采集）、用 USB 做固件升级、做主机读 U 盘 |
| 为什么不展开 | 难点是协议栈：描述符、端点状态机、枚举、标准请求，代码量是本章例程的十几倍；而且要先学第 5 章，才能把 DFLL48M 闭锁到 USB SOF（手册 Table 37-64 列了可行的时钟方案） |

### I2S：数字音频的专用通道

| 项目 | 内容 |
|---|---|
| 能干什么 | 和外部音频 codec / DAC / ADC 之间的双向同步数字音频链路；支持标准 I2S、TDM（1–8 槽）和 PDM（MEMS 麦克风）；2 个 Clock Unit + 2 个 Serializer；主模式下能给 codec 输出 16×fs – 1024×fs 的主时钟 |
| 最小步骤 | ① 打开桥 C 的 APB 时钟（`PM.APBCMASK` 的 I2S 位，**复位默认是关的**）；② 配 `GCLK_I2S_0` / `GCLK_I2S_1`（ID 0x23 / 0x24）；③ 用 PMUX 把 SCKn / FSn / SDm / MCKn 交出去；④ 配 Clock Unit 分频和 Serializer 格式；⑤ 接 DMAC 才能连续收发 |
| 什么时候值得用 | 做音频播放 / 录音、接音频 codec、接数字 MEMS 麦克风 |
| 为什么不展开 | 它几乎必然要和 GCLK 分频、DMAC、双缓冲一起用才跑得起来，单独讲没有意义 |

### PTC：不用外接元件的触摸按键

| 项目 | 内容 |
|---|---|
| 能干什么 | 电容触摸采集，自电容（一个电极一个引脚）和互电容（X-Y 矩阵）都支持；48 脚封装支持 10 个自电容按键 / 120 个互电容节点；有硬件滤波、自动校准和漂移补偿；支持在 Standby 下被触摸唤醒 |
| 最小步骤 | ① 打开桥 C 的 APB 时钟（`PM.APBCMASK` 的 PTC 位，复位默认关）；② 配 `GCLK_PTC`（ID 0x22，48 MHz）——注意 DFP 3.7.262 的 `gclk.h` 里**没有**这个 ID 的宏，要自己写 `(0x22UL << GCLK_CLKCTRL_ID_Pos)`；③ 把电极引脚接上；④ 配置采集通道和增益；⑤ 用采集完成中断读数 |
| 什么时候值得用 | 做触摸按键 / 滑条 / 滚轮，尤其是要低功耗待机、被触摸才唤醒的产品 |
| 为什么不展开 | 寄存器只是入口，真正的工作量在 QTouch 库和调参上：灵敏度、防水、噪声、温漂都要用实测数据慢慢调 |

三个外设的共同点值得记住：**它们都"不只是寄存器"**。USB 背后是协议栈，I2S 背后是时钟树和 DMA，PTC 背后是模拟参数和调试工具。学外设的顺序永远是"先知道它能干什么、什么时候用得上"，等到真要用的时候再回头啃手册。

## 13.20 一个存储与可靠性的完整思维模型

```text
                      存储与可靠性
                           │
     ┌───────────┬─────────┴─────────┬──────────────┐
   NVMCTRL      WDT             DSU.CRC32      USB/I2S/PTC
  （存得住）  （有人管）        （查得出）     （本章只认脸）
 行擦→页写→读回 喂 0xA5→超时复位 种子→算→取反   各自一整章
```

## 13.21 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 读出来还是 0xFF，或变成新旧数据按位与的结果 | 没发 `WP`（或 `MANW=0` 却没写满整页）；忘了先擦 | 有没有发 `WP` / `ER`；`CTRLB.MANW` |
| 擦完数据不对 | `ADDR` 写成了字节地址 | `ADDR = 字节地址 >> 1`，辅助区还要减区起始 |
| `CTRLA` 写完没反应，`PROGE=1` | 钥匙不是 `0xA5`，或没和命令写在一起 | 一次 16 位写入 `CMDEX_KEY` 加命令 |
| 命令被无视且没有报错 | 发命令时 `READY=0` | 先等 `INTFLAG.READY` |
| 写 Flash 时程序明显卡住 | CPU 从同一块 Flash 取指，AHB 被 stall | 正常：擦 6 ms、写 2.5 ms，安排到空闲时段 |
| 用 `uint8_t` 指针写 Flash 直接进异常 | 页缓冲不接受 8 位写 | 改成 16 / 32 位写 |
| 48 MHz 下读 Flash 出错 | 等待周期没配上 | 提频前先设 `CTRLB.RWS=1` |
| 改了 User Row 没生效 | 辅助区的值只在复位时加载 | 复位；另外 `ADDR` 别忘了减 `0x00800000` |
| 板子一上电就重启，调试器连不上 | WDT 被 `ALWAYSON` 打开又没喂狗 | `PM.RCAUSE` 的 `WDT` 位；用整片擦除清掉 User Row |
| 程序跑飞了却没有复位 | WDT 根本没使能，或喂狗喂在中断里 | `CTRL.ENABLE`；喂狗点是否覆盖最坏路径 |
| CRC32 和 PC 上算的对不上 | 没取反、初值不对、长度没对齐 | `~DSU.DATA`；种子 `0xFFFFFFFF`；长度是 4 的倍数 |

## 13.22 寄存器速查

NVMCTRL（基址 `0x41004000`，桥 B，AHB/APB 时钟复位默认已开，中断号 5）：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `CTRLA` | 0x00 | bit6:0 `CMD`、bit15:8 `CMDEX`（必须 0xA5） |
| `CTRLB` | 0x04 | `RWS[4:1]` 等待周期、`MANW`(7)、`SLEEPPRM[9:8]`、`READMODE[17:16]`、`CACHEDIS`(18) |
| `PARAM` | 0x08 | 只读：`NVMP` 页数、`PSZ[18:16]` 页大小、`RWWEEP`（本芯片为 0） |
| `INTENCLR` / `INTENSET` / `INTFLAG` | 0x0C / 0x10 / 0x14 | bit0 `READY`（只读）、bit1 `ERROR`（写 1 清） |
| `STATUS` | 0x18 | `PRM`(0)、`LOAD`(1)、`PROGE`(2)、`LOCKE`(3)、`NVME`(4)、`SB`(8)，错误位写 1 清 |
| `ADDR` | 0x1C | 半字偏移：（字节地址 − 区起始）/ 2 |
| `LOCK` | 0x20 | 16 个区域锁位，**1 = 未锁**、0 = 锁住 |

WDT（基址 `0x40001000`，桥 A，中断号 2）与 DSU（基址 `0x41002000`，桥 B，PAC1 默认保护）：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `WDT.CTRL` | 0x00 | `ENABLE`(1)、`WEN`(2)、`ALWAYSON`(7) |
| `WDT.CONFIG` | 0x01 | `PER[3:0]`、`WINDOW[7:4]`，取值 0x0–0xB 对应 8 – 16384 个时钟 |
| `WDT.EWCTRL` | 0x02 | `EWOFFSET[3:0]` |
| `WDT.INTENCLR` / `INTENSET` / `INTFLAG` | 0x04 / 0x05 / 0x06 | bit0 `EW` |
| `WDT.STATUS` | 0x07 | bit7 `SYNCBUSY` |
| `WDT.CLEAR` | 0x08 | 写 `0xA5` 喂狗，别的值立刻复位 |
| `DSU.CTRL` | 0x00 | `SWRST`(0)、`CRC`(2)、`MBIST`(3)、`CE`(4) 整片擦除 |
| `DSU.STATUSA` | 0x01 | `DONE`(0)、`CRSTEXT`(1)、`BERR`(2)、`FAIL`(3)、`PERR`(4)，全部写 1 清 |
| `DSU.ADDR` / `LENGTH` / `DATA` | 0x04 / 0x08 / 0x0C | 起始地址（4 对齐）/ 字节数（4 的倍数）/ CRC 种子与结果 |
| `DSU.DID` | 0x18 | 器件 ID（型号、系列、版本） |
| `PM.RCAUSE` | 0x38 | bit5 是 WDT 复位标志 |

## 13.23 最重要的几句话

1. Flash 只能把 1 写成 0，要把位变回 1 只能整行擦——**写之前必须先擦，擦除的最小单位是 256 字节的行**。
2. 往 Flash 地址上赋值只是装进 64 字节的页缓冲，**要发 `WP` 命令才真的写进 Flash**；页缓冲只接受 16 / 32 位写，8 位写会触发系统异常。
3. `CTRLA` 的钥匙 `0xA5` 必须和命令在同一次写里落下，否则不执行并置 `PROGE`。
4. `ADDR` 里是**半字偏移**：主阵列是"字节地址 >> 1"，辅助区还要先减掉区起始地址。
5. 同一行最多连续写 8 次；擦写寿命最小 25k 次，频繁保存的数据要摊开写。
6. 擦写期间 CPU 从同一块 Flash 取指会被 stall 最长 6 ms，这颗芯片没有 RWWEE 可以绕。
7. WDT 是不依赖 CPU 和主时钟的计数器，喂 `CLEAR=0xA5` 续命，超时就复位（窗口模式连"喂得太早"也抓）；启动最开头要读 `PM.RCAUSE` 的 bit5，看这次复位是不是它干的。
8. CRC32 在 DSU 里而不是独立外设；结果要取反，DSU 的寄存器默认受 PAC1 保护。

## 13.24 自测

1. 为什么"往 `0x0003FFC0` 写一个 32 位数"不会改变 Flash 的内容？那一次写到底写到哪里去了？
2. 只想改 Flash 里的 1 个字节，为什么实际上要动 256 个字节？这个数字是哪里来的？
3. 有人只写了 `NVMCTRL_REGS->NVMCTRL_CTRLA = NVMCTRL_CTRLA_CMD_ER;`，擦除没发生。说出至少两个可能原因，以及各自对应的状态位。
4. 要擦除 `0x0003FF00` 开始的那一行，`NVMCTRL_ADDR` 应该写什么？换成 User Row（`0x00804000`）呢？两次计算的区别在哪？
5. 主循环里要求"每 100 ms 存一次参数"，直接照 13.11 的例程写有什么长期风险？有哪些改法？
6. 为什么 WDT 的超时时间用"周期数"而不是"毫秒"来描述？窗口模式比普通模式多抓到了哪一类故障？

## 附录：以后查手册怎么找

NVMCTRL、WDT、DSU 分别在第 22、18、13 章，查法都一样：① 找外设章节 → ② 看 Overview / Features 知道它能干什么 → ③ 看 Functional Description 搞清流程 → ④ 看 Register Summary 找偏移 → ⑤ 看具体寄存器描述的 bit 表。

| 我想知道 | 去哪一节 |
|---|---|
| Flash 怎么组织、页多大 | 手册 10.2 / Table 10-3、22.6.2 |
| 命令有哪些、编号是多少 | 手册 22.8.1 的 `CMD` 表 |
| 写一次的完整流程 | 手册 22.6.4.3（手动写页）、22.6.4.5（擦行） |
| `ADDR` 到底怎么填 | 手册 22.8.8 的例子 |
| 擦写时间、寿命、主频与等待周期 | 手册 Table 41-33 / 41-34 / 41-36 |
| User Row 里有什么、默认值多少；校准值和序列号在哪 | 手册 Table 10-7、10.3.2、10.3.3 |
| WDT 怎么使能、怎么喂、窗口时序 | 手册 18.6.2.3 / 18.6.2.4 / 18.6.2.5、18.8.1 – 18.8.8 |
| 复位原因 | 手册 16.8.14（`RCAUSE`） |
| CRC32 怎么用、怎么解释结果 | 手册 13.11.3 / 13.11.3.1 / 13.11.3.2 |
| USB / I2S / PTC 的入口 | 手册 32 / 29 / 36 章的 Overview 和 Features |

查寄存器时的两条经验：**名字以 DFP 头文件为准**（`samd21a\include\component` 下的 `nvmctrl.h`、`wdt.h`、`dsu.h`），**位定义以手册的 bit 表为准**；两者对不上时，先看手册，再回头确认自己引用的头文件版本。