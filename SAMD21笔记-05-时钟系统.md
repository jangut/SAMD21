
# 第 5 章 时钟系统：从 1 MHz 到 48 MHz

> **芯片**：ATSAMD21G18A（Cortex-M0+，主频上限 48 MHz）
> **本章目标**：搞清时钟从哪来、怎么分频、怎么送到外设；然后把复位后的 1 MHz 主频改成 48 MHz，并用流水灯看出速度变化。
> **学习顺序**：为什么需要时钟 → 同步时钟和异步时钟 → 7 个时钟源 → GCLK 的 9 个发生器 → 把通用时钟分配给外设 → PM 的门控 → 同步等待 → 复位默认状态 → 寄存器 → 从 1 MHz 到 48 MHz → 完整例程 → 常用配方 → 初始化清单 → 思维模型 → 常见问题 → 速查 → 自测。

## 5.0 为什么 MCU 需要一个时钟

第 4 章的 GPIO 代码里一个"时钟"字都没出现，但芯片内部每一刻都在用它。原因是：**数字电路由一排排触发器组成，触发器只在时钟边沿才把输入搬到输出**，没有节拍，电路就不知道"什么时候该更新状态"。所以时钟不是"给 CPU 提速用的开关"，而是整块芯片动作的节拍器。频率 f 是每秒的节拍数（48 MHz = 每秒 4800 万个节拍），周期 T = 1/f 是一个节拍的长度（48 MHz → 约 20.8 ns）；产生节拍的电路叫"时钟源"，把节拍变慢叫"分频"（8 MHz ÷ 8 = 1 MHz）。

那为什么不干脆用一个固定的最高频率？三个理由：**功耗**（频率越高越费电）、**有些外设天生要特定频率**（USB 必须 48 MHz，RTC 要 32.768 kHz，否则计时不准）、**精度和启动时间要取舍**（内部 RC 起振快但会漂，外部晶振准但起振可能要几百毫秒）。所以 SAMD21 的做法是：备好几个性格不同的振荡器，再用一套"配电盘"把它们分给需要的地方。

如果有 STM32 的基础，两边其实一一对应，只是名字不同：

| 要做的事 | STM32 上的叫法 | SAMD21 上的叫法 |
|---|---|---|
| 振荡器开关与就绪标志 | RCC 的 HSE/HSI/PLL 使能位 | SYSCTRL 的 `XOSC`/`OSC8M`/`DFLLCTRL` 与 `PCLKSR` 就绪位 |
| 系统时钟切换与分频 | RCC 的 `SW` 与 AHB/APB 预分频 | GCLK 发生器 + PM 的 `CPUSEL`/`APBxSEL` |
| 外设时钟使能 | RCC 的 `APBxENR` | PM 的 `APBAMASK`/`APBBMASK`/`APBCMASK` |
| 外设功能时钟 | 通常就是它所在的那条 APB 时钟 | 独立的通用时钟 GCLK（可以另给一个频率） |

SAMD21 比 STM32 多一层：**外设的总线接口和它的内部核心跑在两个不同的时钟上**——这一层是本章所有"麻烦"的来源。

## 5.1 同步时钟与异步时钟

手册 14.2 给的官方定义很短：**Synchronous Clocks（同步时钟）指 CPU 和总线时钟；asynchronous clocks（异步时钟）指 GCLK 产生、送给外设的时钟**。"同步"的意思是：两个电路如果由同一路时钟驱动，节拍完全一致，信号可以直接互相传递；如果各跑各的时钟，频率和相位都没关系，直接传信号就可能正好撞上信号变化的瞬间，这叫异步。所以 SAMD21 的每个外设内部被切成两半：

```text
CPU / AHB / APBx ──► 外设的「寄存器接口」（跑同步时钟）
                          ┊  跨时钟域，必须同步
某个时钟源 → GCLK ──► 外设的「核心」（跑通用时钟 GCLK_PERIPHERAL）
```

左边那一半是"CPU 能看到的那部分"——第 4 章写的 `PORT_REGS->GROUP[0].PORT_OUTSET` 走的就是它；右边那一半才是真正干活的电路，比如定时器计数、串口移位。手册 14.3.1.1 说得很清楚：**总线接口里的寄存器不需要同步，核心里的寄存器写入时需要同步**。这带来四个后果：

1. **外设可以跑在和 CPU 完全不同的频率上**。CPU 跑 48 MHz 时，某个定时器可以只跑 32.768 kHz——省电，而且定时更准。
2. **改外设核心的寄存器要等一个"同步完成"标志**，这就是各外设里那个 `SYNCBUSY` 位的来历，GCLK 自己也有（5.6）。
3. **外设核心的时钟越慢，响应也越慢**。手册 14.7 的大意是：给外设的时钟越低，它同步回 CPU 域的等待就越久，响应时间越差。省电和反应快在时钟上是矛盾的。
4. **同步动作一定会发生，哪怕两边频率一样**（手册 14.3.1.1）。所以不能说"反正都是 48 MHz，我跳过等待"。

## 5.2 七个时钟源：SYSCTRL 管的七个振荡器

SAMD21 一共 7 个时钟源，全部由 SYSCTRL 外设控制（手册 17.5.3 列的就是这 7 个）。"时钟源"就是"一个能持续产生稳定方波的电路"，它只负责产生节拍，不负责送给谁——送是 GCLK 的事。

| 时钟源 | 频率 | 类型 | 精度 | 起振 | 典型用途 |
|---|---|---|---|---|---|
| OSC8M | 8 MHz，可分频 1/2/4/8 | 内部 RC | 出厂校准，7.8–8.16 MHz | 快 | 复位后的默认主时钟；不在意频率精确度的场合 |
| OSC32K | 32.768 kHz | 内部 RC | 需自己写校准值 | 1–2 个周期 | 想要 32k 又不想外接晶振 |
| OSCULP32K | 32.768 kHz + 1.024 kHz | 内部 RC，超低功耗 | 出厂校准，但温漂大 | 10 个周期 | 永远在线的计时（WDT、RTC 低功耗） |
| XOSC | 0.4–32 MHz | 外部晶振或外部时钟 | 取决于晶振 | 慢 | 需要精确主频（USB、串口） |
| XOSC32K | 32.768 kHz | 外部晶振或外部时钟 | 取决于晶振 | 可长达 1 秒 | DFLL 的参考、RTC |
| DFLL48M | 48 MHz | 内部锁频环（FLL） | 开环 45–49 MHz；闭环跟随参考 | 几微秒 | 主频 48 MHz |
| FDPLL96M | 48–96 MHz | 内部锁相环（PLL） | 跟随参考 | 锁定需时间 | 给外设提供 96 MHz 或非整数倍频率 |

两个词先解释：**RC 振荡器**靠电阻电容充放电产生节拍，做在芯片内部，便宜、起振快，但频率随温度和电压漂；**晶振**靠石英晶体机械谐振，频率非常准（几十 ppm），但必须外接元件，起振也慢。**FLL（锁频环）和 PLL（锁相环）**都不是独立振荡器，而是"把一个慢而准的参考频率，乘成快而准的输出"——它们的作用就是把晶振的准，搬到高频上去。

### 5.2.1 三个内部 RC：OSC8M、OSC32K、OSCULP32K

`OSC8M` 是复位后唯一在跑的振荡器。它有 `PRESC[1:0]` 分频位，复位值是 `0b11` 也就是 **÷8**，所以芯片一上电主频只有 8 MHz ÷ 8 = **1 MHz**——这就是第 4 章说的"复位后跑得很慢"。它的频率由 `CALIB` 和 `FRANGE` 控制，两个都在启动时从 Flash 工厂校准区自动载入，一般不用动。

`OSC32K` 和 `OSCULP32K` 都是 32.768 kHz，区别在"精度换功耗"：

| | OSC32K | OSCULP32K |
|---|---|---|
| 功耗 | 0.67 µA 典型 | 125 nA 典型（超低功耗） |
| 精度（3.3 V、25 ℃） | 32.276–33.260 kHz | 31.293–34.570 kHz |
| 精度（全温区） | 28.508–34.734 kHz | 25.559–38.011 kHz |
| 校准值 | **必须由软件写入**（来自 NVM 校准区） | 启动时自动载入，也可覆盖 |
| 能不能关 | 能 | **不能，永远在跑** |

手册 17.6.5 给了一句结论：**功耗优先于频率稳定度时用 OSCULP32K，否则用 OSC32K**。至于"更准"，两者都做不到——要准就上 XOSC32K。

### 5.2.2 两个外部晶振：XOSC 和 XOSC32K

`XOSC` 接 0.4–32 MHz 晶振，`XOSC32K` 接 32.768 kHz 晶振。它们的关键位是 `ENABLE`（打开振荡器）、`XTALEN`（1 = 引脚上接的是晶振，0 = 引脚上接外部送进来的方波）、`STARTUP`（起振等待时间，起振期间输出被硬件屏蔽，防止不稳的时钟传进逻辑），外加睡眠相关的 `RUNSTDBY`/`ONDEMAND`（第 6 章再讲）。引脚固定，不用配 PORT（手册 7.2.1 节 Table 7-3）：XOSC 用 XIN = PA14、XOUT = PA15；XOSC32K 用 XIN32 = PA00、XOUT32 = PA01。

还有一条容易踩的规则：**振荡器使能后，引脚就被 SYSCTRL 接管了**。晶振模式下 XIN/XOUT 两个脚都不能再当 GPIO；外部时钟模式下只有 XIN 被占用，XOUT 还能当普通 IO。

"稳定了没有"看 `SYSCTRL_REGS->SYSCTRL_PCLKSR`：`XOSCRDY`、`XOSC32KRDY`、`OSC32KRDY`、`OSC8MRDY`、`DFLLRDY`。**任何时候要拿一个振荡器当参考，都必须先等它对应的 RDY 位变 1。** 手册还专门叮嘱：起振期间不要进 Standby 模式。

### 5.2.3 两个倍频器：DFLL48M 和 FDPLL96M

这两个是"把慢时钟变快"的器件，也是本章的主角。`DFLL48M` 有**两种工作模式**，这是最需要先分清的地方：

| | 开环（Open-Loop） | 闭环（Closed-Loop） |
|---|---|---|
| `DFLLCTRL.MODE` | 0 | 1 |
| 输出频率由谁决定 | `DFLLVAL.COARSE`/`FINE` 直接决定 | 由公式 `fout = MUL × fref` 决定 |
| 精度 | 45–49 MHz（典型 47 MHz） | 47.963–47.981 MHz（参考为 32.768 kHz 晶振时） |
| 需不需要参考时钟 | 不需要 | 需要一个 ≤ 33 kHz 的稳定参考 |
| 适合谁 | 没有外部晶振、只要"差不多 48 MHz" | 有 32.768 kHz 晶振、要 USB 或精确串口 |

开环模式下的 `DFLLVAL.COARSE` 不是一个随便的值：芯片出厂时测好了一个校准值放在 NVM 里（手册 10.3.2 表 10-8，位 63:58，叫 `DFLL48M COARSE CAL`），**把这个值写进 COARSE，输出就接近 48 MHz**；不写的话频率会跑得很远。

闭环模式下有个硬性要求，很多教程会漏：**参考时钟必须 ≤ 33 kHz，而且误差不超过 2%**（手册 Table 37-7 里 `fGCLK_DFLL48M_REF` 的上限是 33 kHz；DFLL 特性表注明闭环参考必须在 2% 误差以内）。手册 17.6.7.1.5 写得更直白：**"外部振荡器推荐，内部振荡器禁止"**，最好的选择是 XOSC32K。也就是说"用 1 MHz 的 OSC8M 当参考、乘 48 倍"这种做法不符合手册规格——虽然它偶尔也能锁上。

`FDPLL96M` 输出 48–96 MHz，参考可以从 XOSC32K、XOSC 或者一路 GCLK 里选（`DPLLCTRLB.REFCLK`），参考频率上限 2 MHz。倍率公式是 `fout = fref × (LDR + 1 + LDRFRAC/16)`，`LDR` 是整数部分、`LDRFRAC` 是 1/16 步进的分数部分。手册举的例子：参考 32 kHz、要 48 MHz，倍率是 1500，所以 `LDR = 1499`；如果要 48.006 MHz，倍率是 1500.1875 = 1500 + 3/16，所以 `LDR = 1499`、`LDRFRAC = 3`。**FDPLL96M 的输出最低就是 48 MHz，而 CPU 又最高只能 48 MHz，所以它实际上不是给 CPU 用的，是给需要 96 MHz 的外设用的**（5.11 会讲什么时候值得用）。

## 5.3 GCLK：9 个发生器和一堆"插座"

时钟源只会产生节拍，不会自己找上门。中间那一层叫 **GCLK（Generic Clock Controller，通用时钟控制器）**，它做两件事：把源加工成想要的频率（**发生器 Generator**），再把加工好的频率分给具体外设（**通用时钟通道 Generic Clock**）。用配电打比方：**发生器是发电机组**（决定发多少赫兹），**通道是墙上的插座**（决定这台电器插哪一路电）。要用一台电器，得同时做两件事：让发电机组转起来，并且把插座接到那一路。

SAMD21 有 **9 个发生器**，编号 GCLKGEN0–GCLKGEN8。

| 发生器 | 复位后的状态 | 特殊之处 |
|---|---|---|
| GCLKGEN0 | 源 = OSC8M，已使能 | 输出就是 **GCLK_MAIN**，唯一直接送给 PM 的那一路，决定 CPU 和总线的速度；**不能被 WRTLOCK 锁**，任何复位都会把它恢复成已知状态 |
| GCLKGEN1 | 关闭（源 = XOSC） | **唯一能当其它发生器源的发生器**（`SRC = 0x02`），但不能当自己的源 |
| GCLKGEN2 | 源 = OSCULP32K，已使能 | 复位后专门给 WDT 用 |
| GCLKGEN3–8 | 关闭 | 一般给外设用，随便分配 |

（手册 15.6.1 的一句正文写成 "eight Generic Clock Generators"，但寄存器表和各处框图都是 9 个（ID 0x0–0x8）。以寄存器表为准。）每个发生器的源从这 9 个里选一个（手册 Table 15-2，寄存器里的 `GENCTRL.SRC`）：

| `SRC` | 源 | `SRC` | 源 |
|---|---|---|---|
| 0x00 | XOSC | 0x05 | XOSC32K |
| 0x01 | GCLKIN（外部从引脚送进来的时钟） | 0x06 | OSC8M |
| 0x02 | GCLKGEN1 的输出 | 0x07 | DFLL48M |
| 0x03 | OSCULP32K | 0x08 | FDPLL96M |
| 0x04 | OSC32K | 0x09–0x1F | 保留 |

分频由两个寄存器配合完成：`GENDIV.DIV` 给分频值，`GENCTRL.DIVSEL` 决定怎么解释这个值。`DIVSEL = 0` 时输出 `fsrc / DIV`（`DIV = 0` 或 `1` 都表示不分频）；`DIVSEL = 1` 时输出 `fsrc / 2^(DIV+1)`，只需要 2 的幂时用这个。各发生器能写多少位不一样（手册 GENDIV 寄存器说明）：GEN0 只有 8 位（最大 ÷512），GEN1 有 16 位（最大 ÷131072），GEN2 只有 5 位（最大 ÷64），GEN3–GEN8 是 8 位。还有一个细节：**奇数分频时占空比不是 50/50**，要 `GENCTRL.IDC = 1` 才能修成 50%，给外设当采样时钟时很重要。整条链条长这样：

```text
7 个时钟源 ──► 9 个发生器(GENDIV÷, GENCTRL 选源) ──► 通道(CLKCTRL) ──► 外设
                                            └── GCLKGEN0 ──► GCLK_MAIN ──► PM ──► CPU/AHB/APBx
```

## 5.4 把通用时钟分配给外设：CLKCTRL 的三个字段

通道的配置全在 `GCLK_CLKCTRL` 这一个 16 位寄存器里：

| 字段 | 位置 | 回答什么问题 |
|---|---|---|
| `ID[5:0]` | bit 5:0 | 这一路时钟给谁？（SERCOM0_CORE 是 0x14） |
| `GEN[3:0]` | bit 11:8 | 由哪个发生器供？（GCLK0–GCLK8 对应 0x0–0x8） |
| `CLKEN` | bit 14 | 这一路现在通不通？ |
| `WRTLOCK` | bit 15 | 锁死，防止被后续代码误改（只有 Power Reset 能解） |

**ID 是"插座"（给谁），GEN 是"接到哪一路发电机组"，CLKEN 是"这个插座的闸合不合"**——理解这三个字段的分工，就理解了 GCLK。那 `ID` 的编号从哪来？两个地方必须对着看：手册 GCLK 章 CLKCTRL 寄存器说明里的 **Table 15-3**，和 DFP 的 `component/gclk.h` 里的宏 `GCLK_CLKCTRL_ID_xxx_Val`——两者一一对应，写代码时永远用宏，不要写数字。挑几个常用 ID：

| ID | DFP 里的值宏 | 给谁 |
|---|---|---|
| 0x00 | `GCLK_CLKCTRL_ID_DFLL48_Val` | DFLL48M 的参考输入 |
| 0x01 / 0x02 | `_FDPLL_Val` / `_FDPLL32K_Val` | FDPLL96M 的参考 / 内部 32k 计时 |
| 0x03 / 0x04 / 0x05 / 0x06 | `_WDT_Val` / `_RTC_Val` / `_EIC_Val` / `_USB_Val` | WDT、RTC、EIC、USB |
| 0x13 | `GCLK_CLKCTRL_ID_SERCOMX_SLOW_Val` | 六个 SERCOM 共用的慢速时钟 |
| 0x14–0x19 | `GCLK_CLKCTRL_ID_SERCOM0_CORE_Val` … `_SERCOM5_CORE_Val` | 每个 SERCOM 各一路核心时钟 |
| 0x1A–0x1D | `_TCC0_TCC1_Val` / `_TCC2_TC3_Val` / `_TC4_TC5_Val` / `_TC6_TC7_Val` | 定时器/计数器，两两共用一路 |
| 0x1E / 0x21 | `_ADC_Val` / `_DAC_Val` | ADC / DAC |

注意 0x1A–0x1D 那几行：**TCC0 和 TCC1 共用同一路通用时钟**，你没法让 TCC0 跑 1 MHz 而 TCC1 跑 8 MHz。这跟 STM32 上每个定时器一个独立时钟不同。

### 为什么 SERCOM 要 CORE 和 SLOW 两路

手册 25.5.3 的原话是：**SERCOM 用两个通用时钟 `GCLK_SERCOMx_CORE` 和 `GCLK_SERCOMx_SLOW`；核心时钟在 SERCOM 作为主机（host）工作时是必需的，慢速时钟只在某些特定功能下才需要。** 为什么一个外设要两路？因为外设里不同部分对节拍的要求不一样：**CORE 是"干活的节拍"**——串口移位、SPI 打拍子、I2C 产生 SCL，它决定通信速率，所以要能自由选频率；**SLOW 是"辅助功能的节拍"**——某些模式下需要一路与总线异步的慢时钟做超时检测、总线空闲判断之类的事，频率低、要求不严。把它独立出来，就不用为了一个边角功能去迁就 CORE 的频率。实际后果：**做普通 UART/SPI/I2C 时 SLOW 通常可以先不配**（具体看对应模式章节的要求），但一旦用到那几种特殊功能，忘了配 SLOW 就会表现为"别的都对，就是某个功能不工作"。

如果某个通道已经开着，你想换一个发生器，手册 15.6.3.3 规定必须按这个顺序，否则切换瞬间会产生毛刺（glitch），外设可能收到一个畸形的窄脉冲：**先写 `CLKCTRL.CLKEN = 0`，读回确认它真的是 0，再写 `CLKCTRL.GEN` 换源，最后 `CLKCTRL.CLKEN = 1`。**

## 5.5 PM 的门控：和 GCLK 完全是两件事

新手最容易混的就是这里。**"外设有没有时钟"其实包含两个独立的问题**：

| 问题 | 谁回答 | 不开的后果 |
|---|---|---|
| 外设内部的逻辑有没有节拍在跑 | GCLK 通道的 `CLKEN` | 外设核心不工作，寄存器能读能写但没反应 |
| CPU 能不能读写这个外设的寄存器 | PM 的 `APBAMASK`/`APBBMASK`/`APBCMASK` | 寄存器**读出来全是 0**，写进去被丢弃 |

手册 14.4 把"启用一个外设"的四件事列全了：一个在跑的时钟源 → 发生器选好源并使能 → 通道选好发生器并 `CLKEN = 1` → 最后在 PM 里把这个外设的总线接口解封。第四件事漏掉时的现象很典型：**手册原话说"寄存器会读出全 0，任何写操作都会被丢弃"**——不报错，静悄悄什么都没发生。四种组合：

| GCLK `CLKEN` | PM 掩码位 | 现象 |
|---|---|---|
| 0 | 0 | 完全没反应，寄存器读全 0 |
| 0 | 1 | 寄存器能读写，但外设不动作（比如定时器不计数） |
| 1 | 0 | 寄存器读全 0、写被丢弃；外设内部可能仍在跑，只是 CPU 看不见它 |
| 1 | 1 | 正常 |

三个掩码寄存器的复位值不一样，这决定了"点灯为什么不用配时钟"：`APBAMASK`（0x18）复位值 0x0000007F，已开 PAC0、PM、SYSCTRL、GCLK、WDT、RTC、EIC；`APBBMASK`（0x1C）复位值 0x0000007F，已开 PAC1、DSU、NVMCTRL、**PORT**、DMAC、USB、HMATRIX；`APBCMASK`（0x20）复位值 0x00010000，**只有 ADC**。第 4 章点灯不需要写任何时钟配置，就是因为 **PORT 在桥 B 上，而桥 B 的掩码位复位就是 1**。反过来，**SERCOM、TC、TCC、DAC、AC、PTC、I2S、EVSYS 全都在桥 C 上，复位后默认关闭**——以后学串口、定时器时，第一步永远是先 `PM_REGS->PM_APBCMASK |= PM_APBCMASK_xxx_Msk;`。

拿 SERCOM0 举例，把它从零配到"能开始写自己的寄存器"，一共四步（这也是手册 14.1 那张图想表达的）：

```c
PM_REGS->PM_APBCMASK |= PM_APBCMASK_SERCOM0_Msk;              /* ① 开总线接口 */

GCLK_REGS->GCLK_GENCTRL = GCLK_GENCTRL_SRC_DFLL48M            /* ② 发生器 3：源 = DFLL48M */
                        | (3UL << GCLK_GENCTRL_ID_Pos)
                        | GCLK_GENCTRL_GENEN_Msk;
while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_SERCOM0_CORE        /* ③ 通道：SERCOM0 的核心时钟 */
                        | (3UL << GCLK_CLKCTRL_GEN_Pos)       /*    接发生器 3 */
                        | GCLK_CLKCTRL_CLKEN_Msk;
while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
/* ④ 最后才去写 SERCOM0 自己的寄存器（SERCOM 要先选身份，见 5.8 开头） */
```

## 5.6 同步：什么时候必须等 SYNCBUSY

前面反复出现 `while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }`，这里说清它为什么存在、什么时候必须等。GCLK 的配置寄存器跑在总线时钟域，而被配置的对象（发生器、通道）跑在各自的时钟域，改设置要让新值"过一道门"，这道门需要时间。硬件用 `STATUS.SYNCBUSY` 位表示"我正在搬"。手册 15.6.6 列了 GCLK 里**写同步**的寄存器：`GENCTRL`、`GENDIV`、`CTRL`。`CLKCTRL` 也参与同步，只是它没有自己的 busy 位，表现为 **`CLKCTRL.CLKEN` 会一直读回旧值，直到同步完成**。

那不等会怎样？手册 15.6.6 说得很明确：**如果在一个需要同步的操作还没完成时又发起新操作，总线会被 stall（停等），最终所有操作都会成功，只是 CPU 被停住、中断被挂起。** 所以"不等"通常不会算错，只是变慢。真正必须等的是这两种：

| 场景 | 为什么必须等 |
|---|---|
| 切换 `GCLK0` 的源（改主频） | CPU 自己就吃这路时钟。不等的话，接下来的代码在新时钟稳定前就跑出去了，延时、波特率、Flash 访问时序全按错的频率算 |
| 换通道的源 | 手册要求确认 `CLKEN` 已经读回 0（5.4），本质就是等同步完成 |

养成习惯：**每次写完 `GENCTRL`、`GENDIV`、`CLKCTRL`，下一句就等一次 `SYNCBUSY`。** 多等不会错，少等会随机出错——这种错最难查。顺带区分两个"忙"位：GCLK 的 `GCLK_STATUS.SYNCBUSY` 表示 GCLK 自己的寄存器搬完了没有；DFLL 的 `SYSCTRL_PCLKSR.DFLLRDY` 属于 SYSCTRL，表示 DFLL48M 的设置生效、振荡器在跑了。**DFLL 是 SYSCTRL 的外设，它的忙闲位不在 GCLK 的 `STATUS` 里。**

## 5.7 复位之后芯片是什么状态

理解了这一节，第 4 章的代码为什么能跑就完全说得通了。手册 14.8 和 16.6.2.1 给的是：

| 复位后的状态 | 值 | 结果 |
|---|---|---|
| OSC8M | 使能，`PRESC = 0b11`（÷8） | 输出 1 MHz |
| `GENCTRL[0]` | 源 = OSC8M，`GENEN = 1` | GCLK_MAIN = 1 MHz |
| `GENDIV[0]` | `DIV = 0` | 不分频 |
| `CPUSEL` / `APBxSEL` | 全 0 | CPU、AHB、APBx 都不分频 |
| `GENCTRL[2]` | 源 = OSCULP32K，`GENEN = 1` | GCLK2 在跑 |
| `CLKCTRL`（WDT，ID = 0x03） | `GEN = 0x02`，`CLKEN = 1` | WDT 的时钟默认来自 GCLK2 |

所以复位后的实际速度链条是：**OSC8M 8 MHz → ÷8 → 1 MHz → GCLK0 不分频 → GCLK_MAIN = 1 MHz → CPU 不分频 → fCPU = 1 MHz**。复位值本身就能读出这些信息，以 `GENCTRL[0]` 为例：

```text
GENCTRL[0] 复位值 = 0x00010600
 bit21..17   bit16   bit12..8          bit3..0
 RUNSTDBY…   GENEN   SRC               ID
  00000        1     0b00110 = OSC8M     0000
```

WDT 那条要多说一句：CLKCTRL 的复位值取决于 **NVM User Row 里的 WDT Enable 位**（位 25）。只有这一位为 1（配置成"上电就开看门狗"）时，WDT 的通用时钟才会在复位后自动接上 GCLK2。**如果板子上 WDT 是开着的，而你把 GCLK2 的源改了，看门狗的超时时间就会跟着变**——这属于"改了时钟之后莫名其妙复位"的经典原因。最后一条：**32 kHz 的时钟源只在 Power Reset 时复位**，其他复位不影响它们（手册 14.8），因为 32.768 kHz 晶振起振太慢（最长 1 秒），复位时把它关掉会带来很长的恢复时间。

## 5.8 寄存器：每个都回答一个问题

现在才看寄存器。下面按"我要做什么 → 所以有哪个开关"组织。先说工程上的写法：**本书统一使用 SAMD21_DFP 3.7.262 的写法**——外设基址是一个 `xxx_REGS` 宏，寄存器成员名是"外设名_寄存器名"。

| 外设 | 基址宏 | 寄存器怎么写 | 子结构 |
|---|---|---|---|
| GCLK | `GCLK_REGS` | `GCLK_REGS->GCLK_GENCTRL = …;` | 无 |
| SYSCTRL | `SYSCTRL_REGS` | `SYSCTRL_REGS->SYSCTRL_DFLLCTRL = …;` | 无 |
| PM | `PM_REGS` | `PM_REGS->PM_APBCMASK \|= …;` | 无 |
| NVMCTRL | `NVMCTRL_REGS` | `NVMCTRL_REGS->NVMCTRL_CTRLB = …;` | 无 |
| PORT | `PORT_REGS` | `PORT_REGS->GROUP[0].PORT_OUTSET = …;` | `GROUP[n]` |
| SERCOM0 | `SERCOM0_REGS` | `SERCOM0_REGS->USART_INT.SERCOM_CTRLA = …;` | `USART_INT`/`I2CM`/`SPIM`… |

三条配套规律：**① 没有 `->REG.reg` 也没有 `->REG.bit.XXX`**，直接写成员；**② 字段宏只有三种**——`_Msk`（掩码）、`_Val`（枚举值）、`_Pos`（位偏移），单位字段用 `_Msk`，多位字段要自己移位；**③ SERCOM 要先选身份**（`USART_INT`/`I2CM`/`SPIM`…）才能摸到它的寄存器。

第 ② 条里藏着一个**能编译但功能错**的陷阱：多位字段的 `_Val` 是**没有移位**的裸值。比如某字段在 bit4:2、`_Val` 是 0x5，直接 `REG = XXX_Val` 会把 0x5 写到 bit2:0 上去，编译一句警告都没有。判断方法很机械：**看 `_Pos`，不为 0 就必须移位**——要么用 DFP 里已经移好位的完整宏（如 `GCLK_GENCTRL_SRC_DFLL48M`、`GCLK_CLKCTRL_ID_SERCOM0_CORE`），要么显式写成 `(XXX_Val << XXX_Pos)`。

### 5.8.1 SYSCTRL：时钟源的总开关面板

| 寄存器 | 偏移 | 它回答的问题 |
|---|---|---|
| `XOSC` | 0x10 | 外部 0.4–32 MHz 开了吗？是晶振还是外部时钟？起振等多久？ |
| `XOSC32K` | 0x14 | 外部 32.768 kHz 同上，外加 `EN32K`（打开 32 kHz 输出） |
| `OSC32K` | 0x18 | 内部 32 kHz 开了吗？校准值写了多少？ |
| `OSCULP32K` | 0x1C | 只有 `CALIB` 和 `WRTLOCK`——它永远在跑，没有 ENABLE |
| `OSC8M` | 0x20 | 8 MHz RC 开了吗？÷1/2/4/8 选哪个（`PRESC`）？ |
| `DFLLCTRL` | 0x24 | DFLL48M 开了吗（`ENABLE`）？开环还是闭环（`MODE`）？锁定前要不要挡住输出（`WAITLOCK`）？ |
| `DFLLVAL` | 0x28 | 开环时输出多少（`COARSE`/`FINE`）；闭环时读回锁到的值 |
| `DFLLMUL` | 0x2C | 闭环时参考频率乘几倍（`MUL`）？每步最多调多少（`CSTEP`/`FSTEP`）？ |
| `DFLLSYNC` | 0x30 | 想在闭环下读 `DFLLVAL`，先往这里写 `READREQ` |
| `PCLKSR` | 0x0C | 7 个源"稳了没有"的指示灯 |
| `DPLLCTRLA`/`DPLLRATIO`/`DPLLCTRLB`/`DPLLSTATUS` | 0x44/0x48/0x4C/0x50 | FDPLL96M 的开关、倍率、参考选择、锁定状态 |

`DFLLVAL` 为什么要分 `COARSE` 和 `FINE`？因为它的振荡频率由两部分电流/电容阵列决定：粗调（`COARSE[5:0]`，64 档）跨度大、精度差；细调（`FINE[9:0]`，1024 档）跨度小、精度高。开环时两个值都要你自己给（粗调用工厂校准值、细调常用中间值 512）；闭环时硬件自动在这两级上搜索，`DFLLVAL` 变成只读。`DFLLCTRL` 里本章要用的位：

| 位 | 名字 | 作用 |
|---|---|---|
| 1 | `ENABLE` | 打开 DFLL 振荡器 |
| 2 | `MODE` | 0 = 开环，1 = 闭环 |
| 3 | `STABLE` | 0 = 细调值持续跟随（默认），1 = 锁定后固定不再调 |
| 4 | `LLAW` | 睡眠醒来后要不要丢掉锁定 |
| 7 | `ONDEMAND` | 默认 1：没有外设请求时就停，省电 |
| 9 / 8 | `QLDIS` / `CCDIS` | 快速锁定 / 冷静周期，默认都是"不禁止" |
| 10 | `BPLCKC` | 跳过粗锁。前提是先把粗调起点写准（就是写工厂校准值那一步） |
| 11 | `WAITLOCK` | 0 = 没锁上也输出时钟；1 = 锁定后才输出 |

`DFLLMUL` 的 `MUL[15:0]` 只在闭环有效，公式就是 `fout = MUL × fref`；`CSTEP`/`FSTEP` 是每步允许的最大调整量，**设小了锁得慢但过冲小，设大了锁得快但频率可能冲过头**，手册建议不要超过 `DFLLVAL.COARSE`/`FINE` 满量程的一半（也就是 `CSTEP ≤ 31`、`FSTEP ≤ 511`）。

### 5.8.2 GCLK：五个寄存器

| 寄存器 | 偏移 | 它回答的问题 |
|---|---|---|
| `CTRL` / `STATUS` | 0x00 / 0x01 | 想把 GCLK 全部恢复默认？（`SWRST`）/ 上一次改动搬完了吗？（bit7 `SYNCBUSY`） |
| `CLKCTRL` | 0x02 | 哪一路通道（`ID`）接哪个发生器（`GEN`）？通了没（`CLKEN`）？ |
| `GENCTRL` | 0x04 | 哪个发生器（`ID`）用哪个源（`SRC`）？使能没（`GENEN`）？怎么分频（`DIVSEL`、`IDC`）？ |
| `GENDIV` | 0x08 | 哪个发生器（`ID`）分频值多少（`DIV`）？ |

这三个寄存器（`CLKCTRL`、`GENCTRL`、`GENDIV`）都带一个 `ID` 字段，这是**间接寻址**：9 个发生器共用一套寄存器地址，你先把编号写进 `ID`，硬件就把那个发生器的内容"接"到这块地址上。写的时候 `GCLK_GENCTRL = ID(3) | SRC_DFLL48M | GENEN` 改的是发生器 3；**读的时候也要先写 ID**，忘了这一步就会读到上一次选中的那个发生器的值——这是查时钟问题时最常见的"看错数据"。`GENCTRL` 里除了 `SRC`、`GENEN`、`DIVSEL`，还有三个本章用不到但值得知道的位：`OE`（把这个发生器的输出送到对应的 GCLK_IO 引脚，用示波器观察时很有用）、`OOV`（关掉时输出高还是低）、`RUNSTDBY`（睡眠时是否继续输出到引脚）。

### 5.8.3 PM：门控和分频

| 寄存器 | 偏移 | 它回答的问题 |
|---|---|---|
| `CPUSEL` | 0x08 | CPU 在 GCLK_MAIN 基础上再 ÷2^CPUDIV，分几？ |
| `APBASEL`/`APBBSEL`/`APBCSEL` | 0x09/0x0A/0x0B | 三条 APB 总线各自再分几？ |
| `AHBMASK` / `APBAMASK`/`APBBMASK`/`APBCMASK` | 0x14 / 0x18/0x1C/0x20 | 各条总线上每个外设的总线接口开不开？ |
| `INTFLAG.CKRDY` | 0x36 | 刚写的分频值生效了吗？ |

两条使用规则。第一，手册要求 **fCPU ≥ fAPBx**，所以不要给 APB 写一个比 CPU 更小的分频数。第二，手册 16.6.2.5 警告：**`CKRDY` 为 0 时不能再写 `CPUSEL`**，否则系统可能不稳定甚至挂死。所以改完分频要等一下：

```c
/* CPUDIV 在 bit2:0，_Pos = 0，所以 _Val 就是寄存器值；这里显式移位只是养成习惯 */
PM_REGS->PM_CPUSEL = (PM_CPUSEL_CPUDIV_DIV1_Val << PM_CPUSEL_CPUDIV_Pos);
while ((PM_REGS->PM_INTFLAG & PM_INTFLAG_CKRDY_Msk) == 0) { }    /* 等生效，期间别重复写 */
```

### 5.8.4 NVMCTRL：一个容易被忘掉的旁支

`NVMCTRL_REGS->NVMCTRL_CTRLB`（偏移 0x04）的 `RWS[4:1]` 是 Flash 读等待周期。它跟时钟不是一回事，但**主频一改就必须跟着改**，否则可能读到错的 Flash 数据。手册 Table 37-42：1.62–2.7 V 下 0/1/2/3 个等待周期分别对应 14/28/42/48 MHz；2.7–3.63 V 下 0 个对应 24 MHz、1 个对应 48 MHz。所以 **3.3 V 跑 48 MHz 正需要 1 个等待周期**。

顺带提一句写保护：手册里 GCLK、SYSCTRL、PM 这些寄存器都标着 "Write-Protected"，意思是**可以被 PAC（Peripheral Access Controller）可选地写保护**。复位后保护是关的，正常写代码不用管；但如果工程里有人打开过保护，**写操作不但不生效，还会触发一个 CPU 异常**（手册原话："data will not be written, and the peripheral will return an access error"）。要解开就写 `PAC0_REGS->PAC_WPCLR`。这里有个坑：**DFP 里没有任何「每个外设一位」的 PAC 宏**（`component/pac.h` 只有通用的 `PAC_WPCLR_WP_Msk`），所以才要自己写位号——GCLK 在 PAC0 的 bit 3：

```c
PAC0_REGS->PAC_WPCLR = (1UL << 3);   /* bit3 = GCLK，位号见手册 11.6.2 的 PAC0 位图 */
```

## 5.9 从 1 MHz 到 48 MHz：完整步骤

| 步 | 做什么 | 为什么必须在这个位置 |
|---|---|---|
| 1 | 让参考时钟跑起来并稳定 | DFLL 闭环要一个稳的参考；参考没稳，锁出来就是错的 |
| 2 | 写 `DFLLVAL`（粗调起点）和 `DFLLMUL`（倍率） | 这是"目标值"，必须在进闭环之前写好 |
| 3 | 写 `DFLLCTRL`：`ENABLE`，闭环再加 `MODE` | 打开振荡器：这一步之后它才开始产生 48 MHz |
| 4 | 等 `PCLKSR.DFLLRDY`（闭环还要等粗锁/细锁） | 输出稳定之前不能拿它当主时钟 |
| 5 | 把 `NVMCTRL.CTRLB.RWS` 加到 1 | 加多了只是变慢，**加晚了会读到错的 Flash 数据** |
| 6 | 把 `GCLK0` 的源改成 `DFLL48M`，等 `SYNCBUSY` | 这一刻起 CPU 跑在新频率上 |
| 7 | 更新 `SystemCoreClock` | 延时、波特率都按它算 |

注意第 5 步在第 6 步之前——这是有意的。等待周期是"给 Flash 更多时间"，多给永远安全，少给才会出错。所以**先降速、再提速**。

**第 1 步：参考时钟从哪来。** 闭环模式下 DFLL 需要一个参考，**这个参考必须 ≤ 33 kHz**（手册 Table 37-7 的 `fGCLK_DFLL48M_REF` 上限），误差要在 2% 以内，手册还明确"内部振荡器禁止"，所以合格的参考只有 `XOSC32K`（外接 32.768 kHz 晶振）。写法是三步：打开 XOSC32K 并等它稳 → 用一路发生器（这里用 GCLK1，避免和主时钟 GCLK0 打架）来承接它 → 把 `GCLK_DFLL48M_REF`（ID = 0x00）这路通道接到那个发生器上。**没有 32.768 kHz 晶振怎么办？用开环模式**：开环不需要参考，靠工厂校准值也能得到 45–49 MHz 的输出，代价是频率会随温度和电压漂几个百分点——点灯、跑算法没问题，做 USB 或精确串口就不行。

**第 2、3 步：DFLLVAL / DFLLMUL / DFLLCTRL 怎么配合。** 先取工厂校准值：手册 10.3.2 说 NVM Software Calibration Area 在 **0x806020**，表 10-8 里 `DFLL48M COARSE CAL` 占位 63:58；位 63:32 是第二个 32 位字，bit 58 对应它的 bit 26，所以 `coarse = (*(volatile uint32_t *)(0x806020UL + 4UL) >> 26) & 0x3F;`。（DFP 的 `component/fuses.h` 里也有对应位定义 `FUSES_OTP4_WORD_1_DFLL48M_COARSE_CAL_Msk`，位于字偏移 0x04 的 bit 31:26，和上面算出来的是同一个东西；以你工程里的 DFP 头文件为准。）然后按模式填表：

| 寄存器 | 开环 | 闭环 |
|---|---|---|
| `DFLLVAL` | `COARSE` = 校准值、`FINE` = 512 | `COARSE` = 校准值（只作为搜索起点，加快锁定）、`FINE` = 512 |
| `DFLLMUL` | 不写（`MUL` 只在闭环有效） | `MUL` = 1464（32.768 kHz × 1464 ≈ 47.97 MHz）、`CSTEP` = 31、`FSTEP` = 511 |
| `DFLLCTRL` | `ENABLE` | `ENABLE \| MODE \| WAITLOCK \| BPLCKC` |

`MUL = 1464` 是从哪来的？参考是 32.768 kHz，想要 48 MHz，倍率 = 48000000 / 32768 = 1464.84，取整数部分 1464，实际输出 1464 × 32768 = **47.97 MHz**。手册的 DFLL 特性表里给的正是这个例子（`DFLLMUL = 1464`、参考 32.768 kHz、输出 47.963–47.981 MHz）。`BPLCKC`（跳过粗锁）能用，是因为上一步已经把粗调起点写成工厂校准值了——手册给的标准流程就是"先装校准值 → 置 `BPLCKC` → 进闭环"，这样锁定时间只剩细锁那一段。

**第 4 步：等就绪和等锁定。** 两个标志要分清：`PCLKSR.DFLLRDY` 表示寄存器同步完成、振荡器在跑，不能省，写 `DFLLCTRL`/`DFLLMUL`/`DFLLVAL` 之后都要等；`PCLKSR.DFLLLCKC`/`DFLLLCKF` 表示粗锁/细锁完成，只有闭环才有意义，但要精确频率就必须等。**`DFLLRDY` 只保证"在跑"，不保证"跑得准"**——手册 17.6.7.1.3 的大意是：`DFLLRDY` 置起就说明时钟可以用了，但输出频率的准确度取决于锁定到了哪一级。

**第 6 步：切换 GCLK0。** 手册 15.6.2.6 说**发生器的源可以边跑边换**：如果新源还没准备好，发生器会继续用旧源；新源一旦就绪就自动切过去，所以从 OSC8M 切到 DFLL48M 不需要先停 CPU。同一条里还有一句提醒：**切换 `GCLKGEN0` 之前，先确认旧源（这里是 OSC8M）的 `ONDEMAND` 是打开的**，以保证切换平滑；好消息是这几个源的 `ONDEMAND` 复位默认就是 1，通常不用管。切换就是写一次 `GENCTRL`：

```c
GCLK_REGS->GCLK_GENCTRL = GCLK_GENCTRL_SRC_DFLL48M     /* 源 = DFLL48M（ID 段为 0 = 发生器 0） */
                        | GCLK_GENCTRL_IDC_Msk         /* 占空比修成 50% */
                        | GCLK_GENCTRL_GENEN_Msk;      /* 保持使能 */
while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }   /* 等这一下生效 */
```

**第 7 步：SystemCoreClock 要自己更新。** `SystemCoreClock` 是个全局变量，声明在 `system_samd21g18a.h` 里。注意它**不会**被 `sam.h` 自动带进来——`samd21g18a.h` 里那句 `#include "system_samd21g18a.h"` 被 `#if defined USE_CMSIS_INIT` 包着，所以要自己写一行 `#include "system_samd21g18a.h"`（或者在编译选项里加 `-DUSE_CMSIS_INIT`）。软件里所有"按时间算"的东西都该引用它（`delay_ms`、波特率分频、超时计数）；改完主频不更新它，现象就是"延时函数慢了 48 倍"这种莫名其妙的问题。这里有个**必须知道的坑**：SAMD21_DFP 3.7.262 里 `system_samd21g18a.c` 把 `__SYSTEM_CLOCK` 写死成 1000000，而 `SystemInit()` 和 `SystemCoreClockUpdate()` **都只是把这个编译期常量赋一遍，不读任何寄存器**。所以调 `SystemCoreClockUpdate()` 没用，三种做法选一种：

| 做法 | 怎么做 | 适合 |
|---|---|---|
| 手动赋值 | 切完时钟后写 `SystemCoreClock = 48000000UL;` | 最快，本章例程用这个 |
| 改常量 | 把 `__SYSTEM_CLOCK` 改成 48000000 并重新编译 | 主频固定的工程 |
| 自己实现 | 读 `GCLK_GENCTRL` 的 `SRC` 和 `SYSCTRL_OSC8M` 的 `PRESC` 算出来 | 需要在运行时来回切主频的工程 |

## 5.10 完整例程：切到 48 MHz，用流水灯验证

沿用第 4 章的接法：PA00–PA07 接 8 个 LED（低电平点亮，串限流电阻），不接晶振。用 PA00–PA07 只是为了掩码好看；注意 **PA00/PA01 正好是 XIN32/XOUT32**，如果板上焊了 32.768 kHz 晶振，就不能拿这两脚点灯（会互相打架）。

验证思路是：用**同一份代码**烧两次，只改一个宏 `SWITCH_TO_48M`（0 = 不调 `clock_48m_init()`，跑在复位默认的 1 MHz；1 = 先调它，跑在 48 MHz）。延时用**固定循环次数**写死，前后完全一样；主频提高 48 倍，同样的循环次数耗时就缩短到约 1/48，比值一算就知道时钟有没有真的切换成功。

```c
#include "sam.h"
#include "system_samd21g18a.h"   /* SystemCoreClock 的声明在这里，sam.h 不会自动带进来 */

#define LED_MASK                0x000000FFUL   /* PA00~PA07，低电平点亮 */
#define BOARD_HAS_32K_CRYSTAL   0              /* PA00/PA01 有 32.768 kHz 晶振就改成 1 */
#define SWITCH_TO_48M           1              /* 第一次烧 0，第二次烧 1，对比闪烁速度 */

static void leds_init(void)
{
    PORT_REGS->GROUP[0].PORT_OUTSET = LED_MASK;   /* 先准备电平（高 = 灭），再开输出 */
    PORT_REGS->GROUP[0].PORT_DIRSET = LED_MASK;
}

/* 忙等：循环次数写死，这样主频变了耗时就跟着变，正好用来验证。 */
static void delay_loop(uint32_t n)
{
    for (volatile uint32_t i = 0; i < n; i++) { __NOP(); }   /* volatile 防止整个循环被优化掉 */
}

/* ---- 打开 DFLL48M ---------------------------------------------------- */
static void dfll48m_init(void)
{
    /* 手册 10.3.2：NVM 软件校准区在 0x806020，DFLL48M COARSE CAL 占 bit 63:58。
       bit 63:32 是 +4 那个字，bit 58 对应它的 bit 26。 */
    uint32_t coarse = (*(volatile uint32_t *)(0x806020UL + 4UL) >> 26) & 0x3FUL;

#if BOARD_HAS_32K_CRYSTAL
    /* ① 闭环：先给 DFLL 一个 32.768 kHz 的参考。STARTUP = 2 约 62 ms，
          晶振起振慢，必须等 RDY 才能当参考用。 */
    SYSCTRL_REGS->SYSCTRL_XOSC32K = SYSCTRL_XOSC32K_ENABLE_Msk
                                  | SYSCTRL_XOSC32K_XTALEN_Msk              /* 接的是晶振 */
                                  | SYSCTRL_XOSC32K_EN32K_Msk
                                  | (2UL << SYSCTRL_XOSC32K_STARTUP_Pos);
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_XOSC32KRDY_Msk) == 0) { }

    /* ② GCLK1 = XOSC32K。用 GCLK1 而不是 GCLK0，避免和主时钟互相影响。 */
    GCLK_REGS->GCLK_GENDIV  = (1UL << GCLK_GENDIV_ID_Pos)                      /* 发生器 1 */
                            | (1UL << GCLK_GENDIV_DIV_Pos);                    /* DIV = 1 → 不分频 */
    GCLK_REGS->GCLK_GENCTRL = (1UL << GCLK_GENCTRL_ID_Pos)
                            | GCLK_GENCTRL_SRC_XOSC32K
                            | GCLK_GENCTRL_GENEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* ③ 把 GCLK_DFLL48M_REF（ID = 0x00）这路通道接到 GCLK1。 */
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_DFLL48
                            | (1UL << GCLK_CLKCTRL_GEN_Pos)
                            | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* ④ 先让振荡器跑起来。DFLLRDY 表示寄存器同步完成、振荡器在跑。 */
    SYSCTRL_REGS->SYSCTRL_DFLLCTRL = SYSCTRL_DFLLCTRL_ENABLE_Msk;
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_DFLLRDY_Msk) == 0) { }

    /* ⑤ 目标值：32.768 kHz × 1464 ≈ 47.97 MHz。CSTEP/FSTEP 是每步最大调整量，
          不超过满量程一半（31 / 511）。 */
    SYSCTRL_REGS->SYSCTRL_DFLLMUL = (1464UL << SYSCTRL_DFLLMUL_MUL_Pos)
                                  | (31UL   << SYSCTRL_DFLLMUL_CSTEP_Pos)
                                  | (511UL  << SYSCTRL_DFLLMUL_FSTEP_Pos);

    /* ⑥ 先写粗调起点（工厂校准值），BPLCKC 才能安全地跳过粗锁。 */
    SYSCTRL_REGS->SYSCTRL_DFLLVAL = (coarse << SYSCTRL_DFLLVAL_COARSE_Pos)
                                  | (512UL  << SYSCTRL_DFLLVAL_FINE_Pos);

    /* ⑦ 进闭环。WAITLOCK：锁定之前不输出时钟，避免把不稳的时钟送给 CPU。 */
    SYSCTRL_REGS->SYSCTRL_DFLLCTRL = SYSCTRL_DFLLCTRL_ENABLE_Msk
                                   | SYSCTRL_DFLLCTRL_MODE_Msk
                                   | SYSCTRL_DFLLCTRL_WAITLOCK_Msk
                                   | SYSCTRL_DFLLCTRL_BPLCKC_Msk;
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_DFLLRDY_Msk) == 0) { }
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_DFLLLCKC_Msk) == 0) { }  /* 粗锁 */
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_DFLLLCKF_Msk) == 0) { }  /* 细锁后才准 */
#else
    /* 没有晶振：开环。不需要参考时钟，频率完全由 DFLLVAL 决定；
       写工厂校准值后有 45~49 MHz（手册 Table 37-52），点灯够用。 */
    SYSCTRL_REGS->SYSCTRL_DFLLVAL = (coarse << SYSCTRL_DFLLVAL_COARSE_Pos)
                                  | (512UL  << SYSCTRL_DFLLVAL_FINE_Pos);
    SYSCTRL_REGS->SYSCTRL_DFLLCTRL = SYSCTRL_DFLLCTRL_ENABLE_Msk;   /* MODE = 0，开环 */
    while ((SYSCTRL_REGS->SYSCTRL_PCLKSR & SYSCTRL_PCLKSR_DFLLRDY_Msk) == 0) { }
#endif
}

/* ---- 把主频切到 48 MHz ----------------------------------------------- */
static void clock_48m_init(void)
{
    dfll48m_init();                       /* 先准备好新时钟，再切过去 */

    /* 提速之前先给 Flash 加等待周期：3.3 V 下 48 MHz 需要 1 个（手册 Table 37-42）。
       加多了只是变慢，加晚了会读到错的数据，所以顺序是先加后提。RWS 是 CTRLB 的 bit4:1。 */
    NVMCTRL_REGS->NVMCTRL_CTRLB = (NVMCTRL_REGS->NVMCTRL_CTRLB & ~NVMCTRL_CTRLB_RWS_Msk)
                                | (1UL << NVMCTRL_CTRLB_RWS_Pos);

    /* 把 GCLK0 的源从 OSC8M 换成 DFLL48M。CPU 就吃这一路，
       切换后必须等同步完成，否则后面的代码会按错的频率去算时间。 */
    GCLK_REGS->GCLK_GENCTRL = GCLK_GENCTRL_SRC_DFLL48M    /* ID 段 = 0 → 发生器 0 */
                            | GCLK_GENCTRL_IDC_Msk        /* 占空比 50% */
                            | GCLK_GENCTRL_GENEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* DFP 自带的 SystemCoreClockUpdate() 不读寄存器，只会把常量重新赋一遍，
       所以这里手动维护。delay、波特率以后都靠它。 */
    SystemCoreClock = 48000000UL;
}

int main(void)
{
    leds_init();

#if SWITCH_TO_48M
    clock_48m_init();                     /* 这一句之后，下面的循环快约 48 倍 */
#endif

    uint32_t mask = 1UL;
    PORT_REGS->GROUP[0].PORT_OUTCLR = mask;      /* 先点亮第一个（低电平点亮） */

    while (1)
    {
        delay_loop(200000);                      /* 次数写死，前后两次一样 */
        uint32_t next = (mask & 0x80UL) ? 1UL : (mask << 1);
        PORT_REGS->GROUP[0].PORT_OUTTGL = mask | next;   /* 一次翻转两个脚 */
        mask = next;
    }
}
```

先在 1 MHz 下把 `delay_loop` 的参数调到"每步大约 0.2 秒"（肉眼看得清但不拖沓），记下这个数，然后只把 `SWITCH_TO_48M` 改成 1 重新编译烧写：

| 观察方式 | `SWITCH_TO_48M = 0`（1 MHz） | `SWITCH_TO_48M = 1`（48 MHz） |
|---|---|---|
| 肉眼看 | 亮点一步步走，每步约 0.2 s | 8 个灯几乎同时亮，看起来像全亮 |
| 秒表数 20 步 | 约 4 s，好数 | 约 0.08 s，根本按不过来 |
| 示波器看 PA00 方波周期 | 约 0.4 s | 约 8 ms，**比值应该接近 48** |
| 把 `delay_loop` 参数乘 48 再看 | — | 又变回肉眼能看清的速度 |

第三行是这个实验的关键：**如果比值不是 48 而是 1，说明时钟根本没切过去**；如果比值只有 2、3，说明 DFLL 的输出频率不对（多半是没写工厂校准值，或者闭环没锁上）。没有示波器也没关系：把参数放大 48 倍后现象应当和 1 MHz 时一模一样，这本身就是证据。

## 5.11 常用配方

| 目标 | 源 | 发生器 | 通道 / 关键动作 |
|---|---|---|---|
| 48 MHz 主频（无晶振） | DFLL48M 开环 | GCLK0 | 写 `DFLLVAL` 工厂校准值 → `ENABLE` → 等 `DFLLRDY` → GCLK0 换源 |
| 48 MHz 主频（有 32.768 kHz 晶振） | XOSC32K → DFLL48M 闭环 | GCLK1 当参考、GCLK0 当主时钟 | `GCLK_DFLL48M_REF`（ID 0x00）接 GCLK1；`MUL = 1464` |
| 外部晶振直接当主时钟 | XOSC | GCLK0 | 配 `GAIN`/`STARTUP`/`XTALEN` → 等 `XOSCRDY` → GCLK0 换源 |
| 省电时降主频 | OSCULP32K 或 OSC8M | GCLK0 | 换源，或加大 `GENDIV.DIV`；还不够再动 `PM_CPUSEL` |
| 精确 32.768 kHz 给 RTC | XOSC32K | 任意发生器 | `GCLK_CLKCTRL_ID_RTC`（ID 0x04） |
| 96 MHz 给外设 | FDPLL96M | 任意发生器（如 GCLK3） | `DPLLCTRLB.REFCLK` + `DPLLRATIO.LDR` → `DPLLCTRLA.ENABLE` → 等 `DPLLSTATUS.LOCK` |

**外部晶振直接当主时钟**是最简单的一条路：XOSC 输出多少，CPU 就跑多少，没有倍频误差。前提是晶振频率 ≤ 48 MHz（`fCPU` 上限，手册 Table 37-7），而且要按晶振的 ESR 和负载电容配对 `GAIN` 档位，配错了会不起振；这条适合"我就想要 12 MHz 或 16 MHz 这种整数频率"。

**省电降频**要注意顺序：主频降到 24 MHz 以下之后，可以把 `NVMCTRL.CTRLB.RWS` 减回 0 换回性能（可做可不做，不做只是慢一点）；降到很低时还可以用 `PM_CPUSEL` 再分频，让外设保持原速而 CPU 变慢——这正是手册 16.6.2.4 说的"CPU 降速、APB 不变"的用法。

**FDPLL96M 什么时候值得用**：需要 96 MHz（比如高速 SPI 时钟、TCC 的高分辨率 PWM），或者需要一个不是 48 MHz 整数分频能得到的频率。它的参考只能从 XOSC32K、XOSC、GCLK_DPLL 三选一，参考上限 2 MHz，输出范围 48–96 MHz。要记牢一点：**CPU 和总线最高 48 MHz，所以 96 MHz 只能送给外设，不能当主频**；用它的完整流程比 DFLL 长（还要配 `DPLLCTRLB` 的滤波器和锁定时间），用到时再回头读手册 17.6.8。

## 5.12 初始化清单

时钟相关的初始化，永远按这个顺序想，就不会漏：**源（SYSCTRL）→ 发生器（GENDIV + GENCTRL）→ 通道（CLKCTRL）→ 门控（PM）→ 等同步（SYNCBUSY / RDY）→ 更新 SystemCoreClock（如果改了主频）**。

| 步骤 | 要问自己的问题 | 对应的寄存器 |
|---|---|---|
| 1 源 | 我要的节拍从哪个振荡器来？它开了吗？稳定了吗？ | `SYSCTRL_OSC8M`/`SYSCTRL_XOSC`/`SYSCTRL_DFLLCTRL`… 与 `SYSCTRL_PCLKSR` |
| 2 发生器 | 用哪个发生器？源选对了吗？要不要分频？使能了吗？ | `GCLK_GENDIV`（先）、`GCLK_GENCTRL`（后） |
| 3 通道 | 这个外设的 ID 是多少？接哪个发生器？开闸了吗？ | `GCLK_CLKCTRL` |
| 4 门控 | 它在哪条总线上？PM 掩码位开了吗？ | `PM_APBAMASK`/`PM_APBBMASK`/`PM_APBCMASK` |
| 5 同步 | 每次写完 GCLK 寄存器都等了吗？ | `GCLK_STATUS.SYNCBUSY` |
| 6 主频 | 如果动了 GCLK0，NVM 等待周期和 `SystemCoreClock` 改了吗？ | `NVMCTRL_CTRLB.RWS`、`SystemCoreClock` |

## 5.13 思维模型

```text
                              时钟系统
        ┌───────────────┬────────────────┬──────────────────┬─────────────────┐
   ① 源(SYSCTRL)    ② 发生器(GCLK)    ③ 通道(CLKCTRL)    ④ 同步时钟(PM)
   7 个振荡器/倍频器  9 个 GEN：选源+分频  ID + GEN + CLKEN   GCLK_MAIN→CPU/AHB/APBx
   开关 + PCLKSR 就绪  GCLKGEN0 → GCLK_MAIN  一路给一个外设     + APBxMASK 门控
```

四层各记一句话：**源管"有没有节拍"，发生器管"节拍多快"，通道管"节拍送给谁"，PM 管"CPU 能不能碰它"。** 任何时钟问题，先定位它卡在哪一层。

## 5.14 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 外设寄存器读出来全是 0，写进去没反应 | PM 里的 APB 时钟没开 | `PM_APBAMASK`/`PM_APBBMASK`/`PM_APBCMASK` 对应位 |
| 总线开了，外设还是不动 | 通用时钟没配或没使能 | `GCLK_GENCTRL.GENEN`、`GCLK_CLKCTRL.CLKEN` |
| 换了 GCLK0 的源以后程序跑飞 | 新源没稳定就切过去了 | 先等 `PCLKSR.DFLLRDY`，切换后等 `GCLK_STATUS.SYNCBUSY` |
| 48 MHz 下偶尔死机或算错数据 | Flash 等待周期不够 | `NVMCTRL_CTRLB.RWS`（3.3 V @48 MHz = 1） |
| 延时函数快慢和预期不符 | `SystemCoreClock` 没更新 | DFP 的 `SystemCoreClockUpdate()` 是空壳，要自己赋值 |
| DFLL 闭环锁不上或频率乱跳 | 参考不稳或超过 33 kHz | 参考要在 32.768 kHz 量级、误差 ≤2%，优先 XOSC32K |
| 开出 DFLL 后频率差了很远 | 没写工厂校准的粗调值 | `DFLLVAL.COARSE` 是否等于 `DFLL48M COARSE CAL` |
| 写时钟寄存器完全不生效，还进了异常 | PAC 把这个外设写保护了 | 用 `PAC0_REGS->PAC_WPCLR` 清掉对应位（GCLK 是 bit3） |
| 换通道的源之后外设时好时坏 | 带毛刺切换 | 先 `CLKEN = 0` 并读回确认，再改 `GEN`，最后 `CLKEN = 1` |
| 读 `GCLK_GENCTRL` 读到的值不对 | 间接寻址没先写 ID | 读之前先把 ID 段写成要看的那个发生器 |
| 改了 GCLK2 之后看门狗时间变了 | WDT 的通用时钟默认来自 GCLK2 | `GCLK_CLKCTRL` 的 WDT（ID 0x03） |
| 32.768 kHz 晶振起振要等很久 | 晶振启动慢（可长达 1 秒） | 等 `PCLKSR.XOSC32KRDY`，起振期间不要进 Standby |
| 改了 `PM_CPUSEL` 后系统挂死 | `CKRDY = 0` 时重复写了 `CPUSEL` | 写一次，然后等 `PM_INTFLAG.CKRDY` |

## 5.15 寄存器速查

| 外设 | 寄存器 | 偏移 | 关键位 |
|---|---|---|---|
| SYSCTRL | `PCLKSR` | 0x0C | bit0 `XOSCRDY`、bit1 `XOSC32KRDY`、bit2 `OSC32KRDY`、bit3 `OSC8MRDY`、bit4 `DFLLRDY`、bit6 `DFLLLCKF`、bit7 `DFLLLCKC` |
| | `XOSC` / `XOSC32K` / `OSC32K` / `OSCULP32K` | 0x10 / 0x14 / 0x18 / 0x1C | `ENABLE`、`XTALEN`、`EN32K`、`STARTUP`、`ONDEMAND`（bit7） |
| | `OSC8M` | 0x20 | bit1 `ENABLE`、bit9:8 `PRESC`（0 = ÷1，3 = ÷8） |
| | `DFLLCTRL` | 0x24 | bit1 `ENABLE`、bit2 `MODE`、bit10 `BPLCKC`、bit11 `WAITLOCK` |
| | `DFLLVAL` / `DFLLMUL` | 0x28 / 0x2C | bit9:0 `FINE`、bit15:10 `COARSE` / bit15:0 `MUL`、bit25:16 `FSTEP`、bit31:26 `CSTEP` |
| | `DPLLCTRLA`/`DPLLRATIO`/`DPLLCTRLB`/`DPLLSTATUS` | 0x44/0x48/0x4C/0x50 | `ENABLE`、`LDR`/`LDRFRAC`、`REFCLK`、`LOCK` |
| GCLK | `CTRL` / `STATUS` | 0x00 / 0x01 | `SWRST` / bit7 `SYNCBUSY` |
| | `CLKCTRL` | 0x02 | bit5:0 `ID`、bit11:8 `GEN`、bit14 `CLKEN`、bit15 `WRTLOCK` |
| | `GENCTRL` | 0x04 | bit3:0 `ID`、bit12:8 `SRC`、bit16 `GENEN`、bit17 `IDC`、bit20 `DIVSEL` |
| | `GENDIV` | 0x08 | bit3:0 `ID`、bit23:8 `DIV` |
| PM | `CPUSEL`/`APBASEL`/`APBBSEL`/`APBCSEL` | 0x08/0x09/0x0A/0x0B | `CPUDIV`/`APBxDIV`，÷2^n |
| | `AHBMASK` / `APBAMASK` / `APBBMASK` / `APBCMASK` | 0x14 / 0x18 / 0x1C / 0x20 | 复位值 0x7F / 0x7F / 0x7F / 0x00010000 |
| | `INTFLAG` | 0x36 | bit0 `CKRDY` |
| NVMCTRL | `CTRLB` | 0x04 | bit4:1 `RWS` 读等待周期 |

基址（DFP 头文件里的 `xxx_REGS`）：`GCLK_REGS` 0x40000C00、`SYSCTRL_REGS` 0x40000800、`PM_REGS` 0x40000400、`NVMCTRL_REGS` 0x41004000。

## 5.16 最重要的几句话

1. 时钟不是"提速开关"，是整块芯片的节拍；选哪个源、分多少频、送给谁，是三件不同的事。
2. 同步时钟 = CPU 和总线；异步时钟 = GCLK 送给外设的时钟。两者之间必须同步，这就是 `SYNCBUSY` 的来历。
3. 7 个时钟源里，内部 RC 快而漂，外部晶振准而慢，DFLL48M 和 FDPLL96M 负责把"准"搬到"快"。
4. `GCLK_MAIN` 只由 GCLKGEN0 产生，它直接决定 CPU 和总线的速度；其余发生器只服务外设。
5. 配一个外设要同时满足两件事：GCLK 的 `CLKEN` 给它节拍，PM 的 `APBxMASK` 让 CPU 能碰它。少任何一件都表现为"没反应"。
6. 复位后 OSC8M ÷8 得 1 MHz，GCLK0 取它，CPU 就跑 1 MHz；WDT 的时钟默认来自 GCLK2 的 OSCULP32K。
7. DFLL48M 开环靠 `DFLLVAL` 的工厂校准值（45–49 MHz），闭环靠 `fout = MUL × fref`（参考必须 ≤33 kHz、误差 ≤2%，首选 XOSC32K）。
8. 每次写 `GCLK_GENCTRL`/`GCLK_GENDIV`/`GCLK_CLKCTRL` 都等一次 `SYNCBUSY`；提速前先给 Flash 加等待周期；最后别忘了自己更新 `SystemCoreClock`。

## 5.17 自测

1. 为什么点灯（第 4 章）不需要写任何时钟配置，而以后用 SERCOM 就必须写？分别对应哪个寄存器的哪一位？
2. GCLK 的"发生器"和"通道"分别解决什么问题？如果只想给一个外设换一个频率，要动哪个寄存器？改的时候为什么必须先关 `CLKEN`？
3. `GCLK_CLKCTRL` 里 `ID` 和 `GEN` 分别填什么？SERCOM3 的核心时钟 ID 是多少，这个编号你从哪两个地方能查到？
4. 复位后 `GENCTRL[0]` 的值是 `0x00010600`。请说出它的 `ID`、`SRC`、`GENEN` 各是什么，并推出此时 `fCPU` 是多少（提示：还要看 OSC8M 的 `PRESC`）。
5. 把主频从 1 MHz 改到 48 MHz，为什么要先准备 DFLL48M、再改 Flash 等待周期、最后才切 `GCLK0`？如果把顺序反过来会出什么问题？
6. 下面这段代码少了一句什么？少了之后现象是什么？

```c
GCLK_REGS->GCLK_GENCTRL = GCLK_GENCTRL_SRC_DFLL48M | GCLK_GENCTRL_GENEN_Msk;

SystemCoreClock = 48000000UL;
```

## 附录：以后查手册怎么找时钟

时钟相关内容散在好几个章节，按这个顺序查最快：

```text
① 想知道全局结构         →  14. Clock System（14.1 框图、14.2 同步/异步、14.8 复位默认值）
② 想配某个时钟源         →  17. SYSCTRL（17.2 特性、17.6 功能、17.8 寄存器）
③ 想把时钟分配给外设     →  15. GCLK（15.6.2 初始化流程、15.8 寄存器 + Table 15-3 的 ID 表）
④ 想看总线速度和门控     →  16. PM（16.6.2 同步时钟与掩码、16.8 寄存器）
⑤ 想知道某个外设要什么时钟 →  那个外设章节里的 "Clocks" 小节（例如 25.5.3 讲 SERCOM）
⑥ 想知道频率上限         →  37.6 / 37.7（Maximum Clock Frequencies）
⑦ 想找振荡器引脚         →  7.2.1 Oscillator Pinout（Table 7-3）
```

第 ⑥ 条有个坑：电气特性按**器件型号最后一位字母**分成 Variant A / B,C,D,L 几套表。ATSAMD21G18A 的最后一位是 **A**，所以要看 "Device Variant A" 那几张表。查找顺序记成一句话：**先找外设章节 → 再找该外设的 "Clocks" 小节 → 看它要哪几路 GCLK → 回 GCLK 章查 ID → 回 SYSCTRL 章配源。**

常用章节号备查：GCLK 章 CLKCTRL 的 ID 对照表是 **Table 15-3**；NVM 软件校准区地址和各字段位置是 **Table 10-8**（基址 0x806020）；NVM User Row 里 WDT Enable 是 **Table 10-7** 的 bit 25；DFLL48M 特性是 **Table 37-52/37-53**；FDPLL96M 特性是 **Table 37-58**；Flash 等待周期是 **Table 37-42**；振荡器引脚是 **Table 7-3**。
