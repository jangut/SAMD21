# 第 6 章 电源与低功耗：怎么睡、怎么醒

> **芯片**：ATSAMD21G18A
> **本章目标**：理解"省电"这件事在硬件上到底发生了什么，知道 Idle 0/1/2 与 Standby 各自停掉了谁，知道怎么进睡眠、谁能把你叫醒，最后做出一个"按键唤醒的 Standby"最小例程。
> **学习顺序**：为什么要省电 → PM 管哪两件事 → 睡眠是什么 → Idle 与 Standby → 进入睡眠的顺序 → 唤醒源 → SleepWalking → BOD/VREG → 寄存器 → 例程。

## 6.0 省电的三条路

电流消耗 = **动态功耗**（时钟在跑、电路在翻转就耗电，和频率成正比）+ **静态功耗**（只要通着电就有的漏电、基准源、稳压器开销）。所以省电只有三个方向：把频率降下来、把不用的模块的时钟关掉、让 CPU 干脆停下来。这一章讲后两条；降频属于第 5 章（`PM.CPUSEL` / `PM.APBxSEL`），这里只引用结论。

有一件事必须先建立直觉：**能省电的最小单位不是"某个外设"，而是"某个时钟"。** 一个模块只要还有时钟进来，它的数字逻辑就在翻转、就在耗电；没有时钟，它就只是一堆保持状态的触发器，几乎不耗电。所以整章的每一句话，最后都能翻译成"这个时钟还在不在跑"。

| 手段 | 硬件上做了什么 | 省电幅度（手册 Table 37-8，25 °C，Device Variant A） |
|---|---|---|
| 什么都不做（Active） | CPU 跑 `while(1)` | 3.37 mA |
| 关外设时钟 | `PM.APBxMASK` 把不用的模块停表 | 每个外设几十 µA 量级 |
| Idle 睡眠 | 关掉 CPU / AHB / APB 时钟域 | 2.04 / 1.46 / 1.17 mA（Idle 0/1/2） |
| Standby 睡眠 | 振荡器也停，稳压器转低功耗 | 4.06 µA（RTC 在跑）、2.70 µA（全停） |

**注意表里最后一行的量级差**：Active 是毫安，Standby 是微安，中间差了 800 倍以上。所以低功耗设备的第一原则不是"省一点是一点"，而是**尽可能长时间待在 Standby**。

## 6.1 PM 模块管两件事

PM = Power Manager（电源管理器），手册第 16 章。它不控制电压，而是管两件完全不同的事：

| 职责 | 回答的问题 | 相关寄存器 |
|---|---|---|
| 时钟门控 | 哪个外设还有时钟 | `AHBMASK`、`APBAMASK`、`APBBMASK`、`APBCMASK`、`CPUSEL`、`APBxSEL` |
| 睡眠控制 | 现在睡不睡、睡多深 | `SLEEP.IDLE`、`SCR.SLEEPDEEP`、`WFI` 指令 |

它还顺带收集复位源（`RCAUSE`），但那是复位的话题，本章只看它和睡眠的关系。

把这两件事分开记很重要：**"关外设时钟"和"进睡眠"是两件独立的事。** 你可以在 Active 下就把 SERCOM 的时钟关掉省电；也可以在睡眠前把外设时钟全开着，只是那时它们照样耗电。真正省电的写法是两件一起做。

在手册 16.6.2.8 里有一句必须记住的警告：

> 进入 Standby 之前，用户必须确认大量时钟和外设已经被关闭，否则内部稳压器会过载。

原因是 Standby 下稳压器要切到低功耗模式（驱动能力变弱）。如果这时候还有一堆外设在要电流，稳压器撑不住，电压会掉。这句话就是"进睡眠前先关外设"这条规矩的来源。

## 6.2 时钟门控：给不用的外设停表

外设挂在三条 APB 桥上（桥 A / B / C），每条桥有一个 32 位的 mask 寄存器，一位对应一个外设。位 = 1 表示时钟开着，位 = 0 表示时钟停掉。

```text
PM.APBAMASK   bit0 PAC0  bit1 PM  bit2 SYSCTRL  bit3 GCLK
              bit4 WDT   bit5 RTC bit6 EIC                ← 复位值 0x7F，全开
PM.APBBMASK   PAC1 / DSU / NVMCTRL / PORT / DMAC / USB     ← 复位值 0x7F，全开
PM.APBCMASK   PAC2 / SERCOMx / TCx / ADC / AC / DAC / TCCx ← 复位值 0x00010000，只有 ADC 开着
```

（复位值出自手册 16.7 寄存器汇总，和第 4 章那条结论一致：点灯不用配时钟，因为 PORT 在桥 B，复位后就是开的。）

关时钟有两条必须知道的后果。第一，**时钟一关，这个外设的寄存器就读写不了**——不是"值不对"，是总线访问直接出问题。第二，有两个模块绝对不能关：`NVMCTRL`（CPU 取指要读 Flash）和 `PM` 自己（mask 寄存器就在它里面，关了以后再也写不回来，只能等复位）。手册 16.6.2.6 的 Note 就是这么写的。

## 6.3 睡眠是什么：让 CPU 停下来

前面说的都是"关别人的表"，睡眠是"把自己也停掉"。执行一条 ARM 的 `WFI`（Wait For Interrupt，等待中断）指令，CPU 就停在原地不动了——它不再取指、不再执行，但**寄存器、SRAM 内容、外设状态全部保留**。等到一个中断来了，CPU 从 `WFI` 的下一条指令继续跑，就像什么都没发生过。

这里有个新手最容易搞混的点：**睡眠不是复位。** 醒来以后程序不是从头开始，而是接着睡之前那一行往下走。所以睡眠特别适合"每 10 秒采一次数据"这类应用：程序是一个大循环——醒来 → 采样 → 处理 → 发送 → 配好唤醒源 → `WFI` 睡 10 秒 → 再醒来，睡眠只是循环里的一步。

## 6.4 Idle 和 Standby：两种睡眠，两种代价

SAMD21 只有两大类睡眠模式。它们的区别可以用一句话概括：**Idle 只停 CPU，Standby 把时钟源也停了。**

```text
GCLK_MAIN ──▶ [8 位预分频] ──┬──▶ CLK_CPU  ──▶ CPU
                             ├──▶ CLK_AHB  ──▶ AHB（HPB0/1/2 桥）
                             └──▶ CLK_APBx ──▶ APBx（桥 A/B/C 上的外设）
```

Idle 停的是这条链上的**同步时钟域**；Standby 停的是更上游的**振荡器本身**。停得越靠上游，省得越多、醒得越慢。

| 对比项 | Idle | Standby |
|---|---|---|
| CPU 时钟 | 停 | 停 |
| AHB / APB 时钟 | 按 Idle 0/1/2 逐级停 | 全停 |
| 振荡器（时钟源） | 继续跑 | 除设了 `RUNSTDBY` 的以外全停 |
| 主时钟 CLK_MAIN | 跑 | 停 |
| 稳压器 | 正常模式 | 低功耗模式 |
| SRAM | 正常 | 低功耗，**内容保留** |
| 唤醒源 | 同步 + 异步（Idle 2 只剩异步） | 只有异步 |
| 唤醒时间 | 4.0 / 12.1 / 13.0 µs | 19.6 µs |
| 典型电流（25 °C） | 2.04 / 1.46 / 1.17 mA | 4.06 µA |

（电流和唤醒时间出自手册 Table 37-8、Table 37-11，条件：3.3 V、DFLL48M 作主时钟、BOD33 关闭、25 °C。唤醒时间是用 OSC8M 做主时钟、Cache 关闭时测的。）

**怎么选**：需要 1 µs 级响应、或者外设必须全速跑 → Idle。电池供电、几秒才醒一次 → Standby。绝大多数低功耗产品用 Standby。

## 6.5 Idle 0 / 1 / 2 分别停掉哪些时钟

Idle 不是一个模式，是三档。档位写在 `PM.SLEEP.IDLE[1:0]` 里：

| `SLEEP.IDLE` | 名字 | 停掉的时钟域 | 手册原话（16.8.2） |
|---|---|---|---|
| 0x0 | Idle 0 | CPU | The CPU clock domain is stopped |
| 0x1 | Idle 1 | CPU + AHB | The CPU and AHB clock domains are stopped |
| 0x2 | Idle 2 | CPU + AHB + APB | The CPU, AHB and APB clock domains are stopped |
| 0x3 | — | 保留，不要写 | Reserved |

为什么是逐级累加，而不是三个独立开关？因为这三个域是**同步**的：AHB 时钟和 CPU 时钟同频，APB 由主时钟分频而来，手册明确要求 fCPU ≥ fAPBx。CPU 停了，下游才有条件停；反过来先停 APB 是不成立的。所以硬件的做法是"从上游往下游一级一级关"。

三档的实际差别要看手册 Table 16-4（这张表的列很容易看错，下面按它的本意重排）：

| 睡眠档 | CPU 时钟 | AHB 时钟 | APB 时钟 | 振荡器 | 主时钟 | 稳压器 | RAM |
|---|---|---|---|---|---|---|---|
| Idle 0 | 停 | 跑 | 跑 | 按需 | 跑 | 正常 | 正常 |
| Idle 1 | 停 | 停 | 跑 | 按需 | 跑 | 正常 | 正常 |
| Idle 2 | 停 | 停 | 停 | 按需 | 跑 | 正常 | 正常 |
| Standby | 停 | 停 | 停 | 除 `RUNSTDBY` 外全停 | 停 | 低功耗 | 低功耗 |

两个容易漏掉的细节。第一，**Idle 下模块会被无条件停住，跟 `PM.AHBMASK` / `PM.APBxMASK` 怎么设无关**（手册 16.6.2.8.1 原话：The module will be halted regardless of the bit settings of the mask registers）。第二，**Idle 2 把 APB 也停了，所以 APB 上产生的中断（同步中断）叫不醒它**，只有异步中断可以。

"同步中断"和"异步中断"的定义手册写在 Table 16-3 的注里：**异步 = 由通用时钟、外部时钟或外部事件产生的中断；同步 = 由 APB 时钟产生的中断。** 翻译成人话：外设如果靠 APB 总线时钟工作，它的中断就是同步的；如果外设自己有独立的 GCLK 在跑，它的中断就是异步的。

## 6.6 Standby：把时钟源也停掉

Standby 的规则只有一条：**所有时钟都停，除了设了 `RUNSTDBY` 的那个**（手册 16.6.2.8.2）。主时钟停、AHB/APB 停、CPU 停、稳压器和 RAM 进低功耗——但 RAM 里的内容不会丢，变量还在。

这时候谁的时钟还在跑，谁就还能干活、还能把你叫醒。所以"Standby 下哪个外设还活着"这个问题，答案永远是两段拼出来的：

```text
外设还活着  =  外设的 GCLK 还在跑  +  外设自己没被睡眠关掉
                     ▲                        ▲
              时钟源设了 RUNSTDBY        外设 CTRLA.RUNSTDBY=1
              或者源是 OSCULP32K        （EIC / RTC / WDT 不需要）
```

**OSCULP32K 是个例外，它永远在跑、也关不掉**（手册 Table 17-1 的注：This does not apply to the OSCULP32K oscillator, which is always running and cannot be disabled）。所以低功耗设计里，凡是"要在 Standby 下继续工作"的东西，时钟源尽量都接到 OSCULP32K 上——这是最省心的一条路，本章例程就是这么做的。

## 6.7 振荡器的"随叫随到"：ONDEMAND 与 RUNSTDBY

上一节留了个问题：振荡器怎么知道该不该跑？答案是每个振荡器（OSC8M、OSC32K、XOSC、XOSC32K、DFLL48M、FDPLL96M）都有两个控制位：

- `ONDEMAND`：置 1（复位默认）时，**没人要就不跑**——外设通过 GCLK 发"时钟请求"上来，有请求才启动。置 0 就是"一直跑"，换来的是没有启动延迟，代价是一直耗电。
- `RUNSTDBY`：置 1 时，**在 Standby 下也能被请求唤醒并运行**；置 0 则 Standby 下一律停。

手册 Table 17-2 把两种睡眠下的行为写全了：

| 睡眠模式 | ONDEMAND | RUNSTDBY | 行为 |
|---|---|---|---|
| Idle 0/1/2 | 0 | 任意 | 一直跑 |
| Idle 0/1/2 | 1 | 任意 | 有外设请求才跑 |
| Standby | 0 | 0 | 停 |
| Standby | 0 | 1 | 一直跑 |
| Standby | 1 | 0 | 停 |
| Standby | 1 | 1 | 有外设请求才跑 |

注意最后一行：**`ONDEMAND=1` + `RUNSTDBY=1` 就是 SleepWalking 的硬件基础**——外设要用时钟时才把振荡器拉起来，用完自动停。

## 6.8 怎么进入睡眠：`__WFI`、SLEEPDEEP、`__DSB` 的顺序

进睡眠不是一个函数调用，是"把参数准备好 → 执行 `WFI`"两件事。参数有两个来源：

| 参数 | 在哪 | 决定什么 |
|---|---|---|
| `SCR.SLEEPDEEP` | ARM 内核的系统控制寄存器 `SCB->SCR`（bit 2） | 大类：0 = Idle，1 = Standby |
| `PM.SLEEP.IDLE[1:0]` | PM 的 `SLEEP` 寄存器（偏移 0x01，bit1:0） | Idle 下停到哪一档 |

**Standby 不理会 `SLEEP.IDLE`**：手册 16.6.2.8.2 只说"`SLEEPDEEP=1` 时执行 `WFI` 就进 Standby"。这是好事，说明你从 Idle 切到 Standby 只需要改一个内核位。

顺序是硬件逼出来的：

```text
配好唤醒源（EIC / RTC / WDT …）   ← 先有"谁能叫我"，才敢睡
配 SLEEP.IDLE，再配 SCR.SLEEPDEEP   ← 参数先备好
__DSB()                             ← 把前面所有写操作排空到外设
__WFI()                             ← CPU 停在这里，PM 按参数关时钟
异步中断到来 → 时钟恢复 → 从 __WFI() 下一行继续
```

**为什么 `__DSB` 不能省？** `__WFI()` 只要几个周期就能生效，而你刚写下的那些寄存器配置（EIC 的 CONFIG、GCLK 的 CLKCTRL、PM 的 mask）可能还堵在写缓冲里没到外设。如果时钟在配置到达之前就被关了，醒来以后你会发现"明明配了却没生效"。`__DSB`（Data Synchronization Barrier）是一条"等所有挂起的访问真正完成"的屏障指令，它保证 `WFI` 生效时硬件已经看到你写的全部配置。ARM 对 Cortex-M 的建议顺序就是 `__DSB()` 然后 `__WFI()`。

还有两个可选的位，各一句话：

- `SCB->SCR` 的 `SLEEPONEXIT`（bit 1）：置 1 后，CPU 退出最低优先级中断服务程序时会**自动再睡回去**，不需要在 `main` 里循环写 `WFI`。适合"只有中断来了才干活"的程序。
- `SCB->SCR` 的 `SEVONPEND`（bit 4）：置 1 后，即使中断被屏蔽，挂起的中断也能把 CPU 从 `WFE` 唤醒。本章用不到。

## 6.9 谁能把我叫醒：唤醒源

手册 Table 16-3 把每个睡眠档允许的唤醒源列得很清楚：

| 模式 | 唤醒源 |
|---|---|
| Idle 0 | 同步（APB、AHB）+ 异步 |
| Idle 1 | 同步（APB）+ 异步 |
| Idle 2 | 只有异步 |
| Standby | 只有异步 |

"异步"这个词在这里很值钱：**只要一个外设的时钟不依赖 APB 总线时钟，它的中断就能把芯片从任何睡眠模式叫醒。** 常见的外设是：

| 唤醒源 | 手册怎么说 | 在 Standby 下要满足什么 |
|---|---|---|
| EIC（外部引脚） | 任何睡眠模式下，EXTINTx 引脚匹配 CONFIG 的条件就能唤醒（21.6.8） | `WAKEUP.WAKEUPEN[x]=1` + `INTENSET`，且 GCLK_EIC 的源要在跑 |
| RTC（定时） | 溢出 / 比较 / 闹钟中断都是异步中断，可从任何睡眠模式唤醒（19.6.5） | GCLK_RTC 的源要在跑 |
| WDT（看门狗） | 使能后在活动模式和**所有**睡眠模式下都运行，中断可唤醒（18.1 / 18.5.2） | 不需要额外配置 |
| BOD33（掉电检测） | `PCLKSR.BOD33DET` 的 0→1 是异步中断，可从任何睡眠模式唤醒（17.6.13） | `BOD33.ACTION=INTERRUPT`、`BOD33.RUNSTDBY=1` |
| USB | 总线空闲且进入 Suspend 时，USB 可以把 MCU 放进任何睡眠模式；总线恢复时能唤醒（32 章） | Standby 下 48 MHz 时钟已停，实际项目要实测确认 |
| TC / TCC / SERCOM / ADC … | 各自的 Sleep Mode Operation 一节会说明 | 通常要写 `CTRLA.RUNSTDBY=1` |

EIC 值得多说一句，因为它是"人按键"最直接的入口，也是本章例程用的。EIC 的初始化和使用顺序在手册 21.6.2.1 里写死了四步：

```text
① 使能 CLK_EIC_APB（复位后已开，一般不用管）
② 要边沿检测或滤波，就必须使能 GCLK_EIC
③ 写 EIC 配置寄存器（EVCTRL / WAKEUP / CONFIGy）
④ 使能 EIC（CTRL.ENABLE）
```

第 ② 步是新手的坑：**不做边沿检测就不用 GCLK_EIC，但按键唤醒几乎一定要边沿检测。** 而且这个 GCLK 的源必须在 Standby 下还活着，否则睡着了 EIC 也跟着停摆。

另外手册 21.6.8 有一句明确的不推荐：`WAKEUPEN[x]=1` 而 `INTENSET=0` 不要这么用（那样只会醒来但不进中断服务程序，容易漏事件）。所以正常写法是两个一起置 1。

**一个芯片特有的坑**：不是每个引脚都能做 EIC。EIC 的输入叫 EXTINT[15:0]，每个 EXTINT 只能从固定的几个引脚里选一个，而且引脚必须先通过 PORT 的 PMUX 交给"外设功能 A"才连得上 EIC。第 4 章里那个按键用的 PA08 就不是 EXTINT，它是 **NMI**（不可屏蔽中断）引脚——所以第 4 章的按键不能直接拿来当本章的 EIC 唤醒源。本章例程改用 **PA04（EXTINT[4]）**。

## 6.10 SleepWalking：外设自己叫醒自己的时钟

先看没有 SleepWalking 时的问题。假设你要"每 1 ms 采一次 ADC，超过阈值就报警"。最直觉的写法是让 CPU 不睡，每隔 1 ms 读一次 ADC：

```text
普通轮询：CPU 一直醒着 ──▶ 每 1 ms 读一次 ADC ──▶ 99% 的时间在空转（mA 级）
SleepWalking：CPU 睡在 Standby（µA 级）──▶ ADC 到点自己请求时钟、采完再放下
              只有真正超过阈值时，才用中断把 CPU 叫醒
```

SleepWalking 就是后半句（手册 16.6.3）：**设备可以临时把时钟"叫醒"给某个外设干活，而 CPU 完全不参与**；干完活，外设要么用中断把 CPU 叫醒，要么让设备回到 Standby。

它的实现机制就是 6.7 节那条"按需时钟请求"：外设要时钟时，GCLK 发生器把时钟源拉起来；用完了自动撤销请求，源自己停。

一句话总结差别：**没有 SleepWalking 时，"要不要处理"这件事必须由 CPU 来判断；有了 SleepWalking，"要不要处理"由外设判断，CPU 只在"真要处理"时才醒。** 省下的正是那 99% 的空转电。

SAMD21 上 SleepWalking 只支持 GCLK 时钟（手册 16.6.3 原话：supported only on GCLK clocks by using the on-demand clock principle）。典型玩家是 ADC（连续采样，只在超阈值时中断）、AC（模拟比较器）、PTC（触摸检测）。

## 6.11 SYSCTRL：BOD 和 VREG 怎么影响功耗

SYSCTRL（手册第 17 章）是"系统控制器"，管振荡器和几个模拟系统模块。低功耗相关的是两个：BOD33 和 VREG。

**BOD33** = 3.3 V Brown-Out Detector，掉电检测器。它盯着 VDDANA，电压跌破阈值就产生复位或中断（`BOD33.ACTION` 选）。它的电流完全取决于工作模式（手册 Table 38-9，25 °C）：

| BOD33 模式 | 控制位 | 典型电流 | 说明 |
|---|---|---|---|
| 连续模式（复位默认） | `MODE=0` | 25 µA | 一直在比较，最费电 |
| 采样模式 | `MODE=1` + `PSEL` | 0.034 µA（Active/Idle） | 按 OSCULP32K 的 1 kHz 分频打点，到点才开一下 |
| 采样模式（Standby） | `MODE=1` + `RUNSTDBY=1` | 0.132 µA | 睡着时也能检测 |

**这几个数字要记住，因为 25 µA 在 Standby 里是个灾难**：芯片 Standby 一共才 4 µA，BOD33 保持连续模式就多出 6 倍电流。所以低功耗项目进 Standby 前，基本都会把 BOD33 切到采样模式。采样模式的代价是阈值会有最多 5% 的偏移（手册 17.6.9.3 解释了原因：基准电压没被别人用着的时候，它请求得太晚）。

`BOD33` 的 `LEVEL`、`ACTION`、`HYST`、`ENABLE` 在启动时会从 Flash 的 User Row 里加载，也就是说**不改代码也能改这些默认值**。第一次调低功耗时，一定要先读一读 `SYSCTRL_REGS->SYSCTRL_BOD33` 的实际值，别假设它是 0。

**VREG** = 内部稳压器，产生核心逻辑电源 VDDCORE。它有两个位影响睡眠：

| 位 | 值 | 效果（手册 17.8.15） |
|---|---|---|
| `RUNSTDBY` (bit 6) | 0 | Standby 下稳压器进低功耗配置（省电，但驱动能力弱） |
| | 1 | Standby 下稳压器保持正常配置（费电，但能带更多负载） |
| `FORCELDO` (bit 13) | 0 | Standby 下低功耗 + 低驱动 |
| | 1 | Standby 下低功耗 + 高驱动 |

`VREG.ENABLE`（bit 1）手册写的是 "Must be set to 1"，别去动它。

`RUNSTDBY` 怎么选，和 6.1 节那句警告是同一件事：Standby 下还有外设在跑、或者唤醒后要立刻大电流工作，就置 1；纯粹为了最低功耗，就置 0。手册里所有 Standby 功耗数字，除注明外都是 `VREG.RUNSTDBY=1` 测的——这一条很容易被忽略，导致"我实测怎么比手册还低"。

## 6.12 实测功耗时要注意的坑

手册 Table 37-8 / 37-11 的测试条件写得很细：3.3 V、DFLL48M 作主时钟、CPU/AHB 不分频、APBA 四分频、桥 B/C 关、只有 NVMCTRL 和 APBA 的 AHB 时钟在跑、只有 PM/SYSCTRL/RTC 的外设时钟在跑、I/O 不接外部电路且内部上拉、Cache 打开、**BOD33 关闭**、25 °C。你的板子只要有一条不一样，读数就对不上。常见的坑：

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 电流比手册大几十 µA 甚至翻倍 | 调试器还连着，PM 会把时钟一直留着给调试器用 | 手册 16.5.7：调试模式下测不出功耗。脱机、复位后单独供电再测 |
| 多出 25 µA 左右 | BOD33 还是复位默认的连续模式 | `BOD33.MODE=1` 切采样模式 + `PSEL` |
| 明明关了外设还是很费电 | 某个振荡器 `ONDEMAND=0`，即使没人用也一直跑 | 各振荡器的 `ONDEMAND` 位 |
| Idle 电流比手册高不少 | 主频、APB 分频、还有哪些外设时钟没关 | `PM.CPUSEL`、`PM.APBxSEL`、四个 mask 寄存器 |
| 想测 µA 却测不准 | 万用表 µA 档有"负担电压"，串进去会拉低芯片供电 | 用高边采样电阻 + 示波器，或专用的功耗分析仪 |
| 平均电流和瞬时电流对不上 | 睡眠是占空比工作：醒 1 ms 花 3 mA、睡 999 ms 花 4 µA | 算**一个周期的平均电流 / 能量**，不要盯着瞬时值 |
| 醒来后外设不工作 | Standby 把外设的时钟也停了，有些状态需要重配 | 唤醒后重新初始化受影响的外设 |
| Flash 写一半就睡，数据丢了 | NVMCTRL 进睡眠会自动进低功耗，页缓冲丢失；Standby 下无解 | 写命令必须在进睡眠前发完 |
| RTC 定时唤醒不生效 | GCLK_RTC 的时钟源在 Standby 停了 | 把源换成 OSCULP32K，或给源置 `RUNSTDBY` |
| 引脚悬空反而更费电 | 浮空输入在睡眠时被外部噪声推着翻转 | 把没用的脚配成输入 + 上下拉，或输出一个固定电平 |

## 6.13 把硬件概念翻译成寄存器

现在才看寄存器。每个寄存器都应该能回答一个明确的问题：

| 我要做什么 | SAMD21 的配置 |
|---|---|
| 关掉某个外设的时钟 | `PM.APBAMASK` / `APBBMASK` / `APBCMASK` / `AHBMASK` 对应位清 0 |
| 选 Idle 的档位 | `PM.SLEEP.IDLE[1:0]` |
| 选 Idle 还是 Standby | `SCB->SCR` 的 `SLEEPDEEP`（内核寄存器） |
| 真的睡下去 | `__DSB()` 然后 `__WFI()` |
| 醒来后自动再睡 | `SCB->SCR` 的 `SLEEPONEXIT` |
| 让 EIC 能唤醒 | `EIC.WAKEUP.WAKEUPEN[x]` + `EIC.INTENSET` |
| 给 EIC 配时钟 | `GCLK.CLKCTRL`（ID = EIC）+ 振荡器的 `RUNSTDBY` |
| 降低 BOD33 电流 | `SYSCTRL.BOD33` 的 `MODE` / `PSEL` / `RUNSTDBY` |
| 调 Standby 下稳压器强度 | `SYSCTRL.VREG` 的 `RUNSTDBY` / `FORCELDO` |
| 看这次为什么重启 | `PM.RCAUSE` |

**寄存器不是新知识，它只是硬件的控制开关。** 例如"进 Standby 之前先关掉 SERCOM0 的时钟"这件事：

```text
我要让 SERCOM0 不再耗电
      ↓ 硬件上
把 SERCOM0 的 APB 时钟门关掉（表停了，逻辑就不翻转了）
      ↓ SAMD21 上
PM.APBCMASK 的 SERCOM0 位清 0
```

**关于代码里的名字**：本书统一使用 SAMD21_DFP 3.7.262 的写法——外设指针是 `PM_REGS` / `EIC_REGS` / `PORT_REGS` / `GCLK_REGS`，寄存器成员带外设前缀，例如 `PM_REGS->PM_SLEEP`。网上很多教程（以及更老的 Atmel 版 DFP）用的是 `PM->SLEEP.reg`、`PORT->Group[0].DIRSET.reg` 这种写法，两者指的是同一块硬件，只是头文件封装不同：

| 本章写法（DFP 3.7.262） | 老写法 / 网上常见 | 说明 |
|---|---|---|
| `PM_REGS->PM_SLEEP` | `PM->SLEEP.reg` | 8 位寄存器 |
| `EIC_REGS->EIC_CONFIG[0]` | `EIC->CONFIG[0].reg` | 32 位 |
| `PORT_REGS->GROUP[0].PORT_PINCFG[4]` | `PORT->Group[0].PINCFG[4].reg` | 8 位 |
| `GCLK_REGS->GCLK_CLKCTRL` | `GCLK->CLKCTRL.reg` | 16 位 |

以你工程里实际用的 DFP 头文件为准；把上表左右两列对着看，就很容易互相翻译。

## 6.14 逐个看本章用到的寄存器

### PM.SLEEP：回答"Idle 停到哪一档"

偏移 0x01，8 位，只有 bit1:0 有效。名字叫 SLEEP，但它跟 Standby 没关系——Standby 由内核的 `SLEEPDEEP` 决定。这就是为什么它只解释 Idle 的三档。

```c
PM_REGS->PM_SLEEP = PM_SLEEP_IDLE(PM_SLEEP_IDLE_APB_Val);   /* Idle 2：CPU + AHB + APB 全停 */
```

DFP 提供了 `PM_SLEEP_IDLE_CPU_Val` / `_AHB_Val` / `_APB_Val` 三个值；宏名对不上时，直接用数字 0 / 1 / 2 也可以。

### SCB->SCR：回答"睡哪一大类"

这不是 SAMD21 的寄存器，是 Cortex-M0+ 内核的。`SCB` 由 CMSIS 提供，所以 `#include "sam.h"` 之后就能直接用：

```c
SCB->SCR |=  SCB_SCR_SLEEPDEEP_Msk;    /* 选 Standby */
SCB->SCR &= ~SCB_SCR_SLEEPDEEP_Msk;    /* 选 Idle（复位默认就是 0） */
```

写成 `|=` 和 `&= ~` 而不是直接赋值，是因为同一个寄存器里还有 `SLEEPONEXIT`、`SEVONPEND` 两位，直接赋值会把它们一起清掉。

### PM.APBAMASK / APBBMASK / APBCMASK / AHBMASK：回答"谁还有表"

四个 32 位寄存器，位 = 1 开、位 = 0 关。它们只影响**总线时钟**，不影响外设自己的 GCLK——一个外设要工作，两者都得有（这是第 2 章那条公式）。关时钟前先想清楚两件事：这个外设以后还读不读寄存器？它的中断还要不要用？

```c
PM_REGS->PM_APBCMASK &= ~PM_APBCMASK_SERCOM0_Msk;   /* 把 SERCOM0 的 APB 时钟关掉 */
```

### GCLK.CLKCTRL：回答"EIC 的时钟从哪来"

EIC 的边沿检测需要一个通用时钟。`CLKCTRL` 由 ID（哪个外设）、GEN（哪个发生器）、CLKEN（开不开）三段拼成，写入是跨时钟域的，要等 `GCLK_STATUS.SYNCBUSY` 归零：

```c
GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_EIC
                        | GCLK_CLKCTRL_GEN_GCLK2
                        | GCLK_CLKCTRL_CLKEN_Msk;
while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
```

为什么要等 `SYNCBUSY`？因为 GCLK 的配置寄存器工作在通用时钟域，而 CPU 写在 APB 时钟域，跨域写入需要一个同步过程；同步没完成就再写一次，会出问题。

为什么选发生器 2？因为**复位后发生器 2 的源就是 OSCULP32K 不分频**（手册 15.8 的 GENCTRL 复位值表：ID 2 为 `0x00010302`，即 SRC = OSCULP32K、GENEN = 1），而 OSCULP32K 永远在跑。这样 EIC 在 Standby 下自然还有时钟，不用额外配 `RUNSTDBY`。GCLK 的完整用法在第 5 章。

### EIC 的五个寄存器：回答"哪个脚、什么条件、要不要记住"

| 寄存器 | 偏移 | 它回答的问题 |
|---|---|---|
| `CTRL` | 0x00 | EIC 开了没（`ENABLE` bit1） |
| `CONFIG[0]` / `CONFIG[1]` | 0x18 / 0x1C | 每个引脚按什么条件触发（`SENSEx`）、要不要滤波（`FILTENx`） |
| `INTENSET` | 0x0C | 哪些引脚的中断要送进 NVIC |
| `INTFLAG` | 0x10 | 哪个引脚命中了（写 1 清除） |
| `WAKEUP` | 0x14 | 哪些引脚允许**从睡眠中唤醒**（`WAKEUPENx`） |

`CONFIG[0]` 管 EXTINT0–7，`CONFIG[1]` 管 EXTINT8–15，两者的位布局完全一样（每个引脚占 4 位：3 位 SENSE + 1 位 FILTEN）。所以 DFP 头文件里只定义了 `EIC_CONFIG_SENSE0`～`SENSE7` 这一组宏名，`CONFIG[1]` 里的第 0 组位置要复用 `SENSE0` 的宏——写 EXTINT8 以上的引脚时特别容易在这里搞错。

`INTENSET` 和 `WAKEUP` 为什么要分开？因为"唤醒"和"进中断"是两件事：`WAKEUP` 决定睡眠时这个脚能不能把芯片弄醒，`INTENSET` 决定醒来后要不要跑中断服务程序。手册 21.6.8 明确不推荐只置 `WAKEUPEN` 而不置 `INTENSET`，正常用法是两个一起开。

### SYSCTRL.BOD33 / SYSCTRL.VREG：回答"模拟部分吃多少电"

`BOD33` 偏移 0x34，`VREG` 偏移 0x3C，字段位置见速查表。有一个特性值得单独说：`BOD33` 的 `LEVEL` / `ACTION` / `HYST` / `ENABLE` 复位值是从 Flash User Row 加载的，**所以复位后它不是 0**。改 `LEVEL` 前必须先把 `ENABLE` 清 0，否则可能产生误复位（手册 17.8.14 原文：any change to the LEVEL field should be done when the BOD33 is disabled）。

`VREG.FORCELDO` 只在 Standby 下起作用，它决定的是稳压器的**驱动能力**而不是开关——低功耗模式下如果外设要的电流大，就得把它设成高驱动。

### PM.RCAUSE：回答"我这次是怎么起来的"

偏移 0x38，只读，复位值 0x01（POR）。它记录的是**复位原因**，不是唤醒原因。低功耗设备里它很有用：设备从野外收回来，先读一眼 `RCAUSE` 就知道是掉电重启、看门狗重启还是按键复位。

```c
uint8_t cause = PM_REGS->PM_RCAUSE;   /* bit0 POR, bit1 BOD12, bit2 BOD33, bit4 EXT, bit5 WDT, bit6 SYST */
```

## 6.15 完整例程：进 Standby，按 PA04 唤醒

### 硬件需求

按键一端接 PA04、一端接 GND，用内部上拉。按下时 PA04 从 1 变 0——**一个下降沿**。

```text
          内部上拉（PULLEN=1, OUT=1）
                   │
PA04 ──────────────┴──── 按键 ──── GND
松开 → 上拉 → 1      按下 → 接 GND → 0        EIC 抓这个下降沿
```

PA04 在 SAMD21G 上对应 `EXTINT[4]`（手册 Table 7-1 的 EIC 列）。同时 PB04、PA20 也是 `EXTINT[4]`，只要不把它们也配成 EIC 功能就不会打架。

### 从需求推到寄存器顺序

```text
按键要能唤醒  →  引脚交给 EIC（PMUX=A）+ 开输入缓冲 + 上拉
              →  EIC 抓下降沿（CONFIG）+ 允许中断（INTENSET）+ 允许唤醒（WAKEUP）
              →  EIC 要有时钟（GCLK，源用永远在跑的 OSCULP32K）
              →  进 Standby（SCR.SLEEPDEEP = 1）
              →  __DSB 排空 → __WFI 睡下
```

顺序不能乱：**唤醒源没配好就睡，就再也醒不来了**（除了复位）。调试阶段建议先留着调试器，或者给睡眠加一个开关，配好之后再打开。

### 代码

```c
#include "sam.h"

#define KEY_PIN     4                       /* PA04，对应 EXTINT[4] */
#define KEY_MASK    (1UL << KEY_PIN)
#define KEY_EXTINT  EIC_INTENSET_EXTINT4_Msk
#define KEY_WAKEUP  EIC_WAKEUP_WAKEUPEN4_Msk
#define KEY_FLAG    EIC_INTFLAG_EXTINT4_Msk

/* 给 EIC 挑一个"睡着了也在跑"的时钟源。发生器 2 复位后就是 OSCULP32K，不分频。 */
static void eic_clock_init(void)
{
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
    GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_EIC
                            | GCLK_CLKCTRL_GEN_GCLK2
                            | GCLK_CLKCTRL_CLKEN_Msk;
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
}

static void key_wake_init(void)
{
    /* ① 上拉方向先备好：OUT=1 + PULLEN 才是上拉（第 4 章那条规矩） */
    PORT_REGS->GROUP[0].PORT_OUTSET = KEY_MASK;

    /* ② 把 PA04 交给外设：PMUXEN 打开复用，INEN 让电平进得来，PULLEN 打开内部上拉 */
    PORT_REGS->GROUP[0].PORT_PINCFG[KEY_PIN] = PORT_PINCFG_PMUXEN_Msk
                                             | PORT_PINCFG_INEN_Msk
                                             | PORT_PINCFG_PULLEN_Msk;

    /* ③ PA04 是偶数脚，选 PMUXE 半字节；功能 A = EIC（手册 Table 7-1 的 A 列） */
    PORT_REGS->GROUP[0].PORT_PMUX[KEY_PIN / 2] = PORT_PMUX_PMUXE_A;

    eic_clock_init();

    /* ④ 抓下降沿。EXTINT4 占 CONFIG[0] 的第 16 位起，宏名就是 SENSE4 */
    EIC_REGS->EIC_CONFIG[0] = EIC_CONFIG_SENSE4_FALL;

    /* ⑤ 允许这个脚产生中断，并且允许它把芯片从睡眠里叫醒（21.6.8：两个一起开） */
    EIC_REGS->EIC_INTENSET = KEY_EXTINT;
    EIC_REGS->EIC_WAKEUP   = KEY_WAKEUP;

    /* ⑥ 最后才使能 EIC。顺序来自手册 21.6.2.1：先配寄存器，后开 ENABLE */
    EIC_REGS->EIC_CTRL = EIC_CTRL_ENABLE_Msk;

    /* 中断怎么进、怎么出是第 12 章的内容，这里只把 EIC 这一条挂上 NVIC */
    NVIC_EnableIRQ(EIC_IRQn);
}

/* 按键中断：清标志，顺便记一次数，方便在调试器里看 */
volatile uint32_t wake_count = 0;

void EIC_Handler(void)
{
    /* INTFLAG 写 1 清除。不清的话会一直重复进中断 */
    if (EIC_REGS->EIC_INTFLAG & KEY_FLAG)
    {
        EIC_REGS->EIC_INTFLAG = KEY_FLAG;
        wake_count++;
    }
}

static void standby_once(void)
{
    /* 关"不用的表"：本条用不到桥 C 上的外设，ADC 是桥 C 复位后唯一开着的，先关掉它 */
    PM_REGS->PM_APBCMASK &= ~PM_APBCMASK_ADC_Msk;

    /* SLEEP.IDLE 只管 Idle；Standby 忽略它。写 2 是为了以后切回 Idle 时直接是最省那档 */
    PM_REGS->PM_SLEEP = PM_SLEEP_IDLE(PM_SLEEP_IDLE_APB_Val);

    /* Standby 下稳压器默认转低功耗。这里还有外设要跑，保持正常配置更稳 */
    SYSCTRL_REGS->SYSCTRL_VREG |= SYSCTRL_VREG_RUNSTDBY_Msk;

    /* SLEEPDEEP=1 → 下一次 WFI 进 Standby；只动这一位，别把 SLEEPONEXIT 冲掉 */
    SCB->SCR |= SCB_SCR_SLEEPDEEP_Msk;

    /* __DSB：等前面所有寄存器写操作真正到达外设。时钟马上就停了，不能有半路货 */
    __DSB();

    /* 睡下。按键下降沿到来时，时钟恢复，CPU 从这里往下继续跑 */
    __WFI();
}

int main(void)
{
    key_wake_init();

    while (1)
    {
        standby_once();      /* 睡一次；醒来后继续循环，再睡回去 */
    }
}
```

### 醒来之后发生了什么

```text
按下按键 → PA04 下降沿 → EIC 内部逻辑（GCLK_EIC 还在跑）置起 INTFLAG.EXTINT[4]
        → 异步中断 → Standby 的时钟恢复 → CPU 跳到 EIC_Handler
        → 清标志、计数 +1 → 返回 → 继续 while(1) → 再睡
```

用万用表串在 VDD 上，你会看到电流在几 µA 和几 mA 之间跳——µA 那个值就是 Standby，mA 那一段是按键抖动引发的多次唤醒加上 CPU 运行；把 `wake_count` 放进调试器的 watch 窗口就能看清次数。如果只想先验证"能睡能醒"，把 `EIC_Handler` 里换成翻转一个 LED 引脚，比看电流表直观得多。

### 顺带一个 Idle 版本

只要把 `SLEEPDEEP` 清掉，同一份代码就变成 Idle 睡眠，唤醒源也从"只有异步"放宽到"同步也行"：

```c
SCB->SCR &= ~SCB_SCR_SLEEPDEEP_Msk;                            /* 选 Idle 大类 */
PM_REGS->PM_SLEEP = PM_SLEEP_IDLE(PM_SLEEP_IDLE_APB_Val);      /* 再选 Idle 2 */
__DSB();
__WFI();
```

对比一下电流：Idle 2 大约 1.17 mA，Standby 大约 4 µA。代价是唤醒慢一点（19.6 µs 对 13.0 µs）——**这个交换几乎总是值得的**。

## 6.16 初始化清单

以后遇到"要进低功耗"，按这个顺序想就不会漏：

**进睡眠前**：配唤醒源（含它自己的 GCLK 和 `WAKEUPEN`）→ 关掉不用的外设时钟（`PM.APBxMASK`）→ 处理 BOD33 / VREG（要不要切采样模式、`RUNSTDBY` 怎么设）→ 写 `SLEEP.IDLE` 和 `SCR.SLEEPDEEP` → `__DSB()` → `__WFI()`。

**醒来后**：确认中断标志清了（EIC 要写 1 清 `INTFLAG`）→ 检查被关过时钟的外设要不要重新初始化 → 确认唤醒原因（防止某个源一直在叫）。

```c
/* 每睡一次都要走的一小段 */
PM_REGS->PM_APBCMASK &= ~PM_APBCMASK_ADC_Msk;   /* 关不用的表 */
SCB->SCR |= SCB_SCR_SLEEPDEEP_Msk;              /* 选 Standby */
__DSB();                                        /* 排空写操作 */
__WFI();                                        /* 睡 */
```

**运行中**：不要在中断里改 `SLEEP.IDLE` 或睡眠模式；也不要一边写 Flash 一边进睡眠。这两件事都会让"睡下去"变成"卡死"。

## 6.17 一个低功耗的完整思维模型

```text
                        低功耗
                           │
        ┌──────────────────┼───────────────────┐
    ① 门控              ② 睡眠              ③ 唤醒
  PM.APBxMASK        SLEEP.IDLE /         EIC·RTC·WDT·
  PM.AHBMASK         SCR.SLEEPDEEP        BOD33·SleepWalking
  （外设停表）        __DSB → __WFI        （异步中断）
```

三件事的关系是：**①决定"睡着时还剩多少负担"，②决定"睡多深"，③决定"多久醒一次"。** 低功耗设计的全部工作，就是在这三者之间找平衡：睡得越深越省，但唤醒源越少、醒得越慢；醒得越频繁越费，但响应越及时。

## 6.18 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 执行 `WFI` 后立刻又继续跑 | 有中断标志没清，或者中断一直挂着 | 各外设的 `INTFLAG`；EIC 要写 1 清 |
| 睡了就再也醒不来 | 唤醒源没配对，或它的时钟在 Standby 停了 | `WAKEUPEN`、`INTENSET`、GCLK 源是不是 OSCULP32K |
| 配好的寄存器醒来后像是没生效 | `WFI` 前没加 `__DSB()`，写还在路上 | `__DSB()` 必须在 `__WFI()` 前面 |
| 按键唤醒会连续进好几次中断 | 机械抖动，一次按下产生多个边沿 | 用 `CONFIG.FILTEN` 打开多数表决滤波 |
| Idle 2 下串口中断叫不醒 | 串口的中断是 APB 时钟产生的同步中断 | 改用 Idle 0/1，或换成异步唤醒源 |
| `WAKEUPEN=1` 但中断服务程序不执行 | `INTENSET=0`，只醒了没进中断（手册明确不推荐） | `INTENSET` 也要置 1 |
| 按键接在 PA08 上配不通 EIC | PA08 是 NMI 引脚，不是 EXTINT | 换一个有 EXTINT 的引脚，例如 PA04 |
| 复用成 EIC 的引脚读不到电平 | 只开了 `PMUXEN`，忘了 `INEN` | `PINCFG` 三个位要一起配 |

## 6.19 寄存器速查

PM（基址 `0x4000_0400`，代码里 `PM_REGS`）：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `CTRL` | 0x00 | 本手册未定义有效位（保留） |
| `SLEEP` | 0x01 | bit1:0 `IDLE[1:0]`：0 CPU / 1 CPU+AHB / 2 CPU+AHB+APB |
| `CPUSEL` | 0x08 | bit2:0 `CPUDIV[2:0]`：÷1～÷128 |
| `APBASEL` / `APBBSEL` / `APBCSEL` | 0x09 / 0x0A / 0x0B | 各桥 APB 分频 |
| `AHBMASK` | 0x14 | 复位 0x7F |
| `APBAMASK` | 0x18 | 复位 0x7F（PAC0 / PM / SYSCTRL / GCLK / WDT / RTC / EIC） |
| `APBBMASK` | 0x1C | 复位 0x7F（PAC1 / DSU / NVMCTRL / PORT / DMAC / USB） |
| `APBCMASK` | 0x20 | 复位 0x0001_0000（只有 ADC 开） |
| `INTENSET` / `INTFLAG` | 0x35 / 0x36 | bit0 `CKRDY`：时钟设置生效 |
| `RCAUSE` | 0x38 | bit0 POR / bit1 BOD12 / bit2 BOD33 / bit4 EXT / bit5 WDT / bit6 SYST |

内核（不是 SAMD21 的外设，由 CMSIS 提供）：

| 寄存器 | 位 | 作用 |
|---|---|---|
| `SCB->SCR` | bit1 `SLEEPONEXIT` | 退出最低优先级 ISR 时自动再睡 |
| | bit2 `SLEEPDEEP` | 0 = Idle，1 = Standby |
| | bit4 `SEVONPEND` | 挂起中断也能唤醒 `WFE` |

EIC（基址 `0x4000_1800`，代码里 `EIC_REGS`）：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `CTRL` | 0x00 | bit0 `SWRST`、bit1 `ENABLE` |
| `INTENSET` | 0x0C | bit x 对应 `EXTINT[x]` |
| `INTFLAG` | 0x10 | 写 1 清除 |
| `WAKEUP` | 0x14 | bit x `WAKEUPEN[x]`：允许从睡眠唤醒 |
| `CONFIG[0]` / `CONFIG[1]` | 0x18 / 0x1C | 每脚 4 位：`SENSE[2:0]` + `FILTEN`；`SENSE`：1 上升沿 / 2 下降沿 / 3 双边沿 / 4 高电平 / 5 低电平 |

GCLK / SYSCTRL：

| 寄存器 | 偏移 | 作用 |
|---|---|---|
| `GCLK.CLKCTRL` | 0x02 | 16 位：`ID[5:0]` + `GEN[3:0]` + `CLKEN`(bit14) |
| `GCLK.STATUS` | 0x01 | bit7 `SYNCBUSY`，写完要等它归零 |
| `GCLK.GENCTRL` | 0x04 | bit16 `GENEN`、bit21 `RUNSTDBY`、`SRC[12:8]` |
| `SYSCTRL.BOD33` | 0x34 | bit1 `ENABLE`、bit2 `HYST`、bit4:3 `ACTION`、bit6 `RUNSTDBY`、bit8 `MODE`、bit9 `CEN`、bit15:12 `PSEL`、bit21:16 `LEVEL` |
| `SYSCTRL.VREG` | 0x3C | bit1 `ENABLE`（必须为 1）、bit6 `RUNSTDBY`、bit13 `FORCELDO` |
| `SYSCTRL.PCLKSR` | 0x0C | 状态位，含 `BOD33DET` |

GCLK 发生器复位值：ID 0 = `0x00010600`（OSC8M → GCLK_MAIN，GENEN=1）；ID 2 = `0x00010302`（OSCULP32K，GENEN=1），其余发生器复位值等于自己的 ID、未使能。复位后唯一被使能的通用时钟是 WDT 的那一条。

## 6.20 最重要的几句话

1. 省电的最小单位是"时钟"：模块有时钟就在耗电，没时钟就几乎不耗。
2. PM 管两件事——门控（谁还有表）和睡眠（睡多深），两者独立配置、一起生效。
3. Idle 只停同步时钟域（CPU / AHB / APB 逐级停），Standby 连振荡器一起停，所以差 800 倍。
4. Standby 忽略 `SLEEP.IDLE`；选大类靠内核的 `SCR.SLEEPDEEP`。
5. `__DSB()` 必须在 `__WFI()` 之前，否则配置还没到外设，时钟就停了。
6. Idle 2 把 APB 也停了，只有异步中断能唤醒它；Standby 更是只剩异步。
7. OSCULP32K 永远在跑，是 Standby 下最省心的时钟源（发生器 2 复位后就是它）。
8. BOD33 连续模式 25 µA，采样模式 0.034 µA——它在 Standby 里是最大的隐形开销。
9. SleepWalking = 外设按需把时钟叫起来干活，CPU 全程不参与，只在真有事时才被叫醒。
10. 调试器连着就测不出真实功耗；测低功耗必须脱机。

## 6.21 自测

1. 同样是"关掉 CPU"，Idle 0、Idle 1、Idle 2 分别多停了什么？为什么只能按这个顺序逐级停？
2. Standby 和 Idle 最本质的区别是什么？为什么这个区别能带来几百倍的电流差？
3. `SLEEPDEEP` 在哪？`SLEEP.IDLE` 在哪？进 Standby 时哪个起作用、哪个被忽略？
4. 为什么 `__WFI()` 前面要加 `__DSB()`？去掉会发生什么？
5. 一个按键接在 PA04 上，要让它把芯片从 Standby 唤醒，除了配 EIC 的 `CONFIG` / `INTENSET` / `WAKEUP`，还必须保证什么？（提示：想两件事——引脚的复用，和 EIC 的时钟）
6. 用一句话解释 SleepWalking 解决了什么问题；它靠哪两个位实现？

## 附录：以后查手册怎么找低功耗

手册里的 PM 章（第 16 章）不用通读，按这个顺序查：

```text
① 16.2 Features                  → 看它总共能做什么
② 16.6.2.8 Sleep Mode Controller → 睡眠模式的规则和两张关键表（Table 16-3 / 16-4）
③ 16.6.3 SleepWalking            → 按需时钟
④ 16.8 寄存器描述                 → 只看你要写的那几个
```

再往外扩，按问题找章节：

| 想知道什么 | 去哪一节 |
|---|---|
| 某个外设在睡眠下还工不工作 | 该外设章节的 Sleep Mode Operation / Power Management |
| 振荡器的 `ONDEMAND` / `RUNSTDBY` | 17.6 各振荡器小节 + Table 17-1 / 17-2 |
| 具体的电流、唤醒时间数字 | 第 37 章 Power Consumption / Wake-up Time（85 °C 档），第 38 / 39 章是 105 / 125 °C 档 |
| 唤醒时间从哪算起 | 表前面那句：from the edge of the wakeup signal to the execution of the first instruction fetched in flash |
| BOD33 的阈值对应多少伏 | 第 37 章 BOD33 LEVEL Value 表 |
| 复位原因 | `PM.RCAUSE`（16.8.14）+ 16.6.2.7 Reset Controller |
| 这条引脚有没有 EXTINT | 第 7 章 Table 7-1 的 EIC 列 |

本章涉及的偏移：`PM.SLEEP 0x01 / PM.APBAMASK 0x18 / PM.APBBMASK 0x1C / PM.APBCMASK 0x20 / PM.RCAUSE 0x38 / EIC.CTRL 0x00 / EIC.INTENSET 0x0C / EIC.INTFLAG 0x10 / EIC.WAKEUP 0x14 / EIC.CONFIG[0] 0x18 / GCLK.CLKCTRL 0x02 / SYSCTRL.BOD33 0x34 / SYSCTRL.VREG 0x3C`。

## 本章结束

学完这一章，你应该能把一段低功耗代码从"寄存器魔法"翻译成人话。例如

```c
SCB->SCR |= SCB_SCR_SLEEPDEEP_Msk;
__DSB();
__WFI();
```

不是"记住这三句"，而是：**下次执行 `WFI` 时请进 Standby（连振荡器一起停）；在停之前，先等我把所有寄存器写操作真正送到硬件；然后 CPU 在这里停住，等一个异步中断把我叫回来。**

再例如

```c
GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_EIC | GCLK_CLKCTRL_GEN_GCLK2 | GCLK_CLKCTRL_CLKEN_Msk;
EIC_REGS->EIC_INTENSET = EIC_INTENSET_EXTINT4_Msk;
EIC_REGS->EIC_WAKEUP   = EIC_WAKEUP_WAKEUPEN4_Msk;
```

应该翻译成：**给 EIC 一路"睡着了也不会停"的时钟（发生器 2 = OSCULP32K）；EXTINT[4] 命中时既进中断、也允许它把芯片从睡眠里叫醒。** 能把寄存器操作翻译成硬件动作，低功耗才算真正学会。
