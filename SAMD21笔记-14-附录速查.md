
# 第 14 章 附录：速查表

> **怎么用**：正文讲"为什么"，附录只放"是什么"。遇到具体问题先回正文，查不到再来这里。
> **数据来源**：数据手册 DS40001882L（本仓库的转换稿 [SAM-D21-DA1-Family-Data-Sheet-DS40001882.md](markdown/SAM-D21-DA1-Family-Data-Sheet-DS40001882.md)）＋ SAMD21_DFP 3.7.262 头文件；**目标器件是 ATSAMD21G18A**。

## 14.1 三条外设桥与外设基址

外设地址 = **桥基址 + 该外设的偏移**。每个桥只有 64 KB 窗口，地址不做重映射。

| 桥 | 基址 | 外设（基址 + 偏移） |
|---|---|---|
| A | `0x40000000` | PAC0 +0x000、PM +0x400、SYSCTRL +0x800、GCLK +0xC00、WDT +0x1000、RTC +0x1400、EIC +0x1800 |
| B | `0x41000000` | PAC1 +0x000、DSU +0x2000、NVMCTRL +0x4000、PORT +0x4400、DMAC +0x4800、USB +0x5000、MTB +0x6000 |
| C | `0x42000000` | PAC2 +0x000、EVSYS +0x400、SERCOM0–5 +0x800 起每实例 +0x400、TCC0–2 +0x2000 起每实例 +0x400、TC3–5 +0x2C00 起每实例 +0x400、ADC +0x4000、AC +0x4400、DAC +0x4800、PTC +0x4C00、I2S +0x5000 |

展开成绝对地址（写代码时最常用）：

| 外设 | 基址 | 外设 | 基址 |
|---|---|---|---|
| PM | `0x40000400` | SERCOM0 | `0x42000800` |
| GCLK | `0x40000C00` | SERCOM1 | `0x42000C00` |
| WDT | `0x40001000` | SERCOM2 | `0x42001000` |
| RTC | `0x40001400` | SERCOM3 | `0x42001400` |
| EIC | `0x40001800` | SERCOM4 | `0x42001800` |
| NVMCTRL | `0x41004000` | SERCOM5 | `0x42001C00` |
| PORT Group 0（PA） | `0x41004400` | TCC0 / TCC1 / TCC2 | `0x42002000` / `0x42002400` / `0x42002800` |
| PORT Group 1（PB） | `0x41004480` | TC3 / TC4 / TC5 | `0x42002C00` / `0x42003000` / `0x42003400` |
| DMAC | `0x41004800` | ADC / AC / DAC | `0x42004000` / `0x42004400` / `0x42004800` |
| USB | `0x41005000` | PTC / I2S | `0x42004C00` / `0x42005000` |

> 手册 Table 12-1 还列了 TC6、TC7、AC1、TCC3（基址 `0x42006000`）——**它们不在 ATSAMD21G18A 上**，见 14.3。

## 14.2 中断号

中断号决定 `NVIC_EnableIRQ(...)` 里填什么，以及中断向量表里的位置。下表是该器件编译时**真实存在**的中断（来源：SAMD21_DFP 3.7.262 里 `samd21g18a.h` 的 `IRQn_Type`）：

| IRQ | 名称 | 外设 | IRQ | 名称 | 外设 |
|---|---|---|---|---|---|
| 0 | PM_IRQn | 电源管理 | 14 | SERCOM5_IRQn | 串口 5 |
| 1 | SYSCTRL_IRQn | 系统控制器 | 15 | TCC0_IRQn | 定时器 TCC0 |
| 2 | WDT_IRQn | 看门狗 | 16 | TCC1_IRQn | 定时器 TCC1 |
| 3 | RTC_IRQn | 实时计数器 | 17 | TCC2_IRQn | 定时器 TCC2 |
| 4 | EIC_IRQn | 外部中断 | 18 | TC3_IRQn | 定时器 TC3 |
| 5 | NVMCTRL_IRQn | Flash 控制器 | 19 | TC4_IRQn | 定时器 TC4 |
| 6 | DMAC_IRQn | DMA 控制器 | 20 | TC5_IRQn | 定时器 TC5 |
| 7 | USB_IRQn | USB | 23 | ADC_IRQn | 模数转换 |
| 8 | EVSYS_IRQn | 事件系统 | 24 | AC_IRQn | 模拟比较器 |
| 9 | SERCOM0_IRQn | 串口 0 | 25 | DAC_IRQn | 数模转换 |
| 10–13 | SERCOM1–4_IRQn | 串口 1–4 | 26 | PTC_IRQn | 触摸控制器 |
| | | | 27 | I2S_IRQn | I2S |

**21、22 号中断在这个器件上是空的**（那是 TC6/TC7 的位置）。EIC 除了这条件 4 号中断，还有一条独立的 NMI。

## 14.3 这颗芯片上到底有哪些外设

手册 Table 12-1 列的是**整个 SAM D21 家族**，不是你手上这颗。判断某外设是否存在，看两处：手册的变体脚注 + 器件头文件里有没有对应的寄存器结构体。

| 外设 | ATSAMD21G18A 上的实例 | 备注 |
|---|---|---|
| SERCOM | 6（SERCOM0–5） | 每个都能配成 USART / SPI / I2C |
| TCC | 3（TCC0–2） | TCC3 只在 SAMD21x17D 上，G18A 没有 |
| TC | 3（TC3–5） | **TC6/TC7 不在 E 和 G 封装上**（本器件是 G） |
| ADC | 1（12 位；**通道数随封装变化**，48 脚器件少于 64 脚的 20 路，写代码前查手册 Table 2-1） | |
| AC | 1 | AC1 不在本器件上 |
| DAC / PTC / I2S | 各 1 | |
| DMAC | 12 通道 | |
| EVSYS | 12 通道 | |
| EIC | 16 条 EXTINT + 1 条 NMI | |
| PORT | 两组：PA00–PA31、PB00–PB31 | |
| 其它 | PM、SYSCTRL、GCLK、WDT、RTC、NVMCTRL、DSU、MTB、USB、PAC0–2 | |

验证方法（自己查一遍最保险）：打开 DFP 头文件搜外设名，例如 `SERCOM3_REGS`；搜不到 `TC6_REGS` 就说明这颗没有 TC6。

## 14.4 复位后的默认状态

这些值决定了"你什么都不配的时候，芯片是什么样"。

| 项目 | 复位后的值 | 含义 |
|---|---|---|
| CPU / 总线主频 | **1 MHz** | OSC8M 八分频，GCLK0 来自 OSC8M |
| GCLK 发生器 | 只有 GCLK0 使能 | 其余发生器关闭 |
| GCLK2 | 来自 OSCULP32K | WDT 的通用时钟默认挂在 GCLK2 |
| `PM_REGS->PM_APBAMASK` | `0x0000007F` | 桥 A 外设的总线时钟**全开** |
| `PM_REGS->PM_APBBMASK` | `0x0000007F` | 桥 B 外设的总线时钟**全开**（PORT 就在桥 B） |
| `PM_REGS->PM_APBCMASK` | `0x00010000` | 桥 C 只有 **ADC** 开着，其余全关 |
| 引脚 | 三态 + 输入缓冲关闭 | 既不是输入也不是输出，读 `IN` 无效 |
| `SystemInit()` | 空的 | 只把 `SystemCoreClock` 设成 1000000，不做任何配置 |

## 14.5 寄存器访问的三条规矩

| 规矩 | 怎么做 | 违反的后果 |
|---|---|---|
| 只改要改的位 | 用 SET / CLR / TGL 类寄存器（如 `OUTSET`/`OUTCLR`/`OUTTGL`、`INTENSET`/`INTENCLR`） | 用 `reg \|= ...` 是读-改-写，可能被中断打断或误改别的位 |
| 写保护（PAC） | 先写 `PACx_REGS->PAC_WPCLR = 位` 解锁，改完 `PACx_REGS->PAC_WPSET = 位` 上锁 | 写被丢弃，并且触发一次 CPU 异常；连续两次加锁或解锁同样异常 |
| 同步（SYNCBUSY） | 写完同步寄存器后等状态位清零，例如 `while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) {}` | 配置可能丢失或行为不确定 |

PAC 的三个实例与复位值：

| 实例 | 管哪条桥 | WPCLR 偏移 | WPSET 偏移 | 复位值（即默认被保护的位） |
|---|---|---|---|---|
| PAC0 | 桥 A | 0x00 | 0x04 | `0x000000`（都不保护） |
| PAC1 | 桥 B | 0x00 | 0x04 | `0x000002`（第 1 位 = DSU） |
| PAC2 | 桥 C | 0x00 | 0x04 | `0x00800000` |

PAC1 的位对应：bit6 MTB、bit5 USB、bit4 DMAC、bit3 PORT、bit2 NVMCTRL、bit1 DSU。
PAC2 的位对应：bit1 EVSYS、bit2–7 SERCOM0–5、bit8–10 TCC0–2、bit11–13 TC3–5、bit16 ADC、bit17 AC、bit18 DAC、bit19 PTC、bit20 I2S。


## 14.6 常用寄存器偏移速查

**PORT**（Group 0 基址 `0x41004400`，Group 1 = `0x41004480`，组内偏移相同）

| 偏移 | 名称 | 作用 | 偏移 | 名称 | 作用 |
|---|---|---|---|---|---|
| 0x00 | DIR | 方向（1 = 输出） | 0x14 | OUTCLR | 输出低 |
| 0x04 | DIRCLR | 设为输入 | 0x18 | OUTSET | 输出高 |
| 0x08 | DIRSET | 设为输出 | 0x1C | OUTTGL | 翻转 |
| 0x0C | DIRTGL | 方向翻转 | 0x20 | IN | 只读引脚电平 |
| 0x10 | OUT | 当前输出值 | 0x24 | CTRL | 输入采样模式 |
| 0x28 | WRCONFIG | 批量配置引脚（只写） | 0x30 + n | PMUX[n] | 一字节管 2 个脚 |
| 0x40 + n | PINCFG[n] | bit0 PMUXEN、bit1 INEN、bit2 PULLEN、bit6 DRVSTR | | | |

**GCLK**（基址 `0x40000C00`）

| 偏移 | 名称 | 说明 |
|---|---|---|
| 0x00 | CTRL | 软件复位 SWRST |
| 0x01 | STATUS | bit7 = SYNCBUSY |
| 0x02 | CLKCTRL（16 位） | ID[5:0] 选外设、GEN[3:0] 选发生器、CLKEN 使能、WRTLOCK 锁定 |
| 0x04 | GENCTRL | ID[3:0] 选发生器、SRC[4:0] 选时钟源、GENEN 使能、DIVSEL 分频源 |
| 0x08 | GENDIV | ID[3:0] 选发生器、DIV[15:0] 分频值 |

**PM**（基址 `0x40000400`）

| 偏移 | 名称 | 说明 |
|---|---|---|
| 0x00 / 0x01 | CTRL / SLEEP | 睡眠模式控制 |
| 0x08–0x0B | CPUSEL / APBASEL / APBBSEL / APBCSEL | CPU 与三条桥的时钟分频 |
| 0x14 | AHBMASK | AHB 时钟门控 |
| 0x18 / 0x1C / 0x20 | APBAMASK / APBBMASK / APBCMASK | 桥 A / B / C 的 APB 时钟开关 |
| 0x34 / 0x35 / 0x36 | INTENCLR / INTENSET / INTFLAG | 时钟就绪中断 |

**EIC**（基址 `0x40001800`）

| 偏移 | 名称 | 说明 |
|---|---|---|
| 0x00 / 0x01 | CTRL / STATUS | ENABLE、SWRST / SYNCBUSY |
| 0x04 | EVCTRL | 事件输出使能 |
| 0x08 / 0x0C | INTENCLR / INTENSET | 逐条 EXTINT 的中断开关 |
| 0x10 | INTFLAG | 中断标志，写 1 清除 |
| 0x14 | WAKEUP | 允许从睡眠唤醒 |
| 0x18 / 0x1C | CONFIG0 / CONFIG1 | 每条 EXTINT 占 4 位（SENSE + FILTEN），CONFIG0 管 EXTINT0–7 |

**PAC**（PAC0/PAC1/PAC2 基址 = 各自桥基址）

| 偏移 | 名称 | 说明 |
|---|---|---|
| 0x00 | WPCLR | 写 1 → 解除对应外设写保护 |
| 0x04 | WPSET | 写 1 → 加上写保护 |

其余外设的寄存器偏移见对应章节的速查表：SYSCTRL → 第 5 章；SERCOM → 第 7、8 章；TC/TCC/RTC/WDT → 第 9 章；ADC/DAC/AC → 第 10 章；DMAC/EVSYS → 第 11 章；NVMCTRL → 第 13 章。

## 14.7 引脚复用表怎么读

手册 Table 7-1 是"哪个引脚能当什么用"的总表。它的列就是 PORT 的复用功能编号：

| 列 | PMUX 值 | 内容 |
|---|---|---|
| A | 0 | EIC（外部中断 EXTINT[n]） |
| B | 1 | 模拟功能（REF / ADC / AC / PTC / DAC 等） |
| C | 2 | SERCOM（USART / SPI / I2C） |
| D | 3 | SERCOM-ALT（同一个引脚的另一个 SERCOM 实例） |
| E | 4 | TC |
| F | 5 | TCC |
| G | 6 | COM |
| H | 7 | 其它（如 AC） |

读表三步：**① 在"Pin"列找到你的引脚 → ② 横向看你需要的功能在哪一列 → ③ 那一列的字母换算成 PMUX 值（A=0 … H=7）写进 `PINCFG.PMUXEN=1` 加 `PMUX[n]`**。

两条容易忽略的说明（手册 Table 7-1 脚注）：

- **模拟功能都在 B 列**，而且选了 B 就会关掉该脚的数字功能——所以 ADC 引脚不需要（也不应该）再配数字方向；
- **只有部分引脚能进 I2C 模式**，见下表。

**支持 I2C 的引脚**（手册 Table 7-5）：

| 封装 | 可用引脚 |
|---|---|
| 32 脚 | PA08、PA09、PA16、PA17、PA22、PA23 |
| 48 脚 | 上面的 6 个，加上 PA12、PA13 |
| 64 脚 | 上面的 8 个，加上 PB12、PB13、PB16、PB17、PB30、PB31 |

## 14.8 手册章节 → 本笔记章节

| 手册章节 | 讲什么 | 本笔记 |
|---|---|---|
| 1–9 | 特性、选型、封装、引脚、复用、电源与上电、存储映射 | 第 1 章 |
| 10 | 存储器与物理存储映射 | 第 1、13 章 |
| 11 | 内核与总线、PAC、寄存器访问行为 | 第 2 章 |
| 12 | 外设配置总表（基址、时钟、中断号） | 第 1、14 章 |
| 13 | DSU（调试与芯片擦除） | 第 3 章 |
| 14 / 15 / 16 / 17 | 时钟系统 / GCLK / PM / SYSCTRL | 第 5、6 章 |
| 18 / 19 | WDT / RTC | 第 9 章 |
| 20 / 21 | DMAC / EIC | 第 11、12 章 |
| 22 | NVMCTRL（Flash 读写） | 第 13 章 |
| 23 | PORT（GPIO） | 第 4 章 |
| 24 | EVSYS | 第 11 章 |
| 25–28 | SERCOM 总览 / USART / SPI / I2C | 第 7、8 章 |
| 29 | I2S | 第 13 章（速览） |
| 30 / 31 | TC / TCC | 第 9 章 |
| 32 | USB | 第 13 章（速览） |
| 33 / 34 / 35 / 36 | ADC / AC / DAC / PTC | 第 10 章 |
| 37–41 | 电气特性（85 ℃ / 105 ℃ / 125 ℃ / 车规 / DA1） | 按需查阅 |
| 44 | 封装信息 | 第 1 章 |
| 46 / 47 | 约定 / 缩写表 | 本章 |

## 14.9 缩写表

| 缩写 | 全称 | 一句话 |
|---|---|---|
| GPIO | General Purpose Input/Output | 通用输入输出引脚 |
| PORT | I/O Pin Controller | 管理引脚的外设 |
| PMUX / PINCFG | Peripheral Multiplexing / Pin Configuration | 引脚的复用选择与属性配置 |
| PAC | Peripheral Access Controller | 寄存器写保护 |
| GCLK | Generic Clock Controller | 通用时钟控制器，9 个发生器 |
| PM | Power Manager | 时钟门控与睡眠控制 |
| SYSCTRL | System Controller | 振荡器、BOD、VREG |
| SERCOM | Serial Communication Interface | 可配置成 USART / SPI / I2C 的串口 |
| TC / TCC | Timer/Counter / Timer-Counter for Control | 通用定时器 / 面向控制的定时器 |
| ADC / DAC / AC | Analog-to-Digital / Digital-to-Analog / Analog Comparator | 模数 / 数模 / 比较器 |
| DMAC | Direct Memory Access Controller | 数据搬运 |
| EVSYS | Event System | 外设之间的硬件事件连线 |
| EIC | External Interrupt Controller | 引脚中断 |
| NVMCTRL | Nonvolatile Memory Controller | Flash 控制器 |
| DSU | Device Service Unit | 调试探针检测、芯片擦除、器件识别 |
| WDT / RTC | Watchdog Timer / Real-Time Counter | 看门狗 / 实时计数器 |
| BOD | Brown-Out Detector | 欠压检测 |
| DFLL / FDPLL | Digital Frequency Locked Loop / Fractional Digital PLL | 48 MHz 闭环源 / 分数分频 PLL |
| RWWEE | Read-While-Write Emulation Section | 可同时读写的 Flash 区（256 KB 型号没有） |
| SWD | Serial Wire Debug | 两线调试接口 |
| IOBUS | CPU local bus | CPU 直连 PORT 的单周期总线 |

## 14.10 各章速查表位置

| 想查什么 | 去哪一章 |
|---|---|
| GPIO 寄存器与位定义 | 第 4 章「寄存器速查」 |
| 时钟源、GCLK、PM 门控 | 第 5 章 |
| 睡眠模式与唤醒源 | 第 6 章 |
| I2C 流程与 SERCOM 寄存器 | 第 7 章 |
| SPI / USART 寄存器与波特率 | 第 8 章 |
| 定时器寄存器与频率计算 | 第 9 章 |
| ADC / DAC / AC 配置顺序 | 第 10 章 |
| DMAC 通道与 EVSYS 用户 | 第 11 章 |
| 中断号与 NVIC | 第 12 章 + 本附录 14.2 |
| Flash 读写命令 | 第 13 章 |

## 14.11 电气要点（接线前必看）

| 项目 | 数值 | 出处 |
|---|---|---|
| 工作电压（SAM D21） | 1.62–3.63 V | 手册 Features |
| 绝对最大 VDD | 3.8 V | Table 37-2 |
| 引脚电压范围 | GND−0.6 V ～ VDD+0.6 V | Table 37-2 |
| 单个 VDD 引脚电流 | ≤ 92 mA | Table 37-2 |
| 单个 GND 引脚电流 | ≤ 130 mA | Table 37-2 |
| 每个 GPIO 簇：拉出 / 灌入 | ≤ 46 mA / ≤ 65 mA | Table 37-2 脚注 |
| 单个 GPIO：默认驱动 | 灌 2.5 mA / 拉 2 mA | Table 40-14 |
| 单个 GPIO：加强驱动（DRVSTR=1） | 灌 10 mA / 拉 7 mA | Table 40-14 |
| 数字输入门限 | VIL ≤ 0.3×VDD，VIH ≥ 0.55×VDD | Table 40-14 |
| 输入漏电流 | ±1 µA（上下拉关闭时） | Table 40-14 |
| 存储温度 | −60 ～ 150 ℃ | Table 37-2 |

三条实践结论：**① LED 必须串限流电阻，单脚电流压在 5–10 mA 以内；② 同时点很多灯时注意"簇"的总电流上限（一个簇是物理上相邻的一组引脚，见手册 Table 7-6）；③ 引脚电压不能超过 VDD+0.6 V——5 V 信号直连会损坏引脚，要么分压，要么选 3.3 V 电平的器件。**

## 14.12 常用配方索引

| 我要做… | 去哪 |
|---|---|
| 点灯 | 第 4 章 4.10 |
| 读按键、配内部上拉 | 第 4 章 4.12 |
| 流水灯、用一个掩码操作多个脚 | 第 4 章 4.13–4.14 |
| 建工程、烧写、调试 | 第 3 章 |
| 把主频从 1 MHz 提到 48 MHz | 第 5 章 |
| 进 Standby、按键唤醒 | 第 6 章 |
| 读一个 I2C 传感器 | 第 7 章 |
| SPI 读 Flash ID、串口回环 | 第 8 章 |
| 定时中断、PWM、RTC 日历 | 第 9 章 |
| 读电位器、DAC 输出波形 | 第 10 章 |
| DMA 连续采样、外设互相触发 | 第 11 章 |
| 写中断服务程序 | 第 12 章 |
| 把参数存进 Flash、看门狗 | 第 13 章 |

## 14.13 手册与工具链的已知坑（各章踩出来的）

这一节记录写这套笔记时**真实遇到过**的矛盾与陷阱，每条都注明依据。遇到"手册和代码对不上"时先看这里。

| # | 现象 | 真相与依据 |
|---|---|---|
| 1 | 手册 15.6.1 正文说 GCLK 有 **eight** Generic Clock Generators，但寄存器表和框图都是 **9 个** | 以寄存器为准：GCLKGEN0–GCLKGEN8（ID 0x0–0x8），DFP 里有 `GCLK_CLKCTRL_GEN_GCLK8_Val` |
| 2 | 手册 11.6.2 里 PAC0 的复位值，**表格写 0x000000，正文写 0x000002** | 以 DFP 为准：`PAC0_WPROT_DEFAULT_VAL = 0`、PAC1 = 0x00000002、PAC2 = 0x00800000 |
| 3 | 手册的 USB Clock Recovery Mode 一节提到 `DFLLVAL.MUX` 和 `OSCCTRL.STATUS` | 这两个名字**不属于 SAM D21**（D21 的 DFLLVAL 只有 COARSE/FINE，振荡器控制叫 SYSCTRL）。以 DFP 头文件为准 |
| 4 | 手册 Table 12-1 列了 TC6、TC7、AC1、TCC3 | 那是**整个 SAM D21 家族**的清单。本器件只有 TC3–TC5、TCC0–2、AC 一个模块；DFP 里搜不到 `TC6_REGS` |
| 5 | 网上教程里的 `PORT->Group[0].DIRSET.reg`、`SERCOM0->CTRLA.reg` 在本地编译不过 | 那是 ASF 时代的老式头文件。SAMD21_DFP 3.7.262 用平铺写法，见各章的"新旧写法对照" |
| 6 | 写了 `#include "sam.h"` 却用不了 `SystemCoreClock` | `samd21g18a.h` 里那句 include 被 `#if defined USE_CMSIS_INIT` 包着。要么显式 `#include "system_samd21g18a.h"`，要么编译时定义 `USE_CMSIS_INIT` |
| 7 | 提速到 48 MHz 后，官方的 `SystemCoreClockUpdate()` 也不更新频率 | 它在 DFP 里是空壳，只把编译期常量 1000000 再赋一遍。改主频后必须自己写 `SystemCoreClock = 48000000UL;` |
| 8 | DFLL48M 闭环用 OSC8M 的 1 MHz 当参考 | 不合规：闭环参考要求 **≤33 kHz 且误差 ≤2%**（手册 Table 37-7 与 DFLL 特性表）。首选 XOSC32K，或改用开环 + 工厂校准值 |
| 9 | 手册的 PAC 位图给了每个外设一位，但 DFP 里没有对应宏 | `component/pac.h` 只有通用掩码 `PAC_WPCLR_WP_Msk`，位号要自己写，例如 `#define PAC2_WP_SERCOM0 (1UL << 2)` |

> 第 1–4 条是**手册本身**的问题（多器件共用文档、修订残留），第 5–9 条是**工具链与常见写法**的问题。两条通用规律：**寄存器名与位定义以 DFP 头文件为准；某个外设是否存在，以器件头文件里有没有对应的 `xxx_REGS` 为准。**


## 14.14 外设寄存器偏移总表

每个外设的基址见 14.1，下表给的是**该外设内部的偏移**（写代码时用 `外设_REGS->成员名`，不必自己算地址；这张表是用来对手册和排错的）。数据来自 SAMD21_DFP 3.7.262 头文件，与手册的 Register Summary 一致。

| 外设 | 寄存器偏移 |
|---|---|
| **PORT**（每组） | DIR 0x00、DIRCLR 0x04、DIRSET 0x08、DIRTGL 0x0C、OUT 0x10、OUTCLR 0x14、OUTSET 0x18、OUTTGL 0x1C、IN 0x20、CTRL 0x24、WRCONFIG 0x28、PMUX[n] 0x30+n、PINCFG[n] 0x40+n |
| **GCLK** | CTRL 0x00、STATUS 0x01、CLKCTRL 0x02、GENCTRL 0x04、GENDIV 0x08 |
| **PM** | CTRL 0x00、SLEEP 0x01、CPUSEL 0x08、APBASEL 0x09、APBBSEL 0x0A、APBCSEL 0x0B、AHBMASK 0x14、APBAMASK 0x18、APBBMASK 0x1C、APBCMASK 0x20、INTENCLR 0x34、INTENSET 0x35、INTFLAG 0x36 |
| **SYSCTRL**（DFLL 部分） | DFLLCTRL 0x24、DFLLVAL 0x28、DFLLMUL 0x2C、DFLLSYNC 0x30 |
| **EIC** | CTRL 0x00、STATUS 0x01、NMICTRL 0x02、NMIFLAG 0x03、EVCTRL 0x04、INTENCLR 0x08、INTENSET 0x0C、INTFLAG 0x10、WAKEUP 0x14、CONFIG0 0x18、CONFIG1 0x1C |
| **PAC** | WPCLR 0x00、WPSET 0x04 |
| **SERCOM**（I2C 主机身份） | CTRLA 0x00、CTRLB 0x04、BAUD 0x0C、INTENCLR 0x14、INTENSET 0x16、INTFLAG 0x18、STATUS 0x1A、SYNCBUSY 0x1C、ADDR 0x24、DATA 0x28 |
| **TC**（COUNT16 视图） | CTRLA 0x00、CTRLBCLR 0x04、CTRLBSET 0x05、INTFLAG 0x0E、STATUS 0x0F、COUNT 0x10、CC0 0x18、CC1 0x1A |
| **TCC** | CTRLA 0x00、SYNCBUSY 0x08、WAVE 0x3C、PER 0x40、CC0 0x44（CC1–CC3 依次往后） |
| **RTC** | CTRL 0x00、READREQ 0x02、EVCTRL 0x04、INTENCLR 0x06、INTENSET 0x07、INTFLAG 0x08、STATUS 0x0A、DBGCTRL 0x0B、FREQCORR 0x0C、COUNT/CLOCK 0x10、PER 0x14、COMP/ALARM0 0x18、MASK 0x1C |
| **WDT** | CTRL 0x00、CONFIG 0x01、EWCTRL 0x02、INTENCLR 0x04、INTENSET 0x05、INTFLAG 0x06、STATUS 0x07、CLEAR 0x08 |
| **NVMCTRL** | CTRLA 0x00、CTRLB 0x04、PARAM 0x08、INTENCLR 0x0C、INTENSET 0x10、INTFLAG 0x14、STATUS 0x18、ADDR 0x1C、LOCK 0x20 |
| **EVSYS** | CTRL 0x00、CHANNEL 0x04、USER 0x08、CHSTATUS 0x0C、INTENCLR 0x10、INTENSET 0x14、INTFLAG 0x18 |
| **ADC** | CTRLA 0x00、REFCTRL 0x01、AVGCTRL 0x02、SAMPCTRL 0x03、CTRLB 0x04、WINCTRL 0x08、SWTRIG 0x0C、INPUTCTRL 0x10、EVCTRL 0x14、INTENCLR 0x16、INTENSET 0x17、INTFLAG 0x18、STATUS 0x19、RESULT 0x1A、WINLT 0x1C、WINUT 0x20、GAINCORR 0x24、OFFSETCORR 0x26、CALIB 0x28 |
| **DAC** | CTRLA 0x00、CTRLB 0x01、EVCTRL 0x02、INTENCLR 0x04、INTENSET 0x05、INTFLAG 0x06、STATUS 0x07、DATA 0x08、DATABUF 0x0C |
| **DMAC** | 全局：CTRL 0x00、CRCCTRL 0x02、SWTRIG 0x08、INTPEND 0x10、BUSYCH 0x28、PENDCH 0x2C、BASEADDR 0x34、WRBADDR 0x38、CHID 0x3F；通道：CHCTRLA 0x40、CHCTRLB 0x44（步长见第 11 章）。注意 `BTCTRL/BTCNT/SRCADDR/DSTADDR` **不是内存映射寄存器**，而是 SRAM 里的传输描述符，偏移 0x00/0x02/0x04/0x08 |

## 14.15 关键词索引

| 想找的东西 | 去哪 |
|---|---|
| 型号怎么读、E/G/J 区别、变体 A–D | 1.1 |
| 电源域、去耦、上电顺序、复位源 | 1.4、1.5 |
| 三条外设桥、地址空间 | 1.7、2.1 |
| 寄存器三种访问姿势、SET/CLR/TGL | 2.3、2.4 |
| PAC 写保护、位图、违反后果 | 2.6、14.5 |
| SYNCBUSY、Enable-Protected | 2.7、2.8 |
| "写了没反应"排查顺序 | 2.9 |
| 建工程、DFP 目录、启动文件、链接脚本 | 3.2–3.8 |
| SystemInit 是空的、复位后 1 MHz | 3.6、14.4 |
| 烧写与 SWD 调试、Memory 窗口 | 3.9 |
| 引脚方向/上下拉/复用（PMUX、PINCFG） | 4.4–4.9 |
| 点灯、按键、流水灯、WRCONFIG 批量配置 | 4.10–4.16 |
| 时钟源（OSC8M/XOSC/DFLL/FDPLL） | 5.2 |
| GCLK 发生器与 CLKCTRL、CORE/SLOW | 5.4、5.5 |
| 配到 48 MHz 的完整步骤 | 5.9 |
| Idle 0/1/2、Standby、WFI、唤醒源 | 6.3–6.8 |
| SleepWalking、ONDEMAND/RUNSTDBY | 6.9 |
| SERCOM 三合一、四套寄存器身份 | 7.1、7.2 |
| I2C 开漏与上拉、START/ACK/STOP 流程 | 7.4–7.6 |
| I2C 引脚限制、总线卡死恢复 | 7.7、7.12 |
| SPI 四线与 CPOL/CPHA、PAD/DOPO | 8.2–8.4 |
| USART 帧格式、波特率公式与误差 | 8.5、8.6 |
| TC 计数模式、1 秒中断、PWM | 9.2–9.6 |
| TCC 互补输出与死区、故障保护 | 9.7 |
| RTC 三种模式、日历与闹钟 | 9.8 |
| WDT 窗口模式 | 9.9、13.14 |
| ADC 参考电压、采样时间、窗口监控 | 10.2–10.7 |
| DAC 输出、AC 比较器 | 10.8、10.9 |
| EVSYS 三段模型、事件脉冲语义 | 11.1、11.3、11.5 |
| DMAC 通道、描述符、CRC | 11.8–11.11 |
| NVIC、优先级、EIC 配置、NMI | 12.2、12.3、12.5 |
| 中断不进的六环排查 | 12.8 |
| Flash 擦写、页缓冲、命令与 KEY | 13.2–13.6 |
| CRC32、USB/I2S/PTC 速览 | 13.16–13.19 |
| 手册与工具链的已知坑 | 14.13 |


## 14.16 GCLK 时钟 ID 速查

配通用时钟时写的是 `GCLK_REGS->GCLK_CLKCTRL = GCLK_CLKCTRL_ID_xxx | GCLK_CLKCTRL_GEN_GCLKn | GCLK_CLKCTRL_CLKEN_Msk`，其中 `ID` 就是下表的值（手册 Table 15-3，宏名来自 DFP）。

| ID | 外设 | ID | 外设 | ID | 外设 |
|---|---|---|---|---|---|
| 0x00 | DFLL48M（参考） | 0x13 | SERCOMx SLOW（所有 SERCOM 共用） | 0x1B | TCC2 与 TC3（共用一路） |
| 0x01 | FDPLL96M | 0x14–0x19 | SERCOM0–5 CORE | 0x1C | TC4 与 TC5 |
| 0x02 | FDPLL96M 32 kHz | 0x1A | TCC0 与 TCC1（共用一路） | 0x1D | TC6 与 TC7（本器件没有） |
| 0x03 | WDT | 0x1E | ADC | 0x1F | AC 数字部分 |
| 0x04 | RTC | 0x20 | AC 模拟部分 | 0x21 | DAC |
| 0x05 | EIC | 0x22 | **PTC**（DFP 里没有对应宏，要写裸值 `(0x22UL << GCLK_CLKCTRL_ID_Pos)`） | 0x23 / 0x24 | I2S_0 / I2S_1 |
| 0x06 | USB | 0x07–0x12 | EVSYS 通道 0–11 各一路 | | |

两条容易记错的地方：**SERCOM 要配两路**（CORE 管收发、SLOW 管 SMBus 超时等慢速逻辑，SLOW 是六个 SERCOM 共用同一个 ID 0x13）；**TCC0/TCC1 共用 0x1A、TCC2 和 TC3 共用 0x1B、TC4/TC5 共用 0x1C**——所以改一路会同时影响两个外设。


## 14.17 新外设上电自检清单

换一颗没用过的外设、或者代码跑不起来时，按这个顺序问自己一遍，绝大多数问题在第三步之前就暴露了：

| 步骤 | 问什么 | 在哪一章学 |
|---|---|---|
| 1 | 这个外设**在这颗芯片上存在**吗？是不是家族里的其它型号才有 | 1.6、14.3 |
| 2 | 引脚配对了吗？复用选对功能了吗？模拟脚有没有误配成数字 | 4.4–4.9、14.7 |
| 3 | 总线时钟（`APBxMASK`）开了吗？桥 C 的外设默认是关的 | 2.2、14.4 |
| 4 | 通用时钟（`GCLK_CLKCTRL`）给了吗？ID 对不对？SERCOM 有没有给两路 | 5.5、14.16 |
| 5 | 写保护（PAC）挡着吗？在 `PACx_REGS->PAC_WPCLR` 里解锁了吗 | 2.6、14.5 |
| 6 | 外设使能了吗？是 Enable-Protected 的寄存器吗（要先关再配） | 2.8 |
| 7 | 写完同步寄存器，等 `SYNCBUSY` 了吗 | 2.7 |
| 8 | 中断：外设的 `INTENSET` 和 NVIC 两处都开了吗？标志清了吗？函数名对吗 | 12.1、12.8 |
| 9 | 还没反应，就看寄存器实际值：调试器的 Memory 窗口直接读外设基址 | 3.9 |

一句话总结：**"看不见的现象"要回到"看得见的寄存器值"上去找**——先读回来，再判断是硬件没配、时钟没给，还是代码写错了地址。
