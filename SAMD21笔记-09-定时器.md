
# 第 9 章 定时器：TC、TCC、RTC、WDT

> **芯片**：ATSAMD21G18A（48 脚，38 个 I/O）。定时外设清单：**TC3 / TC4 / TC5**、**TCC0 / TCC1 / TCC2**、**RTC**、**WDT**。这颗芯片没有 TC6/TC7，也没有 TCC3，写代码时不要照抄家族手册里的大清单。
> **本章目标**：搞清四种定时外设各自解决什么问题；把"每 1 秒中断一次""输出 1 kHz 的 PWM"翻译成**分频、TOP、CCx** 三个数字；两个例程各写一遍。
> **学习顺序**：定时器是什么 → 四种外设的分工 → TC 的计数模式 → 配置顺序 → 寄存器 → 例程一（1 秒中断）→ PWM 与引脚复用 → TCC 的额外能力 → 互补输出顺序 → 例程二（呼吸灯）→ RTC / WDT → 初始化清单 → 思维模型 → 自测。
> **代码写法**：全书统一使用 MPLAB X 6.30 里 SAMD21_DFP 3.7.262 的写法。外设基址宏是"外设名 + `_REGS`"，寄存器成员带外设前缀：`PORT_REGS->GROUP[0].PORT_DIRSET`、`TC3_REGS->COUNT16.TC_CTRLA`、`TCC0_REGS->TCC_PER`、`RTC_REGS->MODE2.RTC_CLOCK`、`WDT_REGS->WDT_CLEAR`；单位字段用 `_Msk` 掩码，多位字段用 `_Val` 或现成的组合宏（如 `TC_CTRLA_PRESCALER_DIV1024`）。
> **前置**：第 2 章（寄存器读写规矩、SYNCBUSY、INTENSET/INTFLAG）、第 4 章（PORT 与引脚复用）、第 5 章（GCLK 与 PM）。本章代码假设 **GCLK0 已经配成 48 MHz**，时钟怎么配见第 5 章。

## 9.1 定时器是什么：把"时间"变成"数字"

时钟是一串均匀的脉冲，48 MHz 就是每秒 4800 万拍，一拍约 20.8 ns。定时器只做一件事：**数这些脉冲**；数够多少个脉冲就是多长时间，于是"时间"变成了"数字"。

```text
GCLK ──▶ 分频器(÷N) ──▶ 计数器 COUNT ──▶ 比较 COUNT == CCx ──▶ 中断 / 事件
                          │                      │
                          └── 数到 TOP 回零        └── 翻转输出引脚（WO）
```

一个定时器能问的问题只有三种，对应三个数字：

| 数字 | 存在哪里 | 回答的问题 | 改了会怎样 |
|---|---|---|---|
| 分频 N | `CTRLA.PRESCALER` | 每几拍数一下 | 整体变快或变慢 |
| TOP | `PER` / `CC0` / 固定最大值 | 数到几算一轮 | 周期长短 |
| CCx | `CC0` / `CC1` 等 | 一轮里的哪个时刻动手 | 中断时刻 / 占空比 |

后面每个例子都在算同一个公式：

```text
一轮的时钟数 = TOP + 1        一轮的时间 = (TOP + 1) × N ÷ f_GCLK
周期频率 f = f_GCLK ÷ (N × (TOP + 1))        N = 1、2、4 … 1024
```

为什么是 TOP+1 而不是 TOP：计数器从 0 开始数，数到 TOP 之后的**下一个**时钟才回零，所以一轮包含 0、1、2 … TOP 一共 TOP+1 个数。

## 9.2 四种定时外设的分工

"数时间""输出波形""记日历""看门狗"对硬件的要求差别很大，所以芯片把它们拆成四个外设：

| 外设 | 位宽 | 通道 | 时钟 | 擅长什么 |
|---|---|---|---|---|
| **TC** | 8 / 16 位，32 位需要两颗级联 | 2 个比较或捕获 | `GCLK_TCx` | 通用延时、周期中断、普通 PWM、输入捕获 |
| **TCC** | 16 位（TCC2）/ 24 位（TCC0、TCC1） | 2 或 4 个比较 | `GCLK_TCCx` | 面向控制的 PWM：互补输出、死区、故障保护、双缓冲 |
| **RTC** | 32 位，可当日历 | 1 个比较 / 1 个闹钟 | `GCLK_RTC` | 秒以上的计时、日期、闹钟唤醒 |
| **WDT** | 12 档固定周期 | 无 | `GCLK_WDT` | 程序跑飞后强制复位 |

选择法一句话：**只要"到点叫我一声"就用 TC；要驱动半桥、全桥这类功率级就用 TCC；要"现在是几点"用 RTC；要"程序死了能自己重启"用 WDT。**

有一条硬件限制必须先知道：**定时器的通用时钟是成对共用的**，同一对里的两个外设配不出两种频率。本芯片的配对关系是：

| GCLK 通道 | 谁在用 |
|---|---|
| `0x1A` = `GCLK_CLKCTRL_ID_TCC0_TCC1` | TCC0、TCC1（GCLK ID 26） |
| `0x1B` = `GCLK_CLKCTRL_ID_TCC2_TC3` | TCC2、TC3（GCLK ID 27） |
| `0x1C` = `GCLK_CLKCTRL_ID_TC4_TC5` | TC4、TC5（GCLK ID 28） |
| `0x04` = `GCLK_CLKCTRL_ID_RTC`，`0x03` = `GCLK_CLKCTRL_ID_WDT` | RTC（ID 4）/ WDT（ID 3），各自独占 |

所以"TC3 跑 1 kHz、TCC2 跑 1 MHz"这种要求做不到，它们共享同一路通用时钟。另外别忘了：**TC 和 TCC 挂在桥 C**，`PM_REGS->PM_APBCMASK` 里它们的总线时钟复位后是关着的，用之前得打开；RTC 和 WDT 在桥 A（`PM_APBAMASK`），总线时钟复位后就是开的。

## 9.3 TC 的三种计数模式：为什么"模式"决定了寄存器视图

TC 的位宽由 `CTRLA.MODE` 选择，三种模式的寄存器布局不一样：

| 模式 | `MODE` | 计数范围 | `COUNT` 偏移 | 有 `PER` 吗 | `CC0` / `CC1` 偏移 | 32 位怎么来 |
|---|---|---|---|---|---|---|
| `COUNT16`（默认） | `0x0` | 0 到 65535 | 0x10 | 没有 | 0x18 / 0x1A | 不适用 |
| `COUNT8` | `0x1` | 0 到 255 | 0x10 | 有（0x14） | 0x18 / 0x19 | 不适用 |
| `COUNT32` | `0x2` | 0 到 2 的 32 次方减 1 | 0x10 | 没有 | 0x18 / 0x1C | 两颗 TC 级联 |

"为什么模式会影响寄存器视图"值得停一下。寄存器地址永远是 0x10，但它在 8 位模式下必须是 1 字节、16 位模式下 2 字节、32 位模式下 4 字节，**同一个地址，三种宽度**。头文件只好把三种模式做成一个联合体（union），写代码时必须说清用哪一种：

```c
TC3_REGS->COUNT16.TC_COUNT     /* 16 位模式：读计数器 */
TC3_REGS->COUNT8.TC_PER        /* 8 位模式的周期寄存器；16 位模式下这个地址不属于 PER */
```

选模式的判据很直接：**要数多少下**。8 位最多 256 下，16 位最多 65536 下，32 位最多 42 亿下。

级联还有三条规则：`COUNT32` 是把**两颗** TC 拼成一颗，偶数是主（TC4、TC6）、奇数是从（TC5、TC7）；本芯片只有 TC3/TC4/TC5，能拼 32 位的只有 **TC4 + TC5**，拼好后只操作 **TC4** 的寄存器，TC5 的 `STATUS.SLAVE` 被硬件置 1，它自己的寄存器不再反映那个 32 位计数器；**TC3 不能做 32 位**，它没有搭档。

## 9.4 TC 的配置顺序：为什么这样排

定时器的寄存器分三类，顺序就是被这三类属性逼出来的：

| 类别 | 例子 | 什么时候能写 |
|---|---|---|
| enable-protected（使能保护） | `CTRLA` 的大部分位、`EVCTRL.EVACT` | 只能在 `ENABLE = 0` 时写 |
| write-synchronized（写同步） | `COUNT`、`PER`、`CCx`、`CTRLBSET` / `CTRLBCLR` | 随时能写，写完要等 `SYNCBUSY` 变 0 |
| 普通 | `INTENSET` / `INTENCLR`、`INTFLAG`、`CTRLC` | 随时能写 |

于是标准顺序是：**开总线时钟（APBCMASK）→ 分配 GCLK → 趁 `ENABLE = 0` 写模式、波形、分频 → 写 TOP 和 CCx → 开中断 → 最后 `ENABLE = 1`**。

把 `ENABLE = 1` 放最后有两个理由：被使能保护的寄存器一旦使能就写不进去了；而且使能之前计数器不动，不会出现"配到一半先跑了一轮"的中间状态。这和 GPIO 那章"先准备 `OUT`、再打开 `DIR`"是同一个思路：**先把状态摆好，最后再让硬件动起来。**

## 9.5 TC 的关键寄存器：每个都在回答一个问题

### CTRLA：模式、波形、分频，一个寄存器回答三个问题

| 位 | 名称 | 作用 |
|---|---|---|
| 0 | `SWRST` | 写 1 软复位整个 TC（复位前先关 `ENABLE`） |
| 1 | `ENABLE` | 使能，写 1 开始计数 |
| 3:2 | `MODE` | `COUNT16` / `COUNT8` / `COUNT32` |
| 6:5 | `WAVEGEN` | 波形发生方式，决定 TOP 从哪来 |
| 10:8 | `PRESCALER` | `DIV1` 到 `DIV1024`，即除以 1、2、4 直到 1024 |
| 13:12 | `PRESCSYNC` | 分频器与计数器在哪一拍同步（默认值即可） |

为什么 `PRESCALER` 只有 8 档而且都是 2 的幂：硬件上它就是一个二进制计数器，取第 n 位当输出，所以只能得到 2 的 n 次方分频。想除 3 这类分频，只能靠改 TOP，把一轮的时钟数设成 3 的倍数。

### WAVEGEN：TOP 从哪来（TC 最容易踩的一点）

| `WAVEGEN` | 名字 | TOP 是谁 | 谁控制输出 |
|---|---|---|---|
| `NFRQ` | 普通频率 | 8 位模式是 `PER`，16 / 32 位模式是最大值 | CCx 决定翻转点，WO[x] 每次匹配翻转一次 |
| `MFRQ` | 匹配频率 | **`CC0`** | WO[0] 每个周期翻转一次 |
| `NPWM` | 普通 PWM | 8 位模式是 `PER`，16 / 32 位模式是最大值 | CCx 决定占空比，周期固定 |
| `MPWM` | 匹配频率 PWM | **`CC0`** | CC0 定周期，CC1 定占空比 |

一句话记住差别：**`NFRQ` 和 `NPWM` 的周期由位宽锁死，`MFRQ` 和 `MPWM` 的周期由 `CC0` 决定。** 想要"周期正好 1 秒"这种要求，只能选后两种，或者用 8 位模式的 `PER`。

### CTRLBSET / CTRLBCLR：软件"下令"的地方

| 位 | 名称 | 说明 |
|---|---|---|
| 7:6 | `CMD` | `0x1` = `RETRIGGER`（重新开始、重触发），`0x2` = `STOP`（停下并保持当前值） |
| 2 | `ONESHOT` | 一轮结束后自动停（`STATUS.STOP` 置 1），适合延时一次 |
| 0 | `DIR` | 0 向上数，1 向下数 |

要下令就写 `CTRLBSET`，要撤销就写 `CTRLBCLR`；命令执行完 `CMD` 读回来是 0。为什么要有 SET 和 CLR 两个地址：它们各自是"一条写指令"，不需要读-改-写，中断里改 `DIR` 或下一道 `STOP` 命令时不会和主程序打架，理由和第 4 章的 `OUTSET` / `OUTCLR` 完全一样（第 2 章）。

### COUNT 与 CC0 / CC1：数到几、在哪儿动手

`COUNT` 是当前计数值，可读可写，运行中也能写，写进去的值优先于计数。`CCx` 是"比较/捕获值"，同一个寄存器有两种身份：**做比较**时它是你写进去的目标值；**做捕获**时它是硬件把 `COUNT` 快照进来的地方。

具体哪个是 TOP，回到 `WAVEGEN`：`MFRQ` / `MPWM` 下 `CC0` 就是 TOP，此时 `CC0` 不再表示占空比，占空比要用 `CC1`。这就是"明明只改了 CC0，周期却变了"的原因。

### INTENSET 与 INTFLAG：谁负责"到点叫我"

| 标志 | 置位条件 |
|---|---|
| `OVF` | 计数器回绕（数到 TOP 或 ZERO） |
| `MC0` / `MC1` | `COUNT` 与 `CC0` / `CC1` 比较匹配，或捕获完成 |
| `ERR` | 捕获溢出（上一次的捕获值还没被读走） |
| `SYNCRDY` | 同步完成 |

`INTENSET` 写 1 打开某个中断源，`INTENCLR` 写 1 关闭；`INTFLAG` 是**写 1 清除**，`INTFLAG` 写 0 什么都不会发生（第 2 章）。中断服务程序里必须清掉自己那一位，否则退出中断的瞬间硬件看到标志还是 1，会立刻再进来，表现为"进了中断就出不来"。

### STATUS.SYNCBUSY：为什么每写完一笔都要等

TC 的配置寄存器在**总线时钟域（APB）**里被 CPU 写，而计数器在**通用时钟域（GCLK_TCx）**里跑，两者异步。`COUNT`、`PER`、`CCx`、`CTRLBSET` / `CTRLBCLR` 都跨这两个域，写进去要过几拍才真正生效，`SYNCBUSY` 就是"我正在搬，先别写下一笔"的旗子。

```c
TC3_REGS->COUNT16.TC_CC[0] = 46874;
while (TC3_REGS->COUNT16.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }   /* 等这一笔落到 TC 时钟域 */
```

不等会怎样：第二次写可能被丢弃，或者读到只更新了一半的 32 位值。注意三个外设的 `SYNCBUSY` 长在不同地方：TC 和 RTC 在 `STATUS.SYNCBUSY`，TCC 有一个**独立的 `SYNCBUSY` 寄存器**（`while (TCC0_REGS->TCC_SYNCBUSY) { }`），GCLK 在 `GCLK_STATUS.SYNCBUSY`（第 2 章那张表）。

## 9.6 例程一：用 TC3 做 1 秒中断

需求：每 1 秒进一次中断，在中断里翻转 PA05 上的 LED；主程序不再被延时占住。

先把数字算出来。GCLK0 = 48 MHz，选 `DIV1024` 分频：48 MHz ÷ 1024 = 46875 Hz，也就是每 46875 个计数正好 1 秒。16 位模式最多数到 65535，装得下，所以用 `COUNT16` 而不是 32 位；又因为 16 位模式下 `NFRQ` / `NPWM` 的 TOP 固定是 0xFFFF，要精确 1 秒就必须让 TOP 由 `CC0` 决定，也就是选 `MFRQ`。

```text
f = 48 MHz ÷ 1024 = 46875 Hz        一轮 46875 个数 = 1.000 s
MFRQ 下 TOP = CC0  →  CC0 + 1 = 46875  →  CC0 = 46874
```

```c
#include "sam.h"

/* GCLK_TC3 = GCLK0 = 48 MHz，通用时钟的配法见第 5 章 */
static void tc3_1s_init(void)
{
    /* ① TC3 在桥 C：APB 总线时钟复位后是关的，先打开 */
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_TC3_Msk;

    /* ② 通用时钟通道 0x1B 由 TCC2 和 TC3 共用，这里把 GCLK0 接到 TC3 */
    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_TCC2_TC3 |
                                         GCLK_CLKCTRL_GEN_GCLK0   |
                                         GCLK_CLKCTRL_CLKEN_Msk);
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }   /* GCLK 也要同步 */

    /* ③ CTRLA 是使能保护的：模式 / 波形 / 分频必须在 ENABLE = 0 时写 */
    TC3_REGS->COUNT16.TC_CTRLA = TC_CTRLA_MODE_COUNT16        /* 16 位计数 */
                               | TC_CTRLA_WAVEGEN_MFRQ         /* TOP = CC0，不再是 0xFFFF */
                               | TC_CTRLA_PRESCALER_DIV1024;   /* 48 MHz / 1024 = 46875 Hz */

    /* ④ MFRQ 下 CC0 就是 TOP：46874 + 1 = 46875 拍 = 1.000 s */
    TC3_REGS->COUNT16.TC_CC[0] = 46874;
    while (TC3_REGS->COUNT16.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }

    /* ⑤ 比较匹配（COUNT 数到 CC0）产生中断；外设和 NVIC 两处都要开 */
    TC3_REGS->COUNT16.TC_INTENSET = TC_INTENSET_MC0_Msk;
    NVIC_EnableIRQ(TC3_IRQn);

    /* ⑥ 最后使能：此刻计数器从 0 开始跑 */
    TC3_REGS->COUNT16.TC_CTRLA |= TC_CTRLA_ENABLE_Msk;
    while (TC3_REGS->COUNT16.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }
}

void TC3_Handler(void)
{
    /* INTFLAG 写 1 清除。不清，退出中断后会立刻再进来 */
    TC3_REGS->COUNT16.TC_INTFLAG = TC_INTFLAG_MC0_Msk;

    /* PA05 上的 LED 每 1 秒翻转一次（低电平点亮 → 亮 1 秒、灭 1 秒） */
    PORT_REGS->GROUP[0].PORT_OUTTGL = PORT_PA05;
}

int main(void)
{
    /* 先按第 4 章把 PA05 配成输出，初始电平为高（灭） */
    PORT_REGS->GROUP[0].PORT_OUTSET = PORT_PA05;
    PORT_REGS->GROUP[0].PORT_DIRSET = PORT_PA05;

    tc3_1s_init();

    while (1)
    {
        /* 主程序完全自由：可以在这里干别的事，甚至进低功耗 */
    }
}
```

和忙等延时对比一下，差别不在"灯能不能亮"，而在 CPU 归谁用：

| 做法 | CPU 占用 | 时间精度 | 能不能同时干别的 |
|---|---|---|---|
| `delay_ms(1000)` 忙等（第 4 章） | 100% 空转 | 取决于主频和编译器优化 | 不能，主线被卡住 |
| TC 比较匹配中断 | 几乎为 0，只在中断里跑几微秒 | 由时钟源决定，与代码长度无关 | 能，主线继续跑 |

一个小细节：`MFRQ` 下计数器到 TOP 时，`INTFLAG.OVF` 和 `INTFLAG.MC0` 会同时置位。我们只开了 `MC0`，所以只处理 `MC0`；`OVF` 虽然也置 1，但它的中断没开，不会造成额外进入。

## 9.7 PWM：让"占空比"变成可调的数字

PWM（Pulse Width Modulation，脉冲宽度调制）就是**周期固定、高电平时间可调**的方波。高电平时间占整个周期的比例叫**占空比**：

```text
25%: ┌─┐   ┌─┐   ┌─┐          占空比 = 高电平时间 ÷ 周期
     ┘ └───┘ └───┘ └──
75%: ┌───┐ ┌───┐ ┌───┐        LED 亮度、电机转速都跟着占空比走
     ┘   └─┘   └─┘   └
```

回到 `WAVEGEN` 那张表，做 PWM 有两条路，区别只在"周期由谁定"：

| 做法 | 周期（TOP） | 改占空比 | 改频率 | 适合 |
|---|---|---|---|---|
| 普通 PWM（`NPWM`） | 16 位模式固定 0xFFFF；8 位模式由 `PER` 定 | 写 `CCx` | 只能换分频或换 8 位模式改 `PER` | LED 调光、简单调速 |
| 匹配频率 PWM（`MPWM` / `MFRQ`） | **`CC0`** | 写 `CC1` | 写 `CC0` | 频率和占空比都要能调 |

以 16 位 `NPWM` 为例：TOP = 0xFFFF，占空比 = `CC1 ÷ 65536`，频率 = 48 MHz ÷ 65536 ≈ 732 Hz（`DIV1`）。想改频率就得换分频；想同时要"频率可调 + 占空比可调"，就得让 TOP 变成 `CC0`，用 `MPWM`。

```c
/* TC4（16 位模式）输出普通 PWM：周期固定 65536 拍，占空比 = CC1 / 65536 */
static void tc4_pwm_init(void)
{
    /* TC4、TC5 共用通用时钟通道 0x1C；这里只把 TC4 当 16 位用，TC5 不受影响 */
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_TC4_Msk;
    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_TC4_TC5 |
                                         GCLK_CLKCTRL_GEN_GCLK0  |
                                         GCLK_CLKCTRL_CLKEN_Msk);
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    TC4_REGS->COUNT16.TC_CTRLA = TC_CTRLA_MODE_COUNT16
                               | TC_CTRLA_WAVEGEN_NPWM      /* TOP = 0xFFFF，周期固定 */
                               | TC_CTRLA_PRESCALER_DIV1;   /* 48 MHz / 65536 ≈ 732 Hz */
    TC4_REGS->COUNT16.TC_CC[1] = 32768;                     /* 一半高一半低 = 50% */
    while (TC4_REGS->COUNT16.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }

    /* 关键一步：定时器只在芯片内部生成波形，要把它"接到引脚上"才看得见 */
    /* PA23 的复用功能 E 是 TC4/WO[1]（手册 Table 7-1） */
    PORT_REGS->GROUP[0].PORT_PINCFG[23] = PORT_PINCFG_PMUXEN_Msk;
    PORT_REGS->GROUP[0].PORT_PMUX[23 / 2] = PORT_PMUX_PMUXO_E;   /* 23 是奇数脚 → 用 PMUXO */
    PORT_REGS->GROUP[0].PORT_DIRSET = PORT_PA23;

    TC4_REGS->COUNT16.TC_CTRLA |= TC_CTRLA_ENABLE_Msk;
    while (TC4_REGS->COUNT16.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }
}
```

这段代码里有本章第二个最常犯的错误：**PWM 不会自动出现在引脚上**。定时器内部只有 `WO[x]` 这个信号，它要经 PORT 的复用器才能走到焊盘，所以必须做第 4 章那两步——`PINCFG.PMUXEN = 1` 选择外设控制引脚，`PMUX` 选具体是哪一路外设（这里是功能 E）。只配定时器不配引脚，现象是"波形完全看不到"；如果 `DIRSET` 也忘了，引脚连输出驱动都没开。

顺便说清"哪个脚对应哪一路 WO"：这由手册的 I/O 复用表（Table 7-1）决定，**不是随便挑的**。本芯片 48 脚封装上常见的对应关系：PA05 功能 E = TCC0/WO[1]，PA04 功能 E = TCC0/WO[0]，PA22 功能 F = TCC0/WO[4]，PA23 功能 E = TC4/WO[1]。查表要先找引脚所在的行，再看 E / F 两列。

## 9.8 TCC 的额外能力：它比 TC 多什么

TCC 和 TC 的计数内核是同一套思路（分频 → 计数 → 比较），多出来的是**面向功率级的外围电路**。本芯片三个 TCC 的配置并不一样：

| 实例 | 位宽 | 比较通道 | 波形输出 | 输出矩阵 / 死区 / 交换 | 图案生成 |
|---|---|---|---|---|---|
| TCC0 | 24 位 | 4 | 8 路（WO[0]…WO[7]） | 有 | 有 |
| TCC1 | 24 位 | 2 | 4 路 | 无 | 有 |
| TCC2 | 16 位 | 2 | 2 路 | 无 | 无 |

### 多个比较通道与输出矩阵（OTMX）

TC 只有 2 个通道，TCC0 有 4 个，而且输出比通道还多（4 个通道对 8 路输出）。`WEXCTRL.OTMX` 决定"哪个通道驱动哪几路输出"：默认 `0x0` 是通道依次对应（CC0→WO[0]，并有重复映射），`0x1` 把低位通道复制到更多输出（用两个通道控制全桥四个管子），`0x2` 让 CC0 驱动全部输出（配图案生成可以驱动步进电机）。

### 互补输出与死区时间：为什么需要"两个都关"的一小段

驱动半桥时，上管和下管接在同一节点上，**同时导通就是电源直通地，会烧管子**。所以 TCC 把每一对输出做成互补的：低边 `LS` 是正相，高边 `HS` 是反相，一个开另一个必关。但开关管关断需要时间，理想互补仍然会出现瞬间同时导通的窗口，于是硬件在每次翻转后插入一小段"两个都关"的时间，这就是**死区（dead time）**。

```text
OTMX 输出 ──┬──▶ LS = WO[x]        （正相，低边）
            └──▶ HS = WO[WO_NUM/2 + x]（反相，高边）
死区插入：LS 关 ──[DTLS 个时钟]──▶ HS 开       两边都不导通的一小段
```

死区设置寄存器：`WEXCTRL.DTIENx` 打开第 x 对输出的死区发生器，`WEXCTRL.DTLS` 和 `DTHS` 分别是从低到高、从高到低翻转时要插多少个 `GCLK_TCC` 时钟（8 位，1 到 255，按外设时钟节拍算）。这两个寄存器是**使能保护**的，必须在 `ENABLE = 0` 时写。

### 故障保护：出事后把输出拉到安全电平

功率电路最怕"程序还在慢慢处理，管子已经过流了"，所以 TCC 让**事件系统**直接接到波形输出上，硬件自己动手：

| 故障类型 | 来源 | 触发后硬件做什么 | 配在哪 |
|---|---|---|---|
| 可恢复故障 A / B | 事件通道（例如模拟比较器过流信号） | 可以关闭输出、强制重启计数、或只在故障期间停住；恢复后能继续 | `FCTRLA` / `FCTRLB`（`SRC` 选源、`HALT` 停机方式、`RESTART` 重启、`BLANK` / `QUAL` 抗干扰） |
| 不可恢复故障 0 / 1 | 异步事件 `TCE0` / `TCE1` | 立刻停止计数，并把各通道输出强制成 `DRVCTRL.NREx` / `NRVx` 预先定义的安全电平 | `DRVCTRL` 的 `NRE0-7` / `NRV0-7`，再由 `EVCTRL.EVACTn = FAULT` 使能 |

要点是"**硬件直接动手**"：即使 CPU 卡在别处、甚至时钟已经出问题，输出也会按预设值被拉掉。故障的细节（滤波、消隐、捕获）属于进阶内容，用到时再查手册第 31 章 31.6.3.5 / 31.6.3.6 两节。

### 双缓冲：为什么改占空比要经过 CCBUF

运行中直接写 `CCx`，新值会在下一个周期生效，但如果刚好写在计数器经过这个值之后，本周期就会出现一个**畸形的宽脉冲或窄脉冲**（电机上表现为抖动、LED 上表现为闪一下）。TCC 的办法是给每个周期和比较寄存器配一个影子寄存器：`PERB`、`CCBx`，写进去先存着，等到 UPDATE 条件（计数器回绕）再一起搬到 `PER`、`CCx`。

| 你要做的事 | 写哪里 | 什么时候真的生效 |
|---|---|---|
| 立刻生效（调试用） | `CCx` | 同步延迟之后 |
| 无毛刺更新（运行中用） | `CCBx` | 下一个 UPDATE 条件 |

写完 `CCBx` 后 `STATUS.CCBVx` 会置 1，表示"缓冲区里有新值"；`CTRLBCLR.LUPD` 写 1 则禁止自动更新（想手动更新就用 `CTRLBSET.CMD = UPDATE`）。呼吸灯那种每几毫秒改一次的场景，用 `CCBx` 就不会看到跳变。

## 9.9 TCC 输出互补 PWM 的配置顺序

配 TCC 的顺序不是"习惯"，而是被使能保护逼出来的。下面这个顺序每一步都有理由：

**① 关着配时钟**：`ENABLE = 0` 状态下打开 `PM_APBCMASK` 和 `GCLK_CLKCTRL`，等 GCLK 同步。

**② 写 `CTRLA`**：分频 `PRESCALER`；可选 `ALOCK = 1` 把 `WAVE`、`PER`、`CCx` 等关键寄存器锁成只读，防止跑飞的程序改坏波形。

**③ 写 `WAVE`**：`WAVEGEN` 选 `NPWM`（单斜率）或 `DSBOTH`（双斜率、中央对齐，电机常用）；`POLx` 选每个通道的相位；需要交换 LS / HS 时写 `SWAPx`。

**④ 写 `WEXCTRL`**：`OTMX` 决定输出分配，`DTIENx` 打开哪几对的死区发生器，`DTLS` / `DTHS` 给出死区时钟数。**这一步必须在使能前**，否则等于让上下管在没有任何死区保护的情况下先跑起来。

**⑤ 写 `PER` 和 `CCx`**：此时算出周期和初始占空比；运行中要改就走 `CCBx` 双缓冲。

**⑥ 配引脚**：`PINCFG.PMUXEN` + `PMUX` 选功能，`DIRSET` 打开输出。低边在 `WO[x]`，高边在 `WO[WO_NUM/2 + x]`，两个脚都要配。

**⑦ 最后 `CTRLA.ENABLE = 1`**：到这一步所有受保护的寄存器都写完了，再等一次 `TCC_SYNCBUSY`。

哪些寄存器属于"必须使能前写"，看手册的 Enable-Protected 标记：

| 什么时候写 | 寄存器 |
|---|---|
| 必须 `ENABLE = 0` | `CTRLA`（除 `ENABLE` / `SWRST` / `RUNSTDBY`）、`WEXCTRL`、`DRVCTRL`、`FCTRLA` / `FCTRLB`、`EVCTRL` |
| 运行时随便写（写同步） | `WAVE`、`PER`、`CCx`、`CCBx`、`PERB`、`CTRLBSET` / `CTRLBCLR` |

## 9.10 例程二：TCC0 输出 PWM 做呼吸灯

需求：用第 4 章那颗 PA05 上的 LED 做呼吸效果——亮度平滑地由灭到亮再到灭。硬件条件：PA05 低电平点亮，功能 E = TCC0/WO[1]（48 脚封装第 10 脚）。

参数这样定：PWM 频率取 1 kHz，远高于人眼能分辨的闪烁，也远低于 LED 驱动能力的上限；`DIV1` 下 `PER + 1 = 48 MHz ÷ 1 kHz = 48000`，所以 `PER = 47999`（24 位寄存器装得下，分辨率 48000 级，足够平滑）。通道选 1，因为它对应 WO[1]。

```c
#include "sam.h"

static void tcc0_pwm_init(void)
{
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_TCC0_Msk;
    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_TCC0_TCC1 |
                                         GCLK_CLKCTRL_GEN_GCLK0   |
                                         GCLK_CLKCTRL_CLKEN_Msk);
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* WAVE 是使能保护的：趁 ENABLE = 0 一次写完 */
    TCC0_REGS->TCC_CTRLA = TCC_CTRLA_PRESCALER_DIV1;
    TCC0_REGS->TCC_WAVE  = TCC_WAVE_WAVEGEN_NPWM   /* 单斜率 PWM，TOP = PER */
                         | TCC_WAVE_POL1_Msk;      /* 通道 1 反相：低电平点亮时占空比越大越亮 */
    while (TCC0_REGS->TCC_SYNCBUSY) { }

    TCC0_REGS->TCC_PER   = 47999;   /* 1 kHz = 48 MHz / (47999 + 1) */
    TCC0_REGS->TCC_CC[1] = 0;       /* 占空比 0 → 全灭 */
    while (TCC0_REGS->TCC_SYNCBUSY) { }

    /* TCC 内部有波形不等于引脚上有波形：PA05 复用功能 E = TCC0/WO[1] */
    PORT_REGS->GROUP[0].PORT_PINCFG[5] = PORT_PINCFG_PMUXEN_Msk;
    PORT_REGS->GROUP[0].PORT_PMUX[5 / 2] = PORT_PMUX_PMUXE_E;   /* 5 是偶数脚 → PMUXE */
    PORT_REGS->GROUP[0].PORT_DIRSET = PORT_PA05;

    TCC0_REGS->TCC_CTRLA |= TCC_CTRLA_ENABLE_Msk;
    while (TCC0_REGS->TCC_SYNCBUSY) { }
}

static void rough_delay(void)                 /* 约几毫秒的粗糙延时，只为看呼吸效果 */
{
    for (volatile uint32_t i = 0; i < 48000; i++) { }
}

int main(void)
{
    tcc0_pwm_init();

    int32_t duty = 0;
    int32_t step = 200;                       /* 每步 200/48000，来回一次约 2.5 秒 */

    while (1)
    {
        /* 写 CCB 而不是 CC：新值等下一个周期开始才生效，看不到畸形脉冲 */
        TCC0_REGS->TCC_CCB[1] = (uint32_t)duty;

        duty += step;
        if (duty >= 47999) { duty = 47999; step = -step; }
        if (duty <= 0)     { duty = 0;     step = -step; }

        rough_delay();
    }
}
```

这段程序里有两处和"直觉"不一样的地方，值得单独记一下。**第一，占空比最大时 LED 最亮，是因为 `WAVE.POL1` 把通道 1 反相了**：不反相时 `CC1` 越大高电平越长，而我们的 LED 是低电平点亮，会越暗；`POL1 = 1` 之后 `CC1` 直接对应"低电平时间"，看起来就顺了。**第二，改占空比写的是 `CCB[1]`**，它是 `CC[1]` 的影子寄存器，周期边界才搬过去，所以看不到闪烁。

## 9.11 RTC：32 位计数器，或者一本日历

RTC（Real-Time Counter）是一个**独立时钟域**里的 32 位计数器，带 10 位分频器，典型用途是"秒"以上的计时。它可以在任何睡眠模式下继续运行，并用比较匹配、闹钟或周期性事件把 CPU 唤醒（第 6 章）。它有三种模式，**同一个外设在三种模式下的寄存器布局完全不同**：

| 模式 | `CTRL.MODE` | 计数寄存器 | 比较 / 闹钟 | 典型用途 |
|---|---|---|---|---|
| 模式 0：`COUNT32` | `0x0` | `COUNT`（32 位） | `COMP0` 一个 32 位比较 | 长间隔计时、秒计数 |
| 模式 1：`COUNT16` | `0x1` | `COUNT`（16 位）+ `PER` 定周期 | `COMP0` / `COMP1` 两个 16 位比较 | 周期中断，周期由 `PER` 决定 |
| 模式 2：`CLOCK` | `0x2` | `CLOCK`（秒/分/时/日/月/年打包在一个 32 位数里） | `ALARM0` + `MASK.SEL` | 日历、闹钟、定点唤醒 |

和 TC 一样，模式决定了"哪些寄存器存在"：模式 2 下没有 `COMP0`，只有 `CLOCK` / `ALARM0` / `MASK0`；模式 0 下没有 `PER`。**所以配 RTC 的第一件事永远是先定模式**，模式又只能在 `ENABLE = 0` 时写。

时钟这条要单独记：RTC 的通用时钟是 `GCLK_CLKCTRL_ID_RTC`（通道 0x04），而且**手册明确说 RTC 不能用 GCLK0**，必须另配一路。分频器同样是 2 的幂（`DIV1` 到 `DIV1024`）：

```text
f_CLK_RTC_CNT = f_GCLK_RTC ÷ 2^PRESCALER
要做 1 秒一跳：GCLK_RTC 取 1024 Hz（32.768 kHz ÷ 32），再配 PRESCALER = DIV1024
```

为什么不能直接拿 32.768 kHz 配上 `DIV1024` 得到 1 Hz：`DIV1024` 只除 1024，32768 ÷ 1024 = 32 Hz，还差 32 倍，而分频器最大就到 1024。这就是"日历模式必须给 1 Hz"这条要求带来的连锁反应。

模式 2 的配置顺序和别的外设一样：**模式、日历格式、分频都是使能保护的，先写 `CTRL`；再写 `CLOCK` / `ALARM`；最后开中断、使能**。

```c
/* 时钟：假设 GCLK_RTC = 1024 Hz（GCLK 配法见第 5 章；RTC 不能用 GCLK0） */
static void rtc_calendar_init(void)
{
    /* CTRL 里 MODE / PRESCALER / CLKREP / MATCHCLR 都是使能保护的 */
    RTC_REGS->MODE2.RTC_CTRL = RTC_MODE2_CTRL_MODE_CLOCK          /* 模式 2：日历 */
                             | RTC_MODE2_CTRL_PRESCALER_DIV1024;  /* 1024 Hz ÷ 1024 = 1 Hz */
    RTC_REGS->MODE2.RTC_CTRL |= RTC_MODE2_CTRL_ENABLE_Msk;
    while (RTC_REGS->MODE2.RTC_STATUS & RTC_STATUS_SYNCBUSY_Msk) { }

    /* 设成 2024-01-01 00:00:00。YEAR 是从参考年起的偏移，
       参考年必须是闰年（例如 2000），硬件才有正确的闰年补偿 */
    RTC_REGS->MODE2.RTC_CLOCK = RTC_MODE2_CLOCK_YEAR(24) | RTC_MODE2_CLOCK_MONTH(1)
                              | RTC_MODE2_CLOCK_DAY(1)    | RTC_MODE2_CLOCK_HOUR(0)
                              | RTC_MODE2_CLOCK_MINUTE(0) | RTC_MODE2_CLOCK_SECOND(0);
    while (RTC_REGS->MODE2.RTC_STATUS & RTC_STATUS_SYNCBUSY_Msk) { }
}

static void rtc_alarm_at_second(uint8_t sec)
{
    RTC_REGS->MODE2.RTC_ALARM = RTC_MODE2_ALARM_SECOND(sec);
    RTC_REGS->MODE2.RTC_MASK  = RTC_MODE2_MASK_SEL_SS;   /* 只比较"秒"，其余字段忽略 */
    RTC_REGS->MODE2.RTC_INTENSET = RTC_MODE2_INTENSET_ALARM0_Msk;
    NVIC_EnableIRQ(RTC_IRQn);
}

void RTC_Handler(void)
{
    RTC_REGS->MODE2.RTC_INTFLAG = RTC_MODE2_INTFLAG_ALARM0_Msk;   /* 写 1 清除 */
}

/* 读时间也要同步：先请求，再读 */
static uint32_t rtc_now(void)
{
    RTC_REGS->MODE2.RTC_READREQ = RTC_READREQ_RREQ_Msk | RTC_READREQ_ADDR(0x10);
    return RTC_REGS->MODE2.RTC_CLOCK;      /* 0x10 就是 CLOCK / COUNT 的偏移 */
}
```

`MASK.SEL` 是日历模式里最有用的一位组：`SS` 只比秒、`MMSS` 比到分、`HHMMSS` 比到时分秒、一直到 `YYMMDDHHMMSS` 全比。想让闹钟"每天 7:30:00 响一次"，就写 `HHMMSS` 并把 `ALARM0` 的时/分/秒设成 7/30/0；不参与比较的字段写什么都不会影响匹配，但为了可读性一般也填上。

## 9.12 WDT：程序跑飞时的最后一道保险

WDT（Watchdog Timer）是一条**独立时钟**驱动的倒计时器：使能后它一直数，程序必须在超时前把它清零（俗称"喂狗"），否则芯片被强制复位。因为它用自己的时钟源（通常是内部超低功耗 32 kHz 振荡器 OSCULP32K），**即使主时钟坏了它照样能复位芯片**。

它有两种工作模式，区别是"喂狗的时机"：

| 模式 | `CTRL.WEN` | 什么时候喂狗有效 | 能抓到什么错误 |
|---|---|---|---|
| 普通模式 | 0 | 超时前的任何时刻 | 程序卡死、跑飞 |
| 窗口模式 | 1 | 只在"关闭窗口"结束之后、超时之前 | 上面那些，外加"程序喂狗喂得太频繁"（例如某个死循环里在狂喂狗） |

窗口模式的时序是这样：清零之后先进入**关闭窗口**（这段时间喂狗会被判为太早，直接复位），随后是**开放窗口**（这段时间喂狗才算数），两个窗口的总和才是完整的超时时间。

```text
喂狗 ──▶[ 关闭窗口 CONFIG.WINDOW ]──▶[ 开放窗口 CONFIG.PER ]──▶ 还没喂？复位
         这段里喂 = 太早，复位          这段里喂 = 正常，重新计时
```

时间长度由 `CONFIG.PER` 和 `CONFIG.WINDOW` 的 4 位字段给出，都是 8 到 16384 个 `GCLK_WDT` 周期（12 档）。按典型的 1024 Hz 算，就是 8 ms 到 16 s；因为源振荡器精度不高，手册提醒实际超时时间会随器件略有偏差。

```c
/* 时钟：GCLK_WDT（通道 0x03）通常来自内部超低功耗 32 kHz 振荡器，配法见第 5 章 */
static void wdt_window_init(void)
{
    /* CONFIG 和 EWCTRL 是使能保护的：只能在 WDT 关闭时配 */
    WDT_REGS->WDT_CONFIG = WDT_CONFIG_WINDOW_1K   /* 关闭窗口 1 s：这段里喂狗会被判太早 */
                         | WDT_CONFIG_PER_8K;     /* 开放窗口 8 s：总共 9 s 内必须喂一次 */
    WDT_REGS->WDT_CTRL   = WDT_CTRL_WEN_Msk       /* 窗口模式 */
                         | WDT_CTRL_ENABLE_Msk;
    while (WDT_REGS->WDT_STATUS & WDT_STATUS_SYNCBUSY_Msk) { }
}

static inline void wdt_feed(void)
{
    /* 只有 0xA5 是"喂狗"；写任何别的值都会立刻触发系统复位 */
    WDT_REGS->WDT_CLEAR = WDT_CLEAR_CLEAR_KEY;
}
```

几个必须知道的细节：`CTRL` / `CONFIG` / `EWCTRL` 都是**写同步**的，写完要等 `STATUS.SYNCBUSY`；`CTRL.ALWAYSON`（始终开启）一旦写 1，**只有上电复位才能清掉**，而且此时 `CTRL` / `CONFIG` / `EWCTRL` 变成只读，改超时时间必须先断电；上电后这些寄存器的初值不是 0，而是从 NVM User Row（芯片的配置存储区）载入的，所以**烧过程序的芯片可能一上电看门狗就是开的**，调试时如果程序莫名其妙重启，先怀疑这一点（第 13 章）。另外，CPU 停在调试器里时 WDT 会暂停，不用担心单步调试时被复位。

## 9.13 初始化清单

以后遇到定时器需求，按"先算数字、再按顺序配"两步走：**先算出分频 N、TOP、CCx，再去写寄存器**；顺序永远是"时钟 → 关着配 → 写数值 → 开中断 → 使能"。

| 外设 | 顺序 |
|---|---|
| TC | ① `PM_APBCMASK` 打开总线时钟 ② `GCLK_CLKCTRL` 分配通用时钟 ③ `CTRLA` 写模式 / `WAVEGEN` / 分频 ④ 写 `CC0`（`MFRQ` / `MPWM` 下它就是 TOP）和 `CC1` ⑤ `INTENSET` + `NVIC_EnableIRQ` ⑥ `CTRLA.ENABLE = 1` |
| TCC | ① `PM_APBCMASK` ② `GCLK_CLKCTRL` ③ `CTRLA` 分频（可加 `ALOCK`）④ `WAVE`（`WAVEGEN` / `POLx` / `SWAPx`）⑤ `WEXCTRL`（`OTMX` / `DTIENx` / `DTLS` / `DTHS`）⑥ `PER` 和 `CCx` ⑦ 引脚 `PINCFG.PMUXEN` + `PMUX` + `DIRSET` ⑧ `CTRLA.ENABLE = 1` |
| RTC | ① `GCLK_CLKCTRL`（通道 `0x04`，别用 GCLK0）② `CTRL` 写模式 / 日历格式 / 分频 ③ 写 `COUNT` / `CLOCK` / `COMPn` / `ALARM0` ④ `INTENSET` + NVIC ⑤ `CTRL.ENABLE = 1` |
| WDT | ① `GCLK_CLKCTRL`（通道 `0x03`）② `CONFIG`（`PER` / `WINDOW`）和 `EWCTRL` ③ `CTRL`（`WEN` + `ENABLE`）④ 主循环里定期 `CLEAR = 0xA5` |

三句通用提醒：**每个外设都有自己的通用时钟通道，`GCLK_CLKCTRL` 不配就是不走**；**使能保护 = 只能关着配**；**写同步 = 写完等 `SYNCBUSY`**。

## 9.14 一个定时器的完整思维模型

```text
定时器 = 分频器 + 计数器 + 比较器        f = f_GCLK ÷ (N × (TOP + 1))
   ├── TC   16/8/32 位：CC0 / CC1 比较 → 中断或普通 PWM（TOP 由 WAVEGEN 决定）
   ├── TCC  24 位控制型：4 通道 + 输出矩阵 + 死区 + 故障保护 + 双缓冲
   ├── RTC  日历/计数：GCLK_RTC ÷ 2^n = 1 Hz → 秒、分、时、日、月、年 + 闹钟
   └── WDT  看门狗：独立时钟倒数，超时或"喂太早"都复位
```

代码真正做的事也只有两件：**初始化时把 N、TOP、CCx 写成寄存器；运行中改 CCx（或 CCBx）改占空比、清 INTFLAG、喂狗。**

## 9.15 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 频率差整数倍、慢很多 | 通用时钟没配或配到了别的源 | `GCLK_CLKCTRL` 的 ID 和 GEN 是否对，分频与 TOP 是否算对 |
| 寄存器写了没反应 | 使能保护：`ENABLE` 已经是 1 | 先把 `ENABLE` 清 0，再写 `CTRLA` / `WEXCTRL` / `DRVCTRL` |
| 连续写第二笔丢失、读到怪值 | 没等同步 | 每笔写后 `while (... SYNCBUSY)`；TCC 是独立 `SYNCBUSY` 寄存器 |
| PWM 引脚上什么都没有 | 引脚没配复用（或没开输出） | `PINCFG.PMUXEN`、`PMUX` 的功能字母、`DIRSET`；偶数脚用 `PMUXE`、奇数脚用 `PMUXO` |
| 计数范围不对、中断周期差 256 倍 | 计数模式选错 | `CTRLA.MODE`，以及 `CCx` 的偏移随模式变化 |
| 中断进不去 | 只开了一处 | `INTENSET` 和 `NVIC_EnableIRQ` 都要 |
| 进了中断出不来 | `INTFLAG` 没清（或写成 0） | `INTFLAG = 对应位的 _Msk`，写 1 清除 |
| 占空比改了但亮度相反 | 极性没处理 | 低电平点亮的 LED 要 `WAVE.POLx` 反相 |
| RTC 秒走得不对 | GCLK_RTC 不是 1 Hz 能整除的值 | 模式 2 必须让 `CLK_RTC_CNT` = 1 Hz（1024 Hz + `DIV1024`） |
| RTC 完全不走 | 时钟挂在了 GCLK0 上 | 手册明确 RTC 不能用 GCLK0，换一路 |
| 程序莫名重启 | WDT 从上电就是开的，或窗口模式喂太早 | `WDT_CTRL` / `WDT_CONFIG` 初值（来自 NVM User Row）、喂狗时机 |
| 32 位 TC 读数不对 | 用了从机（TC5）的寄存器 | 32 位模式只操作 TC4 的寄存器 |

## 9.16 寄存器速查

TC（偏移随计数模式变化，`x` = 0 或 1）：

| 寄存器 | COUNT8 | COUNT16 | COUNT32 | 关键位 |
|---|---|---|---|---|
| `CTRLA` | 0x00 | 0x00 | 0x00 | `SWRST[0]`、`ENABLE[1]`、`MODE[3:2]`、`WAVEGEN[6:5]`、`PRESCALER[10:8]` |
| `CTRLBCLR` / `CTRLBSET` | 0x04 / 0x05 | 同左 | 同左 | `DIR[0]`、`ONESHOT[2]`、`CMD[7:6]` |
| `CTRLC` | 0x06 | 0x06 | 0x06 | `INVEN0/1[1:0]`、`CPTEN0/1[5:4]` |
| `INTENCLR` / `INTENSET` / `INTFLAG` | 0x0C / 0x0D / 0x0E | 同左 | 同左 | `OVF[0]`、`ERR[1]`、`SYNCRDY[3]`、`MC0[4]`、`MC1[5]` |
| `STATUS` | 0x0F | 0x0F | 0x0F | `SYNCBUSY[7]`、`STOP[3]`、32 位模式另有 `SLAVE[4]` |
| `COUNT` | 0x10（8 位） | 0x10（16 位） | 0x10（32 位） | 当前计数值 |
| `PER` | 0x14 | 不存在 | 不存在 | 8 位模式的 TOP |
| `CC0` / `CC1` | 0x18 / 0x19 | 0x18 / 0x1A | 0x18 / 0x1C | 比较 / 捕获值；`MFRQ` 下 `CC0` 是 TOP |

TCC：

| 寄存器 | 偏移 | 关键位 |
|---|---|---|
| `CTRLA` | 0x00 | `SWRST[0]`、`ENABLE[1]`、`RESOLUTION[6:5]`、`PRESCALER[10:8]`、`ALOCK[14]`、`CPTEN0-3[27:24]` |
| `CTRLBCLR` / `CTRLBSET` | 0x04 / 0x05 | `DIR[0]`、`LUPD[1]`、`ONESHOT[2]`、`CMD[7:5]`（`UPDATE` = 0x3） |
| `SYNCBUSY` | 0x08 | `ENABLE[1]`、`CTRLB[2]`、`WAVE[6]`、`PER[7]`、`CC0-3[11:8]`、`CCB0-3[22:19]` |
| `FCTRLA` / `FCTRLB` | 0x0C / 0x10 | `SRC[1:0]`、`HALT[9:8]`、`RESTART[7]`、`BLANK[6:5]`、`QUAL[4]` |
| `WEXCTRL` | 0x14 | `OTMX[1:0]`、`DTIEN0-3[11:8]`、`DTLS[23:16]`、`DTHS[31:24]` |
| `DRVCTRL` | 0x18 | `NRE0-7[7:0]`、`NRV0-7[15:8]`、`INVEN0-7[23:16]` |
| `INTENCLR/SET/FLAG` | 0x24 / 0x28 / 0x2C | `OVF[0]`、`TRG[1]`、`ERR[3]`、`FAULTx[12-15]`、`MC0-3[19:16]` |
| `STATUS` | 0x30 | `STOP[0]`、`PATTBV[5]`、`WAVEBV[6]`、`PERBV[7]`、`CCBV0-3[19:16]` |
| `WAVE` | 0x3C | `WAVEGEN[2:0]`、`POL0-3[19:16]`、`SWAP0-3[27:24]` |
| `PER` / `PERB` | 0x40 / 0x6C | 周期（TOP）及其缓冲 |
| `CC0-3` / `CCB0-3` | 0x44+4n / 0x70+4n | 占空比及其缓冲 |

RTC（`CTRL` 在三套模式里都占 0x00，后面按模式换名）：

| 寄存器 | 偏移 | 说明 |
|---|---|---|
| `CTRL` | 0x00 | `SWRST[0]`、`ENABLE[1]`、`MODE[3:2]`、`CLKREP[6]`、`MATCHCLR[7]`、`PRESCALER[11:8]` |
| `READREQ` | 0x02 | `RREQ[15]`、`RCONT[14]`、`ADDR[5:0]`（只能指向 0x10 的 `COUNT` / `CLOCK`） |
| `INTENCLR/SET/FLAG` | 0x06 / 0x07 / 0x08 | 模式 0/1：`CMP0`（模式 1 还有 `CMP1`）；模式 2：`ALARM0`；另有 `SYNCRDY[6]`、`OVF[7]` |
| `STATUS` | 0x0A | `SYNCBUSY[7]` |
| `FREQCORR` | 0x0C | `SIGN[7]`、`VALUE[6:0]`，约 1 ppm 一步，校准晶振 |
| `COUNT` / `CLOCK` | 0x10 | 模式 0/1 是计数器，模式 2 是日历 |
| `PER` | 0x14 | 模式 1 的周期 |
| `COMP0/1`、`ALARM0` | 0x18 / 0x1A | 模式 0/1 用 `COMPn`，模式 2 用 `ALARM0` |
| `MASK0` | 0x1C | `SEL[2:0]` 决定闹钟比较哪些字段 |

WDT：

| 寄存器 | 偏移 | 说明 |
|---|---|---|
| `CTRL` | 0x00 | `ENABLE[1]`、`WEN[2]`、`ALWAYSON[7]` |
| `CONFIG` | 0x01 | `PER[3:0]`、`WINDOW[7:4]`，都是 8 到 16384 个时钟周期 |
| `EWCTRL` | 0x02 | `EWOFFSET[3:0]`，早期警告中断的提前量 |
| `INTENCLR/SET/FLAG` | 0x04 / 0x05 / 0x06 | `EW[0]` |
| `STATUS` | 0x07 | `SYNCBUSY[7]` |
| `CLEAR` | 0x08 | 写 `0xA5` 喂狗，写别的值立刻复位 |

## 9.17 最重要的几句话

1. 定时器就是"分频器 + 计数器 + 比较器"，所有频率都套 `f = f_GCLK ÷ (N × (TOP + 1))`，先算数字再写寄存器。
2. TOP 从哪来由 `WAVEGEN` 决定：16/32 位下 `NPWM` / `NFRQ` 固定是最大值，`MPWM` / `MFRQ` 是 `CC0`；想要精确周期就用后两种。
3. `CTRLA`、`WEXCTRL`、`DRVCTRL` 是使能保护的，所以顺序永远是"先配后使能"，最后才写 `ENABLE = 1`。
4. `COUNT` / `PER` / `CCx` 跨时钟域，写完必须等 `SYNCBUSY`；TC / RTC 在 `STATUS` 里，TCC 是独立寄存器，GCLK 在 `GCLK_STATUS`。
5. 定时器的波形不会自己上引脚：必须 `PINCFG.PMUXEN` + `PMUX` 选对功能字母（E / F），再 `DIRSET`。
6. 互补 PWM 的三件套是输出矩阵、死区（`DTIENx` + `DTLS` / `DTHS`）和故障保护；死区配在使能之前，否则等于让上下管裸奔。
7. RTC 模式 2 要 1 Hz，就得让 `GCLK_RTC` 能除到 1 Hz（典型 1024 Hz + `DIV1024`），而且不能用 GCLK0。
8. WDT 的 `CLEAR` 只认 `0xA5`；窗口模式下"喂太早"和"没喂"一样会复位，这正是它能抓到"死循环里狂喂狗"的原因。

## 9.18 自测

1. GCLK_TC3 = 8 MHz，用 TC3 的 16 位模式做 **200 ms** 周期中断：分频选多少、`WAVEGEN` 选哪个、`CC0` 写多少？（要求正好整除）
2. 为什么 `CTRLA` 里的模式和分频必须在 `ENABLE = 0` 时写？把 `ENABLE = 1` 放到最后还有什么好处？
3. 要让 PA22 输出 TCC0/WO[4]，引脚侧和定时器侧各要写哪些寄存器？（提示：先查 Table 7-1 确认功能字母，再想奇偶半字节）
4. TC 在 16 位模式下，`NPWM` 和 `MPWM` 的 TOP 分别是谁？各自适合什么场合？
5. 中断服务程序里写 `TC3_REGS->COUNT16.TC_INTFLAG = 0;` 会发生什么？正确写法是什么？
6. 用 TCC0 输出互补 PWM 驱动半桥，请按顺序列出要写的寄存器，并说明死区为什么不能等 `ENABLE = 1` 之后再配。

## 附录：以后查手册怎么找定时器

手册章节号先记住：**第 30 章 TC**、**第 31 章 TCC**、**第 19 章 RTC**、**第 18 章 WDT**；通用时钟与总线时钟在**第 15 章 GCLK**和**第 16 章 PM**；跨时钟域的同步原理在**第 14.3 节**；引脚复用表是**第 7 章 Table 7-1**；中断控制器（NVIC）在**第 11.2 节**。

查一个寄存器只要四步：**① 找到外设那一章的 Register Summary ② 看 Offset 确认地址 ③ 翻到该寄存器的 Register Description ④ 看位的 Property（Enable-Protected / Write-Synchronized 决定它属于"必须关着写"还是"写完要等"）。**

本章涉及的偏移汇总：TC 的 `CTRLA 0x00 / CTRLBSET 0x05 / INTFLAG 0x0E / STATUS 0x0F / COUNT 0x10 / PER 0x14 / CC0 0x18`；TCC 的 `CTRLA 0x00 / SYNCBUSY 0x08 / WEXCTRL 0x14 / DRVCTRL 0x18 / INTFLAG 0x2C / STATUS 0x30 / WAVE 0x3C / PER 0x40 / CC0 0x44 / CCB0 0x70`；RTC 的 `CTRL 0x00 / STATUS 0x0A / COUNT 或 CLOCK 0x10 / COMP 或 ALARM 0x18 / MASK 0x1C`；WDT 的 `CTRL 0x00 / CONFIG 0x01 / STATUS 0x07 / CLEAR 0x08`。

## 本章结束

学完这一章，看到任何一行定时器代码都应该能翻译成硬件动作。例如

```c
TC3_REGS->COUNT16.TC_CTRLA = TC_CTRLA_MODE_COUNT16 | TC_CTRLA_WAVEGEN_MFRQ | TC_CTRLA_PRESCALER_DIV1024;
TC3_REGS->COUNT16.TC_CC[0] = 46874;
```

不是"记住这两句"，而是：**这颗 16 位计数器每 1024 个 48 MHz 时钟加一，数到 46874 就是 1 秒，到点把 MC0 标志置起来**。能这样翻译，定时器才算真正学会。
