# 第 2 章 外设与寄存器基础

> **芯片**：ATSAMD21G18A（Cortex-M0+ @48 MHz，256 KB Flash，32 KB SRAM，**48 脚 / 38 个 I/O**）
> **工具链**：MPLAB X 6.30 + XC32 5.10 + SAMD21_DFP 3.7.262
> **本章目标**：把"外设"从神秘的名词变成具体的东西——一段地址窗口；知道一个外设要同时满足哪些条件才能工作，条件不满足时怎么写都没反应；知道写寄存器时的三种姿势、三种保护（总线时钟 / PAC / Enable-Protected）和一种等待（SYNCBUSY）。
> **学习顺序**：外设是什么 → 为什么做成寄存器窗口 → 外设工作的四个条件（PORT 与 SERCOM 对比）→ 三条桥的时钟默认值 → 寄存器怎么访问 → SET/CLR/TGL 为什么存在 → 访问宽度规则 → PAC 写保护 → 同步与 SYNCBUSY → Enable-Protected → 完整例程 → 初始化清单 → 思维模型 → 常见问题 → 速查 → 自测。
> **本章不写**：具体外设怎么用（SERCOM 怎么发 I2C、TC 怎么定时，留给后面各章）；程序从哪开始跑（第 3 章）。
> **术语约定**：本章说的"寄存器"都是**外设寄存器**——挂在 APB 总线上的那一片地址，不是 CPU 里的 R0–R15。

## 2.0 外设是什么

你在 STM32 上已经习惯了"外设 = 一组寄存器"，到了 SAMD21 这句话仍然成立，只是**名字和地址不一样**。先把最底层的模型说清楚，后面所有内容都挂在这个模型上。

**外设（peripheral）是一段地址窗口**：芯片内部有一块硬件电路（串口、定时器、ADC……），它把自己的控制开关和状态**映射成一排可以读写的地址**。CPU 往那个地址写一个数，硬件上对应的开关就动一下；CPU 读那个地址，硬件就把当前状态回给你。

```text
CPU  ──写地址 0x42000800──▶  SERCOM0 内部的开关阵列  ──▶  引脚上的电平
CPU  ◀──读同一个地址──────  SERCOM0 内部的状态快照
```

所以"配置外设"这件事，翻译成人话就是：**往一组合适的地址里写合适的数**。整本书剩下的内容，都是在回答"哪个地址、写什么数、什么时候写"。

### 地址 = 基址 + 偏移

每个外设在手册里有一个**基址（base address）**，外设内部的每个寄存器有一个**偏移（offset）**，真正的物理地址是两者相加。手册每个外设章节最后的 Register Summary 表就是一张偏移表。

| 外设 | 基址 | 手册里的偏移 | 物理地址 | 这是什么 |
|---|---|---|---|---|
| SERCOM0 | `0x4200_0800` | `0x00` | `0x4200_0800` | `CTRLA`（模式与使能） |
| SERCOM0 | `0x4200_0800` | `0x1C` | `0x4200_081C` | `SYNCBUSY`（同步状态） |
| PORT Group 0（PA） | `0x4100_4400` | `0x08` | `0x4100_4408` | `DIRSET`（方向置位） |
| PORT Group 1（PB） | `0x4100_4480` | `0x08` | `0x4100_4488` | 同上，但作用于 PB |
| PM | `0x4000_0400` | `0x20` | `0x4000_0420` | `APBCMASK`（桥 C 的时钟开关） |
| PAC2 | `0x4200_0000` | `0x00` | `0x4200_0000` | `WPCLR`（解除写保护） |

**PORT 的 Group 是"第二层基址"**：PORT 本身只有一个基址，但 PA 和 PB 各有一整套同名的寄存器，第 x 组的起点是 `PORT 基址 + 0x80 × x`（手册 23.6.2.2）。所以"PA05 和 PB05 的第 5 位"在代码里是同一个位号、不同的组——这是第 4 章那个坑的根源，这里先记住结构。

### 为什么设计成"寄存器窗口"

因为这样 CPU 不需要为每个外设学一套新指令。地址空间是通用的，读写内存的那几条指令（`LDR`/`STR`）就够控制整颗芯片。代价是：**地址写错了不会有编译错误**。往一个不存在的地址写，编译器一声不响，芯片也一声不响——这就是"写了没反应"能折磨人一整天的原因。

### 从代码到手册的对应关系

| 手册上的东西 | C 代码里的东西 |
|---|---|
| 外设名 + 基址 | `SERCOM0_REGS`、`PM_REGS`、`PORT_REGS`（DFP 3.7.262 的名字） |
| Register Summary 的 Offset | 结构体成员名（偏移由编译器算） |
| Bit 5 – ENABLE | 掩码宏 `SERCOM_USART_INT_CTRLA_ENABLE_Msk` |
| Property 栏的 Write-Protected | 要先写 `PACx_REGS->WPCLR` |
| Property 栏的 Write-Synchronized | 写完要等 `SYNCBUSY` |
| Property 栏的 Enable-Protected | 只能在 `ENABLE=0` 时写 |

> **本书统一使用 SAMD21_DFP 3.7.262 的写法（下称“新写法”）。** 它给每个外设一个指针宏 `PM_REGS` / `PORT_REGS` / `SERCOM0_REGS`（在 `samd21g18a.h` 里定义成 `((pm_registers_t*)0x40000400)` 这种形式），结构体成员是**扁平命名**的——成员名带着外设前缀，例如 `PM_REGS->PM_APBCMASK`、`PORT_REGS->GROUP[0].PORT_DIRSET`、`PAC2_REGS->PAC_WPCLR`。
>
> 外设内部还有模式分支的（SERCOM 分 I2C/SPI/USART，TC 分 8/16/32 位）多一层联合体，要先点模式再点寄存器：`SERCOM0_REGS->I2CM.SERCOM_CTRLA`、`TC3_REGS->COUNT16.TC_STATUS`。字段宏也只有两种形式：`_Msk`（掩码）和 `_Val`（枚举值），多位字段要自己移位。
>
> 网上很多 SAMD21 教程用的是带 `.reg` / `.bit` 联合体的旧式（ASF 时代）头文件，同一个位会写成 `PM->APBCMASK.reg`、`PORT->Group[0].DIRSET.reg`——**含义完全一样，只是名字不同**。看到对不上的写法，先打开工程里的头文件确认，不要怀疑自己理解错了硬件。

## 2.1 一个外设要工作，需要四个条件

这是本章最重要的一节。把四个条件摆出来，后面所有"配置流程"都只是它的展开。

```text
① 总线时钟（APB）   →  CPU 能不能和这个外设说话
② 通用时钟（GCLK）  →  这个外设自己走不走时
③ 引脚              →  信号有没有路通到芯片外面
④ 功能配置          →  它到底干什么活、按什么参数干
```

逐条解释，每一条都回答"没有它会怎样"：

- **① 总线时钟（APB clock）**：外设挂在 APB 总线上，总线时钟由 PM（Power Manager）里的 `APBxMASK` 位控制。**这条没有，外设的寄存器读写全部失效**——手册 14.4 的原话是"寄存器会读回全 0，写操作被丢弃"。注意是**丢弃**，不是报错。
- **② 通用时钟（GCLK）**：外设内部真正干活的电路（移位器、计数器）由 GCLK 驱动。这条没有，寄存器可能读得出、写也进得去，**但外设不动作**。GCLK 在第 5 章展开，本章只需要知道"它必须单独配"。
- **③ 引脚**：外设的信号要出芯片，得让某个引脚交给它（`PMUX`/`PMUXEN`），并且配好 `PINCFG`。这条在第 4 章和第 7/8 章。
- **④ 功能配置**：模式、波特率、位宽、触发源……这些是"具体外设"的事，属于后面各章。

### PORT 和 SERCOM 对比：哪几条是必需的

| 外设 | ① APB 总线时钟 | ② 通用时钟 GCLK | ③ 引脚 | ④ 功能配置 |
|---|---|---|---|---|
| PORT（GPIO） | **需要**，复位后已开（桥 B） | **不需要**——GPIO 没有"走时"这回事 | 本身就是引脚控制器 | `DIR`/`OUT`/`PINCFG` |
| SERCOM（串口/I2C/SPI） | **需要**，复位后是关的（桥 C），要先开 | **需要**，还得是两路：`CORE` + `SLOW` | 需要（复用） | `CTRLB`/`BAUD`/… |
| PM / GCLK / SYSCTRL | 需要，复位后已开（桥 A） | 不需要（它们就是时钟源本身） | 不需要 | 各自的控制位 |
| ADC | 需要，复位后**已开**（桥 C 上的例外） | 需要（`GCLK_ADC`） | 需要（模拟输入脚） | 参考源、分辨率… |

这张表能直接回答第 4 章留下的疑问：**点灯为什么一行时钟代码都不用写？** 因为 PORT 在桥 B（APB 时钟复位后已开），而且 GPIO 根本不需要通用时钟——四个条件里它只占了①和③。

反过来，SERCOM 在桥 C（APB 时钟复位后是关的），而且它必须有一个 GCLK 才能收发——**①和②两条都要**，这就是"第一句就得写时钟"的原因。

### 为什么 SERCOM 要两路 GCLK

| GCLK 名字 | GCLK ID | 给谁用 | 不开会怎样 |
|---|---|---|---|
| `GCLK_SERCOM0_CORE` | `0x14`（DFP 宏 `SERCOM0_GCLK_ID_CORE`，值 20） | 串行移位、波特率发生器 | 收发完全不工作 |
| `GCLK_SERCOMx_SLOW` | `0x13` | SMBus 超时等慢速逻辑 | 普通 UART/I2C/SPI 可以先不管，做 SMBus 才需要 |

GCLK ID 的完整表在手册 15.8 的 `CLKCTRL.ID`；DFP 里对应 `SERCOM0_GCLK_ID_CORE` / `SERCOM0_GCLK_ID_SLOW` 这类宏。本章例程两路都配，省得以后回头补。

## 2.2 三条桥，三种默认值

外设按所在的总线分成三组，地址也按这个分组整齐排列（手册 Table 10-1）：

| 桥 | 基址 | 大小 | 上面挂了什么 | APB 时钟复位默认 |
|---|---|---|---|---|
| A | `0x4000_0000` | 64 KB | PAC0 · PM · SYSCTRL · GCLK · WDT · RTC · EIC | **开** |
| B | `0x4100_0000` | 64 KB | PAC1 · DSU · NVMCTRL · PORT · DMAC · USB · MTB | **开** |
| C | `0x4200_0000` | 64 KB | PAC2 · EVSYS · SERCOM0–5 · TCC0–2 · TC3–7 · ADC · AC · DAC · PTC · I2S | **关**（ADC 例外） |

对应地，PM 里有三个开关寄存器，一位管一个外设（手册 Table 16-1 是权威清单）：

| 寄存器 | 偏移 | 复位值 | 二进制含义 |
|---|---|---|---|
| `APBAMASK` | `0x18` | `0x0000007F` | bit0–bit6 全 1：PAC0 / PM / SYSCTRL / GCLK / WDT / RTC / EIC 全开 |
| `APBBMASK` | `0x1C` | `0x0000007F` | bit0–bit6 全 1：PAC1 / DSU / NVMCTRL / PORT / DMAC / USB 全开 |
| `APBCMASK` | `0x20` | `0x00010000` | **只有 bit16（ADC）是 1，其余全 0** |

`0x00010000` 就是"只有 bit16 为 1"。bit16 是 ADC——**桥 C 上唯一一个复位后就开着总线时钟的外设**。这是手册里的一个例外，不是笔误：ADC 的 APB 时钟默认开启（Table 16-1 里 `CLK_ADC_APB` = Enabled），所以用 ADC 时不需要开 `APBCMASK.ADC`。

于是得到本章第一个可操作的结论：

| 你要用的外设 | 第一件事 |
|---|---|
| PORT、PM、GCLK、NVMCTRL、DMAC、USB… | 什么都不用做，桥 A/B 的 APB 时钟已经开了 |
| SERCOM、TC、TCC、AC、DAC、PTC、I2S、EVSYS | **先写 `PM_REGS->PM_APBCMASK` 打开对应位** |
| ADC | `APBCMASK` 不用动（默认已开），但 GCLK 还是要配 |

**桥 C 忘记开时钟的现象**：寄存器写进去读回来还是 0，外设毫无反应，没有异常、没有报错。这是新手在 SAMD21 上最常见的"卡一天"问题。

## 2.3 寄存器的三种访问姿势

同一个位，DFP 里给了三种写法。以"把 SERCOM0 的 `CTRLA.ENABLE` 置 1"为例：

| 姿势 | 写法 | 特点 | 什么时候用 |
|---|---|---|---|
| 整个寄存器 | 一次性赋值，把模式位和使能位一起写进 `CTRLA`（见 2.10 例程） | 一次写清全部字段，**不需要读** | 初始化：寄存器里没有需要保留的旧值 |
| 用掩码宏只改某几位 | `SERCOM0_REGS->USART_INT.SERCOM_CTRLA` 加上 `SERCOM_USART_INT_CTRLA_ENABLE_Msk`（读-改-写） | 保留其它位，但要多读一次 | 运行中改一位，且没有对应的 SET/CLR 寄存器 |
| 位域访问 | 旧式头文件的 `...->CTRLA.bit.ENABLE = 1;` | 本质仍是读-改-写 | 可读性优先、不在意原子性时 |

**初始化时优先用第一种**。理由是它表达的意图最完整："这个寄存器就应该长这样"，而且顺手避免了"读到一个还没配好的中间状态再写回去"的问题。

`|=` 这种写法在单线程顺序代码里没问题，但它有三步（读 → 或 → 写回）。如果两次修改之间可能插入中断，或者硬件在两次访问之间自己改了别的位，就会丢改动——这就是下一节要解决的问题。

## 2.4 SET / CLR / TGL 三件套为什么存在

SAMD21 上大量寄存器有"兄弟"：一个管置位、一个管清零、一个管翻转。

```text
DIR    0x00  读 / 写当前值          ← 读-改-写
DIRCLR 0x04  写 1 → 对应位清零
DIRSET 0x08  写 1 → 对应位置位
DIRTGL 0x0C  写 1 → 对应位翻转
```

以 PORT 为例（手册 23.7）：

| 偏移 | 名称 | 语义 |
|---|---|---|
| `0x04` | `DIRCLR` | 掩码里为 1 的位清零，为 0 的位**不动** |
| `0x08` | `DIRSET` | 掩码里为 1 的位 置 1，为 0 的位不动 |
| `0x0C` | `DIRTGL` | 掩码里为 1 的位取反，为 0 的位不动 |

### 它解决的问题：读-改-写

假设你想把 PA05 设为输出，写成 `PORT_REGS->GROUP[0].PORT_DIR |= (1UL << 5);`。CPU 实际做三件事：

```text
① 读 DIR          → 得到 0x00000000
② 计算 0 ∪ (1<<5) → 0x00000020
③ 写回 DIR        → 0x00000020
```

三步之间如果有中断进来改了 PA07，或者别的代码改了 DIR 的其它位，第 ③ 步会把①读到的**过时值**一起写回去，PA07 的改动就被抹掉了。

用 `DIRSET` 则是**一条写指令**：

```text
写 DIRSET = 0x00000020   →  硬件只把 bit5 置 1，其余位物理上不受影响
```

三个好处，缺一不可：

- **不会误改别的位**：哪怕你读到的值是过时的，写入也不影响其它引脚。
- **天然原子**：单次写操作，不可能被中断"撕裂"成半个。
- **不需要读**：少一次总线访问；有些寄存器（`WPCLR`/`WPSET`）也不给你读回旧值的意义。

手册 11.6.1 描述 PAC 时专门强调过这个设计："写保护寄存器允许用户在不做读-改-写的情况下解除某个外设的写保护"——理由完全一样。

### 这套兄弟寄存器在芯片上到处都是

| 外设 | SET / CLR 类寄存器 | 用处 |
|---|---|---|
| PORT | `DIRSET/DIRCLR/DIRTGL`、`OUTSET/OUTCLR/OUTTGL` | 方向与电平（第 4 章） |
| 所有外设 | `INTENSET/INTENCLR`、`INTFLAG`（写 1 清标志） | 中断开关与标志（第 12 章） |
| TC / TCC | `CTRLBSET/CTRLBCLR` | 命令、方向、单次模式（第 9 章） |
| PAC | `WPSET/WPCLR` | 写保护本身（本章 2.6） |
| GCLK | 没有——只能整寄存器写 `GENCTRL` | 时钟配置（第 5 章） |

**记忆方式**：看到名字里带 SET / CLR / TGL，就默认"写 1 生效，写 0 无效果"。不要写 `DIRSET = 0` 想着去清零——那是把 0 写进一个只有 1 才有意义的寄存器，什么都不会发生。

## 2.5 寄存器是 8 位、16 位还是 32 位

外设寄存器宽度**不统一**：手册每张寄存器描述的表头会写 Access 和位编号，Register Summary 里也能看出宽度（`Offset` 列写成 `0x08 0x0B` 这种连续区间，表示那是一个字节宽的寄存器）。

| 宽度 | 例子 | 结构体里的类型 | 访问规则 |
|---|---|---|---|
| 8 位 | `GCLK_STATUS`(0x01)、`PM_CTRL`(0x00) | `uint8_t` | 只能 8 位访问 |
| 16 位 | `GCLK_CLKCTRL`(0x02) | `uint16_t` | 16 位访问，或它的两个 8 位半区 |
| 32 位 | `PM_APBCMASK`(0x20)、`PORT_DIR` | `uint32_t` | 32 位访问，8 位四分之一 / 16 位一半也可以直接访问 |

手册 16.8 的原话：**原子 8/16/32 位访问都支持；32 位寄存器的 8 位四分之一、16 位一半，以及 16 位寄存器的 8 位一半，都可以直接访问。**

两条实用规则：

- **`APBASEL` / `APBBSEL` / `APBCSEL` 是例外：只能 8 位访问**（手册 16.8 明确写了 Exception）。这三个寄存器决定三条 APB 桥的时钟分频，在 PM 偏移 `0x09` / `0x0A` / `0x0B`。
- **不要用不对齐的宽度去碰寄存器**。用 C 结构体成员（`PM_REGS->PM_APBCMASK`）访问时，编译器会按成员类型生成正确宽度的指令，所以平时感觉不到这条规则；但如果你自己写 `*(volatile uint32_t *)0x40000401` 这种地址，就是在自找麻烦。

还有一个容易踩的点：**结构体成员类型就是访问宽度**。想把一个 8 位寄存器当 32 位一次写掉，C 语言层面就会被拆成 8 位访问，硬件看到的是若干次 8 位写，而不是一次 32 位写。

## 2.6 PAC 写保护：为什么需要它

**问题**：如果所有寄存器随时都能写，一段跑飞的代码（野指针、栈溢出、算错的数组下标）可以悄悄改掉时钟配置或引脚复用，芯片行为变得无法解释，而且查不出是谁改的。

**方案**：Peripheral Access Controller（PAC），**每条 APB 桥配一个**，管桥上各外设寄存器的写权限（手册 11.6.1）。

| 实例 | 基址 | 管哪条桥 | 复位时已经保护了谁 |
|---|---|---|---|
| PAC0 | `0x4000_0000` | 桥 A | 复位值 `0x000000`，都不保护 |
| PAC1 | `0x4100_0000` | 桥 B | 复位值 `0x000002` → **DSU 默认受保护**（bit1） |
| PAC2 | `0x4200_0000` | 桥 C | 复位值 `0x00800000` → **TCC3 默认受保护**（bit23） |

DFP 头文件里给出的默认值与此一致：`PAC0_WPROT_DEFAULT_VAL = 0x00000000`，`PAC2_WPROT_DEFAULT_VAL = 0x00800000`。

> 手册 11.6.2 这里有一处**自相矛盾**：PAC0 的 WPCLR/WPSET 表格里 Reset 行全是 0，但描述文字写的是 `Reset: 0x000002`；PAC1 的表格和文字都是 `0x000002`。以 DFP 头文件的 `PACx_WPROT_DEFAULT_VAL` 为准最稳（PAC0 = 0，PAC2 = `0x00800000`）。

### 寄存器只有两个，偏移固定

| 偏移 | 名称 | 作用 |
|---|---|---|
| `0x00` | `WPCLR` | 写 1 → **解除**对应外设的写保护 |
| `0x04` | `WPSET` | 写 1 → **加上**对应外设的写保护 |

两个寄存器**读回的值相同**（都反映当前保护位图），但写的行为相反："写一个 1 到 WPCLR 的某位，会同时清掉 WPCLR 和 WPSET 里的这一位"（手册 11.6.1）。

位图就是"桥上有哪些外设"：

| 实例 | 位图（手册 11.6.2） |
|---|---|
| PAC0 | bit6 EIC · bit5 RTC · bit4 WDT · bit3 GCLK · bit2 SYSCTRL · bit1 PM |
| PAC1 | bit6 MTB · bit5 USB · bit4 DMAC · bit3 PORT · bit2 NVMCTRL · bit1 DSU |
| PAC2 | bit24 TCC3 · bit21 AC1 · bit20 I2S · bit19 PTC · bit18 DAC · bit17 AC · bit16 ADC · bit15–9 TC7–TCC1 · bit8 TCC0 · bit7–2 SERCOM5–SERCOM0 · bit1 EVSYS |

SERCOM0 在 PAC2 的 **bit2**——和 `PM_APBCMASK_SERCOM0_Pos = 2` 是同一个 2：两条总线上的外设编号是统一的。

### 违反的后果：写丢失 + CPU 异常

手册 11.6.1 写得很直接：

- **往受保护的外设写**：数据不会写入，并且该外设**返回一次访问错误（CPU 异常）**。在 Cortex-M0+ 上通常表现为进入 HardFault。
- **对同一个外设连续两次加保护，或者连续两次解除保护**：PAC 也会返回异常。

第二条是一个**安全特性**，用来保证程序流程可验证：正常用法永远是"解锁 → 改 → 上锁 → 解锁 → 改 → 上锁"，一解一锁交替出现。如果代码里出现连续两次 `WPSET` 同一位，说明逻辑已经不对了，PAC 直接把你打醒，而不是默默放过。

> **调试器是例外**：通过外部调试器访问时写保护不生效（手册 11.6.1、13.5.7）。在 IDE 里手动改变量看着"能写"，不代表代码里能写。

### 用法范式

```c
#include "sam.h"

/* ① 解锁：往 WPCLR 写 1 到 SERCOM0 那一位（bit2） */
PAC2_REGS->PAC_WPCLR = (1UL << 2);

/* ② 现在可以写 SERCOM0 的受保护寄存器了 */
SERCOM0_REGS->USART_INT.SERCOM_CTRLA = 0;

/* ③ 用完上锁（推荐）：再写 WPSET 的同一位 */
PAC2_REGS->PAC_WPSET = (1UL << 2);
```

**解锁不是一次性开关**：第 ① 步之后该外设一直是"可写"状态，直到你把它锁回去。所以别在循环里反复写 `WPCLR`——第二次就会触发异常。要判断当前状态，读回 `WPSET`（两个寄存器读回同一张位图）。

> DFP 头文件里 `PAC2_REGS->PAC_WPCLR` 是**裸 uint32_t 成员**，`component/pac.h` 只提供了通用掩码 `PAC_WPCLR_WP_Msk`，没有"每个外设一个位宏"。所以 PAC 位图建议自己定义宏，例如 `#define PAC2_WP_SERCOM0 (1UL << 2)`。以你工程里的 DFP 头文件为准。

## 2.7 同步寄存器与 SYNCBUSY

### 为什么会有"同步"这件事

外设内部有**两个时钟域**：一边是总线接口（跑在 APB 主时钟上，CPU 从这边读写寄存器），另一边是外设核心（跑在 GCLK 上）。两边频率和相位都不同。

```text
CPU / APB 域  ────写寄存器────▶  ┃ 同步器 ┃  ────▶  外设核心域（GCLK）
                                     ↑
                          写入要"过一道"才生效
```

手册 14.3 把机制分成两种实现，用哪个取决于外设：

| 同步方案 | 用在哪些外设 | 状态位在哪 |
|---|---|---|
| 公共同步器（common synchronizer） | GCLK · WDT · RTC · EIC · **TC** · ADC · AC · DAC | 外设自己的 `STATUS.SYNCBUSY` |
| 分布式同步器（distributed synchronizer） | SERCOM USART/SPI/I2C · I2S · **TCC** · USB | 独立的 `SYNCBUSY` 寄存器，**每位对应一个寄存器** |

于是不同外设的等待写法**不一样**，这是最容易写错的地方：

| 外设 | 等待写法 | 依据 |
|---|---|---|
| GCLK | `while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }` | `STATUS` 偏移 `0x01`，bit7 = `SYNCBUSY`（手册 15.8.2） |
| TC（8 位模式） | `while (TC3_REGS->COUNT8.TC_STATUS & TC_STATUS_SYNCBUSY_Msk) { }` | `STATUS` 偏移 `0x0F`，bit7（手册 30.7） |
| TCC | `while (TCC0_REGS->TCC_SYNCBUSY) { }` | `SYNCBUSY` 偏移 `0x08`，每位对应一个寄存器（手册 31.8.4） |
| SERCOM | `while (SERCOM0_REGS->USART_INT.SERCOM_SYNCBUSY & SERCOM_USART_INT_SYNCBUSY_ENABLE_Msk) { }` | `SYNCBUSY` 偏移 `0x1C`，bit1 = `ENABLE`（手册 26.8.9） |

**一句话记法：GCLK 和 TC 是"一个位管全外设"；TCC 和 SERCOM 是"每一位管一个寄存器"。** 判断依据在手册里：公共同步器的寄存器写 `Property: Write-Synchronized`，状态就叫 `STATUS.SYNCBUSY`；分布式方案的 `SYNCBUSY` 里则是 `SWRST` / `ENABLE` / `CTRLB` / `PER` / `COUNT` 这些按名字命名的位。

### 哪些寄存器需要同步

| 外设 | 需要同步的寄存器 / 位（手册原文摘录） |
|---|---|
| GCLK | 写同步：`GENCTRL`、`GENDIV`、`CTRL`（15.6.6） |
| TC | 写同步：`CTRLBCLR`、`CTRLBSET`、`CTRLC`、`COUNT`、`PER`、`CCx`；读同步：同一批，另外还有 `CTRLA.SWRST`/`ENABLE`（30.6.6） |
| TCC | 写同步：`CTRLA.SWRST`/`ENABLE`、`CTRLBCLR`/`CTRLBSET`、`STATUS`、`PATT/PATTB`、`WAVE`、`COUNT`、`PER/PERB`、`CCx/CCBx`；读同步：`CTRLBCLR`/`CTRLBSET`、`COUNT`（31.6.6） |
| SERCOM | 写同步：`CTRLA.SWRST`、`CTRLA.ENABLE`、`CTRLB.RXEN`（26.6.6 / 27.6.6 / 28.6.6） |
| PORT | **不同步**——手册第 23 章没有 Synchronization 小节，PORT 寄存器写完就生效 |

表里用的是手册的简称（`GENCTRL`、`CTRLBCLR`……）；到代码里要加外设前缀，例如 `GCLK_GENCTRL`、`TCC_CTRLBCLR`。

### 不等 SYNCBUSY 会怎样

不是"数据一定丢"，而是分两种情况：

- **公共同步器**（手册 14.3.1.2）：同步进行中如果去写核心寄存器、读被读同步的寄存器，或者读你刚写的那个寄存器，**APB 总线会停顿（stall）直到同步完成**。结果是程序比预期慢、时序难预测，但不会静默出错。
- **分布式同步器**（手册 14.3.2.2）：同步进行中对**同一个寄存器**再次写入，**这次写被丢弃并且报错**。

所以：

- **GCLK / TC 这类**：不等也能跑，但"下一笔写"可能被总线停顿拖住；要紧跟着写第二笔时，**必须等**。
- **SERCOM / TCC 这类**：不等就可能**丢写**，必须等。

一个常被忽略的点：`CTRLA.ENABLE` 的写同步**不能**用 Synchronization Ready 中断来判断（手册 14.3.1.6），轮询 `SYNCBUSY` 是最省事的办法。

## 2.8 Enable-Protected：只能在关着的时候配

第三类保护，和 PAC、同步都不同。手册里写作 **Enable-Protected**（Enable-Protection）：**这些寄存器只能在 `CTRLA.ENABLE = 0`（外设关着）的时候写**。

以 SERCOM 为例（手册各模式的 Initialization 小节）：

| 模式 | Enable-Protected 的寄存器 | 例外（外设开着也能写） |
|---|---|---|
| USART | `CTRLA`、`CTRLB`、`BAUD` | `CTRLA.ENABLE`、`CTRLA.SWRST`、`CTRLB.RXEN`、`CTRLB.TXEN` |
| SPI | `CTRLA`、`CTRLB`、`BAUD`、`ADDR` | `CTRLA.ENABLE`、`CTRLA.SWRST`、`CTRLB.RXEN` |
| I2C | `CTRLA`、`CTRLB`、`BAUD`、`ADDR`（从机） | `CTRLA.ENABLE`、`CTRLA.SWRST`、`CTRLB.ACKACT`、`CTRLB.CMD` |

违反的后果，手册写得很清楚："When the I2C is enabled or is being enabled (CTRLA.ENABLE=1), writing to these registers will be discarded." ——写入被**丢弃**，不报错。又是一个"写了没反应"。

于是得到一条通用配置顺序，适用于所有"必须先关再配"的外设：

```text
先确认外设是关的（ENABLE = 0，必要时写 SWRST 做软复位）
   → 配 CTRLA / CTRLB / BAUD / ADDR 这些 Enable-Protected 寄存器
   → 最后写 ENABLE = 1 打开
```

反过来，运行中要改模式或波特率，必须先 `ENABLE=0`，改完再 `ENABLE=1`——中间外设会停止工作，所以这类改动不能随时做。

## 2.9 写了没反应？按这个顺序排查

四类"静默失败"加三类"外部条件"，按从外到内的顺序查，一次到位：

| 顺序 | 查什么 | 怎么查 | 不符合时的现象 |
|---|---|---|---|
| 1 | 地址对不对 | 外设基址 + 偏移对照手册 Register Summary | 完全无反应，或写坏了别的外设 |
| 2 | APB 总线时钟开了吗 | 桥 A/B 默认开；桥 C 查 `PM_REGS->PM_APBCMASK` 对应位 | 读回全 0，写被丢弃 |
| 3 | PAC 写保护锁着吗 | 读 `PACx_REGS->WPSET`，看对应位是不是 1 | 写被丢弃 + CPU 异常 |
| 4 | 是不是 Enable-Protected | 手册该寄存器描述的 `Property` 栏 | 写被丢弃（尤其 `ENABLE=1` 时写 `CTRLA`/`BAUD`） |
| 5 | 通用时钟配了吗 | `GCLK_REGS->GCLK_CLKCTRL` 的 ID / GEN / CLKEN | 寄存器能读写，但外设不动作 |
| 6 | 引脚交出去了吗 | `PORT_REGS->GROUP[x].PORT_PMUX` + `PORT_PINCFG[n]` | 内部逻辑正常，外面量不到信号 |
| 7 | 同步等了吗 | 对应外设的 `STATUS` / `SYNCBUSY` | 偶尔丢配置，或时序莫名其妙 |

**第 2 条和第 4 条的共同点是：写被"静默丢弃"，不报错。** 这是 SAMD21 与 STM32 手感最不一样的地方。

## 2.10 完整例程：把 SERCOM0 从复位状态带到"可以配置"

**需求**：芯片刚复位，我们要让 SERCOM0 处于"寄存器可读写、时钟已就位"的状态，为第 7 章写 I2C 做好准备。

**逐步推导**（每一步都对应 2.1 的四个条件）：

| 步骤 | 硬件动作 | 为什么排在这个位置 |
|---|---|---|
| ① | 解除 PAC2 对 SERCOM0 的写保护 | 后面要写 `CTRLA`，它受 PAC 保护；不解锁就是白写 |
| ② | `APBCMASK` 开 SERCOM0 的 APB 时钟 | 不开的话连寄存器都读不回来，第 ③ 步写进去也没意义 |
| ③ | 配 GCLK0 给 `SERCOM0_CORE` | APB 时钟只管"CPU 能不能说话"，外设自己走时靠 GCLK |
| ④ | 等 GCLK 同步完成 | `CLKCTRL` 写完不是立刻生效，要等 `STATUS.SYNCBUSY` 落 0 |
| ⑤ | 配 `SLOW` 那一路 GCLK | SMBus 超时用；顺手配好，第 7 章不用回头补 |
| ⑥ | 上锁 | 恢复保护，并保持"一解一锁"的完整节奏 |
| ⑦ | （可选）配引脚 | 真正收发才需要，留给第 7 章 |

```c
#include "sam.h"

/* 位号 2 = SERCOM0，来自手册 11.6.2 的 PAC2 位图。
   PAC 没有任何现成的外设位宏（component/pac.h 只给了通用掩码 PAC_WPCLR_WP_Msk），
   所以这一条必须自己定义；其余宏 DFP 里都有现成的。 */
#define PAC2_WP_SERCOM0  (1UL << 2)

static void sercom0_bring_up(void)
{
    /* ① 解锁：SERCOM0 在桥 C，受 PAC2 保护。写完这一位它才允许被配置。 */
    PAC2_REGS->PAC_WPCLR = PAC2_WP_SERCOM0;

    /* ② 打开总线时钟。桥 C 复位后所有 APB 时钟都是关的：
           不开这一位，下面所有对 SERCOM0_REGS 的访问都会被丢弃。 */
    PM_REGS->PM_APBCMASK |= PM_APBCMASK_SERCOM0_Msk;

    /* ③ 把通用时钟发生器 0 的输出接到 SERCOM0 的核心逻辑。
           ID 选外设，GEN 选发生器（GCLK0 复位后 = OSC8M ÷ 8 = 1 MHz），
           CLKEN 才是"真的把时钟放出去"的那一位。 */
    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_SERCOM0_CORE_Val
                                       | GCLK_CLKCTRL_GEN_GCLK0_Val
                                       | GCLK_CLKCTRL_CLKEN_Msk);

    /* ④ 等同步：CLKCTRL 写在 APB 域、生效在 GCLK 域，
           不等就可能被总线停顿拖住，甚至丢掉下一笔写。 */
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* ⑤ 第二路 GCLK：SERCOMx_SLOW。同一套写法，只是 ID 不同。
           普通 UART/I2C 暂时用不到，但先配好省得以后忘。 */
    GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_SERCOMX_SLOW_Val
                                       | GCLK_CLKCTRL_GEN_GCLK0_Val
                                       | GCLK_CLKCTRL_CLKEN_Msk);
    while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }

    /* ⑥ 上锁：恢复对 SERCOM0 的写保护。注意与 ① 交替出现：
           连续两次解锁、或连续两次上锁，都会触发 CPU 异常。 */
    PAC2_REGS->PAC_WPSET = PAC2_WP_SERCOM0;

    /* 到这里 SERCOM0 已经"活着"了：寄存器可读写、核心时钟在跑。
       接下来才是选模式和配波特率（第 7、8 章），本章不做。 */
}
```

**宏名提醒**：这段代码里除 `PAC2_WP_SERCOM0` 之外，所有宏都是 SAMD21_DFP 3.7.262 里的真实名字。三条容易踩的规律——**字段宏只有两种形式**：`_Msk`（单个位的掩码，如 `GCLK_CLKCTRL_CLKEN_Msk`）和 `_Val`（多位字段的枚举值，如 `GCLK_CLKCTRL_ID_SERCOM0_CORE_Val`），没有 `GCLK_CLKCTRL_CLKEN` 这种裸名字；**多位字段用 `_Val` 或整体移位**，别自己瞎拼；**PAC 没有任何现成的外设位宏**（`component/pac.h` 只给通用掩码 `PAC_WPCLR_WP_Msk`），所以 `PAC2_WP_SERCOM0` 必须自己定义。**以你工程里的 DFP 头文件为准**；对不上时打开 `component/pm.h`、`component/gclk.h`、`component/pac.h` 各查一眼即可。

### 这段代码在做什么（人话版）

```text
SERCOM0 复位后 = 一片锁着的、没通电的、没心跳的电路
  ① 解锁           → 允许别人动它的开关
  ② 开 APB 时钟    → CPU 的话能传进去
  ③④ 开 CORE GCLK → 它自己的心跳开始
  ⑤ 开 SLOW GCLK   → 慢速逻辑也有心跳（本章用不到，先给上）
  ⑥ 上锁           → 恢复保护
```

## 2.11 初始化清单

以后给任何新外设写第一段代码，按这个清单过一遍：

| 项 | 检查 | 典型代码 |
|---|---|---|
| 1 地址 | 基址 + 偏移查清了吗 | 手册 Register Summary |
| 2 APB 时钟 | 桥 C 的外设开了吗（ADC 除外） | `PM_REGS->PM_APBCMASK` 加上对应 `_Msk` |
| 3 GCLK | 需要通用时钟的外设配 `GCLK_CLKCTRL` 了吗 | `GCLK_REGS->GCLK_CLKCTRL =` ID、GEN、CLKEN 三部分相或 |
| 4 同步 | 每次写同步寄存器后等了吗 | `while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }` |
| 5 PAC | 要写的寄存器受保护吗 | `PACx_REGS->WPCLR = mask;` … 改完 … `PACx_REGS->WPSET = mask;` |
| 6 Enable-Protected | 现在 `ENABLE=0` 吗 | 先配 `CTRLA/CTRLB/BAUD`，最后再 `ENABLE=1` |
| 7 引脚 | 交给外设了吗 | `PINCFG[n]` + `PMUX`（第 4、7、8 章） |
| 8 功能 | 模式 / 波特率 / 触发源 | 各外设章节 |

## 2.12 一个外设的完整思维模型

```text
                       外设（一段寄存器窗口）
                               │
        ┌──────────┬───────────┼───────────┬────────────┐
        │          │           │           │            │
      地址       ①APB       ②GCLK       ③引脚       ④功能
    基址+偏移  PM.APBxMASK  GCLK.CLKCTRL  PMUX/PINCFG  CTRLx
        │          │           │           │            │
  PACx.WPCLR   （桥A/B默认开）  等SYNCBUSY  （第4章）  Enable-Protected
  PACx.WPSET   （桥C默认关）
```

三条横切规则覆盖所有外设：**能不能说话（APB 时钟 + PAC）、走不走时（GCLK + SYNCBUSY）、活怎么干（Enable-Protected 解除之后再配 CTRLx）**。

## 2.13 常见问题

| 现象 | 原因 | 检查什么 |
|---|---|---|
| 写寄存器读回还是 0 | APB 时钟没开（桥 C） | `PM_REGS->PM_APBCMASK` 对应位 |
| 写寄存器后进 HardFault | 撞上 PAC 写保护 | `PACx_REGS->WPCLR` 先解锁；确认没有连续两次解锁 |
| 外设寄存器都对，就是不动 | 通用时钟没配或没使能 | `GCLK_REGS->GCLK_CLKCTRL` 的 ID / GEN / CLKEN |
| 配置写不进 `CTRLA`/`BAUD` | Enable-Protected：外设还开着 | 先 `ENABLE=0`，配完再 `ENABLE=1` |
| 偶尔丢一笔配置 | 没等 `SYNCBUSY` | 对应外设的 `STATUS` / `SYNCBUSY` |
| `DIRSET = 0` 没反应 | SET/CLR 寄存器写 0 无效果 | 要清零请用 `DIRCLR` |
| 改了 PA07，PA05 跟着变 | 用了 `DIR |= ...` 读-改-写 | 改用 `DIRSET`/`DIRCLR`/`DIRTGL` |
| 调试器里能改，代码里改不了 | 调试器绕过 PAC | 先解锁，再写 |
| 写完 `CLKCTRL` 立刻读回是旧值 | 同步还没完成 | 等 `STATUS.SYNCBUSY` 落 0 再读 |

## 2.14 寄存器速查

| 寄存器 | 所在外设 | 偏移 | 复位值 | 一句话作用 |
|---|---|---|---|---|
| `PAC_WPCLR` | PAC0 / PAC1 / PAC2 | `0x00` | 0 / `0x02` / `0x800000` | 写 1 解除对应外设写保护 |
| `PAC_WPSET` | PAC0 / PAC1 / PAC2 | `0x04` | 同上 | 写 1 加上对应外设写保护 |
| `PM_APBAMASK` | PM | `0x18` | `0x0000_007F` | 桥 A 各外设的 APB 时钟开关 |
| `PM_APBBMASK` | PM | `0x1C` | `0x0000_007F` | 桥 B 各外设的 APB 时钟开关 |
| `PM_APBCMASK` | PM | `0x20` | `0x0001_0000` | 桥 C 各外设的 APB 时钟开关（只有 ADC 默认开） |
| `GCLK_CTRL` | GCLK | `0x00` | `0x00` | 写 `SWRST` 复位 GCLK |
| `GCLK_STATUS` | GCLK | `0x01` | `0x00` | bit7 = `SYNCBUSY` |
| `GCLK_CLKCTRL` | GCLK | `0x02` | `0x0000` | ID[5:0] / GEN[11:8] / CLKEN(bit14) / WRTLOCK(bit15) |
| `GCLK_GENCTRL` | GCLK | `0x04` | `0x0000_0000` | 发生器的时钟源、分频、使能 |
| `SERCOM_SYNCBUSY` | SERCOM | `0x1C` | `0x0000_0000` | bit0 `SWRST` / bit1 `ENABLE` / bit2 `CTRLB`（I2C 主机模式下 bit2 是 `SYSOP`） |
| `TCC_SYNCBUSY` | TCC | `0x08` | `0x0000_0000` | 每位对应一个寄存器（SWRST / ENABLE / CTRLB / STATUS / COUNT / PER…） |
| `TC_STATUS` | TC（8 位模式） | `0x0F` | `0x00` | bit7 = `SYNCBUSY`；`TC_CTRLA` 在偏移 `0x00` |

外设基址：PM `0x4000_0400`、GCLK `0x4000_0C00`、PAC0 `0x4000_0000`、PAC1 `0x4100_0000`、PAC2 `0x4200_0000`、PORT `0x4100_4400`（Group 1 = `+0x80`）、SERCOM0 `0x4200_0800`、TCC0 `0x4200_2000`、TC3 `0x4200_2C00`。

## 2.15 最重要的几句话

1. 外设就是一段地址窗口：**基址 + 偏移**；写它，等于按硬件开关。
2. 一个外设能用 = **开总线时钟 + 分配通用时钟 + 配引脚 + 配功能**；PORT 只要 ①③，SERCOM 要 ①②③④。
3. 桥 A / 桥 B 的 APB 时钟复位后已开，**桥 C 默认全关**——`APBCMASK` 复位值 `0x00010000` 里的那个 1 是 ADC。
4. 桥 C 忘了开时钟，写入被**静默丢弃**，不报错、不复位。
5. `SET`/`CLR`/`TGL` 三件套是为了避免读-改-写：一条写指令，天生原子，不误伤其它位。
6. PAC 保护的是"谁能改配置"：`WPCLR` 解锁、`WPSET` 上锁，**一解一锁交替**；连续两次同向操作会触发 CPU 异常。
7. 跨时钟域的寄存器写完要等 `SYNCBUSY`，但**等待的位置因外设而异**：GCLK/TC 在 `STATUS`，TCC/SERCOM 在独立的 `SYNCBUSY`。
8. `Enable-Protected` 的寄存器只能在 `ENABLE=0` 时写；写不进去时，先想想外设是不是还开着。

## 2.16 自测

1. `PORT_REGS->GROUP[0].PORT_DIRSET = (1UL << 5);` 这条语句最终写到哪个物理地址？为什么不用 `PORT_DIR |= (1UL << 5)`？
2. 点灯不需要写任何时钟配置，而用 SERCOM 就必须先写两处时钟。用"四个条件"解释这个差别。
3. `APBCMASK` 的复位值是 `0x00010000`。这个值说明桥 C 的默认状态是什么？哪个外设是例外，为什么？
4. 一段代码往 `SERCOM0_REGS->USART_INT.SERCOM_CTRLA` 写值，读回来还是 0。列出三个可能的原因，并说明各自的验证方法。
5. 为什么 `GCLK_REGS->GCLK_CLKCTRL` 写完要等 `GCLK_STATUS` 里的 `SYNCBUSY`，而写 `PORT_REGS->GROUP[0].PORT_DIRSET` 不用等？
6. 下面的代码有什么问题？指出两处。

```c
PAC2_REGS->PAC_WPCLR = (1UL << 2);
PAC2_REGS->PAC_WPCLR = (1UL << 2);
SERCOM0_REGS->USART_INT.SERCOM_CTRLA = SERCOM_USART_INT_CTRLA_ENABLE_Msk;
```

## 附录：以后查手册怎么找

```text
① 手册目录找外设章  →  ② 章末 Register Summary 找偏移  →  ③ 寄存器描述看 Property
   →  ④ 该外设 Product Dependencies 小节看它需要哪些时钟  →  ⑤ 需要时回查 11.6（PAC）/ 14.3（同步）
```

几个固定入口，记住位置就不用每次翻目录：

| 我要找什么 | 手册位置 |
|---|---|
| 外设基址、桥分组 | 10.2 Physical Memory Map；各外设章的 Product Mapping |
| 某外设 APB 时钟的默认开关 | 16.6.2.6 与 Table 16-1（PM 章） |
| 寄存器偏移与位定义 | 各外设章 Register Summary + Register Description |
| 寄存器受不受保护 | 每个寄存器描述表头的 `Property:` 行 |
| PAC 位图 | 11.6.2（PAC0 / PAC1 / PAC2 三小节） |
| 同步机制与等待方式 | 14.3（总述）+ 各外设章的 Synchronization 小节 |
| GCLK ID 表 | 15.8 的 `CLKCTRL.ID` 位描述 |
| 哪些寄存器 Enable-Protected | 各外设章 Basic Operation → Initialization |

在本章留下名字的偏移，以后会反复用到：PM `PM_APBAMASK 0x18 / PM_APBBMASK 0x1C / PM_APBCMASK 0x20`；PAC `PAC_WPCLR 0x00 / PAC_WPSET 0x04`；GCLK `GCLK_CTRL 0x00 / GCLK_STATUS 0x01 / GCLK_CLKCTRL 0x02 / GCLK_GENCTRL 0x04`；SERCOM `SERCOM_CTRLA 0x00 / SERCOM_SYNCBUSY 0x1C`；TCC `TCC_SYNCBUSY 0x08`。

## 本章结束

学完这一章，你应该能把任何一段 SAMD21 的初始化代码翻译成硬件动作。例如

```c
PAC2_REGS->PAC_WPCLR   = (1UL << 2);
PM_REGS->PM_APBCMASK  |= PM_APBCMASK_SERCOM0_Msk;
GCLK_REGS->GCLK_CLKCTRL = (uint16_t)(GCLK_CLKCTRL_ID_SERCOM0_CORE_Val
                                    | GCLK_CLKCTRL_GEN_GCLK0_Val
                                    | GCLK_CLKCTRL_CLKEN_Msk);
while (GCLK_REGS->GCLK_STATUS & GCLK_STATUS_SYNCBUSY_Msk) { }
PAC2_REGS->PAC_WPSET   = (1UL << 2);
```

应该读成：**先允许别人动 SERCOM0 的开关；再给它通上总线时钟，让 CPU 的话能传进去；然后把它自己的心跳（GCLK0 → SERCOM0_CORE）接上并等它稳定；最后把锁扣回去。** 到这一步，SERCOM0 才算"存在"了——至于它是当串口还是当 I2C 用，那是后面几章的事。
